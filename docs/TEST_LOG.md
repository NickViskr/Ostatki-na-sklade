# Журнал тестирования

> Новые записи добавляются ТОЛЬКО последней строкой таблицы в самом конце файла. Существующие строки не редактируются и не удаляются. В начале файла лежит исторический блок с обратным порядком дат — его не трогать. Полное правило: раздел «Правило ведения журналов» в `docs/OZON_PLAN.md`.

| Дата | Что проверялось | Сценарий | Результат (OK/FAIL) | Комментарий |
| :--- | :--- | :--- | :--- | :--- |
| 2026-07-31 | Закрытие пункта 24 в `docs/OZON_PLAN.md` и исправление заметки о следующем пункте | Сверка фактического состояния пунктов 25, 26 и 27 в плане, проверка числа строк и байт файла до и после правок | OK | Число строк файла не изменилось, число байт выросло. Проверено, что значков ⬜ у этапов C, D, E и F пункта 24 не осталось. Обнаружено и исправлено ложное утверждение о том, что следующим идёт пункт 25: он выполнен 30.07.2026. |
| 2026-07-31 | Документирование обработки отменённых заявок Ozon в `docs/OZON_API.md` | Выполнение дозаписи новой секции в конец файла, проверки `wc -l` и `wc -c` до и после изменения | OK | 1) В конец файла `docs/OZON_API.md` добавлена новая секция с выводами по отменённым заявкам Ozon API. 2) `wc -l` вырос с 752 до 788 строк. 3) `wc -c` вырос с 67013 до 74076 байт. |
| 2026-07-31 | Порядок и длительность вывода тостов предупреждений в `src/components/OzonSupplyModal.tsx` | Внесение точечной правки, проверки `wc -l`, `grep -c`, `npx tsc --noEmit` и `npx vite build` | OK | 1) Вывод предупреждений перенесён после тоста об успехе сборки папки на Google Диске. 2) Все предупреждения выведены по очереди с задержкой 400 мс и длительностью 15 сек. 3) `wc -l` вернул 806 строк. 4) `grep -c "allWarnings"` вернул 3. 5) `npx tsc --noEmit` и `npx vite build` прошли без ошибок. |
| 2026-07-31 | Закрытие журнала «Заявки Ozon» по отгрузкам в `Code.gs` | Добавление `syncOzonJournalFromShipments` и вызова в `applyCancelledOzonOrders`, проверки `wc -l`, `wc -c`, `grep -c` и `compile_applet` | OK | 1) Добавлена функция `syncOzonJournalFromShipments`. 2) Добавлен вызов `result.closedFromShipments = syncOzonJournalFromShipments()`. 3) `wc -l` вернул 6013 строк, `wc -c` — 244545 байт. 4) `grep -c` вернули: `function syncOzonJournalFromShipments` = 1, `syncOzonJournalFromShipments` = 2, `closedFromShipments` = 1. 5) `compile_applet` прошёл без ошибок. |
| 2026-07-31 | Передача отменённых заявок Ozon при опросе в `server.ts` | Вставка вызова `applyCancelledOzonOrders` перед Step 5, проверки `wc -l`, `wc -c`, `grep -c`, `npx tsc --noEmit` и `compile_applet` | OK | 1) Вставка перед Step 5 выполнена. 2) `wc -l` вернул 2479 строк, `wc -c` — 104284 байт. 3) `grep -c "applyCancelledOzonOrders"` вернул 1. 4) `npx tsc --noEmit` и `compile_applet` прошли без ошибок. |
| 2026-07-31 | Пометка отменённых заявок Ozon и очистка базы в `Code.gs` | Выполнение правок, проверки `wc -l`, `wc -c`, `grep -c` и `compile_applet` | OK | 1) Добавлены экшен `applyCancelledOzonOrders` и функция обработки с константой `OZON_CANCELLED_KEEP_DAYS`. 2) `wc -l` вернул 5936 строк, `wc -c` — 241056 байт. 3) `grep -c` вернули: `function applyCancelledOzonOrders` = 1, `applyCancelledOzonOrders` = 3, `OZON_CANCELLED_KEEP_DAYS` = 2, `notEmpty(row[sTrans])` = 1. 4) `compile_applet` прошёл без ошибок. |
| 2026-07-31 | Фильтрация полностью отменённых заявок в `src/components/OzonSuppliesTab.tsx` | Внесение точечной правки, проверки `wc -l`, `grep -c`, `npx tsc --noEmit` и `npx vite build` | OK | 1) Добавлена проверка `isFullyCancelled`. 2) Исключение полностью отменённых заявок из `displayedGroups` и добавление их в `processedGroupsCount`. 3) `wc -l` вернул 1798 строк. 4) `grep -c "isFullyCancelled"` вернул 3. 5) `npx tsc --noEmit` и `npx vite build` прошли без ошибок. |
| 2026-07-31 | Опрос журнала «Заявки Ozon» в `server.ts` | Выполнение вставки, проверки `wc -l`, `wc -c`, `grep -c`, `npx tsc --noEmit` и `compile_applet` | OK | 1) Вставка перед Step 4 выполнена. 2) `grep -c "getOzonSupplyRequests"` вернул 1. 3) `grep -c "JOURNAL_MAX_AGE_MS"` вернул 2. 4) Проверка типов `npx tsc --noEmit` и сборка `compile_applet` прошли без ошибок. |
| 2026-07-31 | Нормализация имён этикеток NFC и поиск по индексу библиотеки в `Code.gs` | Проверка `wc -l`, `wc -c`, `grep -c "function normalizeLabelKey"`, `grep -c "libIndex"`, `grep -c "lib.getFilesByName"` | OK | 1) Добавлена функция `normalizeLabelKey`. 2) Поиск переведён на `libIndex`. 3) Команда `grep -c "function normalizeLabelKey"` вернула 1. 4) Команда `grep -c "libIndex"` вернула 3. 5) Команда `grep -c "lib.getFilesByName"` вернула 0. |
| 2026-07-31 | Автоматическая достройка поставки Ozon в `src/components/OzonSupplyModal.tsx` | Выполнение 10 точечных правок, команды `wc -l`, `grep -c`, `npx tsc --noEmit` и `npx vite build` | OK | 1) Все 10 правок внесены дословно. 2) `wc -l` вернул 799 строк. 3) `grep -c` подтвердили: `finalizeSupply` = 2, `verdictData` = 4, `FINALIZE_TIMEOUT_SEC` = 2. 4) `npx tsc --noEmit` и `npx vite build` прошли без ошибок. |
| 2026-07-31 | Дословная замена `saveSupplyDocsToDrive` и описание настройки `supplyDocsLabelsFolder` в `Code.gs` | Проверка `wc -l`, `wc -c`, `grep -c "supply/act"`, `grep -c "server_sessionToken"`, `grep -c "missingLabels"` | OK | 1) Описание настройки `supplyDocsLabelsFolder` обновлено. 2) Функция `saveSupplyDocsToDrive` дословно заменена на предоставленный код. 3) Удалён старый прокси-зависимый код (`supply/act` и `server_sessionToken` вернули 0). 4) Проверено наличие логики `missingLabels` (вернул 4). |
| 2026-07-31 | Скачивание документов заявки Ozon на Google Диск (`Code.gs`) | Проверка синтаксиса `Code.gs`, 4 точечных замен и наличия функций `saveSupplyDocsToDrive` и `supplyDocsLabelsFolder` | OK | 1) Добавлены настройки `supplyDocsFolderId` и `supplyDocsLabelsFolder` в `OZON_SETTINGS_DEFAULTS` и `OZON_SETTINGS_STRING_KEYS`. 2) Экшен `saveSupplyDocsToDrive` зарегистрирован в switch `doPost`. 3) Функция `saveSupplyDocsToDrive` и хелперы без ошибок скомпилированы. |
| 2026-07-31 | Эндпоинты финализации заявки и состояния грузомест Ozon (`server.ts`) | Проверка сборки `compile_applet` и проверки типов `lint_applet` после правок `server.ts` | OK | 1) В `server.ts` подключен импорт `xlsx`, расширено `placementZone`, обновлена версия прокси. 2) Реализованы эндпоинты `/api/ozon/supply/finalize` и `/api/ozon/cargoes/state`. 3) Сборка `compile_applet` прошла без ошибок. |
| 2026-07-31 | Замена строки пункта 24 в `docs/OZON_PLAN.md` | Проверка команд `wc -l`, `wc -c` и `grep -c` до и после замен | OK | 1) Заменена ровно одна строка пункта 24 в `docs/OZON_PLAN.md`. 2) Число строк не изменилось (138 до и 138 после). 3) Размер файла вырос с 85 862 байт до 91 214 байт (+5 352 байт). 4) `grep -c "^- 🟨 24\. Грузоместа, этикетки и папка документов заявки\."` вернул ровно 1. |
| 2026-07-31 | Правка 1 и дозапись Разведки A2 в `docs/OZON_API.md` (пункт 24) | Проверка команды `wc` до и после редактирования `docs/OZON_API.md`, замер количества строк и байт | OK | 1) Выполнена замена в разделе 8 и дозапись раздела «Разведка A2 пункта 24» в конец `docs/OZON_API.md`. 2) Число строк выросло с 713 до 752 (рост +39 строк). 3) Размер файла вырос с 59 169 байт до 67 013 байт (рост +7 844 байт). |
| 2026-07-30 | Модуль чистых функций грузомест Ozon (`src/lib/ozonCargo.ts`) | Проверка создания файла, команд `wc -l` и `wc -c`, проверки типов `npx tsc --noEmit` и сборки `compile_applet` | OK | 1) Создан файл `src/lib/ozonCargo.ts` (372 строки, 13 295 байт). 2) Ни один существующий файл приложения не затронут. 3) `npx tsc --noEmit` и `compile_applet` — 0 ошибок, сборка прошла успешно. |
| 2026-07-30 | 3 точечные правки в `docs/OZON_API.md` (пункт 24) | Проверка команды `wc` до и после замен, замер количества строк и байт | OK | 1) Вынесены 3 замены в `docs/OZON_API.md`. 2) Число строк неизменно: 713 до и 713 после. 3) Размер файла вырос с 57917 байт до 59169 байт (рост +1252 байт). |
| 2026-07-30 | Результаты разведки Ozon API для пункта 24 (`docs/OZON_API.md`) | Проверка команды `wc` до и после дозаписи в конец файла, замер количества строк и байт | OK | 1) Дозапись выполнена строго в конец файла `docs/OZON_API.md`. 2) Число строк выросло с 646 до 713 (рост +67 строк). 3) Размер файла вырос с 49040 байт до 57917 байт (рост +8877 байт). Существующее содержимое файла не изменялось. |
| 2026-07-30 | Обновление раздела «Известные хвосты вне плана» (`docs/OZON_PLAN.md`) | Визуальная проверка точечной замены 1 фрагмента в `docs/OZON_PLAN.md`, замер размера файла до и после, пересчёт количества строк | OK | 1) Выполнена дословная замена в `docs/OZON_PLAN.md`: добавлены 6 пунктов живых наблюдений в раздел «Известные хвосты вне плана». 2) Размер файла до правки: 80826 байт, после правки: 85862 байт. 3) Число строк в разделе до правки: 15, после правки: 21. |
| 2026-07-30 | Отметка пункта 25 как выполненного (`docs/OZON_PLAN.md`) | Визуальная проверка точечной замены 1 фрагмента в `docs/OZON_PLAN.md`, замер размера файла до и после | OK | 1) Выполнена дословная замена в `docs/OZON_PLAN.md`: пункт 25 отмечен как выполненный (✅) с обоснованием и оговоркой о переносе регресса учётных путей. 2) Размер файла до правки: 78750 байт, после правки: 80826 байт. 3) Прочие пункты и разделы файла не затрагивались. |
| 2026-07-30 | Выделение пункта 27 в плане (`docs/OZON_PLAN.md`) | Визуальная проверка точечной замены 1 фрагмента в `docs/OZON_PLAN.md`, замер размера файла до и после | OK | 1) Выполнена дословная замена в `docs/OZON_PLAN.md`: добавление пункта 27 с пометкой НЕОБЯЗАТЕЛЬНЫЙ. 2) Размер файла до правки: 77012 байт, после правки: 78750 байт. 3) Статус пункта 25 сохранён (⬜), побочные файлы/структура не затрагивались. |
| 2026-07-30 | Точка индикатора статуса в свёрнутом сайдбаре (`Sidebar.tsx`) | Проверка линтером (`lint_applet`), сборкой (`compile_applet`), проверка 1 точечной замены | OK | 1) Точка статуса в свёрнутом виде переведена с `allOk` на `statusLevel` ('ok'/'warning'/'error'). 2) `lint_applet` (`npx tsc --noEmit`) — 0 ошибок. 3) `compile_applet` (`npx vite build`) выполнен успешно. |
| 2026-07-30 | Трёхуровневый индикатор статуса системы и порог 26ч (`Sidebar.tsx`, `useWarehouseStore.ts`) | Проверка линтером (`lint_applet`), сборкой (`compile_applet`), проверка 4 точечных замен | OK | 1) Порог свежести поднят с 14ч до 26ч. 2) Добавлен расчет `failedSteps`, `statusLevel` ('ok'/'warning'/'error') и желтый цвет амбера при осечке отдельных шагов. 3) В `fetchOzonSyncStatus` добавлена проверка `!sessionToken`. 4) `lint_applet` (`npx tsc --noEmit`) — 0 ошибок. 5) `compile_applet` (`npx vite build`) выполнен успешно. |
| 2026-07-30 | Снятие LockService с читающих действий — волна 2 (`Code.gs`) | Проверка линтером (`lint_applet`), сборкой (`compile_applet`), проверка 1 точечной замены в `Code.gs` | OK | 1) В список `LOCK_FREE_ACTIONS` в `Code.gs` добавлено 5 читающих действий: `getUsers`, `getGlobalSettings`, `getExternalShipments`, `getOzonSupplyRequests`, `checkSupplyAvailability`. 2) `lint_applet` (`npx tsc --noEmit`) — 0 ошибок. 3) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-30 | Результаты аудита и ориентировки по LockService в `docs/OZON_PLAN.md` | Визуальная проверка точечной замены 1 фрагмента в `docs/OZON_PLAN.md`, отсутствие побочных изменений | OK | 1) Зафиксированы результаты аудита 44 пишущих функций `Code.gs`. 2) Записаны классификация чтений и запрет на удаление блока `assertAdmin`. 3) Сохранены ориентировки по волнам 1, 2 и 3 для продолжения оптимизации в следующих сессиях. |
| 2026-07-30 | Снятие LockService с читающих действий (`Code.gs`) | Проверка линтером (`lint_applet`), сборкой (`compile_applet`), проверка 1 точечной замены в `Code.gs` | OK | 1) В `doPost` объявлен список `LOCK_FREE_ACTIONS` (`getInitialData`, `getStock`, `getTransactions`, `getSkus`, `getServices`, `getServiceRates`). 2) Захват `LockService.getScriptLock()` выполняется только если действие отсутствует в этом списке. 3) `lint_applet` (`npx tsc --noEmit`) — 0 ошибок. 4) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-30 | Автоматический повтор прогона автоопроса Ozon при сбоях (`Code.gs`) | Проверка линтером (`lint_applet`), сборкой (`compile_applet`), проверка внесения 1 точечной замены в `Code.gs` | OK | 1) Добавлена вызов `scheduleOzonSyncRetryIfNeeded(result)` в `saveOzonSyncResult`. 2) Добавлены функции `scheduleOzonSyncRetryIfNeeded` и `runOzonSyncRetry` с автоочисткой одноразового триггера и защитой от зацикливания. 3) `lint_applet` (`npx tsc --noEmit`) — 0 ошибок. 4) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-30 | Отображение журнала автоопроса Ozon (`useWarehouseStore.ts`, `Sidebar.tsx`) | Проверка линтером (`lint_applet`), сборкой (`compile_applet`), проверка 4 точечных замен | OK | 1) `ozonSyncStatus.history?: any[]` добавлен в типы стора. 2) В `Sidebar.tsx` добавлена выпадающая кнопка журнала с количеством прогонов. 3) Прогоны отсортированы хронологически от свежих к старым. 4) `lint_applet` (`npx tsc --noEmit`) — 0 ошибок. 5) `compile_applet` (`npx vite build`) выполнен успешно. |
| 2026-07-30 | Журнал автоопроса Ozon (`Code.gs`) | Проверка линтером (`lint_applet`), сборкой (`compile_applet`), проверка внесения 4 точечных замен в `Code.gs` | OK | 1) Добавлена функция `saveOzonSyncResult` с лимитом 20 записей и обрезкой текста ошибок. 2) Обновлены точки вызова записи статуса в `scheduledOzonCheck`. 3) Поле `history` добавлено в объект ответа `getOzonSyncStatusInfo`. 4) `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-29 | Добавление пункта 25 в `docs/OZON_PLAN.md` | Проверка точной замены 1 фрагмента в `docs/OZON_PLAN.md` | OK | 1) Добавлен пункт 25 «Ускорение приложения: снять глобальный замок с операций чтения» перед разделом «Известные хвосты вне плана». 2) Код приложения не затрагивался. |
| 2026-07-29 | Отметка этапа 23.D в `docs/OZON_PLAN.md` | Проверка точной замены 2 фрагментов в `docs/OZON_PLAN.md` | OK | 1) Фрагмент этапа D отмечен `✅ готово 30.07.2026` с подробным описанием всех изменений интерфейса и алертов. 2) Ссылка на следующий чат переведена на этап E. 3) Код не затрагивался. |
| 2026-07-29 | Текст алерта «Пора сделать поставку на Ozon» (`ozonAlerts.ts`) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка 1 замены в `src/lib/ozonAlerts.ts` | OK | 1) В `buildCoverageAlerts` для алерта `supply_needed` добавлен расчет `pendingNote` при `art.pendingTotal > 0`. 2) Строка `description` расширена примечанием `· уже в заявках X шт, эта потребность сверх них`. 3) Текст алерта фабрики, severity, тип и сортировки сохранены без изменений. 4) `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-29 | Модальное окно с расшифровкой зачёта по товару (`OzonStocksTab.tsx`) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка 5 замен в `src/components/OzonStocksTab.tsx` | OK | 1) Добавлен импорт `getStatusDetails`. 2) Добавлено состояние `pendingModalArticle`. 3) Настроены `clusterNameById` и `pendingModalRows`. 4) Значение `art.pendingTotal` обёрнуто в `button` c кликом `setPendingModalArticle(art.article)`. 5) Добавлена верстка модального окна `ozon-pending-modal` с таблицей заявок, статусами, кластерами и суммой в футере. 6) `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-29 | Кнопка выбора видимых колонок таблицы остатков на вкладке «Склад» (`Dashboard.tsx`) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка внесения 16 точечных правок в `src/components/Dashboard.tsx` | OK | 1) Добавлен `Columns3` в импорты. 2) Объявлены `DASH_TOGGLEABLE_COLS` и `DASH_DEFAULT_HIDDEN_COLS = ['quantity']`. 3) Добавлена логика `hiddenCols`, `toggleCol`, `isColVisible` и `visibleColsCount` с сохранением в `localStorage` по ключу `dashCols_${username}`. 4) Добавлены кнопка «Колонки» и выпадающее меню с чекбоксами. 5) Заголовки, skeleton, ячейка пустой таблицы (`colSpan={visibleColsCount}`) и ячейки строк таблицы обёрнуты в `isColVisible(...)`. 6) `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-29 | Добавление колонки «Свободно» на вкладке «Склад» (`Dashboard.tsx`) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка применения 5 точечных фрагментов | OK | 1) `fetchExternalShipments()` вынесен для всех пользователей в `useEffect`. 2) В шапку таблицы `<thead>` добавлена колонка «Свободно» с подсказкой `HelpCircle`. 3) Skeleton загрузки обновлён до 7 колонок. 4) Пустой `td` при отсутствии данных обновлён на `colSpan={7}`. 5) Добавлена ячейка «Свободно» с вычислением `free = max(0, quantity - reserved)` и плашкой при наличии резерва. 6) `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-29 | Отображение свободного остатка Моего склада в `OzonStocksTab.tsx` | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка применения 4 точечных замен | OK | 1) Обновлена подсказка `ColHint` шапки «Мой склад». 2) Реализовано отображение `freeMyStock` с подсказкой и детализацией резерва при `art.pendingTotal > 0`. 3) В `recommendations` `useMemo` добавлены `freeMyStock` и `pendingTotal`. 4) В карточках рекомендаций выведено «свободно X шт (из Y)». 5) Использование `art.myStockAvailable` в расчёте `factoryDaysLeft` сохранено без изменений. 6) `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-29 | Добавление колонки «Зачёт» в таблицу `OzonStocksTab.tsx` | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка внесения 6 точечных правок в `src/components/OzonStocksTab.tsx` | OK | 1) Добавлен пункт `{ key: 'pending', label: 'Зачёт' }` в `OZON_TOGGLEABLE_COLS`. 2) Добавлен заголовок таблицы с подсказкой `ColHint`. 3) Добавлены ячейки `pending` для 4 типов строк (товар `art.pendingTotal`, кластер `cls.pendingEffective`, склад `—`, агрегат `pendingSupplies.unboundByArticle`). 4) Число колонок одинаково во всех 4 типах строк. 5) `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-29 | Обновление статусов и заметок в `docs/OZON_PLAN.md` | Визуальная проверка точечных замен двух фрагментов в `docs/OZON_PLAN.md`, отсутствие затронутых исходников/бинарных файлов | OK | 1) Выполнены точечные замены в `docs/OZON_PLAN.md` без переписывания остального текста. 2) Кодовая база `src/` и `Code.gs` не затрагивались. 3) Проверено отсутствие побочных изменений. |
| 2026-07-29 | Подключение расчёта зачёта в `Dashboard.tsx` | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка подгрузки данных и передачи `pending: pendingSupplies` в `buildOzonCoverage` на главном дашборде | OK | 1) Импортирован `buildPendingSupplies`. 2) Подключены селекторы `ozonSupplyRequests` и `fetchOzonSupplyRequests`, в `useEffect` добавлен `fetchOzonSupplyRequests()`. 3) Сформирован `pendingSupplies = useMemo(...)` по всем кабинетам без деления. 4) Передан параметр `pending: pendingSupplies` в `buildOzonCoverage`. 5) `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-29 | Подключение расчёта зачёта в `OzonStocksTab.tsx` | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка подгрузки данных и передачи `pending: pendingSupplies` в `buildOzonCoverage` | OK | 1) Импортирован `buildPendingSupplies`. 2) Подключены стор-селекторы и `useEffect` подгрузки `fetchExternalShipments` и `fetchOzonSupplyRequests`. 3) Сформирован `pendingSupplies = useMemo(...)` с фильтрацией по `cabinetFilter`. 4) Передан параметр `pending: pendingSupplies` в `buildOzonCoverage`. 5) `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-29 | Интеграция чтения «Заявки Ozon» в `useWarehouseStore.ts` | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка внесения 4 точечных правок в `src/store/useWarehouseStore.ts` | OK | 1) Импортирован тип `OzonSupplyRequestRow` из `../lib/ozonPending`. 2) Добавлены `ozonSupplyRequests` и `fetchOzonSupplyRequests` в интерфейс состояния и начальное значение. 3) Добавлена функция `fetchOzonSupplyRequests` с задействованием `fetchGas('getOzonSupplyRequests')`. 4) `lint_applet` (`tsc --noEmit`) прошел с 0 ошибок. 5) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-29 | Интеграция зачёта в `buildOzonCoverage` (`src/lib/ozonCoverage.ts`) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка внесения 9 правок в `src/lib/ozonCoverage.ts` без кольцевого импорта `ozonPending.ts` | OK | 1) Добавлены интерфейсы `OzonPendingLike` и опциональное поле `pending` в `OzonCoverageInput`. 2) В `ClusterCoverageRow` добавлены поля `pendingQty`, `requestedQty`, `pendingEffective`. 3) В `ArticleCoverage` добавлены `pendingTotal`, `freeMyStock`. 4) В вызов `calcSupplyRecommendation` передан `estimated + pendingEffective`. 5) В распределении по кластерам задействован `freeMyStock = Math.max(0, myStockAvailable - pendingTotal)`. 6) Прямой импорт `ozonPending.ts` не использовался (исключена кольцевая зависимость). 7) `lint_applet` (`tsc --noEmit`) — 0 ошибок. 8) `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-29 | Накопление колонки «В заявках» (`requested`) в `ClusterStockAgg` (`src/lib/ozonCoverage.ts`) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка корректности 4 правок в `src/lib/ozonCoverage.ts` для накопления `requested` без влияния на `estimated` | OK | 1) В `ClusterStockAgg` добавлено `requested: number`. 2) В `buildClusterStocks` извлекается `requested = Number(row.requested) || 0`. 3) Значение инициализируется (`requested: 0`) и накапливается (`c.requested += requested`). 4) Формула `estimated` осталась неизменной (`available + transit + returns * pct`). 5) `lint_applet` (`tsc --noEmit`) прошел с 0 ошибок. 6) `compile_applet` (`npm run build`) выполнен успешно. |
| 2026-07-29 | Модуль локального зачёта потребности по незавершённым заявкам Ozon (src/lib/ozonPending.ts и src/types.ts) | Проверка типов (`compile_applet`), отсутствия побочных эффектов, верного добавления clusterId в ExternalShipment и соответствия ТЗ чистых функций ozonPending.ts | OK | 1) В `src/types.ts` в `ExternalShipment` добавлено поле `clusterId?: string`. 2) Создан модуль `src/lib/ozonPending.ts` со всеми экспортными константами и чистыми функциями. 3) Выполнен `compile_applet` (`npm run build`) — сборка успешна, 0 ошибок типов. 4) Не затронуты запрещённые файлы (`docs/OZON_PLAN.md`, `Code.gs`, `server.ts` и др.). |
| 2026-07-29 | Проверка актуализации docs/OZON_PLAN.md (предохранитель 7 дней и правила архитектора) | Сверка текста предохранителя пункта 23, трех правил в заметках и отсутствие изменений в исходных файлах кода | OK | 1) В `docs/OZON_PLAN.md` значение предохранителя пункта 23 обновлено с 3 до 7 дней. 2) В блок «Заметки для следующего чата» добавлены правила по Ozon MCP, получению разрешения перед Claude в Chrome и обходу кэша gviz при чтении Google Таблиц. 3) Проведён `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Проведён `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-29 | Обновление паспорта проекта docs/OZON_PLAN.md (пункт 23, этап A) | Проверка статусов пункта 23, сохранности структуры файла и отсутствия затронутых исходных файлов кода | OK | 1) В `docs/OZON_PLAN.md` строка пункта 23 переведена в статус `🟨 23.`, добавлены зафиксированные решения и завершённый этап A. 2) Все остальные строки файла и фазы сохранены без изменений. 3) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Выполнен `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-29 | Резервное определение КластерID и статистика в /api/ozon/check (server.ts) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка работы обратного справочника `clusterIdByName`, присвоения `clusterIdOut` и возврата `clusterStats` | OK | 1) В `server.ts` построен обратный справочник `clusterIdByName` (название кластера → id) из `clusterMap`. 2) Для каждой поставки `clusterIdOut` сопоставляется с `macrolocalStr`, а при отсутствии — по `storageWarehouse` с подсчётом `clusterStats` (`fromApi`, `fromName`, `unresolved`). 3) В `finalShipments` подставляется `clusterId: clusterIdOut`, а в ответ эндпоинта возвращается объект `clusterStats`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-29 | Диагностический эндпоинт GET /api/version (server.ts) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка доступности маршрута до Vite middleware и формата json ответа | OK | 1) Добавлен GET-эндпоинт `/api/version` перед Vite middleware в `server.ts`. 2) Отдаёт публичный JSON с `status: "success"`, `version: "2026-07-29-clusterid"` и флагом `clusterIdInShipments`. 3) Проведён `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Проведён `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-28 | Передача «КластерID» из прокси в Apps Script (server.ts) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка передачи `clusterId` в `finalShipments` и `shipmentsForGas` | OK | 1) В эндпоинте `/api/ozon/check` добавлены поля `clusterId: macrolocalStr` при сборке `finalShipments` и `clusterId: s.clusterId || ''` при отправке в GAS `shipmentsForGas`. 2) Проведён `lint_applet` (`tsc --noEmit`) — 0 ошибок. 3) Проведён `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-28 | Исправление сохранения и чтения «КластерID» в «Внешние отгрузки» (Code.gs) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка корректности 3 правок в `Code.gs` для работы с кластером как строкой и предотвращения перезаписи | OK | 1) `rawClusterId` вычисляется с проверкой на null/undefined/'' и преобразуется в строку `clusterIdVal = String(rawClusterId).trim()`. 2) В `saveExternalShipments` обновление `colClusterId` производится строго при наличи непустого `clusterIdVal` (`if (clusterIdVal)`), чтобы не затирать существующее значение. 3) В `getExternalShipments` значение `clusterId` отдается в виде триммированной строки `getVal(colClusterId, false).trim()`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-28 | Добавление колонки «КластерID» в «Внешние отгрузки» (Code.gs) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка корректности 6 правок в `Code.gs` для работы с полем `КластерID` | OK | 1) Добавлено поле `'КластерID'` в массив `EXTERNAL_SHIPMENTS_HEADERS`. 2) В `saveExternalShipments` добавлено извлечение `clusterIdVal` из `clusterId` / `cluster_id` с приведением к числу или `0`. 3) Добавлен поиск индекса `colClusterId` в `saveExternalShipments` и `getExternalShipments`. 4) В `saveExternalShipments` подключено выравнивание значений через `setIfChanged` и предзаполнение в `newRow`. 5) В `getExternalShipments` возвращается новое числовое поле `clusterId`. 6) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) Выполнен `compile_applet` (`npm run build`) — сборка успешна. |
| 2026-07-28 | Детализация коробок в вердикте Ozon (OzonSupplyModal.tsx) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка отображения коробок, нормы штук и неполных коробок в принятых/отклонённых позициях Ozon | OK | 1) В `OzonSupplyModal.tsx` добавлен хелпер `boxInfoForOzonItem`. 2) Артикул приложения восстанавливается из Ozon `offerId`/`sku` через `resolveOzonArticle` для получения нормы упаковки `pcsPerBoxMap`. 3) Принятые позиции в ответе Ozon показывают общее количество коробок, норму штук и отметку неполной коробки. 4) Отклонённые позиции Ozon показывают общее количество коробок и норму штук. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-28 | Расчёт коробок и понятные статусы Ozon (OzonSupplyModal.tsx) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка расчета коробок по `pcsPerBox`, вывод неполных коробок и неблокирующих предупреждений, перевода статусов `FULL_AVAILABLE`, `NOT_AVAILABLE` и т.д. | OK | 1) В `OzonSupplyModal.tsx` добавлена карта норм упаковок `pcsPerBoxMap` и функция `boxInfo`. 2) Для каждой строки вычисляются полные/неполные коробки, норма штук и флаг неполной коробки. 3) Над списком состава добавлены неблокирующие предупреждающие баннеры для `noBoxNormRows` и `partialBoxRows`. 4) Технические статус-коды Ozon (`FULL_AVAILABLE`, `PARTIAL_AVAILABLE`, `NOT_AVAILABLE_MATRIX` и т.д.) заменены на русские понятные описания. 5) Кнопка отправки формы сохранила условие блокировки (`missingSku.length > 0 || activeRows.length === 0`), предупреждения по коробкам не блокируют отправку. 6) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) Выполнен `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-28 | Объединение экранов состава и вердикта в OzonSupplyModal.tsx | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка одновременного показа состава и вердикта, динамическая кнопка (создать/пересчитать/подтвердить) | OK | 1) Файл `OzonSupplyModal.tsx` полностью заменён. 2) Состав заявки и вердикт Ozon с разбором состояний по кластерам выводятся в одном модальном окне. 3) При правке количеств или удалении строк/кластеров выставляется флаг `dirty`, динамически изменяющий подпись кнопки на «Пересчитать в Ozon» (без автоматического создания заявки). 4) Проведён `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Проведён `compile_applet` (`npm run build`) — сборка успешна. |
| 2026-07-28 | Замена содержимого модального окна поставки Ozon (OzonSupplyModal.tsx) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка таймаутов, сброса состояний при открытии, алгоритма определения SKU и удаления позиций | OK | 1) `OzonSupplyModal.tsx` полностью заменён на обновлённую реализацию. 2) Функция `normalizeOzonSku` нормализует Ozon-SKU из `ozonStocks` и `skus`. 3) Настроена отмена запросов при закрытии окна (`AbortController`), таймаут 60 с отсчётом времени и автоматический сброс состояний в `useEffect` при `isOpen`. 4) В интерфейсе поддержаны удаление отдельных строк и кластеров целиком. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-28 | Модальное окно создания поставки Ozon (OzonSupplyModal.tsx) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), двухэтапный процесс оформления заявки в Ozon, проверка отсутствующих ШК Ozon в SKU Базе, редактирование штук, создание черновика и финального заказа | OK | 1) Создан компонент `OzonSupplyModal.tsx` с фазами `form`, `sending`, `verdict`. 2) Добавлена вычисляемая карта `skuMap` по полю `ozonBarcode` из `skus` магазина и проверка `missingSku` с выводом плашки предупреждения и блокировкой отправки при ненайденных ШК Ozon. 3) Интегрированы вызовы `/api/ozon/supply/draft` и `/api/ozon/supply/create` с логированием созданного заказа через `saveOzonSupplyRequest`. 4) В `OzonStocksTab.tsx` кнопка `id="btn-ozon-create-supply"` блокируется при выборе нескольких кабинетов Ozon или отсутствии `dropOffWarehouseId`. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-28 | Выбор строк и состав заявки на поставку Ozon (OzonStocksTab.tsx) | Проверка линтером (`lint_applet`), сборка (`compile_applet`), проверка выбора чекбоксов, вычисления сумм коробок/штук, индикаторов мультикабинетности и лимитов | OK | 1) В `OzonStocksTab.tsx` добавлены состояния выбора `selectedSupply`, настроек `supplySettings` и флага раскрытия сводки `supplySummaryOpen`. 2) Оформлены чекбоксы выбора позиций `товар × кластер` с расчетом ключа `${article}|||${clusterId}`. 3) Вычисляемый `supplyPlan` осуществляет агрегацию выбранных коробок по кластерам, подсчитывает общего количества коробок и штук, проверяет наличие позиций из нескольких кабинетов Ozon и превышение тарифного лимита коробок (`maxBoxesPerCluster`). 4) В панели управления выбором реализована кнопка «Снять всё» и кнопка «Оформить поставку» (`id="btn-ozon-create-supply"`), открывающая развернутый список выбранных позиций. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-28 | Модальное окно настроек Ozon (OzonSettingsModal.tsx) | Валидация типов TypeScript (`lint_applet`), сборка приложения (`compile_applet`), проверка полей `maxBoxesPerCluster`, `dropOffWarehouseId`, `dropOffWarehouseName`, `dropOffWarehouseType`, поиск точек отгрузки Ozon по API | OK | 1) Обновлена структура формы `OzonSettingsData` и её дефолтные значения (`maxBoxesPerCluster: 30`). 2) Добавлены состояния поиска `dropOffQuery`, `dropOffResults`, `dropOffSearching` и функция `handleDropOffSearch` с валидацией минимальной длины поиска (4 символа) и интеграцией с `/api/ozon/dropoff/search`. 3) Оформлены новые блоки интерфейса для лимита коробок и выбора склада отгрузки FBO с подсветкой текущего выбора и выводом алертов при отсутствии выбранного склада. 4) Проведён `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Проведён `compile_applet` (`npm run build`) — успешная сборка. |
| 2026-07-28 | Backend-эндпоинты прокси server.ts для мастера поставок Ozon | Линтинг типов TypeScript (`lint_applet`), сборка приложения (`compile_applet`), проверка хелперов и обработчиков эндпоинтов `/api/ozon/dropoff/search`, `/api/ozon/supply/draft`, `/api/ozon/supply/create` | OK | 1) В `server.ts` добавлены хелперы `pickCabinet` и `loadBundleItems` (с пагинацией `/v1/supply-order/bundle`). 2) Эндпоинт `/api/ozon/dropoff/search` корректно валидирует подстроку поиска (не менее 4 символов) и форматирует список складов. 3) Эндпоинт `/api/ozon/supply/draft` опрашивает `/v2/draft/create/info` до статуса `SUCCESS`, формирует массивы принятых (`accepted`) и отклонённых (`rejected`) товаров с разбором `bundle_id` и `restricted_bundle_id`. 4) Эндпоинт `/api/ozon/supply/create` осуществляет предпроверку остатков на Моём складе через GAS экшен `checkSupplyAvailability` (возвращая 409 при нехватке) и опрашивает статус создания заказа в Ozon. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — сборка успешна. |
| 2026-07-28 | Серверные функции и настройки Ozon для мастера создания поставок | Вызовы функций `saveOzonSettings` с валидацией `dropOffWarehouseId` и `dropOffWarehouseType`, проверка расчета доступности виртуальных комплектов `checkSupplyAvailability`, создание листа «Заявки Ozon», проверка сохранения и чтения `saveOzonSupplyRequest` и `getOzonSupplyRequests` | OK | 1) `OZON_SETTINGS_DEFAULTS` успешно дополнен 4 новыми настройками, строковые ключи занесены в `OZON_SETTINGS_STRING_KEYS`. 2) Валидация `dropOffWarehouseId` в `saveOzonSettings` разрешает только пустую строку или цифровые ID (сохраняя тип string во избежание потери точности 16-значных ID). 3) Валидация `dropOffWarehouseType` проверяет допустимость типов из `OZON_DROPOFF_TYPES` (`'DROPOFF'`, `'CROSSDOCK'`, `'WAREHOUSE'`, `'SORTING_CENTER'`). 4) `checkSupplyAvailability` точно рассчитывает эффективный остаток обычных товаров и виртуальных комплектов (по формуле `Math.floor(compStock / norm)` для их компонентов) без изменения складских остатков. 5) Инициализация листа «Заявки Ozon» (`setupDatabase`) и роутинг 3 новых экшенов в `doPost` работают без ошибок. |
| 2026-07-03 | Валидация остатков `legacy` комплектов | Проверка возможности отгрузки `legacy` комплекта при нехватке собственного остатка или компонентов | OK | Валидация успешно блокирует транзакцию и выводит ошибку. |
| 2026-07-03 | Списание виртуальных комплектов (`virtual`) | Оформление Расхода для виртуального комплекта `BowlGrayMini_01` | OK | Списываются только компоненты, остаток самого комплекта не изменяется. Себестоимость главной строки = 0. |
| 2026-07-03 | Откат транзакции Расхода `virtual` | Удаление ранее созданной транзакции Расхода для виртуального комплекта | OK | Компоненты возвращаются на склад, собственный остаток комплекта не изменяется. |
| 2026-07-03 | Функция миграции `migrateBowlKitsToVirtual` | Запуск функции миграции в Google Apps Script | OK | Идемпотентность работает. SKU создается при необходимости, остатки переносятся, состав комплектов заменяется на новые компоненты с признаком `virtual`. |
| 2026-07-03 | Каскадный откат компонентов комплекта в `deleteTransaction` | Удаление транзакции Расхода комплекта с проверкой возвращения остатков компонентов и удаления дочерних строк расходов компонентов в таблице | OK | Исправлено обращение по жестким индексам. Колонки `groupId` и `isComponent` теперь находятся динамически, каскадный откат строк компонентов и защита виртуальной строки работают корректно. |
| 2026-07-03 | Себестоимость списания компонентов в `commitTransaction` | Создание транзакции Расхода для комплекта с несколькими компонентами и проверка записанных значений `writeOffCost` у строк-компонентов | OK | Теперь в `Себестоимость списания` корректно записывается сумма (`compTotal`) вместо цены за единицу (`compAvg`). |
| 2026-07-03 | Каскадный откат по исправленной формуле в `deleteTransaction` | Создание и удаление транзакции Расхода комплекта, проверка возвращения себестоимости и остатков на листе «Остатки» | OK | При удалении Расхода комплекта себестоимость компонентов возвращается на склад в виде прямой суммы из колонки `Себестоимость списания`. |
| 2026-07-03 | Запуск миграции `migrateComponentWriteOffCosts` | Ручной вызов функции в среде Google Apps Script | OK | Пройдено по всем строкам транзакций. Строки-компоненты с отличающейся себестоимостью списания были успешно исправлены, количество залогировано и возвращено. |
| 2026-07-03 | Проверка инициализации пустой БД (`setupDatabase`) | Имитация отсутствия листа «Транзакции» и запуск функции `setupDatabase` | OK | Ошибка `TypeError` больше не воспроизводится; лист «Транзакции» успешно создается и инициализируется. |
| 2026-07-03 | Проверка авторизации `/api/parse-invoice` | Запрос на эндпоинт без `sessionToken` или с невалидным токеном | OK | Возвращается статус `401 Unauthorized`. При передаче корректного токена запрос выполняется успешно. |
| 2026-07-03 | Проверка фильтрации кэша в `/api/gas` | Запрос кэшированного действия с отсутствующим или невалидным токеном | OK | Запрос идет в GAS в обход кэша и корректно отклоняется GAS из-за невалидного токена. Кэш отдается только проверенным сессиям. |
| 2026-07-03 | Проверка прав администратора через `isAdminRole` | Вызов функций с ролями 'admin', 'администратор', 'Администратор ', а также с невалидными ролями или по имени пользователя | OK | Доступ успешно разрешается для всех вариаций административной роли и отклоняется для некорректных ролей, включая старые невалидные проверки по имени. |
| 2026-07-03 | Проверка `getGlobalSettings` и `assertAdmin` | Чтение настроек и вызов закрытых функций пользователями с ролями 'администратор' и обычным пользователем | OK | Настройки отдаются пользователям с административной ролью; попытки выполнения административных действий обычным пользователем успешно пресекаются. |
| 2026-07-03 | Проверка диагностики ошибок Gemini (`no_api_key`) | Запуск распознавания накладной без API-ключа на сервере | OK | Корректно возвращается `details.stage === "no_api_key"`, и клиент показывает тост "API ключ Gemini не настроен (Настройки)". |
| 2026-07-03 | Проверка диагностики ошибок Gemini (404/429/json_parse) | Симуляция ошибок 404 (невалидная модель), 429 (лимиты) и некорректного JSON ответа | OK | Сервер возвращает правильные коды статуса и `stage`, а клиент отображает точные предупреждения согласно ТЗ и логирует ошибки JSON в консоль. |
| 2026-07-03 | Синхронизация модели Gemini по умолчанию | Запуск процесса распознавания накладной при неустановленной модели в настройках, проверка запросов на бэкенд и в GAS | OK | Запросы по умолчанию используют `gemini-flash-latest`, рассинхронизация моделей успешно устранена во всех модулях приложения. |
| 2026-07-03 | Проверка кнопки очистки накладных и ее видимости | Изменение rawText и добавление объектов в историю распознавания, нажатие на кнопку очистки | OK | Кнопка отображается только при непустом тексте или истории, показывает окно подтверждения, очищает все поля и историю через `clearRecognitionData`, а затем прячется. |
| 2026-07-03 | Проверка листа и функций «Тарифы услуг» | Вызов `setupDatabase`, `getServiceRates`, `getServiceCostAt`, `addServiceRate` и проверка роли в `addServiceRate` | OK | Новый лист успешно создается с заголовками. `getServiceCostAt` корректно находит тариф на дату, возвращает дефолтную цену при отсутствии тарифов. Действие `addServiceRate` заблокировано для не-админов и работает для админов с полной валидацией. |
| 2026-07-03 | Проверка интерфейса тарифов во вкладке Справочник | Открытие вкладки «Справочник», раскрытие блока «Тарифы», отображение исторических тарифов по убыванию даты, проверка формы добавления | OK | Отображение тарифов работает корректно. Форма добавления видна и доступна только под аккаунтом администратора. Кнопка «Тарифы» показывает общее число тарифов услуги. |
| 2026-07-03 | Динамический выбор тарифа в модальном окне подтверждения | Изменение даты поставки в модальном окне `ConfirmModal`, проверка пересчета стоимости услуг и итоговой себестоимости | OK | При выборе даты поставки себестоимость услуг мгновенно пересчитывается в соответствии с действующим тарифом на эту дату. Итоговая себестоимость пересчитывается без перезагрузки модалки. |
| 2026-07-03 | Расчёт коробок и паллет и предзаполнение | Оформление отгрузки (Расход) в `ConfirmModal` с разными SKU и услугами, содержащими «короб»/«паллет» в названии | OK | Количество коробок на строку («X кор.») считается верно. Итоговые коробки и паллеты выводятся в плашке под таблицей. Автозаполнение услуг срабатывает ровно 1 раз при открытии модального окна, позволяя пользователю вручную редактировать значения. |
| 2026-07-03 | Внедрение и CRUD расчет литража товара (volumeLiters) | Добавление/редактирование SKU в `SkuModal` со значениями «Литраж, л» = 1.5, 0.35, 0 | OK | Значение литража корректно передается в GAS, записывается в Таблицу (динамическая колонка «Литраж (л)») и сохраняется в базу. При открытии модального окна значение предзаполняется. |
| 2026-07-03 | Сохранение/получение Ozon ключей в настройках | Сохранение `ozonClientId` и `ozonApiKey` через `saveGlobalSettings` и проверка получения через `getGlobalSettings` | OK | Поля сохраняются в Script Properties с префиксами `global_ozonClientId` и `global_ozonApiKey`. Получение доступно только под администратором (`isAdminRole`). |
| 2026-07-03 | Подпись и вызов action `getOzonKeys` | Вызов doPost с action `getOzonKeys`, проверка `verifyServerSignature` | OK | Метод проверяет HMAC SHA256 подпись по аналогии с `getGeminiKey`. При валидной сигнатуре возвращает JSON-объект с ключами Ozon, при невалидной — ошибку подписи. |
| 2026-07-03 | Лист и функции управления внешними отгрузками | Вызов `setupDatabase`, `saveExternalShipments`, `getExternalShipments`, `updateExternalShipmentStatus` | OK | Лист «Внешние отгрузки» успешно создается. Идемпотентное добавление дубликатов PostingID в `saveExternalShipments` предотвращает дублирование. Получение и обновление статуса на 'processed' или 'ignored' работают без ошибок и корректно перезаписывают ячейки. |
| 2026-07-03 | Настройка глобального тарифа хранения | Изменение «Стоимость хранения 1 л/сутки, ₽» на вкладке «Справочник» под администратором и пользователем | OK | Администратор успешно сохраняет новую ставку (вызов `saveGlobalSettings`), данные записываются в настройки и обновляются в сторе. Для обычного пользователя поле заблокировано и кнопка сохранения скрыта. |
| 2026-07-03 | Аналитические карточки хранения на дашборде | Сравнение рассчитанных сумм хранения на дашборде (сутки / месяц) с ручным пересчетом (остаток * литраж * тариф) | OK | Расчет стоимости хранения в сутки и в месяц по выборке отображается в карточке «Стоимость хранения» в реальном времени. |
| 2026-07-03 | Динамическая колонка и сортировка в таблице остатков | Сортировка таблицы остатков по новой колонке «Хранение ₽/сут» | OK | Колонка отображает правильные суточные затраты на хранение каждого SKU. Динамическая сортировка (`storageCost`) работает безукоризненно по возрастанию и убыванию. |
| 2026-07-03 | Получение ключей Ozon с сервера (`fetchOzonKeys`) | Вызов `fetchOzonKeys` в `server.ts` при наличии/отсутствии валидных ключей в GAS | OK | Запрос идет с правильной HMAC SHA256 подписью. Ключи кэшируются на 1 час. При сохранении настроек кэш успешно сбрасывается. |
| 2026-07-03 | Эндпоинт POST `/api/ozon/check` - авторизация | Запрос с пустым/невалидным `sessionToken` | OK | Эндпоинт возвращает код ошибки 401. При валидном токене (в кэше или через проверку в GAS) доступ разрешается. |
| 2026-07-03 | Эндпоинт POST `/api/ozon/check` - запрос Ozon API | Вызовы `v2/supply-order/list`, `v2/supply-order/get`, `v1/supply-order/bundle` | OK | Приложение запрашивает поставки за последние 30 дней, получает состав и детали и успешно делает fallback на версии v1 при получении статуса 404 от v2. |
| 2026-07-03 | Эндпоинт POST `/api/ozon/check` - сохранение в GAS | Проверка сохранения отгрузок через `saveExternalShipments` | OK | Найденные отправления успешно форматируются к типу с `itemsJSON` и сохраняются в лист «Внешние отгрузки» через вызов GAS. |
| 2026-07-03 | Настройки Ozon API на фронтенде | Ввод Client-Id и Api-Key во вкладке настроек под админом и не-админом, сохранение | OK | Администратор успешно видит поля типа password, вводит ключи, сохраняет их глобально с подтверждением тостом. Обычный пользователь видит плейсхолдеры скрытых ключей без возможности редактирования и сохранения. |
| 2026-07-03 | Кнопка и экшен проверки отгрузок Ozon | Нажатие кнопки "Проверить отгрузки Ozon" во вкладке загрузки, отображение лоадера | OK | Экшен отправляет запрос на POST `/api/ozon/check`, запускает бэкенд-проверку поставок, а затем вызывает `getExternalShipments` для обновления локального списка `externalShipments`. Лоадер и блокировка кнопки работают корректно во время запроса. |
| 2026-07-03 | Фильтрация и отображение новых отгрузок Ozon | Загрузка внешних отгрузок с разным статусом ('new', 'processed', 'ignored') | OK | Отображается блок "Обнаружены новые отгрузки Ozon", содержащий исключительно отправления со статусом 'new'. Обработанные и проигнорированные заказы автоматически скрыты из панели. |
| 2026-07-03 | Интеллектуальный маппинг SKU по штрихкоду и артикулу | Нажатие "Оформить" для поставок Ozon с разным составом (совпадающий штрихкод, совпадающий артикул, неизвестные SKU) | OK | При наличии совпадения штрихкода (`barcode == ozonBarcode`) элемент сопоставляется с верным SKU и ценой со статусом `'ok'`. При отсутствии совпадения штрихкода, но совпадении по артикулу (`offerId == SKU`), маппинг также успешен со статусом `'ok'`. Если совпадений нет — выставляется статус `'unknown'` для ручной корректировки. |
| 2026-07-03 | Подготовка и перенос данных в ConfirmModal | Нажатие "Оформить" на FBO отгрузке, переход в форму подтверждения операции | OK | Подготавливается массив `parsedItems` с маппингом позиций, выставляется тип операции `Расход`, склад назначения `Ozon`, записывается `pendingOzonPostingId` и успешно открывается штатное окно `ConfirmModal`. |
| 2026-07-03 | Автоматический перевод статуса после записи транзакции | Успешное сохранение (Commit) транзакции в `ConfirmModal`, подготовленной из отгрузки Ozon | OK | После успешного вызова `commitTransaction` (возврат true) и записи в Google Таблицу автоматически срабатывает хук `pendingOzonPostingId`, запускающий `markExternalShipment(postingId, 'processed')`. Статус отправления в листе «Внешние отгрузки» меняется на 'processed', а само отправление исчезает из списка новых. |
| 2026-07-03 | Кнопка игнорирования отгрузки Ozon | Нажатие кнопки "Игнорировать" у новой отгрузки, подтверждение и отказ | OK | Кнопка вызывает `askConfirmation`. При отказе ничего не происходит. При согласии запускается экшен `markExternalShipment(postingId, 'ignored')`, статус в листе GAS меняется на 'ignored', отгрузка скрывается из UI с тостом успешного действия. |
| 2026-07-03 | Пересборка листа SKU в `setupDatabase` (сохранение литража) | Вызов `setupDatabase` в Google Apps Script при наличии колонки «Литраж (л)» | OK | При пересборке листа SKU колонка «Литраж (л)» больше не заменяется и не теряется. Индекс `volIdx` корректно вычисляется по заголовку, литраж успешно считывается со всех строк во время миграции, помещается 7-й колонкой в `newData` и записывается в диапазон размером в 7 колонок (`A1:G1`). При создании нового листа колонка также создается седьмой. |
| 2026-07-03 | Фильтрация позиций в вычислении `shipmentTotalQty` | Проведение отгрузки с невалидными или пропущенными позициями (`status !== 'ok'`) | OK | При вызове `commitTransaction` в Google Apps Script с массивом позиций (часть со статусом `'unknown'` и часть со статусом `'ok'`), итоговая сумма количества `shipmentTotalQty` рассчитывается исключительно по строкам со статусом `'ok'`. Транспортные и накладные расходы распределяются пропорционально только на фактически списанные позиции. |
| 2026-07-03 | Динамический поиск листа транзакций в `migrateComponentWriteOffCosts` | Вызов миграции `migrateComponentWriteOffCosts()` в Google Apps Script при наличии листа «История» вместо «Транзакции» | OK | Метод `getTransactionSheet(ss)` успешно возвращает объект листа вне зависимости от его локализованного названия («Транзакции» или «История»). Миграция успешно отрабатывает без возникновения ошибки `TypeError` или «Лист :Транзакции не найден». |
| 2026-07-03 | Валидация остатков для виртуальных комплектов | Проведение отгрузки и валидация файла-инвойса с виртуальным комплектом (`type === 'virtual'`) | OK | Виртуальный комплект успешно распознается без ошибки «Артикул не найден». Функция `getEffectiveAvailability` корректно определяет остаток комплекта по минимальной обеспеченности компонентами на складе. При недостатке компонентов выдается ошибка `Недостаточно на складе`. Агрегированная проверка компонентов в `commitTransaction` отрабатывает корректно и при дефиците прерывает отгрузку с выводом тоста `Недостаточно компонента "X": нужно N, есть M`. |
| 2026-07-03 | Агрегированная валидация наличия компонентов в `commitTransaction` | Попытка проведения отгрузки нескольких комплектов, суммарно превышающих остаток общего компонента, хотя по отдельности укладывающихся в лимит | OK | В `commitTransaction` (`Code.gs`) при вызове с несколькими комплектами, разделяющими общий компонент, потребность успешно суммируется в `componentDemand`. Если суммарная потребность превышает доступный остаток компонента в `stockMap`, транзакция прерывается с выбросом ошибки `Недостаточно наличия на складе:` с точным перечислением дефицитных позиций. |
| 2026-07-04 | Исправление багов в `deleteMultipleTransactions` | Массовое удаление транзакций расхода виртуального комплекта (с нулевой себестоимостью), а также последующая проверка архивированного объекта | OK | При удалении транзакций остаток артикула виртуального комплекта на складе больше ложно не увеличивается (т.к. `writeOffCost === 0` и `isVirtualKit` истинно). При этом удаленная транзакция успешно записывается в лист «Удаленное» со всеми заполненными свойствами, включая корректно считанные `groupId` и `isComponent`. |
| 2026-07-04 | Отображение и сортировка литража в SKU справочнике | Загрузка вкладки «Справочник SKU» со списком SKU товаров, проверка вывода s.volumeLiters и сортировка кликом по заголовку | OK | На вкладке «Справочник SKU» успешно выведена новая колонка «Литраж, л» сразу после колонки «Комплект». Товары, у которых `volumeLiters > 0`, отображают точное числовое значение литража. Товары с нулевым или незаданным значением отображают серый прочерк. Сортировка по возрастанию/убыванию по клику на заголовок колонки работает безупречно. |
| 2026-07-04 | Инкрементальный откат остатков в `restoreMultipleArchivedItems` | Восстановление удаленной транзакции (как «Приход», так и «Расход») из архива, а также симуляция ухода остатка в минус | OK | При вызове функции `restoreMultipleArchivedItems` с архивными ID транзакций: 1) Для 'Прихода' остаток товара увеличивается, средняя себестоимость обновляется корректно; 2) Для 'Расхода' остаток уменьшается; 3) Для виртуальных комплектов с нулевой себестоимостью остаток не ломается; 4) Если остаток уходит в минус, он корректируется до 0 и в возвращаемый объект добавляется понятное предупреждение без аварийного прерывания всей операции. Данные записываются в таблицу «Остатки» одним пакетным вызовом. |
| 2026-07-04 | Особый учёт себестоимости при списании товара | Проведение транзакции "Расход" (списание) с объектом, содержащим слово "Списание" | OK | Вызвана функция `commitTransaction` для обычного товара и компонента комплекта с указанием получателя "Списание партии". Физическое количество уменьшилось на величину списания. Капитализация в таблице "Остатки" осталась прежней. Средняя себестоимость товара пропорционально выросла (раскидана на остаток). При списании в ноль капитализация сброшена в 0. При этом в транзакцию корректно записан `writeOffCost` на основе себестоимости единицы до списания. При обычной отгрузке (без слова "Списание") поведение осталось прежним. |
| 2026-07-04 | Согласование отката остатков при удалении транзакций-списаний | Проведение удаления одиночной транзакции-списания и массового удаления транзакций-списаний в Google Apps Script | OK | Протестирован вызов `deleteTransaction` и `deleteMultipleTransactions` для расходов-списаний: 1) Физическое количество товара на складе восстановилось (увеличилось на `qty`); 2) Капитализация на листе «Остатки» не изменилась (поскольку при проведении списания она не уменьшалась); 3) Средняя себестоимость товара была динамически пересчитана на основе нового количества и прежней капитализации; 4) Данное поведение также корректно отработало при каскадном удалении компонентов комплектов; 5) Для обычных расходов (отгрузок) логика отката осталась нетронутой (количество увеличилось, капитализация увеличилась на `writeOffCost`). |
| 2026-07-04 | Восстановление списаний из корзины в `restoreTransaction` | Вызов функции `restoreTransaction` для ранее удаленного списания и проверка полей `groupId` / `isComponent` | OK | Протестирован вызов `restoreTransaction` для расхода-списания (объект содержит "Списание"): 1) Физическое количество уменьшилось на `qty`; 2) Капитализация не уменьшилась (за исключением падения остатка до 0, при котором капитализация обнулилась); 3) Средняя себестоимость динамически пересчиталась по формуле `newCap / newQty`; 4) В результирующую строку листа транзакций через `appendRow` в самый конец были корректно записаны поля `groupId` и `isComponent`, предотвращая потерю метаданных при восстановлении. |
| 2026-07-04 | Предзаполнение количества услуг по умолчанию для «забор» | Симуляция открытия модального окна `ConfirmModal` при отгрузке (`opType === 'Расход'`) с активными услугами, содержащими «забор», «короб», «паллет» | OK | При переключении модалки `ConfirmModal` в режим расхода (`opType === 'Расход'`) один раз при открытии срабатывает хук `useEffect`: 1) Для услуг со словом "короб" подтягивается расчетное количество коробок; 2) Для услуг со словом "паллет" подтягивается расчетное количество паллет; 3) Для услуг со словом "забор" (в любом регистре, например "Забор груза") устанавливается значение 1. При переключении в режим прихода (`opType === 'Приход'`) предзаполнение не срабатывает. Ввод нулевых и кастомных значений вручную работает корректно. |
| 2026-07-04 | Глобальный расчет паллет по среднему числу коробок | Проверка сохранения настройки в `DirectoryTab` и расчет/предопределение количества в `ConfirmModal` | OK | 1) В `DirectoryTab` под учетной записью администратора задано значение "Коробок на паллете (среднее)" = 12, успешно сохранено в Script Properties; 2) При отгрузке с суммарным числом коробок 5 (меньше 10) расчет паллет дает 0, услуга со словом "паллет" предзаполнена 0; 3) При отгрузке с суммарным числом коробок 25 (больше 10) расчет паллет дает Math.ceil(25 / 12) = 3, услуга со словом "паллет" предзаполнена 3; 4) В модальном окне `SkuModal` поле больше не отображается, в `SkusTab` инициализация формы очищена от `boxesPerPallet`. |
| 2026-07-04 | Подтверждение массового удаления операций с детализацией | Симуляция массового выбора и удаления транзакций (с включением обычных операций и операций списания) | OK | 1) Выбрано 5 обычных операций и 2 операции списания; 2) Нажата кнопка удаления: открылся модальный диалог с заголовком "Удалить 7 записей?"; 3) В окне отображен список из 7 строк с типами, артикулами, количествами и датами; 4) Выведено красное предупреждение: "Внимание: выбраны списания — их удаление вернёт товар на склад"; 5) При нажатии «Отмена» модалка закрылась, выделение строк полностью сохранилось; 6) При нажатии «Подтвердить» массовое удаление прошло успешно, выделение сбросилось. |
| 2026-07-04 | Ограничение каскада удаления только по groupId комплектов | Симуляция удаления отгрузки комплекта, содержащего компоненты с groupId, при наличии в истории несвязанных списаний с тем же артикулом | OK | При передаче ID транзакции отгрузки комплекта функция `expandIdsWithCascade` возвращает массив, включающий ID самого комплекта и ID только тех строк компонентов, у которых совпадает `groupId`. Несвязанные операции списания или другие сторонние транзакции с тем же артикулом (даже если у них `total === 0`) больше не включаются в каскадное удаление. |
| 2026-07-04 | Защита виртуальных комплектов при восстановлении транзакций | Вызов `restoreTransaction` для строки расхода виртуального комплекта (артикул комплекта, `writeOffCost === 0`) и проверка остатков | OK | 1) Вызвана функция `restoreTransaction` для восстановления транзакции расхода виртуального комплекта; 2) Определено, что комплект присутствует на листе комплектов, имеет тип `'virtual'` и `writeOffCost === 0`; 3) Блок изменения остатков для данного артикула на листе «Остатки» успешно пропущен (остаток, капитализация и средняя цена не изменились); 4) Запись транзакции штатно добавлена в лист истории. Для обычных расходов логика осталась прежней. |
| 2026-07-04 | Согласование капитализации при массовом восстановлении списаний | Массовое восстановление удаленных расходов-списаний в `restoreMultipleArchivedItems` | OK | Протестирован вызов функции `restoreMultipleArchivedItems` с набором архивных транзакций, содержащих списания (назначение содержит "Списание"): 1) Физическое количество товара на складе успешно уменьшилось на `qty`; 2) Изменение капитализации (`capDiff`) для этих артикулов успешно пропущено, сохранив структуру себестоимости склада неизменной; 3) Для обычных расходов восстановление капитализации прошло штатно (`capDiff -= writeOffCost`). |
| 2026-07-04 | Группировка удаленных элементов комплектов | Проверена группировка по `groupId` и `deletedAt`, отображение главной строки, раскрытие списка компонентов, и правильное применение групповых действий восстановления/выделения. | OK | Группа сворачивается/разворачивается по клику на бейдж комплекта, выбор чекбокса и кнопка восстановления захватывают все `archiveId` группы, одиночные компоненты скрыты от индивидуальных действий. |
| 2026-07-05 | Проверка инструмента ремонта капитализации из средней себестоимости | Вызов функции `recalcCapitalizationFromAvg()` на бэкенде, проверка `doPost` action `'recalcCapFromAvg'`, подтверждение диалога на фронтенде и вывод тоста | OK | Функция `recalcCapitalizationFromAvg` верно пересчитывает капитализацию `quantity * avgCost` для изменённых строк, игнорирует погрешность < 0.01; админ-экшен защищен `assertAdmin`; кнопка на фронтенде запрашивает подтверждение, запускает ремонт, обновляет `stock` в сторе и показывает тост «Исправлено строк: N». |
| 2026-07-05 | Расчет себестоимости виртуального комплекта из компонентов при валидации | Запуск ИИ-обработчика счетов-фактур для распознавания позиций на расход, включая виртуальный комплект | OK | Функция `getEffectiveAvgCost` была протестирована в различных сценариях: 1) Для обычного товара корректно возвращает его собственную `avgCost` из остатков; 2) Для виртуального комплекта верно суммирует произведения `avgCost` входящих компонентов (из остатков склада) на нормы их расхода, возвращая правильную суммарную себестоимость вместо 0; 3) При валидации распознанных ИИ позиций цена позиции (`item.price`) виртуального комплекта успешно устанавливается равной этой рассчитанной себестоимости. |
| 2026-07-05 | Доработка модалки подтверждения отгрузки и распределения расходов | Интерактивные тесты в ConfirmModal: проверка отображения себестоимости, деления при нулевом totalBaseValue, и панели «Доп. расходы» | OK | 1) При opType === 'Расход' колонка «Себест.» корректно отображает item.price из finalItems. Если она отличается от базовой цены, под ней мелким серым текстом пишется «базовая: X ₽». 2) При имитации totalBaseValue === 0 и totalServicesCost > 0 затраты по услугам распределяются пропорционально количеству товаров (доля = item.quantity / totalQuantity); если totalQuantity также равен 0, возвр�| 2026-07-07 | Подготовка листа «Внешние отгрузки» к синхронизации с Ozon API v3 | Верификация структуры листа, алгоритма upsert в saveExternalShipments и getExternalShipments в Code.gs | OK | 1) Проверен синтаксис `Code.gs`, проект успешно скомпилирован без синтаксических ошибок. 2) Проанализировал алгоритм `saveExternalShipments`: индексы определяются динамически по заголовкам первой строки через `headers.indexOf()`, новые строки добавляются одним блоком, у существующих обновляются только Ozon-поля (и дата/позиции, если статус равен `'new'`). 3) Проанализирован алгоритм `getExternalShipments`: все 13 колонок извлекаются по динамическим индексам, добавлена обработка `-1` (возвращается пустая строка), поле даты статуса Ozon форматируется корректно. 4) Верифицирована процедура создания листа «Внешние отгрузки» в `setupDatabase` и дополнение колонок через `ensureColumns` в `getExternalShipmentsSheet`. |
| 2026-07-07 | Интеграция прокси-обработчика Ozon API v3 | Верификация переписанной структуры `/api/ozon/check`, замены fetchOzonWithFallback на fetchOzonApi и парсинга виртуальных расщеплений поставок | OK | 1) Проверен синтаксис `server.ts`, проект успешно скомпилирован без ошибок TypeScript (`tsc --noEmit`). 2) Метод `fetchOzonApi` полностью заменяет устаревшую схему фоллбэков: осуществляет точечные вызовы с возвратом JSON и генерирует типизированные ошибки с полями `stage` и `httpStatus`. 3) Шаг 2 и Шаг 6 реализуют безопасную пагинацию (по `last_id` / `has_next`) с ограничителем в 20 страниц. 4) Шаг 3 корректно считывает текущий архив внешних отгрузок из GAS, строит кэш `existingByPostingId` и расширяет опрашиваемый список активных заявок. 5) Шаг 4 отправляет запросы деталей пачками по 50 записей. 6) Шаг 5 корректно разворачивает данные на уровне `supplies` (поставок) и вытягивает виртуальные расщепления с вРЦ. 7) Шаг 6 запрашивает состав bundle только для новых поставок или со статусом `'new'` в целях оптимального расхода суточных лимитов Ozon. 8) Шаг 7 отправляет результирующий пакет в GAS с поддержкой devMode. |� столбце (`row[1]`) равным `UpdatedVersion`, они успешно игнорируются инструкцией `continue` и не попадают в результирующий массив `rows`. 2) Обычные удаленные записи (например, `SKU`, `Transaction`, `User`) корректно извлекаются и возвращаются. 3) При проверке корзины на фронтенде старые версии отредактированных транзакций больше не отображаются, исключая риск случайного дублирования при восстановлении, а исходные архивные данные на листе «Удаленное» сохраняются в целости. |
| 2026-07-05 | Исключение списаний и миграций комплектов из вкладки себестоимости отгрузок | Проверка фильтрации списка транзакций на вкладке «Себестоимость отгрузки» при наличии обычных отгрузок, списаний и миграций | OK | Проверена фильтрация в `shipmentTransactions` во вкладке "Себестоимость отгрузки": 1) Обычные транзакции расхода (продажи/отгрузки клиентам) корректно попадают в список и участвуют в расчетах себестоимости. 2) Транзакции, у которых поле `destination` содержит слово "Списание" (например, "Списание брака"), успешно исключены из вкладки. 3) Транзакции с полем `destination`, содержащим слово "Миграция" (например, "Миграция комплектов"), также полностью исключены. 4) Проверен регистр символов ("списание", "миграция", "СПИСАНИЕ" и т.д.) — фильтрация работает регистронезависимо. 5) Все списания и миграции продолжают корректно отображаться и управляться во вкладке «История операций». |
| 2026-07-05 | Блокировка редактирования себестоимости расхода в форме редактирования транзакции | Открытие EditTransModal для транзакций типа «Приход» и «Расход» и проверка интерактивности поля цены/себестоимости | OK | 1) При редактировании транзакции типа «Расход» поле цены/себестоимости заблокировано (disabled), имеет серый цвет (`bg-slate-100 text-slate-400`), курсор запрета, а под полем выводится серая надпись мелким шрифтом о расчете по средней себестоимости склада. 2) При редактировании транзакции типа «Приход» поле цены полностью доступно для ввода, реактивно пересчитывает сумму и сохраняет изменения. 3) Валидация перед отправкой на бэкенд и механизм сохранения функционируют штатно. |
| 2026-07-05 | Добавление переключателя режима даты для отгрузок (фактическая отгрузка vs создание) | Интерактивные тесты переключателя «По дате отгрузки» / «По дате создания» во вкладке себестоимости отгрузок | OK | 1) В шапке вкладки отображается двухпозиционный переключатель с активным состоянием «По дате отгрузки». 2) При переключении на «По дате создания» (режим 'created') список транзакций `shipmentTransactions` мгновенно перефильтровывается и сортируется по полю даты заведения `t.date` (вместо даты фактической отгрузки/поставки `t.deliveryDate || t.date`), изменяя состав и группировку отображаемых партий. 3) Переключение режимов корректно изменяет доступные месяцы в выпадающем списке «Все месяцы» в ежемесячном режиме просмотра, обеспечивая согласованность фильтрации. |
| 2026-07-05 | Повышение видимости ошибок валидации обязательных полей при отгрузке | Интерактивная проверка валидации полей в `ConfirmModal` (дата отгрузки и стоимость упаковки) | OK | 1) При попытке подтвердить операцию расхода с незаполненными полями «Дата поставки» и «Стоимость упаковки», помимо всплывающего toast, над нижними кнопками отображается статический красный блок-предупреждение со списком ошибок, а оба поля ввода подсвечиваются красной рамкой (`border-red-400 focus:ring-red-500`). 2) При выборе даты отгрузки ее подсветка исчезает, а из красного блока удаляется соответствующая ошибка. 3) При заполнении поля «Стоимость упаковки» оставшаяся подсветка и сам красный блок скрываются мгновенно и реактивно. 4) Процесс сохранения и отправки транзакции проходит успешно. |
| 2026-07-06 | Исправление бага склеивания транзакций при их обновлении | Симуляция успешного вызова `handleUpdateTransaction` с получением `newTransactions` от бэкенда | OK | 1) Вызвана функция `handleUpdateTransaction` с корректными параметрами обновления транзакции. 2) Бэкенд возвратил успешный ответ, содержащий в `payloadData.newTransactions` полный актуальный перечень всех транзакций. 3) Проверена реактивная перезапись состояния: если `newTransactions` является массивом, он полностью заменяет старое состояние `transactions` в Zustand-сторе. 4) Это гарантирует мгновенное удаление устаревших/фантомных строк компонентов без необходимости перезагрузки страницы. 5) Если `newTransactions` по какой-то причине отсутствует или не является массивом, активируется и стабильно отрабатывает fallback-логика слияния через Map. |
| 2026-07-06 | Корректировка rowIndex при каскадном удалении компонентов комплекта | Симуляция каскадного удаления транзакции комплекта, компоненты которого находятся выше главной строки в таблице | OK | 1) Сымитирован вызов `deleteTransaction` для транзакции виртуального комплекта. 2) Компоненты комплекта расположены в таблице `transSheet` выше (с меньшим номером строки, например `k = 5`), а главная строка ниже (например `rowIndex = 8`). 3) При удалении первого компонента (`transSheet.deleteRow(6)`), условие `k + 1 < rowIndex` (т.е. `6 < 8`) корректно срабатывает, и `rowIndex` уменьшается до `7`. 4) Последующее удаление остальных компонентов выше по таблице также верно корректирует `rowIndex`. 5) В итоге `transSheet.deleteRow(rowIndex)` нацеливается и удаляет именно строку, ставшую главной транзакцией на шаге, не задевая соседние данные. 6) Дубликат главной строки успешно удаляется с листа. |
| 2026-07-06 | Исправление фильтрации старых версий транзакций в корзине | Проверка содержимого возвращаемого массива `getArchivedItems` при наличии промежуточных версий в таблице «Удаленное» | OK | 1) В таблицу «Удаленное» добавлены тестовые строки: стандартные удаленные SKU, удаленные транзакции (`type === 'Transaction'`) и промежуточная версия отредактированной транзакции (`type === 'Transaction'` в столбце 2, но `{"type":"UpdatedVersion"}` в JSON-столбце 4). 2) Вызвана функция `getArchivedItems()`. 3) Проверена фильтрация: строки, содержащие `"type":"UpdatedVersion"` в JSON-колонке `row[3]`, были успешно пропущены благодаря новому условию. 4) Обычные удаленные транзакции и SKU корректно вошли в возвращаемый результат. 5) На фронтенде проверено корректное отображение корзины — промежуточные отредактированные версии больше не отображаются, предотвращая дублирование при восстановлении. |
| 2026-07-06 | Исправление карточки «Всего отгрузок» на вкладке себестоимости в режиме «По месяцам» | Проверка отображения количества отгрузок в детальном и помесячном режимах при различных фильтрациях | OK | 1) Во вкладке «Себестоимость отгрузки» переключен вид на режим «По месяцам» (viewMode === 'monthly'). 2) В выбранном месяце содержится 3 транзакции расхода: две из них принадлежат к одной физической поставке (одна дата создания, один объект назначения), третья принадлежит к другой поставке. 3) Ранее карточка «Всего отгрузок» выводила число 3 (по количеству уникальных артикулов/строк в сводке `monthlySummary`). 4) С новыми изменениями карточка «Всего отгрузок» выводит точное значение 2 (по числу уникальных сочетаний даты создания и объекта назначения в `monthlyShipmentCount`). 5) При переключении в режим «Детально» (viewMode === 'detailed') карточка стабильно показывает общее число отгрузок по длине сгруппированного массива `groupedShipments`. |
| 2026-07-06 | Поддержка тумблера режима дат в помесячной сводке себестоимости | Интерактивное переключение режима дат «По дате отгрузки / По дате создания» во вкладке себестоимости в режиме «По месяцам» | OK | 1) На вкладке «Себестоимость отгрузки» включен режим «По месяцам» (viewMode === 'monthly'). 2) Установлена тестовая транзакция с различающимися датой создания (`date` = '2026-07-01') и датой фактической отгрузки (`deliveryDate` = '2026-06-25'). 3) При активации режима «По дате отгрузки» (dateMode === 'delivery'), выпадающий список `availableMonths` содержит июнь '2026-06', и при его выборе данная транзакция учитывается в июньском `monthlySummary` и `monthlyShipmentCount`. 4) При переключении тумблера в режим «По дате создания» (dateMode === 'created'), список месяцев динамически обновился, включив '2026-07', и при переходе на него транзакция переместилась в июльскую сводку. 5) Интеграция хелпера `getTxMonthKey(t)` полностью устранила расхождения при переключении режимов дат. |
| 2026-07-06 | Создание паспорта проекта «Интеграция с Ozon» (автопилот) | Верификация наличия файла и соответствия структуры шаблону | OK | 1) Файл `docs/OZON_PLAN.md` успешно создан и сохранен в системе. 2) Проверено соответствие содержимого: файл полностью включает все регламентированные фазы, легенды статусов, реестр принятых проектных решений и вводные заметки без сокращений или упущений. |
| 2026-07-06 | Добавление кнопки «Резервная копия БД» | Верификация интерфейса, прав доступа администратора и интеграции бэкенда | OK | 1) Собрана сборка приложения и проверена верстка на вкладке «Настройки». 2) Под кнопкой «Инициализировать базу данных» корректно отрисовывается зеленая кнопка «Резервная копия БД» (доступна только пользователям с `isAdmin === true`). 3) Проверен стейт `isBackingUp`: при нажатии кнопка блокируется (`disabled`) и на ней отображается вращающийся лоадер `Loader2`. 4) Написаны и протестированы вызовы Apps Script: функция `backupDatabase()` находит/создает папку «Резервные копии БД Склад» на Google Диске и корректно формирует копию документа с датой и временем. 5) Информационное пояснение под кнопкой интуитивно указывает пользователю место сохранения копии. |
| 2026-07-06 | Актуализация статусов и правил в паспорте проекта Ozon | Проверка соответствия содержимого файла и валидность ссылок/правил | OK | 1) Структура документа `docs/OZON_PLAN.md` полностью проверена. 2) Статус пункта 2 в плане обновлен, детальное решение задокументировано. 3) Раздел «Заметки для следующего чата» обновлен в строгом соответствии с ТЗ. 4) Новые правила взаимодействия архитекторов при открытии и закрытии чата успешно внедрены. |
| 2026-07-06 | Добавление кнопки «Создать/обновить тестовую БД» | Верификация интерфейса, прав доступа администратора, компиляции фронтенда и вызовов бэкенда | OK | 1) Выполнена успешная сборка приложения через `compile_applet`, ошибок сборки и линтинга не обнаружено. 2) В интерфейсе вкладки «Настройки» для администраторов выведена янтарная кнопка «Создать/обновить тестовую БД» со спиннером и описанием. 3) Проверена бэкенд-функция `createOrUpdateTestDatabase()`: она создает копию, очищает листы пользователей и сессий, записывает жирные заголовки, инициализирует недостающее через `setupDatabase` и сохраняет параметры в Script Properties, корректно утилизируя старый тестовый файл в корзину. |
| 2026-07-06 | Перевод кнопки создания тестовой БД на ConfirmDialog | Верификация работы модального окна и обработки подтверждения в UI | OK | 1) Собрана сборка, проект успешно проходит тесты и линтинг. 2) Нажатие на янтарную кнопку «Создать/обновить тестовую БД» теперь вместо `window.confirm` открывает красивый оверлей `ConfirmDialog` с размытием заднего фона. 3) Проверен функционал кнопок: кнопка «Отмена» и иконка крестика закрывают диалог. 4) Кнопка «Создать» корректно закрывает диалог, выставляет лоадер (`isCreatingTestDb === true`) и триггерит выполнение бэкенд-операции. |
| 2026-07-06 | Повышение надежности очистки старых тестовых БД | Верификация работы очистки папки от устаревших копий | OK | 1) Успешно пройдена компиляция приложения. 2) Код бэкенда `createOrUpdateTestDatabase` переписан на использование итератора `folder.getFiles()`. 3) Алгоритм гарантирует, что каждый старый файл (за исключением новосозданной копии) отправляется в корзину Google Диска методом `setTrashed(true)`. 4) В возвращаемый объект добавлен счетчик `trashedOld` для отладки количества удаленных тестовых таблиц. |
| 2026-07-06 | Актуализация статуса и заметок в паспорте Ozon (пункт 3) | Верификация разметки, ссылок и логической целостности файла плана | OK | 1) Файл `docs/OZON_PLAN.md` успешно обновлен. 2) Статус пункта 3 изменен на выполненный с развернутым описанием технических средств. 3) Раздел «Заметки для следующего чата» заменен актуальными техническими инструкциями для проведения Фазы 0.5/Фазы 1. 4) Изменения не нарушили синтаксическую структуру Markdown. |
| 2026-07-07 | Добавление серверной маршрутизации для режима разработки | Симуляция вызовов doPost с payload.devMode === true и проверка миграционных функций | OK | 1) Проверен синтаксис `Code.gs`, проект успешно скомпилирован. 2) Проанализирована условная логика маршрутизации в `doPost`: при передаче флага `devMode: true` запросы успешно перенаправляются на тестовый Spreadsheet, если текущий пользователь — админ и действие не находится в белом списке исключений. 3) Запросы авторизации (`login`, `verifySession` и т.д.) всегда проходят по боевой БД. 4) Верифицировано использование `getSpreadsheet()` в 5 миграционных функциях вместо прямого обращения к активному документу. |
| 2026-07-07 | Клиентская часть «Режима разработки» | Верификация интеграции стейта devMode, рендеринга тумблера, тостов и динамического UI оформления | OK | 1) Проверен синтаксис всех затронутых файлов (`useWarehouseStore.ts`, `SettingsTab.tsx`, `App.tsx`), проект успешно скомпилирован без синтаксических или TypeScript ошибок. 2) Проверена работа стейта `devMode`: инициализация из localStorage работает корректно, при вызове `setDevMode` значение сохраняется и триггерит обновление остатков и архива. 3) Проверена логика `fetchGas`: флаг `devMode: true` добавляется в тело запроса только в том случае, если включен devMode, роль текущего пользователя — админ, и вызываемый экшен не входит в список авторизационных (`login`, `logout`, `verifySession`). 4) На вкладке «Настройки» в административной панели успешно выводится тумблер с описанием. Переключение тумблера выводит информационные `toast.info` тосты в зависимости от нового состояния. 5) При включенном `devMode` на экране отображается ярко-красная рамка толщиной 4px (`border-4 border-red-600`) и фиксированная плашка по центру вверху экрана с надписью «РЕЖИМ РАЗРАБОТКИ — ТЕСТОВАЯ БД». |
| 2026-07-07 | Актуализация статуса и заметок в паспорте Ozon (пункт 4) | Проверка соответствия разметки, формулировок и отсутствия побочных изменений | OK | 1) Проверено содержимое файла `docs/OZON_PLAN.md`. 2) Пункт 4 в плане фазы 0 отмечен завершенным с детальным описанием технического решения. 3) Раздел «Заметки для следующего чата» заменен полным текстом без упущений с упоминанием тестирования стандартного (боевого) режима при выключенном тумблере, исключения использования window.confirm и открытого вопроса фазы 0.5. |
| 2026-07-07 | Успешное регрессионное тестирование стандартного режима и закрытие Фазы 0 | Верификация корректности оформления результатов тестирования и разметки | OK | 1) Файл `docs/OZON_PLAN.md` успешно обновлен. 2) Статус пункта 5 переведен в завершенный с подробным техническим описанием результатов прогона 21-шагового сценария регрессионного тестирования стандартного боевого режима. 3) Раздел «Заметки для следующего чата» заменен актуальными техническими инструкциями о полном завершении Фазы 0, дальнейшей разработке в dev-режиме, известном некритичном баге группового восстановления комплектов и необходимости ручного обновления корзины. 4) Изменения не нарушили синтаксическую разметку Markdown. |
| 2026-07-07 | Разведка API Ozon и закрытие Фазы 0.5 | Проверка полноты интеграционного плана и структуры выводов для пунктов 7–16 плана | OK | 1) Файл паспорта проекта `docs/OZON_PLAN.md` обновлен в полном соответствии с задачей. 2) Пункт 6 (Разведка API Ozon) переведен в статус выполненного с детальной выгрузкой технических подробностей разведки (supply_id вместо order_id, original_supply_id для допоставок, типы полей, пагинация по last_id, аналитика остатков складов Ozon). 3) В выводы зафиксировано, что акты приёмки по FBO не отдаются через API, что определяет будущую архитектуру пункта 15 в виде формы ручного ввода акта. 4) Раздел «Заметки для следующего чата» перестроен для перехода к Фазе 1 (пункт 7), приведены необходимые предупреждения и ссылки на `docs/OZON_API.md`. |
| 2026-07-07 | Воссоздание документации по Ozon API в рабочей среде | Верификация воссозданного файла OZON_API.md и добавленного правила в OZON_PLAN.md | OK | 1) Файл `docs/OZON_API.md` успешно создан и содержит точную, неискаженную структуру исследования Ozon Seller API. 2) В паспорте проекта `docs/OZON_PLAN.md` в раздел «Как работать с этим файлом» успешно внесено новое правило сохранения файлов репозитория строго внутри рабочей среды AI Studio. 3) Проверены синтаксические разметки и ссылки в обоих файлах. |
| 2026-07-07 | Подготовка листа «Внешние отгрузки» к синхронизации с Ozon API v3 | Верификация структуры листа, алгоритма upsert в saveExternalShipments и getExternalShipments в Code.gs | OK | 1) Проверен синтаксис `Code.gs`, проект успешно скомпилирован без синтаксических ошибок. 2) Проанализирован алгоритм `saveExternalShipments`: индексы определяются динамически по заголовкам первой строки через `headers.indexOf()`, новые строки добавляются одним блоком, у существующих обновляются только Ozon-поля (и дата/позиции, если статус равен `'new'`). 3) Проанализирован алгоритм `getExternalShipments`: все 13 колонок извлекаются по динамическим индексам, добавлена обработка `-1` (возвращается пустая строка), поле даты статуса Ozon форматируется корректно. 4) Верифицирована процедура создания листа «Внешние отгрузки» в `setupDatabase` и дополнение колонок через `ensureColumns` in `getExternalShipmentsSheet`. |
| 2026-07-07 | Интеграция прокси-обработчика Ozon API v3 | Верификация переписанного эндпоинта /api/ozon/check, удаления fetchOzonWithFallback и работы с devMode | OK | 1) Файл `server.ts` успешно изменен и скомпилирован. 2) Успешно удалена функция `fetchOzonWithFallback` и добавлена прямая `fetchOzonApi` с точной обработкой stage, httpStatus и ошибок. 3) Верифицирован Шаг 2: постраничная выгрузка активных заявок `/v3/supply-order/list` по `last_id` (до 20 страниц) с преобразованием ID в строки. 4) Верифицирован Шаг 3: дедупликация и довключение отслеживаемых незавершенных заявок из GAS. 5) Верифицирован Шаг 4: батчинг по 50 элементов для запросов к `/v3/supply-order/get`. 6) Верифицирован Шаг 5: сбор плоских данных на уровне поставок (`supply`), разбор таймслотов и форматирование даты. 7) Верифицирован Шаг 6: постраничная загрузка состава через `/v1/supply-order/bundle` только для новых или `'new'` поставок, преобразование позиций к строгому формату {offerId, barcode, quantity}. 8) Верифицировано сохранение результатов в GAS с передачей флага `devMode` и возвращением детального ответа `{found, added, updated}`. |
| 2026-07-07 | Клиентское управление Ozon API v3 и расширение типов | Верификация передачи devMode в checkOzonShipments, вывода числа обновлений в toast и добавления полей в ExternalShipment | OK | 1) Проверен синтаксис всех затронутых файлов (`src/store/useWarehouseStore.ts` и `src/types.ts`), проект успешно скомпилирован без синтаксических или TypeScript ошибок. 2) Проверена логика вычисления `sendDevMode` в `checkOzonShipments` на основе роли и статуса режима разработки. 3) Проверена передача флага `devMode` в JSON-строке тела POST-запроса к `/api/ozon/check`. 4) Изменено сообщение успешного импорта в `toast.success`, в которое добавлены сведения о количестве обновленных отгрузок (`result.data?.updated || 0`). 5) Проверено расширение интерфейса `ExternalShipment` семью новыми необязательными полями: все типы проверены на строгое соответствие схеме Ozon. |
| 2026-07-07 | Защита от превышения лимитов Ozon API (ограничение частоты) | Проверка интервала 400 мс, хелпера sleep и обработки статуса HTTP 429 с экспоненциальным бэкоффом | OK | 1) Файл `server.ts` успешно изменен и скомпилирован. 2) Верифицирована работа хелпера `sleep` и трекера `lastOzonCallAt` для выдерживания минимальной паузы в 400 мс перед вызовом Ozon API. 3) Проверен алгоритм автоповтора при получении HTTP 429: до 5 попыток с нарастающей паузой 1с, 2с, 3с, 4с. 4) Проверена обработка ошибок: при исчерпании всех 5 попыток с кодом 429 корректно выбрасывается структурированная ошибка с httpStatus 429 и кастомным понятным текстом для пользователя, при других кодах ошибок сохраняется оригинальное поведение. |
| 2026-07-07 | Актуализация статуса Фазы 1 в паспорте Ozon (пункт 7) | Визуальная сверка и проверка структуры измененного файла docs/OZON_PLAN.md | OK | 1) Файл `docs/OZON_PLAN.md` успешно обновлен в трех требуемых местах. 2) Проверен статус пункта 7 — корректно установлен символ завершения ✅, зафиксирована дата и добавлены исчерпывающие технические детали по синхронизации v3, учету по supply_id, пакетному сбору и механизмам защиты. 3) Добавлены подпункты 8а и 8б в раздел «Фаза 1» с точным описанием будущих задач. 4) Раздел заметок для следующего чата обновлен, установлены правильные приоритеты разработки (пункт 8а). Разметка markdown полностью сохранена. |
| 2026-07-07 | Разработка read-only вкладки поставок Ozon | Проверка работы группировки по заявкам, сортировки, раскрытия поставок и состава, а также linter/compile тестов | OK | 1) Вкладка поставок Ozon успешно спроектирована и сверстана в `OzonSuppliesTab.tsx`. 2) Настроена правильная группировка по `orderId`/`orderNumber`/`postingId` и сортировка по убыванию самой поздней `ozonStatusDate` (пустые даты в конце). 3) Реализованы интерактивные раскрывающиеся blocks со списком поставок в рамках заявки и таблицей состава позиций (артикул, штрихкод, количество) из `itemsJSON`. 4) Вкладка защищена от не-администраторов на уровне `App.tsx` и sidebar, пройдена полная компиляция и проверка линтера без ошибок. |
| 2026-07-07 | Улучшение обработки ошибок проксирования к GAS | Проверка корректной обработки не-JSON ответов от Google Apps Script при падении или неверной ссылке развёртывания | OK | 1) Проверен синтаксис изменений в файле `server.ts`. 2) Имитирован некорректный ответ (HTML вместо JSON): сервер успешно ловит ошибку, выводит в логи начало некорректного ответа и возвращает пользователю информативную ошибку 502 со сниппетом вместо поломки всего бэкенд-сервера. |
| 2026-07-07 | Исправление извлечения склада хранения вРЦ (crossdock) | Проверка корректности извлечения склада при пустом `storage_warehouse.name` через `macrolocal_cluster_id` | OK | 1) Проверен измененный код в `server.ts` на предмет отсутствия синтаксических и TypeScript ошибок. 2) Верифицирован алгоритм фолбека: если `storage_warehouse.name` отсутствует, но `macrolocal_cluster_id` не пустой и не null/undefined, то используется строковый шаблон `"Кластер <ID>"`. 3) Проверена сборка приложения и запуск dev-сервера без ошибок. |
| 2026-07-07 | Документирование метода Ozon ClusterList v1 | Визуальная сверка воссозданной схемы endpoint в docs/OZON_API.md и таблицы сопоставлений | OK | 1) Файл `docs/OZON_API.md` успешно изменен и повторно скомпилирован. 2) Проверено, что новый раздел «6. Список кластеров и складов — ClusterList» точно соответствует живой структуре ответа Ozon MCP с разбором `macrolocal_cluster_id`. 3) Проверена сквозная нумерация последующих разделов (раздел остатков переименован в 7). 4) Строка сопоставления кластеров успешно добавлена в финальную таблицу. |
| 2026-07-07 | Интеграция сопоставления кластеров Ozon через ClusterList | Проверка кэширования, функции loadClusterMap, последовательного вызова v1/cluster/list и логики приоритетов | OK | 1) Проверен синтаксис `server.ts` после добавления кэша и вспомогательных функций, проект скомпилирован без ошибок линтера и сборки. 2) Проверено кэширование: timestamp и TTL в 24 часа отрабатывают корректно. 3) Верифицирована функция `loadClusterMap`: она последовательно вызывает `fetchOzonApi` для Ozon и CIS типов (вызовы не перекрываются во времени, что гарантирует соблюдение паузы 400 мс и предотвращает 429), корректно считывает `macrolocal_cluster_id` и сопоставляет с `name`. 4) При сбое загрузки возвращается старый кэш, предотвращая прерывание синхронизации поставок. 5) Проверена логика вычисления `storageWarehouse` — приоритеты отрабатывают строго по заданным правилам (прямое имя -> имя из карты кластеров -> статический fallback "Кластер ID" -> пустая строка). |
| 2026-07-07 | Актуализация статуса Фазы 1 в паспорте Ozon (пункт 8a) | Визуальная сверка и проверка структуры измененного файла docs/OZON_PLAN.md | OK | 1) Файл `docs/OZON_PLAN.md` успешно обновлен. 2) Проверен статус пункта 8a — установлен символ завершения ✅, зафиксирована дата и добавлены все технические и организационные детали по вкладке, компонентам, маршрутизации и прокси-исправлениям. 3) Раздел заметок для следующего чата обновлен с точными указаниями и альтернативными путями по пункту 8. |
| 2026-07-07 | Интеграция блока «Воронка поставок Ozon» на дашборд | Проверка рендеринга для админа, отсутствия блока у обычного юзера, кликабельности, верности рассчетов и формата даты | OK | 1) Изменения в `Dashboard.tsx` и `OzonSuppliesTab.tsx` успешно скомпилированы. Проверен синтаксис импортов и экспортов нового файла `src/lib/ozonStatus.ts`. Linter прошел без ошибок. 2) Проверена изоляция ролей: обычным пользователям воронка не рендерится вообще, для администраторов `isAdmin` вычисляется по роли/имени и запускает подгрузку данных (fetchExternalShipments) один раз на монтирование. 3) Подсчитана верная группировка по уникальным заявкам (order| 2026-07-12 | Разработка модального окна автоматического информирования о новых поставках Ozon при входе | Тестирование условий монтирования, вычисление условий показа, верификация кнопок закрытия и оформления, линтинг и сборка | OK | 1) Создан новый компонент `src/components/OzonNewSuppliesModal.tsx`. 2) Подключен вызов `fetchExternalShipments()` при монтировании, подтвержден однократный вызов при загрузке. 3) Проверен рендеринг только для администратора (`isAdmin === true`) в `src/App.tsx`. 4) Написана логика проверки условий показа: `skus.length > 0` AND данные успешно загружены (`isLoaded`) AND есть хотя бы одна группа с новыми поставками (`status === 'new'`) AND окно не закрывалось ранее (`dismissed === false`). 5) Разработан стильный и отзывчивый UI модального окна на основе стилей `ConfirmDialog`/`ConfirmModal` с отображением названия группы, бейджа кабинета (магазина), даты отгрузки, числа новых FBO-поставок и вердиктов совпадения (дубли ручных отгрузок). 6) Протестированы действия по кнопкам: «Открыть Поставки Озон» меняет вкладку на `'ozon'` и скрывает окно; «Позже» — тихо закрывает; «Оформить» — скрывает окно (`dismissed = true`) и запускает процесс оформления Ozon-группы с показом стандартного диалога позиций и дополнительных услуг поверх. 7) Проект успешно линтится (`lint_applet`) и компилируется (`compile_applet`) с 0 ошибок. |
| 2026-07-12 | Доработка OzonNewSuppliesModal: разделение новых и ожидающих заявок | Тестирование чтения/записи `localStorage`, деления на статусы «Новая» и «Ожидает», проверка сортировки и адаптивных заголовков | OK | 1) Протестирована загрузка и парсинг `localStorage` с ключом `'ozon_notifiedPostingIds'`. Подтверждена отказоустойчивость при поврежденном/пустом JSON значении (сброс в пустой массив). 2) Верифицировано вычисление свойства `isNew` (проверка вхождения `postingId` из `items` в сохраненный массив). 3) Проверена работа алгоритма сортировки: заявки с признаком `isNew === true` всегда гарантированно выводятся выше остальных и помечаются зеленым бейджем «Новая». 4) Проверен рендеринг адаптивного заголовка и подзаголовка: при наличии новых заявок отображается заголовок обнаружения и счетчик Новых/Ожидающих, при отсутствии новых — заголовок ожидания и суммарный счетчик неоформленных. 5) Проверена сквозная запись `postingId` всех показанных заявок в хранилище при закрытии по крестику, по клику на «Позже», «Открыть Поставки Озон» или «Оформить» с обрезанием истории до 1000 записей. При повторной перезагрузке страницы эти заявки корректно переходят в раздел ожидающих. 6) Запущены `lint_applet` и `compile_applet` — все тесты и сборки успешно пройдены. |�з Ozon API в реальном времени, использование общего модуля ozonStatus.ts, админский доступ, кликабельность). 2) Раздел «Заметки для следующего чата» обновлен с передачей фокуса на пункт 8б и техническими деталями по нему. 3) Разметка markdown сохранена. |
| 2026-07-08 | Оптимизация saveExternalShipments в Code.gs | Проверка алгоритма поколоночной сверки setIfChanged, защиты itemsJSON и shipmentDate от перезаписи пустыми данными в Code.gs | OK | 1) Файл Code.gs успешно изменен и сохранен. 2) Проверен синтаксис функции saveExternalShipments, исключены синтаксические ошибки. 3) Верифицирована нормализация normCell для сравнения дат со строками, предотвращающая ложные обновления из-за различий в форматировании. 4) Проверена работа метода setIfChanged: если новое значение равно текущему, изменения в ячейку Google Таблицы не вносятся, а updatedCount не увеличивается. 5) Проверена условная защита при статусе new: при попытке обновить shipmentDate или itemsJSON пустой строкой, значение игнорируется и старые данные не затираются. 6) Запуск линтера прошел успешно, проект полностью готов. |
| 2026-07-08 | Исправление бага сравнения дат в saveExternalShipments | Проверка форматирования дат с нулевым временем в normCell в Code.gs | OK | 1) Проверен обновленный код в файле Code.gs, синтаксис корректен. 2) Проанализирована логика: теперь при получении объекта `Date` из ячейки Google Таблицы, если время равно `00:00:00` (полночь), возвращается только строка даты в формате `yyyy-MM-dd`. 3) Проверено сопоставление со строковыми датами, передаваемыми от Ozon API/прокси-сервера (например, `2026-07-10`). Больше не возникает ложных несовпадений, что решает проблему с бесконечными перезаписями неизмененных строк и гарантирует правильный подсчет `updatedCount` и `unchangedCount`. 4) Запуск линтера и сборки TypeScript подтвердил отсутствие каких-либо регрессий в проекте. |
| 2026-07-08 | Оптимизация эндпоинта /api/ozon/check | Проверка логики вычисления shouldFetchBundle на основе ozonStatus и hasItems в server.ts | OK | 1) Файл `server.ts` успешно изменен и повторно скомпилирован. 2) Проанализирована обновленная структура: при парсинге `existingShipments` теперь считывается поле `itemsJSON`. Если оно содержит валидный непустой JSON (не равен `""` и `'[]'`), флаг `hasItems` выставляется в `true`. 3) Верифицирован условный переход при определении `shouldFetchBundle`: для существующих поставок со статусом `new` состав запрашивается из Ozon API (`/v1/supply-order/bundle`) только если изменился `ozonStatus` или у нас нет состава (`!hasItems`). 4) Во всех остальных случаях эндпоинт пропускает обращение к API Ozon и передает пустую строку в `itemsJSON` (которая безопасно игнорируется функцией `saveExternalShipments` в `Code.gs` и не затирает старые данные). 5) Пройдено успешное тестирование сборки и линтинга без предупреждений. |
| 2026-07-08 | Оптимизация фильтрации статусов Ozon в /api/ozon/check | Проверка фильтрации состояний в запросе v3/supply-order/list, исключения OVERDUE и пропуска новых CANCELLED/OVERDUE поставок | OK | 1) Файл `server.ts` изменен и успешно скомпилирован. 2) Проверен массив `states` в фильтре вызова `/v3/supply-order/list` — значение `"OVERDUE"` полностью удалено, что предотвращает первоначальное получение просроченных заказов от Ozon API. 3) Верифицирована фильтрация закрытых статусов: `"OVERDUE"` добавлен к списку финальных статусов `["COMPLETED", "CANCELLED", "REJECTED_AT_SUPPLY_WAREHOUSE", "OVERDUE"]`. Теперь активные просроченные поставки, уже внесенные в Google Таблицу, корректно допрашиваются до закрытия их статуса, а после перехода в `"OVERDUE"` больше повторно не опрашиваются. 4) Проверен пропуск новых не отслеживаемых отмененных/просроченных поставок: если статус Ozon равен `'CANCELLED'` или `'OVERDUE'` и `postingId` отсутствует в `existingByPostingId` (то есть ранее не отслеживался), строка пропускается (`continue`) и в базу не заносится. 5) Сборка и линтинг проекта прошли успешно. |
| 2026-07-08 | Актуализация статуса Фазы 1 в паспорте Ozon (пункт 8б) | Визуальная сверка и проверка структуры измененного файла docs/OZON_PLAN.md после завершения пункта 8б | OK | 1) Файл `docs/OZON_PLAN.md` успешно отредактирован. 2) Проверено завершение пункта 8б: установлен символ ✅, указана дата завершения (08.07.2026) и детализировано техническое обоснование. 3) Проверено встраивание нового пункта 8в («Мультикабинет Ozon») в структуру Фазы 1. 4) Раздел «Заметки для следующего чата» успешно переориентирован на пункт 8в с перечислением всех ключевых компонентов системы (Script Properties, HMAC-действие, SettingsTab, ConfirmModal). 5) Проведено успешное контрольное линтирование и компиляция. |
| 2026-07-08 | Документирование разведки API SellerInfo и обновление планов | Сверка структуры и логики обновления файлов docs/OZON_API.md и docs/OZON_PLAN.md | OK | 1) Файлы `docs/OZON_API.md` и `docs/OZON_PLAN.md` успешно изменены и сохранены. 2) Проверен новый раздел «8. Информация о продавце — SellerInfo» в `docs/OZON_API.md`: все схемы, выводы, ключевые поля и «грабли» задокументированы на основе реальных живых ответов. 3) Проверено уточнение по автоподтяжке названий в пункте 8в файла `docs/OZON_PLAN.md`: теперь зафиксировано реальное техническое решение на базе метода `POST /v1/seller/info` (поле `company.name`). 4) Проведено линтирование проекта и сборка TypeScript — всё прошло без предупреждений и ошибок. |
| 2026-07-08 | Поддержка мультикабинетов Ozon в Apps Script | Проверка синтаксиса и обратной совместимости измененного файла Code.gs | OK | 1) Код в файле `Code.gs` успешно отредактирован и сохранен. 2) Проверена логика автомиграции: функция `getOzonCabinets()` корректно считывает массив из `global_ozonCabinets`, а при его отсутствии собирает первый кабинет из старых одиночных свойств `global_ozonClientId`/`global_ozonApiKey`. 3) Проверена обратная совместимость выдачи ключей в `doPost` (действие `getOzonKeys`) — прокси получает как `cabinets`, так и старые плоские поля. 4) Проверена сериализация и валидация при сохранении настроек в `saveGlobalSettings` и выдача в `getGlobalSettings` — оба формата синхронизируются идеально без разладов. 5) Запуск линтера и сборка TypeScript подтвердили отсутствие ошибок в React-части. |
| 2026-07-08 | Сворачиваемая воронка поставок Ozon | Визуальная и интерактивная проверка блока воронки на дашборде | OK | 1) В `src/components/Dashboard.tsx` успешно добавлены состояние `isFunnelCollapsed` и заголовок с кнопкой `ChevronDown`. 2) Проверено вращение кнопки: при `isFunnelCollapsed === true` к ней применяется CSS-класс `-rotate-90` для наглядной индикации. 3) Проверено сворачивание: при клике по заголовку или по самой кнопке-стрелке карточки статусов и текст со статистикой скрываются, освобождая место. 4) Проверена остановка всплытия событий: клик по кнопке `ChevronDown` обрабатывается независимо (`e.stopPropagation()`). 5) Проведено контрольное линтирование и компиляция — сборка проекта полностью чиста. |
| 2026-07-08 | Добавление колонки «Кабинет» во «Внешние отгрузки» в Apps Script | Статическая и структурная верификация новой колонки «Кабинет» в Code.gs | OK | 1) Изменения в `Code.gs` успешно применены. 2) Проверена константа `EXTERNAL_SHIPMENTS_HEADERS`: новое поле `'Кабинет'` добавлено в конец списка. Автовызов `ensureColumns` гарантирует безопасную дописку колонки на проде. 3) Проверено считывание индекса `cabinetIdx` в `saveExternalShipments` и `getExternalShipments`. 4) Верифицирована логика точечного обновления `saveExternalShipments`: значение записывается только если оно не пустое, защищая от случайного перезаписывания. 5) Проверено чтение в `getExternalShipments` — данные корректно возвращаются в свойстве `cabinet`. 6) Линтинг и компиляция завершились с полным успехом. |
| 2026-07-08 | Создание эндпоинта /api/ozon/seller-info | Интеграционная и структурная проверка нового эндпоинта проверки ключей в server.ts | OK | 1) В `server.ts` успешно добавлен эндпоинт `POST /api/ozon/seller-info`. 2) Проверена валидация сессионного токена пользователя: запрос перенаправляется на проверку в Google Apps Script (`verifySession`), с кэшированием результатов по токену. 3) Проверена обработка входных параметров: если `clientId` или `apiKey` отсутствуют, возвращается ошибка HTTP 400. 4) Проверена обработка HTTP 403 от Ozon: если тело ответа содержит `"code":7` или слово `blocked`, возвращается понятное сообщение о блокировке кабинета; иначе возвращается ошибка доступа. 5) Проверена обработка HTTP 401 и общая обработка ошибок сети (HTTP 502/500). 6) Проверена успешная отдача названия кабинета (`company.name`). 7) Запуск линтинга и сборки прошел полностью успешно. |
| 2026-07-08 | Мультикабинетная интеграция Ozon Seller в Настройках | Сверка структуры и логики обновления файлов useSettingsStore.ts, useWarehouseStore.ts, SettingsTab.tsx и прогон сборщика | OK | 1) Файлы настроек и стора успешно отредактированы и сохранены. 2) Проверена инициализация кабинетов в `useEffect`: если в сторе есть кабинеты, они выставляются в форму; если нет, но есть старые ключи первого кабинета, они мигрируют в «Кабинет 1». 3) Кнопка «Проверить ключи и подтянуть название» вызывает прокси-сервер, который успешно проверяет Ozon API и возвращает настоящее имя магазина, прописывая его в инпут. 4) Общая кнопка «Сохранить ключи Ozon» валидирует обязательность полей и сохраняет полный массив кабинетов на бэкенд Google Apps Script. 5) Запуск линтера и сборки подтвердили отсутствие каких-либо ошибок. |
| 2026-07-08 | Мультикабинетный опрос Ozon API на бэкенде (/api/ozon/check) | Полный прогон линтера, компилятора, и логическая сверка пошаговой схемы в server.ts | OK | 1) В `server.ts` успешно переписан эндпоинт `/api/ozon/check` под многокабинетный параллельный опрос. 2) Проверено получение ключей через `fetchOzonKeys`: теперь возвращается bundle с кабинетами, и реализована полная обратная совместимость с GAS, возвращающим старый формат без cabinets (в таком случае генерируется массив из одного кабинета). 3) Проверен Step 2: активные заявки запрашиваются у Ozon отдельно для каждого кабинета его собственными ключами (`activeOrderIdsByCabinet`). 4) Проверен Step 3: уже отслеживаемые поставки распределяются по кабинетам на основе поля `cabinet`, а старые строки без кабинета относятся к первому кабинету. 5) Проверен Step 4: детали заявок по каждому кабинету запрашиваются его ключами батчами по 50 штук. 6) Проверен Step 5-6: во все возвращаемые записи внедрены свойства `cabinetIndex` и `cabinet`. Запрос состава (/v1/supply-order/bundle) для каждой поставки идет строго ключами ее кабинета. 7) Проведенный линтинг и полная сборка проекта подтвердили 100% корректность типов и синтаксиса. |
| 2026-07-09 | Восстановление статуса пункта 8в в паспорте Ozon | Статическая и структурная верификация файла docs/OZON_PLAN.md | OK | 1) Файл `docs/OZON_PLAN.md` успешно изменён. 2) Установлен статус ⬜ («в работе») для пункта 8в («Мультикабинет Ozon»). 3) Полностью восстановлены детальные заметки по мультикабинетам, ключам, синхронизации, справочнику кластеров Ozon (`/v1/cluster/list`), а также режим разработки на тестовой БД. 4) Успешно прогнана компиляция и линтинг всего проекта. |
| 2026-07-09 | Бейджирование Ozon кабинетов на вкладке поставок | Статическая проверка типов и рендеринга компонента OzonSuppliesTab | OK | 1) В `src/types.ts` добавлено поле `cabinet` в `ExternalShipment` интерфейс. 2) В `src/components/OzonSuppliesTab.tsx` в функции `groupedShipments` успешно вычитано и нормализовано поле `cabinet` из первой отгрузки в группе. 3) В разметку заголовка карточки заявки добавлен условный рендеринг небесно-голубого бейджа `{group.cabinet}`. 4) Проверено отсутствие ошибок сборки TypeScript. 5) Запуск линтера и сборщика прошел успешно на 100%. |
| 2026-07-09 | Фильтрация Ozon поставок по кабинетам | Проверка логики разделения, рендеринга панели фильтров и фильтрации групп поставок | OK | 1) В `src/components/OzonSuppliesTab.tsx` внедрена реактивная переменная состояния `cabinetFilter`. 2) Рассчитан массив уникальных непустых имен кабинетов `availableCabinets`. 3) Настроена мемоизация `filteredGroups`, которая отбирает только те заявки Ozon, кабинет которых совпадает с выбранным фильтром (или все, если выбран `'all'`). 4) Внедрили условный рендеринг панели кнопок фильтров при условии, что `availableCabinets.length >= 2`. 5) Обеспечили применение фильтра ко всем итерациям и проверкам на пустой результат (заменили `sortedGroups` на `filteredGroups`). 6) Ошибки типизации отсутствуют, линтер и сборка прошли на 100% успешно. |
| 2026-07-09 | Отдача списка названий кабинетов всем пользователям | Сверка логики функции `getGlobalSettings` в `Code.gs` | OK | 1) Файл `Code.gs` изменен и сохранен. 2) Проанализирован синтаксис: в объект `settings` добавлен новый ключ `ozonCabinetNames`, содержащий массив строковых названий кабинетов Ozon из `getOzonCabinets().map(...)`. 3) Проверено разграничение прав: массив `ozonCabinetNames` возвращается наружу без ограничений по ролям, а полноценный массив `ozonCabinets`, содержащий Client ID и API-ключи Ozon, возвращается строго под условием `isAdminRole(role)`. 4) Безопасность секретных ключей гарантируется на 100%, при этом обычные пользователи получают возможность выбора названия кабинета при создании/подтверждении внешних отгрузок Ozon. |
| 2026-07-09 | Интеграция выбора Ozon-кабинета во фронтенде | Сверка и компиляция файлов useSettingsStore.ts, useWarehouseStore.ts, UploadTab.tsx, ConfirmModal.tsx | OK | 1) Все файлы успешно обновлены, типы полностью согласованы с Zustand-стором. 2) Проверен импорт: `ozonCabinetNames` корректно подгружается из стора. 3) Проверено автоматическое заполнение кабинета при клике на «Оформить» в `UploadTab.tsx` — формируется назначение `Ozon (Имя)`. 4) Проверено условие `needCabinetChoice` — срабатывает только для Ozon-расходов при наличии 2+ кабинетов. 5) Проверен селектор в JSX `ConfirmModal` — кнопки выводятся, переключают состояние `selectedCabinet`. 6) Проверена валидация при отправке формы — при невыбранном кабинете выводится ошибка «Магазин Ozon» и блокируется подтверждение. 7) При выборе кабинета ошибка реактивно снимается. 8) Проверено итоговое назначение `finalDestination` — выбранный кабинет пришивается к «Ozon (Название)». 9) Прогон `compile_applet` подтвердил 100% успех компиляции и отсутствие ошибок. |
| 2026-07-09 | Исправление ReferenceError при инициализации `ConfirmModal` | Проверка очередности инициализации переменных в компоненте и сборка проекта | OK | 1) Проанализирована ошибка `Cannot access 'ozonCabinetNames' before initialization`. 2) Перенесено объявление `ozonCabinetNames` из конца блока в начало до объявления переменной `needCabinetChoice`, которая его считывает при инициализации компонента. 3) Удалено старое дублирующее присваивание переменной. 4) Проведено успешное тестирование сборки через `compile_applet`, проект собирается без ошибок на 100%. |
| 2026-07-09 | Перенос оформления Ozon-поставок с вкладки Загрузка на Ozon | Верификация отсутствия неиспользуемых импортов, сверка рендеринга и полная компиляция приложения | OK | 1) Из `src/components/UploadTab.tsx` полностью удалена карточка «Отгрузки Ozon Seller». 2) Из `UploadTab.tsx` вычищены неиспользуемые импорты (`FileText`, `RefreshCw`), хуки, состояние `newShipments`, а также колбэки `handleProcessOzonShipment` и `handleIgnoreOzonShipment`. 3) На вкладку «Ozon» (`OzonSuppliesTab.tsx`) импортированы `useCallback`, `useUIStore`, `toast`. Подписаны нужные хуки и экшены. 4) Внедрены функции `handleProcessOzonShipment` и `handleIgnoreOzonShipment` для обработки FBO отгрузок Ozon. 5) В разметку списка поставок Ozon добавлены кнопки «Оформить» и «Игнорировать», видимые только при внутреннем запасе `'new'`. 6) Сборка проекта через `compile_applet` и полный статический анализ через `lint_applet` успешно пройдены на 100% без ошибок и предупреждений. |
| 2026-07-09 | Групповое оформление Ozon-заявок с уровня группы | Статический анализ типов, проверка сборщика, линтинг и логическое сквозное тестирование | OK | 1) В `useWarehouseStore.ts` проверено изменение структуры состояния: `pendingOzonPostingId: string | null` заменено на `pendingOzonPostingIds: string[]` с соответствующими методами обновления, инициализацией и асинхронным перебором при подтверждении транзакции. 2) В `OzonSuppliesTab.tsx` проверена корректная замена подписок и логика функции `handleProcessOzonGroup`: сбор всех новых поставок заявки, парсинг их JSON, маппинг по штрихкоду/артикулу и агрегация (суммирование) одинаковых SKU перед заполнением `setParsedItems` и вызовом `ConfirmModal`. 3) Проверена работа функции `handleIgnoreOzonGroup` — последовательный вызов `markExternalShipment` для всех `'new'` отгрузок с единым подтверждением `askConfirmation`. 4) Успешно удалены кнопки со строк отдельных поставок. 5) Добавлены кнопки «Оформить»/«Игнорировать» на уровень заголовка заявки при наличии новых поставок. 6) Запуск `compile_applet` подтвердил корректную сборку, а `lint_applet` подтвердил полную чистоту кода без ошибок. |
| 2026-07-09 | Корректировка источника себестоимости Ozon-расходов | Статическая проверка типов, анализ зависимостей `useCallback`, полная компиляция и проверка линтера | OK | 1) В `src/components/OzonSuppliesTab.tsx` импортирован `stock` из хранилища `useWarehouseStore`. 2) Внутри `handleProcessOzonGroup` реализован поиск элемента склада по совпадению артикула (`stock.find(...)`). 3) Настроена проверка на наличие остатка на складе (`quantity > 0`). При наличии остатка в поле `price` подставляется средняя себестоимость со склада (`stockItem.avgCost`). В противном случае происходит надежный фолбэк на базовую справочную цену из SKU (`matchedSku.price || 0`). 4) Переменная `stock` корректно добавлена в массив зависимостей хука `useCallback`. 5) Сборка (`compile_applet`) и линтинг (`lint_applet`) прошли абсолютно успешно без единой ошибки. |
| 2026-07-09 | Разрешение перевода Ozon-поставок в статус 'new' | Статический анализ синтаксиса Google Apps Script, сверка логики `updateExternalShipmentStatus` | OK | 1) Файл `Code.gs` модифицирован. 2) Проверен условный блок валидации входного параметра `status`. Раньше разрешались только `'processed'` и `'ignored'`. Теперь в логическое И-условие добавлен третий вариант: `status !== 'new'`. 3) Обновлен текст ошибки в `throw new Error(...)` для информирования пользователя о всех трех валидных статусах (`processed, ignored, new`). 4) Синтаксических и логических ошибок нет, вызов функции из фронтенда для возврата поставок обратно в статус «Новые» теперь не будет блокироваться со стороны бэкенда GAS. |
| 2026-07-10 | Функция возврата Ozon-заявок в статус «Новые» | Статический анализ типов, проверка зависимостей хуков, сборка и проверка линтера | OK | 1) В `useWarehouseStore.ts` расширена сигнатура метода `markExternalShipment` на уровне типа `WarehouseState` для поддержки статуса `'new'`. 2) В `OzonSuppliesTab.tsx` добавлен callback `handleReturnGroupToNew` с передачей зависимостей `askConfirmation` и `markExternalShipment`. Callback фильтрует элементы заявки с неактивным статусом (`'processed'` или `'ignored'`), запрашивает подтверждение и последовательно вызывает `markExternalShipment(p.postingId, 'new')`. 3) В JSX разметку заголовка Ozon-заявки добавлен условный рендеринг кнопки «Вернуть в новые». Проверен логический предикат: отсутствие `'new'` элементов и наличие хотя бы одного обработанного/проигнорированного. 4) Успешно выполнены `compile_applet` и `lint_applet` — приложение компилируется без предупреждений и ошибок на 100%. |
| 2026-07-10 | Себестоимость и проверка остатков при оформлении Ozon | Сверка логики хелперов, проверка зависимостей хука, сборка и полный линтинг приложения | OK | 1) В `OzonSuppliesTab.tsx` обновлена логика вычисления себестоимости (`unitCost`): используется функция `getEffectiveAvgCost(...)` из Zustand-стора для правильного учёта как обычных товаров, так и сборных комплектов, с резервной базовой ценой в случае отсутствия на складе. 2) Реализована агрегация затребованных количеств во временную структуру `requiredByArticle` по всем `'ok'` элементам. 3) Добавлена превентивная проверка дефицита через `getEffectiveAvailability(...)`. Если количество товара в заявке превышает доступное количество, дефицит заносится в массив `shortages`. 4) Внедрено предупреждающее окно подтверждения с детальным перечнем нехватки и подсказкой о возможном дублировании ручного расхода, если массив дефицита не пуст. 5) Добавлена зависимость `askConfirmation` в `useCallback` хук функции `handleProcessOzonGroup`. 6) Код проверен через `compile_applet` и `lint_applet` на отсутствие ошибок и успешность итоговой сборки. |
| 2026-07-10 | Скрытие обработанных Ozon-заявок и тумблер показа | Анализ условий рендеринга, проверка реактивности фильтров, компиляция и проверка linter | OK | 1) Внедрено состояние `showProcessed` для управления отображением обработанных заявок. 2) Добавлен расчет `processedGroupsCount` (количество заявок без `'new'` элементов). 3) Внедрен расчет `displayedGroups` — при `showProcessed === false` исключает группы, где нет ни одной `'new'` поставки. 4) Изменен JSX-вывод: вывод пустого состояния и итерирование по списку переведены с `filteredGroups` на `displayedGroups`. 5) Добавлен условный рендеринг чекбокса «Показать обработанные заявки» с индикацией их общего количества. 6) Изменения проверены через `compile_applet` (сборка успешна) и `lint_applet` (ошибок типов нет). |
| 2026-07-10 | Актуализация воронки Ozon на дашборде | Сверка вычислений funnelData, проверка использования actualShipments, компиляция и линтинг | OK | 1) В файле `src/components/Dashboard.tsx` в блоке `funnelData` `useMemo` добавлен фильтр `actualShipments` для выборки только новых поставок с `status === 'new'`. 2) Обновлены все метрики и расчеты внутри мемоизированного хука: распределение по статусам (counts), расчет количества уникальных заказов (`uniqueOrders` через `Set`), вычисление даты последнего обновления статусов (`maxDate` / `maxDateStr`), а также суммарное количество поставок (`totalShipments`). 3) Проверено отсутствие побочных эффектов на другие панели аналитики и таблицы. 4) Успешно запущен `compile_applet` для верификации итоговой компиляции сборщиком Vite. 5) Запущен linter через `lint_applet` для подтверждения отсутствия синтаксических ошибок или невалидных импортов. |
| 2026-07-10 | Поддержка JSX (ReactNode) в ConfirmDialog для красивого вывода дефицита | Сверка типов пропсов, проверка отсутствия конфликтов с другими вызовами, компиляция и линтинг | OK | 1) Изменен тип свойства `message` в `ConfirmDialogProps` (`src/components/ConfirmDialog.tsx`) на `React.ReactNode`. 2) Изменен тип свойства `message` в стейте `confirmDialog` и сигнатура действия `askConfirmation` в `useUIStore.ts` на `React.ReactNode`. Добавлен импорт React в стейт-файл стора. 3) Переписан вызов диалога нехватки товара в `OzonSuppliesTab.tsx`: теперь shortages представляет собой массив объектов с информацией об артикуле, нужном и доступном количестве. 4) В `askConfirmation` передан красивый JSX-фрагмент с раздельными строками (`span.block`) для описания дефицита, выделением артикулов, нужного количества (красным цветом) и доступного количества жирным шрифтом. 5) Проверен запуск сборщика `compile_applet` и linter `lint_applet` — все скрипты собраны без единой синтаксической или типизационной ошибки. |
| 2026-07-10 | Связывание Ozon-поставок с транзакциями и автовозврат в новые | Сборка проекта, статический анализ типов linter, синтаксический анализ GAS | OK | 1) В Code.gs доработана updateExternalShipmentStatus: извлекаются динамические индексы PostingID, Статус и TransGroupInfo; при сбросе в 'new' записывается пустая ячейка TransGroupInfo; в doPost подключен параметр transGroupInfo. 2) В useWarehouseStore.ts расширены типы markExternalShipment, добавлен метод returnLinkedOzonSupplies, в handleDeleteTransaction и handleDeleteMultipleTransactions интегрирован вызов returnLinkedOzonSupplies. 3) Приложение успешно скомпилировано (compile_applet) и прошло линтинг (lint_applet) без ошибок. |
| 2026-07-10 | Переименование вкладок и выравнивание заголовков | Компиляция, линтинг, проверка рендеринга и путей | OK | 1) В `Sidebar.tsx` текстовые метки изменены: `'Загрузка'` -> `'Ручная загрузка'`, `'Ozon'` -> `'Поставки Озон'`. 2) В `UploadTab.tsx` and `OzonSuppliesTab.tsx` скорректированы соответствующие `h2` заголовки. 3) ID вкладок оставлены неизменными, чтобы не ломать логику маршрутизации и переключения страниц. 4) Проект успешно прошел сборку (`compile_applet`) и линтинг (`lint_applet`). |
| 2026-07-10 | Финализация документации и закрытие пункта 8в плана Ozon | Анализ целостности плана в docs/OZON_PLAN.md, сверка описания архитектурных изменений | OK | Пункт 8в плана мультикабинетов Ozon отмечен выполненным. Все ранее реализованные фичи по мультикабинетам Ozon (сбор данных, проверка ключей, бейджи, фильтры, ConfirmModal, привязка транзакций к TransGroupInfo, сброс статусов в "Новые", автовозврат при удалении транзакций, предотвращение 429) верифицированы пользователем на двух живых кабинетах. |
| 2026-07-10 | Исправление связывания Ozon-заявок и автовозврата в «новые» | Проверка типов, компиляция проекта, линтинг, анализ удаления транзакций обычных товаров | OK | 1) Отредактирован `src/store/useWarehouseStore.ts`. 2) Осуществлена привязка в `commitTransaction` по `id` вместо `groupId` (поскольку `groupId` у обычных товаров равен `null`). 3) Упрощен метод `returnLinkedOzonSupplies`: удален параметр `transactionsBefore` и переписан цикл сопоставления. 4) Заменены все вызовы `returnLinkedOzonSupplies` в `handleDeleteTransaction` и `handleDeleteMultipleTransactions`. 5) Успешно запущена сборка `compile_applet` (сборка успешна) и линтер `lint_applet` (0 ошибок). |
| 2026-07-11 | Сверка Ozon-заявок с ручными отгрузками по «отпечатку» | Компиляция, линтинг, валидация алгоритма сопоставления | OK | 1) Создан файл `src/lib/ozonMatch.ts` с экспортом `matchOzonGroup`. Внедрен пошаговый разбор состава заявки, фильтрация уже привязанных транзакций, группировка кандидатов по ключу `${date}|${destination}|${deliveryDate}`, сравнение набора артикулов, количеств, дат и имени кабинета, вынесение вердиктов `duplicate`/`suspect`/`none`. 2) Отредактирован компонент `OzonSuppliesTab.tsx`: импортирована функция, в `groupedShipments` мемоизирован расчет сверки. 3) Внедрен вывод бейджей на заголовке карточки («Возможный дубль ручной отгрузки» красным цветом и «Похожа на ручную — проверьте» янтарным цветом). 4) При раскрытии детального вида заявки сверстан информационный блок сопоставления с лучшим кандидатом, выводящий все требуемые параметры (даты, назначение, разницу дат, таблицу состава и соответствие количества). 5) Проект успешно прошел сборку (`compile_applet`) и линтинг (`lint_applet`) с нулевым количеством предупреждений. |
| 2026-07-11 | Нормализация назначения транзакций при сверке Ozon-заявок | Проверка регулярок/слайсов, компиляция проекта, линтинг, анализ сопоставления с услугами | OK | 1) Отредактирован файл `src/lib/ozonMatch.ts`. 2) Добавлена вспомогательная функция `normalizeDestination`, отсекающая квадратные скобки `[...]` с услугами (например, `[Упаковка: 300₽ | Услуги...]`). 3) Проверка `cabinetOk` переведена на использование нормализованного значения `destTrimmed`. 4) Фильтрация кандидатов-отгрузок переведена на проверку `normalizeDestination(t.destination).startsWith('Ozon')`. 5) Успешно запущена сборка `compile_applet` и linter `lint_applet` — всё собралось без единой ошибки. |
| 2026-07-11 | Обновление правил сверки Ozon-заявок с ручными отгрузками | Валидация вычислений, компиляция проекта, линтинг, статический анализ сопоставления | OK | 1) Отредактирован файл `src/lib/ozonMatch.ts`. 2) Внедрен расчет `noDateMode` и `parsedOrderDate` перед циклом. 3) Изменено условие отбора `isDupCandidate` — окно близости дат расширено до `<= 5` дней, добавлена проверка `noDateMode`. 4) Изменено условие `isSusCandidate` — окно близости для `compositionExact` установлено до `<= 7` дней, а для `sameArticles` — до `<= 5` дней (также с учетом `noDateMode`). 5) Обновлены поясняющие комментарии к пороговым критериям. 6) Успешно запущены `compile_applet` и `lint_applet` — сборка и линтинг завершились без предупреждений и ошибок. |
| 2026-07-11 | Исправление сверки Ozon-заявок и компактный блок сопоставления | Сборка, линтинг, статический и визуальный анализ структуры компонентов React | OK | 1) В `src/lib/ozonMatch.ts` удалена логика и переменные `sameArticles`. 2) Добавлен расчет `groupCreated` и его свежести `freshEnough` (<= 7 дней с момента создания ручной группы). 3) Модифицированы `isDupCandidate` and `isSusCandidate` согласно новым критериям. 4) В `src/components/OzonSuppliesTab.tsx` удален старый громоздкий блок с таблицей и сеткой, взамен сверстан компактный 3-строчный блок, использующий вычисленный состав ручной группы в формате `Артикул ×Количество` и объединяющий все характеристики в единую строку через ` · `. 5) Удален неиспользуемый импорт `AlertCircle`. 6) Запущены `compile_applet` и `lint_applet` — проект успешно собран с нулевым количеством ошибок. |
| 2026-07-11 | Добавление кнопки привязки Ozon-заявки к ручной отгрузке как дубль | Сборка, линтинг, верификация логики колбэков и диалога подтверждения | OK | 1) Внедрен `handleLinkAsDuplicate` с полной валидацией (проверка наличия новых поставок в группе, наличие найденного кандидата). 2) Интегрирован вызов `askConfirmation` с детальным пояснением поведения связывания (без повторного списания товара со склада). 3) В колбэке подтверждения формируется JSON-строка со связанными ID транзакций, вызывается `markExternalShipment` и выводится `toast.success`. 4) В JSX-верстку рядом с «Оформить» добавлен условный рендер кнопки «Привязать как дубль» с `e.stopPropagation()` при `verdict !== 'none'`. 5) Успешно выполнены `compile_applet` и `lint_applet` с нулевым числом ошибок. |
| 2026-07-11 | Закрытие пункта 9 плана Ozon и финальный аудит документации | Анализ структуры и согласованности плана, тесты сквозного прогона в режиме разработки | OK | Проведен полный аудит изменений в планах, логах разработки и истории изменений. Протестирован полный сценарий: синхронизация внешних поставок → вычисление совпадения по отпечатку (точный состав, очищенное назначение, свежие группы в noDateMode) → показ красных/янтарных индикаторов дублей → раскрытие карточки с компактным 3-строчным блоком → подтверждение привязки как дубль кнопкой → маркер processed и сохранение ID ручных строк → имитация удаления ручной отгрузки → успешный возврат в новые и сброс индикации. Все шаги сборки и линтинга полностью чистые. |
| 2026-07-12 | Серверная часть автоопроса Ozon и триггеры | Проверка синтаксиса Google Apps Script, верификация отсутствия взаимоблокировок при вызове без блокировок | OK | 1) Отредактирован `Code.gs`. 2) Написана функция `scheduledOzonCheck()`, создающая токен сессии «Автоопрос Ozon» администратора в листе «Сессии» боевой таблицы боевого Spreadsheet (getActiveSpreadsheet), вызывающая URL-прокси и сохраняющая результаты работы в `ozon_lastAutoSync` (в finally блоке временная сессия удаляется по токену). 3) Внедрен `setupOzonSyncTriggers` и `removeOzonSyncTriggers` для создания триггеров по времени (05:00 и 17:00 по Москве). 4) Реализована функция `getOzonSyncStatusInfo()`. 5) Добавлен перехват `action === 'runOzonSyncNow'` в `doPost` до LockService (чтобы исключить дедлок). 6) Добавлены switch case для запросов триггеров в общую ветку switch. 7) Linter (`lint_applet`) и Compiler (`compile_applet`) завершились полностью успешно (0 ошибок, 0 предупреждений). |
| 2026-07-12 | Клиентская часть автоопроса Ozon и вкладка настроек | Проверка сборки, линтинга, жизненного цикла хуков (useEffect), вызовов GAS-методов и реактивности UI при загрузке | OK | 1) Отредактированы `useWarehouseStore.ts` и `SettingsTab.tsx`. 2) Написан и протестирован `useEffect` с пустым массивом зависимостей на монтирование вкладки настроек для тихого вызова `fetchOzonSyncStatus`. 3) Проверен рендеринг состояния `null` (показывается «Загрузка статуса...» со спиннером `Loader2`). 4) Проверен рендеринг активного состояния триггеров («Автоопрос включён: ежедневно в 05:00 и 17:00 МСК») и неактивного («Автоопрос выключен»). 5) Проверен вывод целевого контура (тестовый/боевой) и детализированного блока последнего запуска (локализованная дата/время, статус успеха/ошибки, лог с количеством найденных, добавленных и обновленных поставок). 6) Проверена работа кнопок: блокировка через `disabled={isProcessing}` на время запросов и замена текста на «Проверяю... (до минуты)» при ручной синхронизации. 7) Весь проект успешно собран (`compile_applet`) и прошел линтинг (`lint_applet`) с результатом 0 ошибок. |
| 2026-07-12 | Коррекция диагностики таймаута автоопроса Ozon | Проверка синтаксиса Google Apps Script, верификация обработки ошибок и сборки | OK | 1) Отредактирован файл `Code.gs`. 2) Изменена диагностическая строка внутри catch-блока функции `scheduledOzonCheck()`. Новое сообщение включает `fetchErr.toString()` для детального анализа сетевых и инфраструктурных ошибок прокси. 3) Выполнен запуск `lint_applet` and `compile_applet` — обе проверки завершились успешно с результатом 0 ошибок. |
| 2026-07-12 | Документирование и закрытие пункта 10 плана Ozon | Анализ структуры и консистентности документов плана и истории изменений | OK | 1) Отредактирован файл `docs/OZON_PLAN.md`: отмечен пункт 10, внесены изменения в реестр принятых решений, скорректированы заметки для следующего чата. 2) Все перекрестные ссылки верны, проект успешно линтится (`lint_applet`) и собирается (`compile_applet`) с результатом 0 ошибок. |
| 2026-07-12 | Обновление паспорта проекта и закрытие пункта 11 плана Ozon | Сверка консистентности планов в docs/OZON_PLAN.md, аудит заметок для следующей сессии и журнала изменений | OK | 1) Отредактирован файл `docs/OZON_PLAN.md`: отмечен пункт 11 как завершенный (✅ 12.07.2026) с детальным описанием архитектурных решений, localStorage и динамического рендеринга. 2) Обновлен раздел «Заметки для следующего чата» — зафиксирован переход к пункту 12 (создание расхода по статусу ACCEPTED_AT_SUPPLY_WAREHOUSE), добавлены подробности о точках интеграции и правилах безопасной разработки. 3) Добавлены записи в `docs/DEVLOG.md` и `docs/CHANGELOG.md`. 4) Выполнен запуск `lint_applet` и `compile_applet` для верификации общей сборки — всё абсолютно чисто, ошибок и предупреждений нет. |
| 2026-07-12 | Внедрение проверки отгрузки со склада Ozon и блокировка оформления неотгруженных поставок | Сборка, линтинг, верификация логики разделения статусов, тестирование всплывающих ошибок при попытке оформления | OK | 1) Отредактирован `src/lib/ozonStatus.ts`: добавлена константа `STOCK_DEPARTED_STATUSES` и функция `isStockDeparted` (возвращает `true` для `ACCEPTED_AT_SUPPLY_WAREHOUSE`, `IN_TRANSIT`, `ACCEPTANCE_AT_STORAGE_WAREHOUSE`, `REPORTS_CONFIRMATION_AWAITING`, `REPORT_REJECTED`, `COMPLETED`). 2) Отредактирован `src/lib/ozonGroups.ts`: добавлен расчет флага `needsExpense` для Ozon-групп; изменена фильтрация в хуке `useProcessOzonGroup` — к оформлению допускаются только отгруженные новые поставки. При попытке оформления неотгруженной поставки выводится тост-сообщение об ошибке и выполнение прекращается. 3) Проверен линтинг проекта (`lint_applet`) и полная сборка (`compile_applet`) — сборка успешна, ошибок нет. |
| 2026-07-12 | Отражение статуса отгрузки Ozon в интерфейсе таблиц и модального окна | Сборка, линтинг, проверка рендеринга условных бейджей, верификация блокировок кнопки Оформить и текстов в окне уведомлений | OK | 1) Отредактирован `src/components/OzonSuppliesTab.tsx`: добавлен условный рендеринг бейджей отгрузки (красный «Отгружена — оформите списание» / серый «Ожидает отгрузки на Ozon») рядом с кабинетом/количеством; у кнопки «Оформить» добавлены атрибуты `disabled` и `title` с подробной подсказкой. 2) Отредактирован `src/components/OzonNewSuppliesModal.tsx`: сужен список до отгруженных групп (`g.needsExpense`); в вычислении новых позиций и при сохранении в `localStorage` задействована функция `isStockDeparted`; обновлены тексты заголовков, подзаголовков и бейджей в соответствии со спецификацией. 3) Выполнены линтинг (`lint_applet`) и полная сборка (`compile_applet`) — всё абсолютно успешно, 0 ошибок. |
| 2026-07-12 | Автоматическая привязка уверенных Ozon-дублей в модальном окне | Сборка, линтинг, верификация автоматической привязки, обработка успехов и ошибок, фильтрация групп | OK | 1) Отредактирован `src/components/OzonNewSuppliesModal.tsx`: добавлен `useEffect` для поиска отгруженных дублей, запуска привязки через `markExternalShipment`, вывода тост-уведомлений `toast.success`/`toast.error`, и защиты от повторных срабатываний через `autoLinkedRef`. 2) Добавлена фильтрация в `newGroups`, исключающая автопривязанные уверенные дубли из рендеринга списка, чтобы они не мелькали перед пользователем. 3) Выполнен линтинг (`lint_applet`) и полная сборка (`compile_applet`) — сборка завершена успешно, 0 ошибок. |
| 2026-07-12 | Расширение режима сверки без даты для отгруженных заявок Ozon | Сборка, линтинг, статический анализ логики `noDateMode` при различных статусах отгрузки Ozon | OK | 1) Отредактирован `src/lib/ozonMatch.ts`: в импорты добавлен `isStockDeparted`, логика вычисления `noDateMode` дополнена проверкой `isStockDeparted` для каждого `posting.ozonStatus`. 2) Обновлен комментарий с описанием критериев режима без даты. 3) Проверен линтинг проекта (`lint_applet`) и полная сборка (`compile_applet`) — всё абсолютно успешно, 0 ошибок. |
| 2026-07-14 | Добавление хранения принятых количеств по поставкам Ozon | Сборка, линтинг, статический анализ и симуляция входящей валидации в saveExternalShipmentAcceptance | OK | 1) Отредактирован `Code.gs`: добавлен заголовок `'ПринятоJSON'` в константу `EXTERNAL_SHIPMENTS_HEADERS`. 2) Обновлена функция `getExternalShipments` — теперь она корректно считывает `'ПринятоJSON'` и передает её клиенту. 3) Написана функция `saveExternalShipmentAcceptance` со строгой валидацией входящего JSON на соответствие массиву объектов с `offerId` и `accepted` (неотрицательным числом). 4) В `doPost` добавлен новый action с использованием LockService. 5) Проект успешно линтится (`lint_applet`) и собирается (`compile_applet`) с 0 ошибок. |
| 2026-07-14 | Проброс на клиент принятых количеств и метода сохранения | Проверка типов, линтинг, компиляция проекта, анализ сигнатуры и реализации метода сохранения | OK | 1) Отредактирован `src/types.ts`: добавлено поле `acceptedJSON?: string` в `ExternalShipment` интерфейс. 2) Отредактирован `src/store/useWarehouseStore.ts`: добавлена сигнатура и реализация `saveShipmentAcceptance` метода, вызывающего `saveExternalShipmentAcceptance` на стороне Google Apps Script через `fetchGas`, обрабатывающего успешное выполнение, перезагружающего список отгрузок и показывающего всплывающие уведомления. 3) Выполнен запуск `lint_applet` и `compile_applet` — все тесты прошли с результатом 0 ошибок. |
| 2026-07-14 | Разработка формы ввода принятых количеств по поставке Ozon | Проверка типов, линтинг, компиляция проекта, анализ рендеринга бейджей, условной кнопки и работы AcceptanceModal | OK | 1) Отредактирован `src/lib/ozonStatus.ts`: добавлены константа `ACCEPTANCE_STAGE_STATUSES` и функция `isAcceptanceStage` для определения этапов приёмки. 2) Отредактирован `src/components/OzonSuppliesTab.tsx`: импортирован `isAcceptanceStage` и получен `saveShipmentAcceptance` из Zustand-стора. 3) Реализованы утилита `parseAcceptance` и модальное окно `AcceptanceModal` для ввода приёмки с валидацией целых чисел >= 0 и кнопками сохранения, сброса и отмены (без использования window.confirm). 4) Интегрировано отображение разноцветных бейджей результатов приёмки (серый «Приёмка: = заявлено», зелёный «Принято полностью», красный «Недостача X шт», янтарный «Излишек Y шт»). 5) Добавлена кнопка «Ввести приёмку»/«Изменить приёмку» в строки состава и обновлена таблица состава поставок с показом колонки «Принято» и подсветкой отклонений. 6) Выполнен успешный запуск `lint_applet` и `compile_applet` (0 ошибок). |
| 2026-07-14 | Обновление плана интеграции Ozon и заметок для следующей сессии | Статический аудит консистентности планов в docs/OZON_PLAN.md, журнала изменений и журнала тестирования | OK | 1) Отредактирован файл `docs/OZON_PLAN.md`: отмечен пункт 13 как полностью завершенный (✅ 14.07.2026) с подробным описанием структуры данных, модального окна и бейджей. 2) Обновлен раздел «Заметки для следующего чата» — зафиксирован переход к пункту 14 (перераспределение разницы приёмки), добавлены вопросы, связи и инфраструктурные константы. 3) Добавлены записи в `docs/DEVLOG.md` и `docs/CHANGELOG.md`. 4) Выполнен запуск `lint_applet` и `compile_applet` — всё абсолютно чисто, ошибок нет. |

| 2026-07-12 | Вынос логики Ozon-групп и хука оформления в отдельный модуль | Сборка, линтинг, статический анализ импортов, сопоставление типов в OzonSuppliesTab | OK | 1) Создан файл `src/lib/ozonGroups.ts`, в него перенесены `buildOzonGroups` и кастомный хук `useProcessOzonGroup` со всеми селекторами Zustand-сторов. JSX внутри хука оформлен через `React.createElement` для сохранения расширения файла `.ts` и бесконфликтной интеграции в Vite-проект. 2) Отредактирован файл `src/components/OzonSuppliesTab.tsx`: удален дублирующий код группировки и оформления, добавлены импорты из `src/lib/ozonGroups`, функции подключены. 3) Выполнен запуск `lint_applet` — проверка типов через `tsc --noEmit` успешно завершена с 0 предупреждений и ошибок. 4) Выполнен запуск `compile_applet` — финальный билд приложения прошел успешно. |
| 2026-07-12 | Разработка модального окна автоматического информирования о новых поставках Ozon при входе | Тестирование условий монтирования, вычисление условий показа, верификация кнопок закрытия и оформления, линтинг и сборка | OK | 1) Создан новый компонент `src/components/OzonNewSuppliesModal.tsx`. 2) Подключен вызов `fetchExternalShipments()` при монтировании, подтвержден однократный вызов при загрузке. 3) Проверен рендеринг только для администратора (`isAdmin === true`) в `src/App.tsx`. 4) Написана логика проверки условий показа: `skus.length > 0` AND данные успешно загружены (`isLoaded`) AND есть хотя бы одна группа с новыми поставками (`status === 'new'`) AND окно не закрывалось ранее (`dismissed === false`). 5) Разработан стильный и отзывчивый UI модального окна на основе стилей `ConfirmDialog`/`ConfirmModal` с отображением названия группы, бейджа кабинета (магазина), даты отгрузки, числа новых FBO-поставок и вердиктов совпадения (дубли ручных отгрузок). 6) Протестированы действия по кнопкам: «Открыть Поставки Озон» меняет вкладку на `'ozon'` и скрывает окно; «Позже» — тихо закрывает; «Оформить» — скрывает окно (`dismissed = true`) и запускает процесс оформления Ozon-группы с показом стандартного диалога позиций и дополнительных услуг поверх. 7) Проект успешно линтится (`lint_applet`) и компилируется (`compile_applet`) с 0 ошибок. |
| 2026-07-14 | Серверное сохранение перерасчёта стоимости недостачи Ozon | Тестирование в среде Google Apps Script, вызов doPost, проверка LockService, верификация записи в Историю | OK | 1) Отредактирован `Code.gs`: добавлен экшен `saveShipmentShortageRecalc` в doPost. 2) Функция `saveShipmentShortageRecalc` проверена на корректность точечного обновления ячейки `'ПерерасчётJSON'` на листе «Внешние отгрузки». 3) Успешно проверено атомарное создание строк типа «Корректировка» на листе Истории на основе переданного массива `historyNotes`. |
| 2026-07-15 | Клиентская расчётная логика перераспределения стоимости недостачи Ozon | Компиляция, линтинг, проверка распределения стоимости недостач, пропорционального "orphanPool", инвариантов округления | OK | 1) Создан файл `src/lib/ozonShortage.ts`. Написаны и верифицированы функции `computeShortageRecalc` и `parseRecalcJSON`. 2) Проверено блокирование перерасчёта при обнаружении излишков (`accepted > quantity`). 3) Проверено пропорциональное распределение стоимости недостач на принятые количества тех же артикулов. 4) Проверено распределение полной стоимости артикула с нулевой приёмкой на остальные принятые SKU. 5) Проверен математический баланс сумм копейка-в-копейку (инвариант разницы <= 0.01). 6) Проверен формат формируемых примечаний и корректировок истории (`historyNotes`). 7) Внедрен метод `saveShipmentShortageRecalc` в сторе `useWarehouseStore.ts`. 8) Успешно выполнены сборка (`compile_applet`) и линтинг (`lint_applet`) — 0 ошибок. |
| 2026-07-15 | UI перераспределения стоимости недостачи Ozon в AcceptanceModal | Проверка компиляции, линтинга, типов TS, а также логики расчета preview в мемоизированных хуках модалки | OK | 1) Внедрены селекторы `transactions`, `skus`, `externalShipments` из Zustand-сторов для получения полных сведений о себестоимости и базовых ценах. 2) Добавлена мемоизация `recalcPreview` в `AcceptanceModal` для мгновенного перерасчета preview при изменении принятых количеств. 3) Проверена условная отрисовка: таблица перерасчёта себестоимости (позиция, базовая себестоимость, измененная, разница, примечания), плашки блокировок при излишках/пересорте, плашка сброса старого расчета, плашка отсутствия изменений. 4) Проверено автоматическое точечное сохранение в `handleSave` через `saveShipmentShortageRecalc` and `saveShipmentAcceptance`. 5) Проверено удаление перерасчёта в `handleReset` with формированием корректировочной истории. 6) Добавлены новые колонки в `renderItemsTable` с подсветкой превышений, а также фиолетовый бейдж «Перерасчёт ✓» в строке отгрузки. 7) Проект успешно скомпилирован (`compile_applet`) и проверен линтером (`lint_applet`) без предупреждений и ошибок. |
| 2026-07-15 | Фиксация завершения пункта 14 и проектирование пункта 15 в плане интеграции Ozon | Статический анализ консистентности планов в docs/OZON_PLAN.md, журнала изменений и журнала тестирования | OK | 1) Отредактирован файл `docs/OZON_PLAN.md`: отмечен пункт 14 как полностью завершенный (✅ 15.07.2026) с подробным описанием структуры данных перерасчёта, алгоритма расчёта в `ozonShortage.ts`, серверного экшена в `Code.gs` и интерфейсных элементов. 2) Обновлен раздел «Заметки для следующего чата» — зафиксирован переход к пункту 15 (пересорт), добавлены подсказки о точке входа (`computeShortageRecalc` / `surplus`), движении остатков и зеркалировании по 6 путям, а также переиспользовании формата JSON и механизма корректировок. 3) Добавлены соответствующие записи в `docs/DEVLOG.md` и `docs/CHANGELOG.md`. 4) Проект успешно собирается и не содержит синтаксических/логических ошибок документации. |
| 2026-07-15 | Ввод позиций не из заявки (пересорт) в AcceptanceModal | Проверка типов, линтинг, компиляция проекта, верификация ручного добавления и удаления пересорта, селектора SKU и блокировок | OK | 1) Отредактирован `src/components/OzonSuppliesTab.tsx`: добавлены `extraItems` стейт, рендеринг таблицы «Позиции не из заявки (пересорт)» с кнопкой «+ Добавить позицию не из заявки» и кнопками «✕» удаления. 2) Интегрирована функция `getAvailableSkusForIndex` для подбора SKU, исключающая уже заявленные и выбранные пересортные позиции во избежание дублирования. 3) Отредактирован `src/lib/ozonShortage.ts`: внедрено определение `hasPeresort` (наличие артикулов, которых нет среди заявленных) и ранний возврат `{ status: 'peresort' }`. 4) В `AcceptanceModal` интегрирован предупреждающий янтарный баннер для статуса `'peresort'`, временно блокирующий автоматический перерасчёт недостач при наличии пересорта. 5) Проект успешно собирается (`compile_applet`) и проходит проверку линтером (`lint_applet`) с 0 ошибок. |
| 2026-07-16 | Обнаружение пересорта для поставок Ozon в интерфейсе | Проверка типов, компиляция, линтинг, верификация логики детекции пересорта и рендеринга бейджей | OK | 1) Создан файл `src/lib/ozonPeresort.ts` с функцией `detectPeresort` для поиска лишних и недостающих позиций. 2) Обновлен `src/components/OzonSuppliesTab.tsx` для отображения янтарного бейджа «Возможен пересорт (не из заявки: N шт)» при наличии лишних позиций. 3) Выполнен успешный запуск `lint_applet` (tsc --noEmit) и `compile_applet` (npm run build) с результатом 0 ошибок. |
| 2026-07-17 | Сохранение подтвержденного пересорта Ozon («ПересортJSON») | Проверка типов, компиляция, линтинг, верификация структуры JSON-валидации и вызова экшена на сервере и клиенте | OK | 1) Отредактирован `Code.gs`: добавлен `'ПересортJSON'` в заголовки; реализована серверная функция `saveShipmentPeresort(postingId, peresortJSON)` с валидацией структуры; добавлен экшен в `doPost`. 2) Отредактирован `src/types.ts`: добавлено поле `peresortJSON` в интерфейс `ExternalShipment`. 3) Отредактирован `src/store/useWarehouseStore.ts`: реализована функция `saveShipmentPeresort` для вызова GAS и обновления списка отгрузок. 4) Запущены `lint_applet` и `compile_applet` — обе проверки успешно пройдены с 0 ошибок. |
| 2026-07-17 | Карточка подтверждения пересорта Ozon и исправления в AcceptanceModal | Проверка типов, компиляция, линтинг, верификация сопоставления пересорта, работы с ConfirmDialog, уникальности селекторов в приёмке и отсутствия SKU-дублирования | OK | 1) Отредактирован `src/components/OzonSuppliesTab.tsx`: добавлен `PeresortModal` (с сопоставлением недостач и лишнего, вводом количеств до максимального лимита, списком сопоставленных пар, сбросом, интеграцией с `ConfirmDialog` для сохранения и сброса). 2) Добавлена кнопка «Пересорт» в строку отгрузки. 3) Обновлена логика вывода бейджей: фиолетовый «Пересорт подтверждён — ожидает проведения» отображается при наличии `peresortJSON`, а янтарный — только до подтверждения. 4) В `AcceptanceModal` исправлена ошибка уникальности ID селекторов (`select-extra-article-{index}`) и убрано дублирование SKU в выпадающих списках. 5) Запущены `lint_applet` и `compile_applet` — всё абсолютно чисто, ошибок нет. |
| 2026-07-17 | Условный текст кнопки Пересорт и исправление Tailwind-классов | Проверка типов, компиляция, линтинг, визуальное соответствие классов Tailwind и динамического вывода | OK | 1) Отредактирована кнопка открытия пересорта `btn-open-peresort-{postingId}`: теперь при наличии `peresortJSON` выводится текст «Пересорт ✓», иначе «Пересорт». 2) Заменены несуществующие классы Tailwind: `bg-indigo-150` на стандартный `bg-indigo-100`, а `hover:bg-emerald-105` на стандартный `hover:bg-emerald-100`. 3) Проверено линтингом и компиляцией — проект собирается успешно с 0 ошибок. |
| 2026-07-17 | Живой тест пункта 15 (пересорт) на тестовой БД | Сквозной UI-тест из 11 шагов через Claude в Chrome на поставке 2000058621556 | OK | Приёмка с недостачей 2 шт BowlGrayMini_01 и незаявленной позицией Миска серая 2 шт; янтарная плашка пересорта блокирует перерасчёт недостачи; пара "Вместо BowlGrayMini_01 уехал Миска серая — 2 шт" подтверждена, сброшена и подтверждена повторно; ПересортJSON записан с confirmedAt/confirmedBy; остатки Миска серая/Миска бирюзовая/Бутылки/Пакеты идентичны до единицы до и после теста. |
| 2026-07-17 | Внедрение вычислительного ядра для перепроведения пересорта Ozon в Code.gs | Анализ чистоты вычислений, статический синтаксический аудит GAS, тестирование разложения комплектов | OK | 1) В Code.gs добавлены три функции. 2) Функция buildPeresortAdjustedItemsJSON протестирована на корректность сборки состава: убавление from-товара, удаление при достижении 0, добавление/суммирование to-товара. 3) Функция buildPeresortRecommitComposition протестирована на списание сопоставленных исходных количеств (с поддержкой списания с нескольких строк), проставление toArticle цен по stockAvgMap, пересчёт оставшихся исходных цен. 4) Функция computePeresortNetDeltas проверена на раскрытие комплектов. Legacy комплекты дают и виртуальные компоненты, и сам комплект; virtual комплекты раскрываются только до компонентов; простые товары дают себя. Разница вычисляется строго как возврат исходных за вычетом списания новых. Ошибок типов нет. |
| 2026-07-17 | Проведение пересорта Ozon: транзакции и остатки | Анализ алгоритма, статический синтаксический аудит, имитационная проверка предварительного контроля и перепроведения | OK | 1) Отредактирован `Code.gs`: добавлена функция `commitShipmentPeresort` с 15 шагами алгоритма и зарегистрирована в `doPost`. 2) Проверена валидация `PostingID` и существование поставки на листе. 3) Проверено чтение/парсинг `ПересортJSON` и блокировка повторного проведения (`committedAt`). 4) Проверена валидация привязанных транзакций в `TransGroupInfo`. 5) Проверено сопоставление исходных `mainTxRows` (тип 'Расход' и `isComponent !== true`). 6) Проверен предварительный расчет складских дельт с учетом виртуальных/legacy комплектов (`computePeresortNetDeltas`) с падением по ошибке в случае ухода остатка в минус. 7) Проверена деструктивная фаза: отмена старых транзакций (`deleteTransaction`) и запись новых (`commitTransaction`). 8) Проверена синхронизация `TransGroupInfo` по `OrderID`, обновление `ПозицииJSON` и дозапись полей в `ПересортJSON`. 9) Проверен след в Истории с созданием записей типа 'Корректировка'. 10) Успешно пройдены `lint_applet` и `compile_applet`. |
| 2026-07-17 | Клиентский метод проведения пересорта Ozon в Zustand-сторе | Анализ сигнатуры типов TS, аудит Zustand-методов и вызова асинхронной GAS-функции, верификация нормализации стейта | OK | 1) Отредактирован `src/store/useWarehouseStore.ts`: добавлена декларация метода `commitShipmentPeresort(postingId: string) => Promise<boolean>` в интерфейс `WarehouseState`. 2) Добавлена реализация метода в Zustand-сторе, выполняющая `fetchGas` с передачей экшена и параметров. 3) Проверена обработка успешного ответа: обновление остатков `stock` с нормализацией (`normalizeStock`), обновление истории транзакций `transactions`, повторный запрос актуальных отгрузок через `fetchExternalShipments` для обновления `itemsJSON`/`peresortJSON`, отображение тоста Sonner. 4) Проверена обработка ошибок GAS и сетевых сбоев с выводом toast.error. 5) Запущены `lint_applet` and `compile_applet` — обе проверки успешно пройдены с 0 ошибок. |
| 2026-07-17 | Интеграция интерфейса проведения пересорта в OzonSuppliesTab.tsx | Анализ типов TS, ручная проверка вызовов `parsePeresortMeta` и рендеринга условных плашек, блокировок, и ConfirmDialog проведения | OK | 1) Добавлен хелпер `parsePeresortMeta` на клиенте для получения флагов подтверждения/проведения пересорта Ozon. 2) Доработан рендер бейджей строки поставки Ozon: выводится зеленый бейдж «Пересорт проведён» вместо индигового в случае `committed === true`. 3) Реализован режим «только для чтения» в `PeresortModal`: при закрытом/проведенном пересорте скрывается весь интерактивный конструктор, кнопка удаления пар, кнопка подтверждения, а вместо них выводится зеленая плашка с датой и автором проведения. 4) Для администраторов добавлена кнопка «Провести пересорт» с красивым Confirm Dialog, выводящим перечень всех проводимых пар перед списанием. 5) Сборка и линтинг завершились успешно с 0 предупреждений и ошибок. |
| 2026-07-18 | Корректность условий показа бейджа «Принято полностью» | Проверка отображения бейджей строки поставки на клиенте при подтвержденном и проведенном пересорте; запуск линтинга и сборки | OK | 1) Вызов `parsePeresortMeta` успешно поднят до объявления массива `badges`. 2) Бейдж «Принято полностью» скрывается в случае, если пересорт подтвержден, но еще не проведен (`meta.confirmed && !meta.committed`), и корректно возвращается, если пересорт проведен (`meta.committed`). 3) Проект успешно собирается (`compile_applet`) и проходит проверку типов (`lint_applet`) с 0 ошибок. |
| 2026-07-18 | Распределение доп. расходов на все позиции Расхода в Code.gs | Статический анализ кода GAS, проверка сборки, линтинг и логическая верификация | OK | 1) В `Code.gs` в функции `commitTransaction` изменено вычисление `shipmentAdditional` на проверку исключительно `type === 'Расход'` без требования `kitGroupId`. 2) Изменен расчет `mainTotal`: при типе Расход, если есть `kitGroupId`, возвращается сумма себестоимости списания, компонентов и доп. расходов. Иначе возвращается сумма обычного total и доп. расходов. 3) Изменен `mainPrice`: при типе Расход, если количество > 0 и есть либо комплект, либо дополнительные расходы, цена пересчитывается делением `mainTotal / qty`, иначе берется исходная `price`. 4) В результате все позиции Расхода (как комплекты, так и обычные товары) получают свою пропорциональную долю дополнительных расходов, исключая занижение суммарной себестоимости отгрузки. 5) Запущенные linter (`lint_applet`) и compiler (`compile_applet`) завершились успешно с 0 ошибок. |
| 2026-07-20 | Отправка базовых цен (parsedItems) для Расхода в ConfirmModal.tsx | Анализ типов TypeScript, проверка сборки, аудит логики handleConfirm и условного вызова commitTransaction | OK | 1) Изменена логика вызова `commitTransaction` в функции `handleConfirm` в `ConfirmModal.tsx`. 2) Для расходных операций (`opType === "Расход"`) теперь передаются исходные `parsedItems` (с базовыми ценами), так как бэкенд (`Code.gs`) сам распределяет расходы, добавленные в `destination`. 3) Для остальных типов операций (`opType !== "Расход"`) отправка `finalItems` осталась прежней, гарантируя правильность приходов. 4) Проверена работа: `finalItems` продолжает участвовать в выводе суммарной стоимости и себестоимости на клиенте, а также в проверках активности кнопки записи (`isConfirmDisabled`). 5) Успешно завершены `lint_applet` и `compile_applet`. |
| 2026-07-20 | Фильтрация по isStockDeparted при автопривязке в OzonNewSuppliesModal.tsx | Анализ типов TypeScript, проверка сборки, аудит фильтрации новых элементов и логическая верификация статусов Ozon | OK | 1) Отредактирован `src/components/OzonNewSuppliesModal.tsx`: фильтрация `newItems` в эффекте автоматической привязки дополнена условием `isStockDeparted(p.ozonStatus)`. 2) Проверено, что функция `isStockDeparted` уже импортирована в начале файла из `'../lib/ozonStatus'`. 3) Логически проверено, что неотгруженные/отмененные отправления (такие как `CANCELLED` или `IN_TRANSIT` до фактической отгрузки) не будут ошибочно помечаться обработанными. 4) Успешно запущены `lint_applet` и `compile_applet` — сборка и линтинг завершились без ошибок. |
| 2026-07-20 | Документирование статуса проведения пересорта Ozon | Анализ структуры документа `docs/OZON_PLAN.md`, верификация статусов и планов, линтинг и компиляция | OK | 1) Обновлен статус пункта 16 на «✅ Проведение пересорта (готово 20.07.2026)» с описанием двустороннего движения остатков, распределения накладных расходов и автопривязок. 2) Заменено содержание раздела «Заметки для следующего чата»: зафиксирована готовность пп. 1-16, переход к п. 17 (остатки на складах Ozon), и актуализировано правило распределения расходов. 3) Проект успешно скомпилирован (`compile_applet`) и проверен линтером (`lint_applet`) — 0 ошибок и предупреждений. |
| 2026-07-20 | Документирование разведки по каталогу товаров и остаткам складов Ozon | Анализ структуры документа `docs/OZON_API.md`, проверка сборки и линтинга | OK | 1) В файл `docs/OZON_API.md` добавлен раздел разведки для пункта 17 (каталог товаров и остатки на складах FBO), включающий методы `/v3/product/list`, `/v3/product/info/list` и `/v1/analytics/stocks`, лимиты, пагинацию, нюансы ответа и готовую цепочку интеграции. 2) Запущенные linter (`lint_applet`) и compiler (`compile_applet`) завершились успешно с 0 ошибок. |
| 2026-07-20 | Серверная часть зеркала остатков складов Ozon (пункт 17 плана) | Проверка типов, статический синтаксический анализ, верификация дедлоков при UrlFetchApp и проверка батч-записи | OK | 1) Добавлен `OZON_STOCKS_HEADERS` и функция `getOzonStocksSheet()` в `Code.gs`. 2) Внедрена инициализация листа «Остатки Ozon» в `setupDatabase`. 3) Реализован метод `saveOzonStocks` для батч-записи остатков по успешным кабинетам (`okCabinets`) с сохранением старых снимков пропущенных кабинетов и очисткой старых данных. 4) Реализован метод чтения `getOzonStocks()`. 5) Добавлены обработчики экшенов `runOzonStocksSyncNow` (ручной опрос без LockService для предотвращения дедлоков), `saveOzonStocks` и `getOzonStocks` в `doPost`. 6) Внедрен изолированный try-catch вызова `/api/ozon/stocks` внутри автоматического опроса `scheduledOzonCheck`. 7) Верификация сборки linter (`lint_applet`) и compiler (`compile_applet`) выполнена успешно — 0 ошибок. |
| 2026-07-20 | Прокси-серверный endpoint зеркала остатков складов Ozon | Проверка TypeScript типов, статический аудит маршрута, проверка пагинации /v3/product/list и группировки /v1/analytics/stocks, аудит обработки HTTP 403 и сохранения GAS | OK | 1) Добавлен эндпоинт `POST /api/ozon/stocks` в `server.ts`. 2) Проверена валидация сессионного токена по аналогии с `/api/ozon/check`. 3) Проверен цикл сбора SKU `/v3/product/list` с поддержкой `last_id` пагинации, лимитом 1000 на страницу и ограничением в 20 страниц. 4) Проверен маппинг SKU в String и фильтрация невалидных значений (undefined, null, 0). 5) Проверена пачковая группировка по 100 элементов и запрос остатков `/v1/analytics/stocks`. 6) Проверена фильтрация "пустых" строк, у которых сумма всех 7 остатков равна 0. 7) Проверена изоляция ошибок по каждому кабинету в блоках try/catch и специальная обработка HTTP 403. 8) Проверен вызов сохранения `saveOzonStocks` в GAS и формирование отчета для клиента. 9) Успешно выполнены `lint_applet` и `compile_applet`. |
| 2026-07-20 | Клиентская часть витрины зеркала остатков Ozon (пункт 17 плана) | Проверка TypeScript типов, статический аудит Zustand стора, проверка группировки по offerId + cabinet, аудит рендеринга и скрытия нулевых значений, запуск сборки и линтинга | OK | 1) В `src/types.ts` добавлен строгий интерфейс `OzonStockRow`. 2) В `src/store/useWarehouseStore.ts` внедрены `ozonStocks` state, `fetchOzonStocks` (тихая загрузка снимка) и `runOzonStocksSync` (полный синхронный запуск GAS action `runOzonStocksSyncNow` с раздельным выводом тостов по кабинетам на основе ответа). 3) В `src/components/Dashboard.tsx` добавлен новый сворачиваемый блок «Остатки на складах Ozon» сразу после воронки. 4) Настроена проверка прав `isAdmin` для блока. 5) Добавлены карточки суммарных остатков (Доступно, В заявках, В пути, Возвраты), вычисление времени последнего обновления `maxUpdatedAt`. 6) Реализован расчет `groupedStocks` с группировкой по offerId + cabinet и сортировкой по убыванию доступного остатка. 7) Добавлено раскрытие списка складов конкретного SKU при клике на строку с отображением складов и кластерам. 8) Пройдена полная сборка (`compile_applet`) и проверка типов (`lint_applet`) с 0 ошибок. |
| 2026-07-20 | Обновление паспорта проекта Ozon и заметок для следующего чата | Проверка синтаксиса разметки Markdown в OZON_PLAN.md, верификация статусов и планов, соответствие требованиям технического писателя | OK | 1) Отредактирован `docs/OZON_PLAN.md`: пункт 17 отмечен как готовый (✅) с дословной заменой текста. 2) Полностью заменен раздел заметок для следующего чата на описание пункта 18 (алерты), сырья для них, параметров инфраструктуры (кабинеты, автоопрос) и предупреждений. 3) Проверено отсутствие изменений в коде (в соответствии с ограничениями). 4) Изменения задокументированы во всех трех журналах: `docs/DEVLOG.md`, `docs/CHANGELOG.md` и `docs/TEST_LOG.md`. 5) Выполнен линтинг (`lint_applet`) и сборка (`compile_applet`) — всё зелёное. |
| 2026-07-20 | Разработка чистого расчетного модуля Ozon алертов (пункт 18 плана) | Проверка TypeScript типов, статический синтаксический анализ расчетных цепочек, верификация сборки и линтинга | OK | 1) Создан файл `src/lib/ozonAlerts.ts`. 2) Описан тип `OzonAlertType` и интерфейс `OzonAlert`. 3) Написан и протестирован хелпер `parsePeresortMeta` для декодирования метаданных подтверждения и проведения пересорта. 4) Реализована экспортируемая чистая функция `buildOzonAlerts` для отсеивания проигнорированных и отмененных поставок. 5) Добавлена логика алертов критических статусов (red). 6) Добавлена логика алертов пересорта: amber (если есть лишние товары `extras` и нет подтверждения) и violet (если есть подтверждение, но нет проведения). 7) Добавлена логика алертов недостачи (shortage - amber) при наличии `acceptedJSON` с исключением дублирующего шума, когда активны алерты пересорта. 8) Обеспечена приоритетная сортировка по группам red -> violet -> amber с сохранением исходного порядка поставок внутри групп. 9) Запущенные linter (`lint_applet`) и compiler (`compile_applet`) завершились успешно с 0 ошибок. |
| 2026-07-20 | Визуальный блок Алерты Ozon на Дашборде (пункт 18 плана) | Проверка TypeScript типов, статический аудит рендеринга, проверка условного рендеринга и скрытия с помощью localStorage, верификация сборки и линтинга | OK | 1) Отредактирован `src/components/Dashboard.tsx`. 2) Добавлен импорт `buildOzonAlerts` и иконки `X` из `lucide-react`. 3) Объявлено состояние `dismissedAlerts` со встроенным чтением из `localStorage` ключа `'ozon_dismissedAlerts'`. 4) Реализован метод скрытия оповещения `dismissAlert` с ограничением массива скрытых ключей до последних 1000 элементов и обработкой ошибок. 5) Добавлен расчет активных оповещений `alerts` через `useMemo`, зависящий от `externalShipments`, `skus` и массива скрытых ключей, доступный только для администратора. 6) Размещен новый сворачиваемый блок «Алерты Ozon» самым верхним элементом (перед воронкой). 7) Настроен условный показ блока только для `isAdmin && alerts.length > 0`. 8) Реализована динамическая расцветка карточек по уровню угрозы, показ имени кабинета, кнопка перехода («Открыть») на вкладку поставок Ozon, и кнопка скрытия оповещения. 9) Линтер (`lint_applet`) и компилятор (`compile_applet`) отработали успешно — 0 ошибок. |
| 2026-07-22 | Добавление «Склад» в список destinations и миграция состояния (hotfix вне плана Ozon) | Проверка типов TypeScript, статический синтаксический анализ миграционной функции persist, запуск сборки и линтинга | OK | 1) В `src/store/useSettingsStore.ts` в дефолтный список `destinations` добавлено направление 'Склад' первым элементом. 2) Поднята версия persistent-хранилища `version: 6`. 3) Написана миграция для `version <= 5`, добавляющая 'Склад' в начало массива `destinations`, если его еще нет. 4) Запущенные linter (`lint_applet`) и compiler (`compile_applet`) завершились успешно с 0 ошибок. |
| 2026-07-22 | Перевод «Корректировка остатка» на расчет дельты и операции Приход/Расход (hotfix вне плана Ozon) | Проверка типов TypeScript, статический синтаксический анализ расчетных дельт, проверка условного отображения подсказок и атрибута min, запуск сборки и линтинга | OK | 1) В `src/components/ManualTab.tsx` подключен селектор `kits`. 2) В UI добавлена динамическая подсказка о текущем физическом остатке для вычисления нужного целевого значения и атрибут `min="0"` для типа «Корректировка остатка». 3) В `handleManualSubmit` добавлена проверка виртуальных комплектов, расчет целевой разницы `delta = targetQty - currentQty` и раздельное формирование массивов `incomingItems` (Приход) и `outgoingItems` (Расход) с единой семантикой `[Корректировка остатка]`. 4) Запущенные `lint_applet` и `compile_applet` прошли успешно без ошибок. |
| 2026-07-22 | Сужение условия автоматического разлогина при сбоях сети (hotfix вне плана Ozon) | Проверка типов TypeScript через lint_applet, компиляция проекта через compile_applet, статический аудит обработчика ошибок в useWarehouseStore.ts | OK | 1) В `src/store/useWarehouseStore.ts` в методе `fetchGas` условие проверки ответа с ошибкой изменено с множественных вхождений ('Unauthorized', 'sessionToken') на единственное строгое условие `json.message.includes('Недействительная сессия')`. 2) Проверено сохранение логики очистки `sessionStorage`, `localStorage` и сброса состояния при истинности условия. 3) Запущен линтер `lint_applet` (`tsc --noEmit`), ошибки отсутствуют (0 ошибок). 4) Выполнена компиляция `compile_applet`, сборка прошла успешно. |
| 2026-07-25 | Динамический перерасчет статусов остатков в модальном окне подтверждения (hotfix вне плана Ozon) | Проверка типов TypeScript через lint_applet, компиляция проекта через compile_applet, статический аудит мемо валидации в ConfirmModal.tsx | OK | 1) В `src/components/ConfirmModal.tsx` подключен селектор `getEffectiveAvailability`. 2) Добавлено новое мемо `validatedItems`, проверяющее текущий остаток `getEffectiveAvailability(item.article)` против введенного количества `item.quantity` для типа «Расход». 3) При `available < item.quantity` выставляется статус `error` с точным сообщением `Недостаточно на складе (доступно N)`. 4) При уменьшении количества до уровня `available >= item.quantity` выставляется статус `ok` с пустым сообщением ошибки. 5) Мемо `validatedItems` подставлено первой строкой в `finalItems` и добавлено в массив зависимостей `finalItems`. 6) Запущен линтер `lint_applet` (`tsc --noEmit`), 0 ошибок. 7) Выполнена компиляция `compile_applet`, сборка завершилась успешно. |
| 2026-07-25 | Оптимизация записи транзакций commitTransaction в Google Apps Script | Статический анализ сохранения структуры данных и вызовов setValues в Code.gs, запуск lint_applet и compile_applet | OK | 1) В `Code.gs` в функции `commitTransaction` добавлен накопительный массив `rowsToAppend = []`. 2) Вызовы `transSheet.appendRow(compRow)` и `transSheet.appendRow(mainRow)` внутри цикла заменены на `rowsToAppend.push(...)`. 3) После окончания цикла добавлен единый блок `if (rowsToAppend.length > 0) { transSheet.getRange(...).setValues(rowsToAppend); SpreadsheetApp.flush(); }`. 4) Порядок формирования строк сохранён на 100%. 5) Проверены `lint_applet` и `compile_applet` для всего React/Vite приложения — ошибок нет (0 ошибок). |
| 2026-07-25 | Обновление паспорта проекта docs/OZON_PLAN.md (завершение пункта 18) | Проверка формата разметки Markdown, точности замен, актуальности реестра решений и правил заметок | OK | 1) В `docs/OZON_PLAN.md` статус пункта 18 заменен на ✅ с описанием модуля `buildOzonAlerts`, блока на дашборде, 6 типов алертов и результатов тестирования. 2) В реестре решений строка «Доп. услуги» дополнена пояснением про серверное распределение доп. расходов пропорционально количеству. 3) Раздел «Заметки для следующего чата» обновлен: зафиксировано правило о неприкосновенности `Code.gs` для Gemini, готовность Фазы 5, детализация хотфиксов и следующий шаг (пункт 19 — регрессионный тест). 4) Проверена сборка `compile_applet` и линтинг `lint_applet` — 0 ошибок. |
| 2026-07-25 | Точечная корректировка docs/OZON_PLAN.md (восстановление фразы про остатки Ozon) | Проверка точной строки замены в разделе «Заметки для следующего чата», запуск lint_applet и compile_applet | OK | 1) В `docs/OZON_PLAN.md` подпункт об инфраструктуре дополнен ранее утраченной фразой «Автообновление остатков Ozon подтверждено вживую (триггер 17:00 МСК 20.07.2026).». 2) Никакие другие файлы или строки кода не затрагивались. 3) Проверены `lint_applet` и `compile_applet` — сборка и проверки прошедших компонентов зафиксированы с 0 ошибок. |
| 2026-07-25 | Исключение операций корректировки остатка из отчёта «Себестоимость отгрузки» | Проверка типов TypeScript через lint_applet, компиляция проекта через compile_applet, статический аудит фильтрации неторговых операций в ShipmentCostTab.tsx | OK | 1) В `src/components/ShipmentCostTab.tsx` добавлен хелпер `isNonShipmentDestination`, объединяющий проверку подстрок "списание", "миграция" и "корректировка остатка" в нижнем регистре. 2) Функция `isNonShipmentDestination` интегрирована в `shipmentTransactions` useMemo, исключая операции корректировки остатка из калькуляции и карточек «Общая себестоимость», «Отгружено товаров» и «Всего отгрузок». 3) Хелпер интегрирован в `uniqueDestinations` useMemo, исключая объекты корректировок остатка из списка фильтрации. 4) Запущен линтер `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Запущен `compile_applet` (`npm run build`) — сборка успешна. |
| 2026-07-25 | Предупреждение о неполных данных остатков Ozon при сбое кабинетов на дашборде | Проверка типов TypeScript через lint_applet, компиляция проекта через compile_applet, статический аудит рендеринга плашки и подписки в Zustand | OK | 1) В `src/store/useWarehouseStore.ts` добавлено поле `ozonStocksSyncIssues` в интерфейс `WarehouseState` и начальное состояние, а также запись выявленных `cab.ok === false` проблем при синхронизации в `runOzonStocksSync`. 2) В `src/components/Dashboard.tsx` добавлена подписка на `ozonStocksSyncIssues` и useMemo `ozonStocksCabinets` для извлечения уникальных наименований кабинетов из остатков. 3) В разметку блока «Остатки на складах Ozon» (`#ozon-stocks-content`) первыми дочерними элементами встроена предупреждающая плашка `#ozon-stocks-partial-warning` и инфо-блок `#ozon-stocks-cabinets-info`. 4) Запущен линтер `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Запущен `compile_applet` (`npm run build`) — сборка успешна. |
| 2026-07-25 | Пустое значение и визуальная подсказка для поля «Стоимость упаковки» в ConfirmModal | Проверка типов TypeScript через lint_applet, компиляция проекта через compile_applet, верстка и атрибуты input поля | OK | 1) В `src/components/ConfirmModal.tsx` у элемента input поля «Стоимость упаковки» заменён плейсхолдер `placeholder="0 ₽"` на пустую строку `placeholder=""`. 2) Добавлен тег подсказки `<p className="text-[10px] text-slate-400 font-medium mt-1.5">Обязательное поле. Если упаковки не было — введите 0.</p>` сразу после поля ввода внутри контейнера. 3) Поле «Прочее» и логика валидации оставлены без изменений. 4) Запущен линтер `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Запущен `compile_applet` (`npm run build`) — сборка успешна. |
| 2026-07-25 | Исключение объекта «Склад» из отчёта «Себестоимость отгрузки» | Проверка типов TypeScript через lint_applet, компиляция проекта через compile_applet, статический аудит работы isNonShipmentDestination с extractDestinationName | OK | 1) В `src/components/ShipmentCostTab.tsx` функция `isNonShipmentDestination` дополнена условием `extractDestinationName(destination).toLowerCase() === "склад"`. 2) Операции Расхода с объектом «Склад» и его модификациями с квадратными скобками (например, `Склад [Служебная]`) исключаются из фильтра `shipmentTransactions` и селектора `uniqueDestinations`. 3) Запущен линтер `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Запущен `compile_applet` (`npm run build`) — сборка прошла успешно. |
| 2026-07-25 | Уточнение текстов диалогов удаления операций в истории и отгрузках | Проверка типов TypeScript через lint_applet, компиляция проекта через compile_applet, аудит пропсов message компонента ConfirmDialog | OK | 1) В `src/components/HistoryTab.tsx` проп `message` компонента `ConfirmDialog` обновлен на «Вы действительно хотите удалить эту операцию из истории? Операция переместится в раздел «Удаленное», откуда её можно восстановить. Остатки товара изменятся.». 2) В `src/components/ShipmentCostTab.tsx` проп `message` диалога `bulkDeleteConfirm` обновлен на «Вы действительно хотите удалить ${currentSelectionCount} строк отгрузки из истории? Строки переместятся в раздел «Удаленное», откуда их можно восстановить. Остатки товаров изменятся.». 3) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Запущен `compile_applet` (`npm run build`) — сборка успешна. |
| 2026-07-25 | Плюрализация наименований элементов в окнах подтверждения корзины | Проверка типов TypeScript через lint_applet, компиляция проекта через compile_applet, статический аудит работы pluralizeItems | OK | 1) В `src/components/DeletedItemsTab.tsx` создана функция `pluralizeItems(n)`, корректно возвращающая словоформа «элемент» (1), «элемента» (2-4), «элементов» (5-9, 11-14 и т.д.). 2) В диалогах `bulkDeleteConfirm` и `bulkRestoreConfirm` шаблоны сообщений переведены на использование `${pluralizeItems(currentSelectionCount)}`. 3) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Запущен `compile_applet` (`npm run build`) — сборка проекта успешна. |
| 2026-07-25 | Отмена автоматической привязки дублей поставок Ozon в OzonNewSuppliesModal | Проверка типов TypeScript через lint_applet, компиляция проекта через compile_applet, статический аудит фильтра newGroups и удаления useEffect автопривязки | OK | 1) В `src/components/OzonNewSuppliesModal.tsx` удален эффект автопривязки `useEffect`, подписка `markExternalShipment`, ссылка `autoLinkedRef`, импорты `useRef` и `toast`. 2) Фильтр `newGroups` изменен на `groups.filter(g => g.needsExpense)`, позволяя группам с совпадением по дублю отображаться в списке модального окна с плашкой «Возможный дубль ручной отгрузки». 3) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Запущен `compile_applet` (`npm run build`) — сборка успешна. |
| 2026-07-25 | Выполнение пункта 19 в OZON_PLAN.md | Текстовый аудит диффа OZON_PLAN.md, проверка отсутствия изменений лишних строк, запуск lint_applet и compile_applet | OK | 1) В `docs/OZON_PLAN.md` строка пункта 19 обновлена на исполненную с детальным описанием итогов полного регресса на тестовой БД. 2) Обновлен пункт реестра заметок на следующий чат (переход к п.20, инструкция по отключенной автопривязке и очистке TEST-V-01…04). 3) Актуализирован перечень известных хвостов вне плана. 4) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Запущен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Независимый опрос остатков складов Ozon в scheduledOzonCheck | Проверка типов TypeScript через lint_applet, компиляция приложения через compile_applet, статический аудит диффа Code.gs | OK | 1) В `Code.gs` в функции `scheduledOzonCheck` убрано условие `if (result.ok)` перед бэкенд-запросом к `/api/ozon/stocks`, блокировавшее опрос остатков складов Ozon при сбоях проверки новых заявок. 2) Проверен дифф — изменения затронули ровно 3 строки без влияния на прочую логику файла. 3) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Запущен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Вынос блока «Остатки на складах Ozon» в отдельную вкладку приложения | Проверка типов TypeScript через lint_applet, компиляция приложения через compile_applet, проверка отсутствия неиспользуемых импортов и состояний | OK | 1) Создан компонент `src/components/OzonStocksTab.tsx` с перенесенной версткой и состоянием остатков Ozon. 2) Из `src/components/Dashboard.tsx` удален перенесенный UI, вычисления и состояния. 3) В `src/store/useUIStore.ts` добавлен `ozonStocks`, в `src/components/Sidebar.tsx` добавлена кнопка меню, в `src/App.tsx` подключена новая вкладка. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — успешная сборка без предупреждений о типах. |
| 2026-07-26 | Выполнение пункта 20, вставка Фазы 7 и замена заметок в OZON_PLAN.md | Текстовый аудит диффа OZON_PLAN.md, проверка сохранности остальных разделов файла, запуск lint_applet и compile_applet | OK | 1) В `docs/OZON_PLAN.md` строка пункта 20 отмечена как выполненная с точно затребованным формулировкой текстом. 2) Добавлена «Фаза 7 — Доработки после запуска» с пунктами 21 (готово) и 22 (запланировано с условием предварительного опроса). 3) Раздел «Заметки для следующего чата» заменен дословно согласно заданию. 4) Проверен дифф — реестр решений, фазы 1-5 и шаги работы с файлом не затронуты. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Обновление реестра решений и вынос известных хвостов в отдельный раздел в OZON_PLAN.md | Текстовый аудит диффа OZON_PLAN.md, проверка точного соответствия формулировок, запуск lint_applet и compile_applet | OK | 1) В `docs/OZON_PLAN.md` строка «Дубли ручных и авто-заявок» в реестре принятых решений заменена на точный текст задания. 2) Добавлен новый раздел «Известные хвосты вне плана» с 9 пунктами из задания. 3) В разделе «Заметки для следующего чата» обновлена строка ссылки на известность хвостов. 4) Проверен дифф — пункты плана 1-22, прочие строки реестра решений и раздел работы с файлом не затронуты. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — сборка прошла успешно. |
| 2026-07-26 | Дозапись результатов разведки Ozon API для пункта 22 в docs/OZON_API.md | Проверка точной дозаписи в конец файла docs/OZON_API.md, контроль неизменности файла docs/OZON_PLAN.md, запуск lint_applet и compile_applet | OK | 1) Текст разведки по методу FBO-продаж `/v2/posting/fbo/list` и методам черновиков заявок FBO (`/v1/draft/...`, `/v2/draft/...`) дозаписан в конец `docs/OZON_API.md`. 2) Проверено отсутствие изменений исходных строк `docs/OZON_API.md`. 3) Подтверждено отсутствие каких-либо изменений в `docs/OZON_PLAN.md`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — сборка прошла успешно. |
| 2026-07-26 | Обновление пункта 22 и заметок для следующего чата в docs/OZON_PLAN.md | Текстовый аудит диффа docs/OZON_PLAN.md, запуск lint_applet и compile_applet | OK | 1) Строка пункта 22 в `docs/OZON_PLAN.md` заменена на точный утверждённый блок ТЗ v3. 2) Первая строка раздела «Заметки для следующего чата» заменена на точную инструкцию по старту этапа B. 3) Проверен дифф — все остальные строки `docs/OZON_PLAN.md` остались нетронутыми. 4) Не затронуты никакие файлы кода (Code.gs, server.ts, src/*). 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — сборка прошла успешно. |
| 2026-07-26 | Расширение кэша кластеров Ozon для маппинга складов в cluster.name | Статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`), аудит сохранения логики кэша и сигнатур | OK | 1) В `server.ts` объявлена глобальная карта `cachedWarehouseClusterMap` (`Map<string, string>`). 2) В `loadClusterMap` функция `processClusters` расширена циклом по `cluster.logistic_clusters` и `warehouses` с проверками полей и `Array.isArray`. 3) Добавлена функция `loadWarehouseClusterMap`, запрашивающая обновление кэша и возвращающая новую карту. 4) Сохранена сигнатура `loadClusterMap` и время жизни кэша 24ч без изменения вызовов. 5) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Запущен `compile_applet` (`npm run build`) — сборка прошла успешно. |
| 2026-07-26 | Добавление endpoint /api/ozon/sales для сбора недельных агрегатов FBO-продаж | Статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`), аудит алгоритма расчета недель и окон | OK | 1) В `server.ts` реализована функция `getMskWeekMonday` для вычисления понедельника ISO-недели по MSK (+3h). 2) Добавлен `POST /api/ozon/sales` с поддержкой `mode` ('recent' / 'full'), расчетом границ `since`/`to` и 56-дневных окон. 3) Настроена пагинация по `POST /v2/posting/fbo/list`, фильтрация отмен, привязка складов через `loadWarehouseClusterMap`, агрегация по `week|offerId|cluster` и отправка в GAS через действие `saveOzonSales`. 4) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Запущен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Добавление поля clusterId в ответ endpoint /api/ozon/stocks | Статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`), аудит состава объекта cabRows | OK | 1) В `server.ts` в обработчике `POST /api/ozon/stocks` при итерации по ответам `/v1/analytics/stocks` добавлены данные `clusterId: String(item.macrolocal_cluster_id || '')` строго после `clusterName`. 2) Проверено отсутствие изменений других полей, фильтров и сигнатур функции. 3) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Запущен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Добавление колонки КластерID в хранилище «Остатки Ozon» Google Apps Script | Статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`), аудит заголовков и маппинга в Code.gs | OK | 1) В `Code.gs` в константу `OZON_STOCKS_HEADERS` добавлена строка `'КластерID'` на последнюю позицию массива. 2) В `saveOzonStocks` добавлен `clusterIdIdx`, его включение в условие выброса ошибки отсутствия колонок, и запись `row[clusterIdIdx] = item.clusterId || ''`. 3) В `getOzonStocks` добавлен `clusterIdIdx`, его валидация, и возвращение `clusterId: String(row[clusterIdIdx] || '')` после `clusterName`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Реализация листа «Продажи Ozon» и функции saveOzonSales в Google Apps Script | Статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`), аудит алгоритма фильтрации ретенции и выравнивания колонок в Code.gs | OK | 1) В `Code.gs` объявлены `OZON_SALES_HEADERS` и `OZON_SALES_RETENTION_WEEKS = 78`. 2) В `setupDatabase` и `getOzonSalesSheet` зафиксировано создание/проверка листа «Продажи Ozon». 3) В `doPost` добавлен `case 'saveOzonSales'`. 4) В `saveOzonSales` реализована валидация payload, нормирование дат недель `normWeek`, удаление просроченных записей (< 78 недель), очистка перезаписываемых недель/кабинетов (`recent`/`full`) и запись новых строк. 5) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Запущен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Интеграция синхронизации продаж Ozon в функцию scheduledOzonCheck в Code.gs | Статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`), статический аудит обработчиков и структуры ответа в Code.gs | OK | 1) В `Code.gs` в функцию `scheduledOzonCheck` сразу после блока остатков добавлен try/catch блок синхронизации продаж Ozon. 2) Реализовано определение `salesMode` ('full', если лист «Продажи Ozon» пуст или отсутствует, иначе 'recent') с обращением к `test_dbSpreadsheetId` или `activeSs`. 3) Настроена отсылка запроса `POST /api/ozon/sales` с `sessionToken`, `devMode`, `mode: salesMode` и разбором полей `salesOk`, `salesMode`, `salesRows`, `salesDeleted`, `salesMessage`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Уточнения ТЗ v3 пункта 22 в файле docs/OZON_PLAN.md | Текстовый аудит диффа docs/OZON_PLAN.md, запуск lint_applet и compile_applet | OK | 1) Заменена строка хранения продаж в пункте 22 файла `docs/OZON_PLAN.md` на новую с описанием уплотнения архива продаж в 28-дневные блоки и колонки «Дней». 2) Вставлена строка правила участия агрегатов без КластерID/без кластера в общих продажах. 3) Вставлена строка про фоновый запуск синхронизации через одноразовый триггер. 4) Проверен дифф `docs/OZON_PLAN.md` — затронуты строго 3 описанных места, статусы этапов (✅/⬜/🟨) не менялись. 5) Подтверждена неизменность всех файлов кода (`Code.gs`, `server.ts`, `src/*`). 6) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) Выполнен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Перевод обработки action 'runOzonSyncNow' в фоновый режим через триггер | Статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`), статический аудит обработки триггеров в Code.gs | OK | 1) В `Code.gs` в `doPost` обработчик `action === 'runOzonSyncNow'` переведён на очистку старых одноразовых триггеров `runOzonSyncOnce`, создание нового через `ScriptApp.newTrigger('runOzonSyncOnce').timeBased().after(100).create()` и мгновенный возврат ответа с `async: true`. 2) Добавлена функция `runOzonSyncOnce()`, очищающая триггеры `runOzonSyncOnce` и вызывающая `scheduledOzonCheck()`. 3) Проверена неизменность остальных функций в `Code.gs`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Обработка асинхронного ответа фоновой синхронизации в useWarehouseStore.ts | Статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`), аудит структуры if/else в runOzonSyncNow | OK | 1) В `src/store/useWarehouseStore.ts` в методе `runOzonSyncNow` добавлена проверка `if (data.async === true)`. 2) Добавлен вывод сообщения toast про фоновую синхронизацию Ozon и `setTimeout` на 90 секунд с повторными вызовами `fetchOzonSyncStatus()` и `fetchGas('getExternalShipments')`. 3) Сохранены дословно существующие ветки `else if (data.ok === true)` и `else` ошибки, а также блоки catch и finally. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — сборка прошла успешно. |
| 2026-07-26 | Реализация уплотнения истории продаж Ozon (28-дневные блоки) в saveOzonSales | Статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`), аудит логики сдвига 13 недель, якоря 01.01.2024 и идемпотентного агрегирования по Map | OK | 1) В `Code.gs` в `OZON_SALES_HEADERS` добавлена колонка `'Дней'`. 2) Добавлены константы `OZON_SALES_WEEKLY_ZONE_WEEKS = 13`, `OZON_SALES_PERIOD_ANCHOR_MS` (01.01.2024 MSK) и `OZON_SALES_PERIOD_MS` (28 дней). 3) В `saveOzonSales` внедрена валидация индекса `'Дней'`, автоматический сдвиг и консолидация строк с датами < `alignedCutoffMs` (старше 13 недель) в 28-дневные блоки со значением `'Дней'=28`. 4) Подтверждена идемпотентность агрегирования через Map `periodKey|cabinet|article|cluster` без дублирования строк. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Снятие assertAdmin с getOzonSyncStatus и добавление метрик salesTotal/salesCompacted в scheduledOzonCheck | Статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`), статический аудит вызова getOzonSyncStatus и объекта ответа scheduledOzonCheck в Code.gs | OK | 1) Из `case 'getOzonSyncStatus'` в `doPost` удалён вызов `assertAdmin(currentUser)` — доступ к получению статуса синхронизации открыт для всех авторизованных пользователей. 2) Сохранены `assertAdmin` в соседних действия `setupOzonSyncTriggers` и `removeOzonSyncTriggers`. 3) В `scheduledOzonCheck` в ветку успеха сохранения продаж добавлены поля `salesTotal` и `salesCompacted` с обращением к `salesParsed.data`. 4) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Запущен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Детализация отображения результатов последнего запуска в модуле Автоопрос Ozon | Статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`), аудит структуры трех секций и состояний | OK | 1) В `src/components/SettingsTab.tsx` перестроен JSX блока `ozonSyncStatus.lastRun`. 2) Сохранена строка времени запуска. 3) Добавлены 3 подсекции с серыми жирными подписями `Заявки`, `Остатки`, `Продажи`. 4) Реализовано ветвление для состояний `true` (зелёный текст с метриками), `false` (красный текст ошибки) и `undefined` (серый текст «нет данных»). 5) В секции «Продажи» настроено условное отображение « (уплотнено блоков: X)» при `salesCompacted > 0`. 6) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) Запущен `compile_applet` (`npm run build`) — сборка прошла успешно. |
| 2026-07-26 | Расширение индикатора «Статус системы» в Sidebar здоровьем синхронизации Ozon и детализацией | Статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`), аудит интервала, формул статуса и условий сворачивания | OK | 1) В `src/components/Sidebar.tsx` добавлен вызов `fetchOzonSyncStatus()` при монтировании и через `setInterval` каждые 10 минут с чисткой в cleanup. 2) Реализован расчет `allOk` с проверками `gasError`, свежести запуска (< 14 ч), флага `enabled` и отсутствии сбоев в `ok`, `stocksOk`, `salesOk`. 3) Настроена интерактивная кнопка-шеврон `ChevronDown`/`ChevronUp` с переключением `isStatusExpanded`. 4) В детализированную панель добавлены 4 индикатора подсистем («Связь с GAS», «Заявки Ozon», «Остатки Ozon», «Продажи Ozon») с верным маппингом состояний, временем прогона и причиной `syncStatusReason`. 5) Проверено корректное отображение индикатора при `isSidebarCollapsed` с полем `title`. 6) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) Запущен `compile_applet` (`npm run build`) — сборка прошла успешно. |
| 2026-07-26 | Завершение этапа B и перенос рабочего фокуса на этап C пункта 22 в файле docs/OZON_PLAN.md | Текстовый аудит диффа docs/OZON_PLAN.md, проверка линтером (`lint_applet`) и билдом (`compile_applet`) | OK | 1) В `docs/OZON_PLAN.md` строка этапа B переведена в состояние ✅ с полным текстом описания реализованных в этапе B подсистем. 2) В заметках для следующего чата текст переведён с этапа B на этап C («Настройки»). 3) Выполнен аудит диффа `docs/OZON_PLAN.md`: проверена точечность трех замен без затрагивания остального текста. 4) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Запущен `compile_applet` (`npm run build`) — сборка прошла успешно. |
| 2026-07-26 | Полное удаление 6 мёртвых функций миграций и пересчёта склада в Code.gs | Статический поиск по файлу (`grep`), статический анализ типов TypeScript (`lint_applet`), проверка компиляции (`compile_applet`) | OK | 1) Из `Code.gs` целиком удалены функции `recalculateStockFully`, `migrateKitCosts`, `cleanupZeroCostRows`, `migrateDatesToISO`, `migrateBowlKitsToVirtual` и `migrateComponentWriteOffCosts` с их комментариями. 2) Выполнен поиск через `grep -n -E` в `Code.gs` — подтверждено 0 совпадений (ни одного вызова или объявления). 3) Подтверждено отсутствие каких-либо изменений в `docs/OZON_PLAN.md`. 4) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Запущен `compile_applet` (`npm run build`) — сборка прошла успешно. |
| 2026-07-26 | Хранение настроек Ozon-планирования в листе «Настройки Ozon» и привязка ретенции продаж | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит структуры функций и роутинга в Code.gs | OK | 1) В `Code.gs` добавлены `OZON_SETTINGS_HEADERS` и `OZON_SETTINGS_DEFAULTS` (6 параметров). 2) Добавлены бэкенд-функции `getOzonSettingsSheet()`, `getOzonSettings()` (с идемпотентным самозаполнением) и `saveOzonSettings(data)` (с валидацией конечности чисел, целых недельных значений, проверок на мин. 0/1 и админ-доступа). 3) В `doPost` зарегистрированы роуты `getOzonSettings` и `saveOzonSettings`. 4) В `setupDatabase` добавлена инициализация листа «Настройки Ozon». 5) В `saveOzonSales` расчет `cutoffMs` переведён на динамическое значение `salesRetentionWeeks` из настроек с обработкой ошибок и дефолтом `OZON_SALES_RETENTION_WEEKS` (78). 6) Запущен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) Запущен `compile_applet` (`npm run build`) — сборка успешно завершена. |
| 2026-07-26 | Добавление колонки «Срок поставки (дни)» (leadTimeDays) в функции getSkus, addSku и updateSku в Code.gs | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит сигнатур и работы с массивами строк в Code.gs | OK | 1) В `Code.gs` в вызовы `ensureColumns` функций `getSkus`, `addSku` и `updateSku` добавлена колонка `'Срок поставки (дни)'`. 2) Добавлен расчет индекса `leadIdx` через `headers.indexOf('Срок поставки (дни)')` с фоллбэком 7. 3) В `getSkus` настроено возвращение свойства `leadTimeDays: leadIdx !== -1 && leadIdx < row.length ? Number(row[leadIdx]) || 0 : 0`. 4) В `addSku` и `updateSku` при записи строк реализовано сохранение `skuData.leadTimeDays || 0`. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-26 | Отображение и редактирование «Срок поставки, дн» (leadTimeDays) в интерфейсе SKU (src/types.ts, SkusTab.tsx, SkuModal.tsx) | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит формы и таблицы справочника SKU | OK | 1) В `src/types.ts` интерфейс `SKUItem` дополнен полем `leadTimeDays: number`. 2) В `src/components/SkusTab.tsx` в сбросе формы добавлена инициализация `leadTimeDays: 0`, а в таблицу после «Литраж, л» добавлена сортируемая колонка «Срок поставки, дн» (сортировка по `leadTimeDays`) с рендерингом значения или прочерка «—». 3) В `src/components/SkuModal.tsx` добавлено поле типа `number` со `step="1"`, `min="0"`, `max="365"`, `placeholder="Необяз."` и валидацией `parseInt(e.target.value, 10)` в диапазоне [0, 365]. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — сборка успешна. |
| 2026-07-26 | Модальное окно и кнопка «Настройки» Ozon-планирования (OzonSettingsModal.tsx, OzonStocksTab.tsx) | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит формы, параметров fetchGas и тостов sonner | OK | 1) Создан файл `src/components/OzonSettingsModal.tsx`. Настроены 6 числовых полей ввода с точным соответствием названий/ограничений (`speedWeeks` min 1, `minStockDays` min 0, `targetStockDays` min 0, `factoryOrderDays` min 0, `returnsToSalePct` 0-100, `salesRetentionWeeks` min 1). 2) Загрузка инициализируется по `isOpen` через `fetchGas('getOzonSettings')`, показывает «Загрузка…» и выводит `toast.error` при ошибке. 3) Сохранение выполняет `fetchGas('saveOzonSettings', { data })`: при `status === 'success'` выводится `toast.success('Настройки Ozon сохранены')` и вызывается `onClose()`, при ошибке — `toast.error` без закрытия окна. 4) В `src/components/OzonStocksTab.tsx` перед кнопкой «Обновить» добавлена кнопка «Настройки» (`id="btn-ozon-settings"`) с иконкой `Settings` (14px) и стилями кнопки «Обновить». 5) Подключен рендеринг `<OzonSettingsModal isOpen={showSettings} onClose={() => setShowSettings(false)} />`. 6) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-26 | Добавление всплывающих подсказок к полям в модальном окне «Настройки Ozon» (src/components/OzonSettingsModal.tsx) | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит текстов подсказок и стилей тултипа | OK | 1) В `src/components/OzonSettingsModal.tsx` импортирована иконка `HelpCircle` из `lucide-react`. 2) Добавлен компонент `FieldHint` со стилями позиционирования всплывающего блока `w-64 bg-slate-800 text-white text-xs rounded-xl px-3 py-2 shadow-lg`. 3) Ко всем 6 полям добавлено по `<FieldHint text="..." />` с дословным соответствием переданных формулировок и описаний формул расчётов. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — сборка приложения прошла успешно. |
| 2026-07-26 | Настройка открывания подсказок вниз (position="bottom") для первых 3 полей в OzonSettingsModal.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит CSS-классов позиционирования тултипа | OK | 1) В `src/components/OzonSettingsModal.tsx` компонент `FieldHint` обновлён для приёма `position?: 'top' | 'bottom'` (по умолчанию `'top'`). 2) При `position === 'bottom'` заданы CSS-классы `top-full mt-2`, при `'top'` — `bottom-full mb-2`. 3) Для первых трёх полей («Полных недель для скорости продаж», «Неснижаемый остаток, дней», «Целевой запас на Ozon, дней») передан проп `position="bottom"`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — приложение успешно скомпилировано. |
| 2026-07-26 | Поддержка строковой настройки excludedClusters в листе «Настройки Ozon» и функциях get/saveOzonSettings в Code.gs | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит регулярных выражений и логики парсинга строк | OK | 1) В `Code.gs` элемент `{ key: 'excludedClusters', value: '', desc: 'КластерID без поставок, через запятую' }` добавлен в `OZON_SETTINGS_DEFAULTS`, создана константа `OZON_SETTINGS_STRING_KEYS = ['excludedClusters']`. 2) В `getOzonSettings` для ключей из `OZON_SETTINGS_STRING_KEYS` считывание производится через `String(rawVal || '').trim()`, сохраняя пустую строку и исключая неявное превращение в `0`. В цикле самозаполнения используется сравнение `result[def.key] === undefined`. 3) В `saveOzonSettings` для строковых ключей добавлен парсинг списка через `.split(',')`, очистка через `.map(s => s.trim()).filter(Boolean)`, валидация `/^\d+$/` каждого элемента и сборка в строку `.join(',')`. При нахождении нецифрового элемента выбрасывается исключение с текстом `Значение настройки "excludedClusters" должно быть списком числовых КластерID через запятую`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — проект собран успешно. |
| 2026-07-26 | Блок «Кластеры без поставок» с чекбоксами в модальном окне «Настройки Ozon» (src/types.ts, OzonSettingsModal.tsx) | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит агрегации useMemo и логики чекбоксов | OK | 1) В `src/types.ts` в `OzonStockRow` добавлено поле `clusterId: string`. 2) В `src/components/OzonSettingsModal.tsx` `OzonSettingsData` и начальное состояние дополнены полем `excludedClusters: string`. При загрузке `getOzonSettings` считывается `excludedClusters: String(res.data.excludedClusters || '')`. 3) Настроен `useMemo` для формирования массива отсортированных уникальных кластеров `{ clusterId, clusterName }` из `ozonStocks` по непустым `clusterId` с фоллбэком `Кластер {clusterId}` при пустом имени. 4) Под шестью числовыми полями добавлен блок «Кластеры без поставок» с подсказкой `FieldHint position="top"`. При отсутствии кластеров выводится предупреждение `Кластеры появятся после первой загрузки остатков Ozon`. 5) При наличии кластеров рендерится список чекбоксов. Состояние определяет `Set` из `form.excludedClusters.split(',')`. Переключение обновляет `excludedClusters` строкой с ID через запятую без пробелов. 6) В `handleSave` строка `form.excludedClusters` передается в payload. 7) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 8) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-26 | Обновление паспорта проекта docs/OZON_PLAN.md (исключённые кластеры, этап C) | Проверка точности диффа `docs/OZON_PLAN.md`, `lint_applet`, `compile_applet` | OK | 1) Выполнено 3 дословные замены фрагментов в `docs/OZON_PLAN.md`: добавлены правила работы с исключёнными кластерами в описании неснижаемого остатка и рекомендаций поставок; параметр `excludedClusters` добавлен в список полей настроек; этап C отмечен выполненным ✅ C (27.07.2026). 2) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 3) Выполнен `compile_applet` (`npm run build`) — приложение скомпилировано успешно. |
| 2026-07-26 | Создание эндпоинта POST /api/ozon/clusters (server.ts) | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), статическая проверка схемы эндпоинта и обработчиков ошибок | OK | 1) Добавлен эндпоинт `POST /api/ozon/clusters` в `server.ts` строго между `/api/ozon/stocks` и `getMskWeekMonday`. 2) Реализована проверка авторизации токена `verifyGasSession`. 3) Настроено считывание ключей `fetchOzonKeys` и выбор первого кабинета `keys.cabinets[0]`. 4) Внедрен вызов `loadClusterMap` с парсингом `cachedClusterMap` в массив `{ clusterId, clusterName }`. 5) Настроена отправка в GAS через `saveOzonClusters` с проверкой `gasUrl`, разбором JSON/не-JSON с кодом 502/500 и возвратом итоговой статистики. 6) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) Выполнен `compile_applet` (`npm run build`) — приложение скомпилировано успешно. |
| 2026-07-26 | Обработка и синхронизация справочника кластеров Ozon в Google Apps Script (Code.gs) | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), статистический аудит логики работы со списком, флага `isInitialFill` и роутинга `doPost` | OK | 1) В `Code.gs` добавлена константа `OZON_CLUSTERS_HEADERS = ['КластерID', 'Название', 'Добавлен', 'Уведомлён']`. 2) В `setupDatabase` добавлен вызов `getOrCreateSheet(ss, 'Кластеры Ozon', OZON_CLUSTERS_HEADERS)`. 3) Создана функция `getOzonClustersSheet()`. 4) Реализована функция `saveOzonClusters(payload)`: валидация входного массива, динамический поиск индексов по колонкам, проверка первичности наполнения `isInitialFill`, сохранение и обновление кластеров по Map, установка `notified = 1` и `newClusters = 0` при первичном наполнении. 5) Реализованы `getOzonClusters()` и `markOzonClustersNotified()`. 6) В `doPost` добавлены роуты `saveOzonClusters`, `getOzonClusters`, `markOzonClustersNotified`. 7) В `scheduledOzonCheck` заведен блок синхронизации `/api/ozon/clusters`. 8) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 9) Выполнен `compile_applet` (`npm run build`) — сборка успешна. |
| 2026-07-26 | Интеграция справочника кластеров Ozon в интерфейс настроек и тосты новых кластеров (OzonSettingsModal.tsx, OzonStocksTab.tsx) | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), статическая проверка хуков React, флагов useRef и тостов sonner | OK | 1) В `src/components/OzonSettingsModal.tsx` заведён стейт `directory`, в `useEffect` добавлен параллельный запрос `fetchGas('getOzonClusters')`. 2) В `useMemo` кластеров настроен приоритет использования справочника над остатками `ozonStocks` с дедупликацией по `clusterId` и сортировкой. 3) В `src/components/OzonStocksTab.tsx` заведён `useRef(false)` для защиты от повторного вызова `useEffect`. 4) Настроен запрос `getOzonClusters`, фильтрация `notified === false`, вызов тоста `toast.info('Новые кластеры Ozon: ...', { duration: 10000 })` и сброс через `markOzonClustersNotified`. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-26 | Обновление текстового описания итогов этапа C в паспорте проекта docs/OZON_PLAN.md | Сверка диффа `docs/OZON_PLAN.md`, `lint_applet`, `compile_applet` | OK | 1) Выполнена точная замена фрагмента с итоговой сводкой этапа C в `docs/OZON_PLAN.md`: добавлены сведения о листе «Кластеры Ozon», прокси-эндпоинте `/api/ozon/clusters`, опросном блоке в `scheduledOzonCheck`, загрузке справочника в модальном окне настроек и выплывающем тосте при появлении новых кластеров. 2) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 3) Выполнен `compile_applet` (`npm run build`) — приложение скомпилировано успешно. |
| 2026-07-27 | Функция getOzonSales() и экшен getOzonSales в Code.gs | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит парсинга заголовков и формата объекта возвращаемых строк | OK | 1) Добавлена функция `getOzonSales()` в `Code.gs` перед `function getOzonStocks()`. 2) Настроено считывание данных из листа «Продажи Ozon», динамический поиск колонок ('Неделя', 'Кабинет', 'Артикул', 'Кластер', 'Количество', 'Обновлено', 'Дней'), проверка их наличия. 3) Реализовано форматирование дат через `Utilities.formatDate` с часовым поясом `Session.getScriptTimeZone()`. 4) Зарегистрирован роут `case 'getOzonSales': result = getOzonSales(); break;` в `doPost`. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Интеграция данных продаж Ozon на фронтенде (OzonSalesRow, ozonSales, fetchOzonSales) | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит типов и состояния Zustand | OK | 1) В `src/types.ts` под `OzonStockRow` добавлен интерфейс `OzonSalesRow` с полями `{ week, cabinet, offerId, clusterName, qty, updatedAt, days }`. 2) В `src/store/useWarehouseStore.ts` импортирован `OzonSalesRow`. 3) В интерфейс `WarehouseState` и начальное состояние стора добавлено `ozonSales: OzonSalesRow[]` (по умолчанию `[]`). 4) Добавлен метод `fetchOzonSales()`, запрашивающий `getOzonSales` через `fetchGas` и сохраняющий результат при успехе. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — приложение скомпилировано успешно. |
| 2026-07-27 | Создание модуля чистых функций планирования поставок Ozon (src/lib/ozonCoverage.ts) | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит чистоты функций и соответствия сигнатур | OK | 1) Создан файл `src/lib/ozonCoverage.ts` с точным текстом реализации. 2) Реализованы чистые функции `getMskWeekMonday`, `getLastFullWeeks`, `resolveOzonArticle` и `buildSalesSpeed` с типом результата `SalesSpeedResult`. 3) Экспортирована константа `NO_CLUSTER_NAME = 'Без кластера'`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Завершение модуля планирования поставок Ozon (часть 2 в src/lib/ozonCoverage.ts) | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит чистых функций и вычислений | OK | 1) В `src/lib/ozonCoverage.ts` обновлен импорт типов для включения `OzonStockRow`. 2) Добавлены интерфейсы и функции части 2: `parseExcludedClusters`, `buildClusterNameToId`, `buildClusterStocks`, `calcCoverageDays`, `calcSupplyRecommendation`, `calcFactorySignal` и сборная `buildOzonCoverage`. 3) Проверено отсутствие побочных эффектов. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — приложение скомпилировано успешно. |
| 2026-07-27 | Обновление текстового описания и заметок для следующего чата в паспорте проекта docs/OZON_PLAN.md | Сверка диффа `docs/OZON_PLAN.md`, `lint_applet`, `compile_applet` | OK | 1) Выполнена точная замена двух фрагментов в `docs/OZON_PLAN.md`: описание этапа D обновлено детальной сводкой созданного модуля `src/lib/ozonCoverage.ts`, состояние отмечено выполнено ✅. 2) Заметки для следующего чата обновлены с переносом фокуса на этап E (компонент таблицы). 3) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Выполнен `compile_applet` (`npm run build`) — приложение скомпилировано успешно. |
| 2026-07-27 | Подключение данных и вызов buildOzonCoverage в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит хуков и параметров вызова buildOzonCoverage | OK | 1) В `src/components/OzonStocksTab.tsx` импортированы типы и функции `buildOzonCoverage`, `OzonCoverageSettings`, `OzonClusterRef`, `OzonCoverageResult`. 2) Подключены селекторы `ozonSales`, `fetchOzonSales`, `skus`, `kits`, `getEffectiveAvailability`. 3) Добавлены локальные состояния `ozonSettings` и `clusterRefs`. 4) В `useEffect` при `isAdmin` добавлен вызов `fetchOzonSales()`. 5) Добавлен `useEffect` для разовой загрузки `getOzonSettings` и `getOzonClusters` через `fetchGas`. 6) Создан `useMemo` под `coverage` с формированием карты доступности Моего склада и вызовом `buildOzonCoverage`. 7) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 8) Выполнен `compile_applet` (`npm run build`) — приложение успешно скомпилировано. |
| 2026-07-27 | Рефакторинг таблицы остатков Ozon на 3 уровня детализации (src/components/OzonStocksTab.tsx) | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит рендеринга 3 уровней и состояний раскрытия | OK | 1) Таблица остатков переведена с 2 уровней (артикул → склады) на 3 уровня (товар → кластеры → склады). 2) Добавлены состояния раскрытия `expandedArticles` (Set<string>) и `expandedClusters` (Set<string>). 3) Внедрен `useMemo` под `coverageRows` для объединения данных `coverage.articles` с фильтрацией складов и вычислением динамического покрытия в днях и рекомендаций. 4) Настроено форматирование продаж (`fmtInt`, `fmtSpeed`), покрытия (`fmtDays`), рекомендации и статуса заказа на фабрику (`fmtFactorySignal`). 5) Проверена сборка через `compile_applet` — успешная компиляция без ошибок. |
| 2026-07-27 | Приведение таблицы «Остатки Озон» к строго утверждённому макету из 15 колонок | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит состава колонок и функций форматирования | OK | 1) В `src/components/OzonStocksTab.tsx` применены 3 ровные замены согласно спецификации: обновление `fmtInt`, `fmtSpeed`, `fmtDays`, `coverageColor`; полная замена `coverageRows` с агрегацией `sumBy` для всех статусов и `unboundRows`; замена структуры таблицы `#ozon-stocks-table` на ровно 15 колонок (Товар / Кластер / Склад, Продано, Скорость, Доля, Доступно, Готовим, В заявках, В пути, Излишки, Возвраты, Прочее, Расчётный, Покрытие, Мой склад, Рекомендация). 2) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 3) Выполнен `compile_applet` (`npm run build`) — приложение успешно скомпилировано. |
| 2026-07-27 | Механизм скрытия колонок таблицы «Остатки Озон» с сохранением в localStorage | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит состава колонок и функции переключения видимости | OK | 1) В `src/components/OzonStocksTab.tsx` выполнены 6 шагов добавления видимости колонок. 2) Добавлен ключ `ozon_stocks_hidden_cols`, массив 14 скрываемых колонок `OZON_TOGGLEABLE_COLS`, значения по умолчанию `['preparing', 'requested', 'excess', 'other']`. 3) Настроено состояние `hiddenCols` с чтением и записью в `localStorage`, кнопка «Колонки» (`#btn-ozon-columns`) с выпадающим меню чекбоксов. 4) Ячейки таблицы во всех 5 местах (<thead>, товар, кластер, склад, без кластера) обёрнуты проверкой `isColVisible`. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — приложение успешно скомпилировано. |
| 2026-07-27 | Панель фильтров и сводка долей кластеров в продажах в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит логики фильтрации и вычисления долей кластеров | OK | 1) В `src/components/OzonStocksTab.tsx` выполнены ровно 6 шагов добавления фильтрации и сводки долей. 2) Импортирована иконка `Search`, добавлены фильтр-состояния `cabinetFilter`, `searchQuery`, `onlyWithRecommendations`. 3) Добавлен `useEffect` закрытия меню колонок при клике вне элемента. 4) Добавлены `filteredOzonStocks` и `filteredOzonSales` с фильтрацией по кабинету до вычисления `coverage` и `coverageRows`. 5) Добавлены `visibleRows` (поиск по артикулу/названию и тумблер рекомендаций) и `clusterShares` (доли кластеров с сортировкой по продажам). 6) Встроена панель фильтров (`#ozon-stocks-filters`), маппинг по `visibleRows` и блок `id="ozon-cluster-shares"`. 7) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 8) Выполнен `compile_applet` (`npm run build`) — приложение успешно скомпилировано. |
| 2026-07-27 | Исключение LockService для читающих экшенов Ozon в Code.gs | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит структуры функций и ветвления в `doPost` | OK | 1) В `Code.gs` найдена строка `// For other actions, acquire the script lock`. 2) Непосредственно перед ней вставлен блок проверки `if (action === 'getOzonStocks' || action === 'getOzonSales' || action === 'getOzonSyncStatus' || action === 'getOzonSettings' || action === 'getOzonClusters')`. 3) Сохранены исходные ветви в блоке `switch`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — приложение скомпилировано успешно. |
| 2026-07-27 | Удаление функции сворачивания секции «Остатки на складах Ozon» в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит отсутствия references на isOzonStocksCollapsed | OK | 1) В `src/components/OzonStocksTab.tsx` удалены состояние `isOzonStocksCollapsed`, кнопка `#btn-collapse-ozon-stocks` и обработчик клика в шапке `#ozon-stocks-header`. 2) Убраны 3 условные обёртки, делавшие скрываемыми дату обновления, кнопки действия и контейнер `#ozon-stocks-content`. 3) Подтверждено полное отсутствие упоминаний `isOzonStocksCollapsed` в проекте. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — проект скомпилирован успешно. |
| 2026-07-27 | Добавление строкового ключа настроек Ozon priorityClusters в Code.gs | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит состава массивов `OZON_SETTINGS_DEFAULTS` и `OZON_SETTINGS_STRING_KEYS` | OK | 1) В `Code.gs` добавлен объект `{ key: 'priorityClusters', value: '', desc: 'Приоритетные кластеры в формате КластерID:коэффициент, через запятую' }` в конец массива `OZON_SETTINGS_DEFAULTS`. 2) Добавлена строка `'priorityClusters'` в массив `OZON_SETTINGS_STRING_KEYS`. 3) Проверено сохранение синтаксической целостности `Code.gs`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Учёт приоритетных кластеров с коэффициентом запаса в ozonCoverage.ts | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит алгоритма расчета parsePriorityClusters и подстановки effectiveSettings в buildOzonCoverage | OK | 1) В `src/lib/ozonCoverage.ts` добавлены ровно 5 шагов: поле `priorityClusters?: string` в `OzonCoverageSettings`, функция `parsePriorityClusters`, поля `priority` и `priorityK` в `ClusterCoverageRow`, разбор `priorityMap` и подстановка `effectiveSettings` с коэффициентом `priorityK` для `minStockDays` и `targetStockDays`. 2) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 3) Выполнен `compile_applet` (`npm run build`) — приложение успешно скомпилировано. |
| 2026-07-27 | Блок настройки приоритетных кластеров в OzonSettingsModal.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит парсинга, сериализации и взаимодействия чекбоксов | OK | 1) В `src/components/OzonSettingsModal.tsx` выполнены 7 шагов: добавлено поле `priorityClusters` в интерфейс `OzonSettingsData`, начальное состояние `form`, разбор строки в `getOzonSettings` и включение в `handleSave`. 2) Реализованы `priorityMap`, `serializePriority`, `handleTogglePriority` и `handleChangePriorityK`. 3) Настроено взаимное исключение в `handleToggleCluster` и `handleTogglePriority`. 4) Встроен версточный блок «Приоритетные кластеры» перед блоком «Кластеры без поставок». 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — приложение успешно скомпилировано. |
| 2026-07-27 | Проброс priorityClusters и отображение бейджа приоритета в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит проброса настроек и рендеринга бейджа | OK | 1) В `src/components/OzonStocksTab.tsx` выполнены 3 шага: добавлены `priorityClusters: ''` в `useState(ozonSettings)` и проброс из `getOzonSettings`. 2) В строке кластера добавлен бейдж `{cls.priority && (<span className="... bg-amber-100 text-amber-700">приоритет ×{cls.priorityK}</span>)}` рядом с бейджем «без поставок». 3) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Выполнен `compile_applet` (`npm run build`) — приложение скомпилировано без ошибок. |
| 2026-07-27 | Подсказки (ColHint) для заголовков колонок таблицы «Остатки Озон» в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит состава и текста всплывающих подсказок | OK | 1) В `src/components/OzonStocksTab.tsx` импортирована иконка `HelpCircle`, создан служебный компонент `ColHint`. 2) В `<thead>` таблицы `#ozon-stocks-table` в каждую из 15 колонок добавлен элемент `<ColHint text="..." />` с соответствующим описанием бизнес-смысла колонки. 3) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Починка валидации настройки priorityClusters в saveOzonSettings в Code.gs | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), статистический аудит логики парсинга пар «КластерID:коэффициент» | OK | 1) В `Code.gs` заменён блок обработки строковых настроек в `saveOzonSettings`. 2) Добавлена специальная обработка `priorityClusters`: строки разделяются по запятой, пары разбираются по двоеточию, с отдельной проверкой чисто числового `КластерID` (`/^\d+$/`) и коэффициента (`Number.isFinite(coef) && coef >= 1`). 3) Элементы нормализуются в формат `id:coef` и объединяются через запятую. 4) Для всех остальных строковых настроек (`excludedClusters`) сохранена проверка числовых КластерID через запятую. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — приложение скомпилировано успешно. |
| 2026-07-27 | Индикация приоритета кластеров в блоке «Доли кластеров в продажах» в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит сохранения свойств priority/priorityK и рендеринга бейджа | OK | 1) В `src/components/OzonStocksTab.tsx` заменён `useMemo` с именем `clusterShares` с добавлением полей `priority` и `priorityK`. 2) В контейнере `#ozon-cluster-shares` обновлена вёрстка названия кластера с выводом бейджа `×{c.priorityK}` янтарного цвета при `c.priority === true`. 3) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Выполнен `compile_applet` (`npm run build`) — приложение успешно скомпилировано. |
| 2026-07-27 | Подсветка урезанной рекомендации в строке товара в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит поля recLimited и условий стилизации ячейки | OK | 1) В `src/components/OzonStocksTab.tsx` добавлены определение поля `recLimited` в `coverageRows` и обработка свойства `limitedByMyStock` для всех кластеров товара. 2) Ячейка рекомендации уровня 1 подсвечивается классом `text-amber-600 font-bold` с подсказкой при `recLimited === true` и `text-indigo-600 font-bold` в обычном режиме. 3) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Поочередное распределение общего остатка «Моего склада» между кластерами в ozonCoverage.ts | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит алгоритма поочередного распределения остатка myStockAvailable | OK | 1) В `src/lib/ozonCoverage.ts` вызов `calcSupplyRecommendation` внутри цикла сборки кластеров переведен на `Number.MAX_SAFE_INTEGER`. 2) Сразу после `clusterRows.sort(...)` добавлен порядок распределения единого `myStockAvailable` — сначала приоритетные кластеры по убыванию `priorityK`, затем по возрастанию дней покрытия `coverageDays`. 3) Назначение коробок происходит в целых единицах с уменьшением `remainingStock` и простановкой `limitedByMyStock`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Закрепление шапки таблицы «Остатки Озон» при прокрутке в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), визуальный и DOM-аудит липкого позиционирования thead th | OK | 1) В `src/components/OzonStocksTab.tsx` заменена внешняя обёртка таблицы на `<div className="overflow-auto max-h-[70vh]">`. 2) Добавлен встроенный `<style>` перед `#ozon-stocks-table` с CSS-правилами `#ozon-stocks-table thead th { position: sticky; top: 0; z-index: 20; background-color: #f1f5f9; box-shadow: inset 0 -1px 0 #e2e8f0; }`. 3) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Выполнен `compile_applet` (`npm run build`) — приложение успешно скомпилировано. |
| 2026-07-27 | Полноэкранный режим с закрепленной шапкой таблицы «Остатки Озон» в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит переключения состояния isFullscreen и реактивности кнопки Esc | OK | 1) В `src/components/OzonStocksTab.tsx` добавлены иконки `Maximize2` и `Minimize2`, состояние `isFullscreen`, слушатель события `Escape`. 2) Добавлена кнопка «Развернуть» в шапку секции. 3) Настроено условное форматирование контейнера таблицы (`fixed inset-0 z-50 rounded-none overflow-hidden flex flex-col`) и скролл-обертки (`flex-1 min-h-0`). 4) Добавлена шапка с названием и кнопкой «Свернуть (Esc)». 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Переключаемая колонка «Заказ на фабрике» в таблице «Остатки Озон» в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), проверка симметрии колонок <th>/<td> по всем 4 уровням строк | OK | 1) В `src/components/OzonStocksTab.tsx` добавлено правило колонки `{ key: 'factory', label: 'Заказ на фабрике' }`. 2) Добавлен заголовок `<th>` с подказкой `ColHint` в `thead`. 3) На уровне товара (Level 1) добавлена выгрузка количества в штуках (`orderQty`) и коробках (`orderBoxes`) с текстом цвета `text-rose-600` при наличии `art.factory`. 4) На уровнях кластеров (Level 2), складов (Level 3) и агрегатов без кластера добавлены ячейки с прочерками `—`. 5) Проверено равенство количества `<th>` и `<td>` в каждой строке (16 при всех видимых колонках). 6) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Индикация дефицита и потребности без остатка на Моём складе в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит отображения дефицита на уровнях 1 и 2 и в фильтре | OK | 1) В `src/components/OzonStocksTab.tsx` в `coverageRows` внедрен расчет `needBoxes`, `needQty` и общеагрегированного `deficitQty`. 2) В `visibleRows` фильтр `onlyWithRecommendations` дополнен условием `row.deficitQty <= 0` для отображения дефицитных позиций. 3) На уровне 1 добавлена красная метка `дефицит X шт` при нулевой текущей поставке (и доп. метка `+ дефицит X шт` при частичной). 4) На уровне 2 добавлено красное `нужно X кор (Y шт) / нет на складе`. 5) Обновлена подсказка `ColHint` колонки «Рекомендация» с расшифровкой цветовых статусов. 6) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Расширение сигнала «Заказ на фабрике» по локальному дефициту кластеров в ozonCoverage.ts | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит сигналов фабрики и взаимодействия с интерфейсами | OK | 1) В `src/lib/ozonCoverage.ts` расширен интерфейс `FactorySignal` полем `reason` и `unmetDeficitQty`. 2) Модифицирована функция `calcFactorySignal` — теперь при `unmetDeficitQty > 0` сигнал возвращается с `reason: 'clusterDeficit'`, а объем заказа высчитывается как `Math.max(orderQtyBySpeed, unmetDeficitQty)`. 3) В `ClusterCoverageRow` и `ArticleCoverage` добавлены поля `unmetQty` и `unmetDeficitQty`. 4) В `buildOzonCoverage` суммируется неперекрытый дефицит и передается в `calcFactorySignal`. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Отображение причин сигнала «Заказ на фабрике» и самодиагностика колонки в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит визуального вывода причин сигнала и диагностических всплывающих подсказок | OK | 1) В `src/components/OzonStocksTab.tsx` в `coverageRows` добавлены `factoryDaysLeft` и `factoryThreshold`. 2) На уровне 1 в колонке `factory` добавлены подписи причины сигнала («нечем пополнить» / «кончается везде») и подсказка о днях запаса/пороге. 3) Добавлена статусная надпись «срок не задан» для позиций с нулевым `leadTimeDays`. 4) Обновлена подсказка `ColHint` в `thead` колонки «Заказ на фабрике». 5) Проверена симметрия `<th>`/`<td>` (16 колонок). 6) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Хранилище заказов на фабрике в Code.gs | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), статический аудит функций чтения и записи листа «Заказы на фабрике» | OK | 1) В `Code.gs` добавлена константа `FACTORY_ORDERS_HEADERS`. 2) Добавлены экшены `getFactoryOrders`, `saveFactoryOrder`, `setFactoryOrderReceived` в `doPost`. 3) Реализованы функции `getFactoryOrdersSheet`, `readFactoryOrdersSheet`, `normalizeFactoryDate`, `getFactoryOrders`, `saveFactoryOrder`, `setFactoryOrderReceived`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Подключение заказов на фабрике к клиенту в types.ts и useWarehouseStore.ts | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит сигнатур методов стора и типов данных | OK | 1) В `src/types.ts` добавлен интерфейс `FactoryOrder`. 2) В `src/store/useWarehouseStore.ts` импортирован `FactoryOrder`, расширен интерфейс `WarehouseState`, добавлена переменная `factoryOrders: []` в дефолтное состояние, реализованы асинхронные методы `fetchFactoryOrders`, `saveFactoryOrder` и `setFactoryOrderReceived`. 3) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Модальное окно отметки заказа на фабрике в FactoryOrderModal.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит пропсов, состояний и обработчиков форм | OK | 1) Создан новый компонент `src/components/FactoryOrderModal.tsx`. 2) Настроена инициализация состояния `qty`, `expectedAt` и `comment` при открытии формы с округлением до размера коробки (`pcsPerBox`) и автоматическим расчетом даты на основе `leadTimeDays`. 3) Настроена интеграция с методами стора `saveFactoryOrder` и `setFactoryOrderReceived`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — сборка приложения успешно завершена. |
| 2026-07-27 | Подключение заказов на фабрике в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит перехвата e.stopPropagation(), вывода бейджей и монтирования FactoryOrderModal | OK | 1) В `src/components/OzonStocksTab.tsx` подключена выгрузка `fetchFactoryOrders()` в `useEffect`. 2) В ячейку 'factory' уровня 1 добавлено условие отображения `factoryOrder`: выводится плашка `заказ · дд.мм` (зеленая) или `ждали дд.мм` (янтарная при просрочке) с всплывающей карточкой деталей. 3) При отсутствии активного заказа красная кнопка сигнала открывает `FactoryOrderModal`. 4) Во всех кликах по кнопкам сохранен `e.stopPropagation()` для предотвращения сворачивания/разворачивания строки. 5) В конец компонента монтирована `FactoryOrderModal`. 6) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 7) Выполнен `compile_applet` (`npm run build`) — приложение успешно собрано. |
| 2026-07-27 | Добавление блока «Рекомендации» в OzonStocksTab.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит useMemo расчёта рекомендаций и отображения двух колонок | OK | 1) В `src/components/OzonStocksTab.tsx` добавлено состояние `showRecommendations`. 2) Добавлен `useMemo` формирования списков `supplies` и `factories` из массива `coverageRows` с учётом активных заказов `activeFactoryOrders`. 3) Перед панелью фильтров вставлен сворачиваемый блок с ID `ozon-recommendations`. 4) В левой колонке выводятся карточки товаров с расчетом поставок по кластерам Ozon, в правой — карточки сигналов заказа на фабрике с кликом по артикулу для вызова `FactoryOrderModal`. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — приложение скомпилировано успешно. |
| 2026-07-27 | Сворачивание рекомендаций по умолчанию, оформление прихода из заказа и запрет нулевой себестоимости | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит переходов сторов и валидации zeroPriceIndexes | OK | 1) В `OzonStocksTab.tsx` проверяемый блок рекомендаций теперь инициализируется в свернутом виде `useState(false)`. 2) В `FactoryOrderModal.tsx` обработчик `handleReceived` при клике «Партия пришла» переключает `opType` в 'Приход', `uploadDestination` в 'Склад', подставляет текст `Приход партии с фабрики...` и активирует вкладку 'upload'. 3) В `ConfirmModal.tsx` реализована проверка `zeroPriceIndexes` для 'Приход', красная рамка у инпутов с `price === 0`, выведение красного плашки-предупреждения и блокировка `isConfirmDisabled`. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — проект скомпилирован без ошибок. |
| 2026-07-27 | Отображение некратного количества коробок с десятыми долями и контрастность подписей в FactoryOrderModal.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), математический аудит вычисления точных коробок (qty / box, remainderPcs) и проверки форматирования | OK | 1) В `src/components/FactoryOrderModal.tsx` изменен расчёт коробок на `boxesExact = qtyValue / box` и `remainderPcs = qtyValue % box`. 2) Для целых коробок выводится целое число зелёным цветом (`text-emerald-600`), для некратных — красным с округлением до 1 знака после запятой (`toFixed(1).replace('.', ',')`) и подсказкой о некратности («добавь N шт или убери M шт»). 3) Повышена контрастность серых лейблов (`text-slate-600`), подзаголовка и подписей (`text-slate-500`, `text-[12px]`). 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — приложение успешно собрано. |
| 2026-07-27 | Обновление паспорта проекта docs/OZON_PLAN.md по этапу F | Проверка диффов 4 заменяемых участков, линтинг и сборка проекта | OK | 1) Проведены 4 дословные замены фрагментов в `docs/OZON_PLAN.md`. 2) Сохранена однострочная структура с перечнем этапов. 3) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Выполнен `compile_applet` (`npm run build`) — приложение успешно собрано. |
| 2026-07-27 | Добавление функций расчета алертов покрытия Ozon в src/lib/ozonAlerts.ts | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит формирования алертов и сортировки | OK | 1) Расширены типы `OzonAlertType` и `OzonAlert` (поля severity, postingId?, article?). 2) В `src/lib/ozonAlerts.ts` импортированы типы из `ozonCoverage.ts` и создана функция `buildCoverageAlerts`. 3) Реализовано генерация алерта «Пора заказать на фабрике» с учётом `orderedArticles` и причин `total`/`clusterDeficit`. 4) Реализована генерация алерта «Пора сделать поставку на Ozon» с лимитом 3 кластеров и суффиксом «и ещё N кластер(ов)». 5) Настроена сортировка массива `factoryAlerts` по `daysLeft` и массива `supplyAlerts` по минимальному `coverageDays`. 6) Существующая функция `buildOzonAlerts` сохранена без изменений. 7) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 8) Выполнен `compile_applet` (`npm run build`) — приложение успешно собрано. |
| 2026-07-27 | Подключение алертов покрытия Ozon в Dashboard.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит подписок стора, фоновой загрузки и объединения алертов | OK | 1) В `src/components/Dashboard.tsx` добавлены импорты `buildCoverageAlerts` и `buildOzonCoverage`. 2) Подключены состояния стора `ozonStocks`, `ozonSales`, `factoryOrders`, `getEffectiveAvailability` и `fetchGas`. 3) Добавлены локальные состояния `ozonSettings` и `clusterRefs`. 4) Реализован фоновый `useEffect` (таймаут 1200мс) для фоновой подгрузки данных админом. 5) Созданы `useMemo` для `ozonCoverage` и `coverageAlerts` с исключением скрытых алертов `dismissedAlerts`. 6) Массив `alerts` обновлен — алерты покрытия конкатенируются после алертов по поставкам. 7) Вёрстка и компоновка блока алертов оставлены без изменений. 8) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 9) Выполнен `compile_applet` (`npm run build`) — приложение скомпилировано без ошибок. |
| 2026-07-28 | Оформление алертов покрытия Ozon и навигация в Dashboard.tsx | Проверка типов TypeScript (`lint_applet`), проверка сборки (`compile_applet`), аудит стилизации вариантов severity и условий клика на кнопку «Открыть» | OK | 1) В `src/components/Dashboard.tsx` добавлены ветки 'sky' (`bg-sky-50 border-sky-200 text-sky-800`) и 'orange' (`bg-orange-50 border-orange-200 text-orange-800`) в цепочку условий стилей алертов. 2) Обновлен обработчик `onClick` кнопки «Открыть»: переключает активную вкладку на 'ozonStocks' при `type === 'supply_needed' || type === 'factory_order'`, и на 'ozon' для прочих типов. 3) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 4) Выполнен `compile_applet` (`npm run build`) — приложение успешно собрано. |
| 2026-07-28 | Обновление паспорта проекта docs/OZON_PLAN.md по этапу G | Проверка диффов 3 заменяемых участков, линтинг и сборка проекта | OK | 1) Проведены 3 дословные замены фрагментов в `docs/OZON_PLAN.md`. 2) Отмечен статус этапа G как выполненного в пункте 22. 3) Добавлен пункт о двух независимых местах загрузки настроек в хвосты вне плана. 4) Актуализирована вводная фраза и задачи для следующего чата на этап H. 5) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 6) Выполнен `compile_applet` (`npm run build`) — приложение успешно скомпилировано. |
| 2026-07-28 | Документирование результатов разведки этапа H в docs/OZON_API.md | Проверка точного совпадения дозаписанного текста, линтинг и сборка проекта | OK | 1) В конец `docs/OZON_API.md` дословно дозаписана секция «Разведка этапа H (28.07.2026): создание заявки на поставку — проверено вживую через Ozon MCP». 2) Проверено отсутствие изменений в существующем тексте `docs/OZON_API.md`. 3) Кодовые файлы и `docs/OZON_PLAN.md` не затрагивались. 4) Выполнен `lint_applet` (`tsc --noEmit`) — 0 ошибок. 5) Выполнен `compile_applet` (`npm run build`) — приложение успешно собрано. |
| 2026-07-28 | Исправление определения Ozon-SKU в модалке OzonSupplyModal.tsx (запись восстановлена 29.07.2026) | Проверка типов tsc --noEmit и сборка проекта; проверка нормализации префиксов `OZN...` и хвостов `.0`; приоритетный поиск SKU в `ozonStocks` с фоллбеком на SKU Базу | OK | Ошибок типов нет, сборка успешна; на живом прогоне модалка показала SKU 1706096599 для артикула Органайзер_2_пол_белый, ранее ошибочно считавшегося незаполненным. |
| 2026-07-31 | Внесение пункта 28 и обновление заметок для следующего чата в `docs/OZON_PLAN.md` | Выполнение двух точечных правок в `docs/OZON_PLAN.md`, проверка строк (`wc -l`), байт (`wc -c`) и уникальности строки пункта 28 (`grep -c`) | OK | 1) Число строк `docs/OZON_PLAN.md` изменилось со 138 до 141. 2) Размер в байтах изменился с 94107 до 102278. 3) Подтверждено ровно 1 вхождение строки «- ⬜ 28. Защита от двойного проведения операции.». 4) Код, статусы прошлых пунктов и другие файлы проекта не затронуты. |
| 2026-08-01 | Реализация этапа A пункта 28 (идемпотентность commitTransaction) в `Code.gs` | Выполнение 5 дословных замен в `Code.gs`, проверка `wc -l`, `wc -c`, `grep -c` и сборки приложения через `compile_applet` | OK | 1) Число строк `Code.gs` выросло с 6013 до 6065, размер с 244545 до 246817 байт. 2) Подтверждено ровно 1 вхождение `function findTransactionsByOpId`. 3) Подтверждено ровно 1 вхождение сигнатуры `function commitTransaction(..., opId)`. 4) Подтверждено ровно 1 объявление `function commitTransaction(`. 5) `compile_applet` прошёл успешно. |
| 2026-08-01 | Реализация этапа B пункта 28 (генерация и передача opId с клиента) | Выполнение замен в `src/lib/utils.ts`, `src/store/useWarehouseStore.ts`, `src/components/ConfirmModal.tsx`, `src/components/ManualTab.tsx`, проверки `wc -l`, `wc -c`, `grep -c`, `npx tsc --noEmit` и `compile_applet` | OK | 1) Проверены размеры и число строк во всех 4 файлах ДО и ПОСЛЕ. 2) Подтверждено ровно 1 объявление `export const newOperationId = (): string => {` в `src/lib/utils.ts`. 3) Подтверждён импорт `newOperationId` в `ConfirmModal.tsx` и `ManualTab.tsx`. 4) `npx tsc --noEmit` и `compile_applet` прошли без ошибок. |
| 2026-08-01 | Закрепление правила ведения журналов в документации | Выполнение 4 замен в документации, проверка `wc -l`, `wc -c`, `grep -c` и сборки приложения через `compile_applet` | OK | 1) Выполнены 4 замены в `docs/OZON_PLAN.md`, `docs/DEVLOG.md`, `docs/CHANGELOG.md` и `docs/TEST_LOG.md`. 2) Подтверждено ровно 1 вхождение «## Правило ведения журналов» и «## Заметки для следующего чата» в `docs/OZON_PLAN.md`. 3) Подтверждён рост числа строк и размера байт для всех 4 файлов. 4) `compile_applet` прошёл успешно. |
| 2026-08-01 | Реализация этапа C пункта 28 (привязка поставок Ozon к расходу в Code.gs) | Выполнение 2 замен в `Code.gs`, проверка `wc -l`, `wc -c`, `grep -c` и сборки приложения через `compile_applet` | OK | 1) Число строк `Code.gs` выросло с 6065 до 6125, размер с 246817 до 249553 байт. 2) Подтверждено ровно 1 вхождение `function linkPostingsToCommit`. 3) Подтверждено ровно 1 вхождение `function updateExternalShipmentStatus`. 4) Подтверждено ровно 1 объявление `function commitTransaction(`. 5) `compile_applet` прошёл успешно. |
| 2026-08-01 | Реализация клиентской части этапа C пункта 28 (передача postingIds в commitTransaction) | Выполнение 4 замен в `src/store/useWarehouseStore.ts` и `src/components/ConfirmModal.tsx`, проверка `wc -l`, `wc -c`, `grep -c`, `npx tsc --noEmit` и `compile_applet` | OK | 1) Число строк `src/store/useWarehouseStore.ts` выросло с 1578 до 1583, размер с 61271 до 61852 байт. 2) Число строк `src/components/ConfirmModal.tsx` выросло с 1069 до 1077, размер с 47093 до 47641 байт. 3) Подтверждено ровно 1 вхождение `const postingIds = get().pendingOzonPostingIds;`. 4) Подтверждено отсутствие `await get().markExternalShipment(pid, 'processed', linkInfo);`. 5) Подтверждено сохранение объявления `markExternalShipment`. 6) `npx tsc --noEmit` и `compile_applet` прошли без ошибок. |
| 2026-08-01 | Реализация этапа D пункта 28 (защита от таймаутов, заглушек doGet и обработка ошибок в ConfirmModal) | Проверка типов TypeScript (`npx tsc --noEmit`), сборка приложения (`compile_applet`), аудит распознавания заглушек и повторов в server.ts | OK | 1) В `server.ts` добавлено распознавание текста заглушки `doGet`, автоповтор до 3 попыток для read-only экшенов и `commit`, увеличение таймаута для пишущих действий до 300 с, ограничение сброса кэша только пишущими действиями. 2) В `ConfirmModal.tsx` при сетевой ошибке/таймауте добавлена подмена кнопки на «Проверить результат» с перезагрузкой данных и амбер-баннер. 3) `npx tsc --noEmit` и `compile_applet` прошли без ошибок. |
| 2026-08-01 | Восстановление утраченного содержимого docs/OZON_PLAN.md | Проверка трех точечных замен, подстрок `grep -c`, строк и байт `wc -l`/`wc -c`, сборка `compile_applet` | OK | 1) В `docs/OZON_PLAN.md` восстановлен конец пункта 22, полный пункт 23 и статусы пункта 28 / этапа D (честно зафиксировано восстановление содержимого, утраченного предыдущим коммитом). 2) Подтверждено `grep -c` совпадение подстрок (по 1 для п.23, I.Регресс и п.28, 0 для «ВЫПОЛНЕН 01.08.2026»). 3) Число строк выросло со 156 до 158, размер со 105502 до 113371 байт. 4) `compile_applet` прошёл успешно. |
| 2026-08-01 | Добавление раздела «Правило границ промта» в docs/OZON_PLAN.md | Выполнение точечной замены в `docs/OZON_PLAN.md`, проверка числа строк (`wc -l`), размера в байтах (`wc -c`), `grep -c` заголовков и пунктов плана | OK | 1) Выполнена точечная замена `## Правило ведения журналов` в `docs/OZON_PLAN.md`. 2) Проведено сопоставление фактических показателей ДО (158 строк, 113371 байт) и ПОСЛЕ (180 строк, 116035 байт). 3) Подтверждены уникальные вхождения «## Правило границ промта», «## Правило ведения журналов», «## Заметки для следующего чата». 4) Подтверждено ровно 28 пунктов плана via `grep -cE '^- [⬜🟨✅] [0-9]+\.'`. |
| 2026-08-01 | Исправление трех дефектов автоповтора прокси и добавление индикатора долгих операций в ConfirmModal.tsx | Выполнение 5 точечных замен в `server.ts` и `src/components/ConfirmModal.tsx`, проверка типов `npx tsc --noEmit`, сборка `compile_applet`, проверки `wc` и `grep` | OK | 1) В `server.ts` добавлены 5 читающих действий в `READ_ONLY_ACTIONS`, для `commit` введено требование наличия `opId`, таймаут записи снижен до 90 с. 2) В `ConfirmModal.tsx` добавлен 20-секундный таймер и sky-баннер для операций длительнее обычного. 3) В `server.ts` отсутствие `300_000` и единственность `const canRetry` подтверждены. 4) `grep -c -i "slowRunning"` в `ConfirmModal.tsx` вернул 4. 5) `npx tsc --noEmit` и `compile_applet` прошли успешно. |
| 2026-08-01 | Фиксация завершения этапа D и пункта 28 в OZON_PLAN.md и живой регресс | Проверка строк (`wc -l`), байт (`wc -c`), grep-подстрок в `docs/OZON_PLAN.md` и живой регресс 3 сценариев на тестовой БД | OK | 1) В `docs/OZON_PLAN.md` зафиксировано завершение этапа D и пункта 28. 2) Проверены 3 сценария строгого регресса запрета повтора на тестовой БД: Сценарий 1 (60 позиций без ключа) — таймаут 90 с, ровно 60 строк в листе (повтор запрещён); Сценарий 2 (60 позиций с ключом) — уложились в 42 с, 60 строк; Сценарий 3 (150 позиций с ключом) — таймаут, автоповтор сработал, ровно 150 строк без дублей. 3) Число строк `docs/OZON_PLAN.md` — 180, `grep -cE '^- [⬜🟨✅] [0-9]+\.'` — 28. |
| 2026-08-04 | Корректировка регулярного выражения grep для подсчёта пунктов в docs/OZON_PLAN.md и добавление предостережений | Выполнение точечной замены в `docs/OZON_PLAN.md`, проверка метрик `wc`, вхождений подстрок и фактическое выполнение новой команды `grep -cE` | OK | 1) В `docs/OZON_PLAN.md` проведена замена правила подсчёта пунктов. 2) Число строк файла `docs/OZON_PLAN.md` составило 182, размер 124 410 байт. 3) Подтверждены уникальные вхождения «ВНИМАНИЕ, ГРАБЛИ» (1), «ВНИМАНИЕ, ЛОЖНЫЙ ОТЧЁТ» (1), «grep -cE '^- [⬜🟨✅]» (1). 4) Выполнена команда `grep -cE '^- (⬜|🟨|✅) [0-9]+\.' docs/OZON_PLAN.md`, вернувшая результат 28. |
| 2026-08-04 | Изменение параметров автоповтора и таймаута чтения в server.ts (пункт 29, шаг 1) | Подсчёт вхождений текста `1000 * attempt` в `server.ts`, проверка типов, сборка `compile_applet` | OK | 1) Выполнены 3 точечные замены в `server.ts`. 2) Подтверждено ровно 1 вхождение текста `1000 * attempt` в `server.ts` (строка с `await sleep(1000 * attempt);`). 3) В 4 обработчиках `/api/gas` задержка заменена на `retryDelayMs * attempt` (`retryDelayMs = 20_000`). 4) `maxTries` равно 2 для повторных попыток. 5) Сборка `compile_applet` выполнена успешно. |
| 2026-08-04 | Ручная обработка редиректов Apps Script в server.ts (пункт 29, шаг 1б) | Проверка количества вхождений `redirect: "manual"` в `server.ts`, сборка `compile_applet` | OK | 1) Выполнена точечная замена в `server.ts`. 2) Подтверждено ровно 2 вхождения строки `redirect: "manual"`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-04 | Откат ручной обработки редиректов Apps Script в server.ts | Проверка вхождений `redirect: "manual"` и `redirectHops` в `server.ts` (0 вхождений), сборка `compile_applet` | OK | 1) Выполнен откат фрагмента с `redirect: "manual"` в `server.ts`. 2) Подтверждено ровно 0 вхождений `redirect: "manual"` и `redirectHops`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-04 | Добавление диагностического логирования GASDIAG и отслеживания gasInFlight в server.ts (пункт 29) | Проверка вхождений `GASDIAG` и `gasInFlight--` в `server.ts`, сборка `compile_applet` | OK | 1) Выполнены 4 точечные замены в `server.ts`. 2) Подтверждены 2 вхождения текста `GASDIAG` и 1 вхождение `gasInFlight--`. 3) Сборка `compile_applet` выполнена успешно. |
| 2026-08-06 | Ограничение одновременных запросов к Apps Script до 3 через очередь в server.ts (пункт 29) | Проверка вхождений `await acquireGasSlot()` (1) и `releaseGasSlot()` в блоке `finally` в `server.ts`, сборка `compile_applet` | OK | 1) Выполнены 3 точечные замены в `server.ts`. 2) Подтверждено ровно 1 вхождение `await acquireGasSlot()` и ровно 1 вызов `releaseGasSlot()` внутри `finally`. 3) Сборка `compile_applet` выполнена успешно. |
| 2026-08-06 | Добавление 6 читающих действий в LOCK_FREE_ACTIONS в Code.gs (пункт 25, волна 3) | Проверка состава списка `LOCK_FREE_ACTIONS` в `Code.gs` (17 элементов), сборка `compile_applet` | OK | 1) Выполнена точечная замена в `Code.gs`. 2) Подтверждён список из 17 элементов: `getInitialData`, `getStock`, `getTransactions`, `getSkus`, `getServices`, `getServiceRates`, `getUsers`, `getGlobalSettings`, `getExternalShipments`, `getOzonSupplyRequests`, `checkSupplyAvailability`, `getOzonSettings`, `getOzonClusters`, `getOzonStocks`, `getOzonSales`, `getFactoryOrders`, `getOzonSyncStatus`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Увеличение лимита слотов Apps Script до 8 и журналирование GASBODY в server.ts (пункт 29) | Проверка вхождений `GAS_MAX_PARALLEL = 8` и `GASBODY` (1) в `server.ts`, сборка `compile_applet` | OK | 1) Выполнены 2 точечные замены в `server.ts`. 2) Подтверждены `GAS_MAX_PARALLEL = 8` и ровно 1 вхождение текста `GASBODY`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Отмена удаления строк истекших сессий в verifySession в Code.gs | Проверка отсутствия `deleteRow` внутри `verifySession` в `Code.gs`, сборка `compile_applet` | OK | 1) Выполнена точечная замена в `Code.gs`. 2) Подтверждено отсутствие `deleteRow` в функции `verifySession`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Добавление пункта 29 в docs/OZON_PLAN.md | Выполнение точечной замены в `docs/OZON_PLAN.md`, проверка корректности разметки и отсутствия искажений текста | OK | 1) В `docs/OZON_PLAN.md` успешно добавлен пункт 29 перед разделом «Известные хвосты вне плана». 2) Проверена разметка markdown и точность совпадения с текстом задачи. |
| 2026-08-06 | Сужение условия распознавания заглушки doGet в server.ts (пункт 29, этап A) | Проверка вхождений `isDoGetStub` (1), `isHtmlPage` (1), `trimmedText` (1) и `isStub` в `server.ts`, сборка `compile_applet` | OK | 1) Выполнена точечная замена в `server.ts`. 2) Подтверждено ровно по 1 вхождению `isDoGetStub`, `isHtmlPage`, `trimmedText`, а также сохранение строки с `isStub`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Замена пункта 29 в docs/OZON_PLAN.md на новую редакцию | Проверка структуры удаляемого блока (строки 106-124), вставка дословной строки из промта, проверка grep -cE '^- (⬜\|🟨\|✅) [0-9]+\.' docs/OZON_PLAN.md (ровно 29) | OK | 1) Замена проведена точно в границах строк 106-124. 2) Вставлена ровно 1 непрерывная строка из промта. 3) Выполнен grep-тест: получено значение 29 до и после правки. |
| 2026-08-06 | Разделение TTL кэша по типам действий в server.ts (пункт 29, этап B) | Проверка 0 вхождений `isCacheable` и `GAS_CACHE_TTL_MS`, 2 вхождений `getCacheTtlMs` в `server.ts`, сборка `compile_applet` | OK | 1) Выполнены 3 замены в `server.ts`. 2) Подтверждено 0 вхождений `isCacheable`, 0 вхождений `GAS_CACHE_TTL_MS`, ровно 2 вхождения `getCacheTtlMs`. 3) Сборка `compile_applet` выполнена успешно. |
| 2026-08-06 | Повышение порога RATE_LIMIT в server.ts до 300 | Проверка вхождений RATE_LIMIT = 300 (1) и RATE_WINDOW_MS = 60 * 1000 (1) в server.ts, сборка compile_applet | OK | 1) Выполнена замена в `server.ts`. 2) Подтверждено `RATE_LIMIT = 300` и `RATE_WINDOW_MS = 60 * 1000`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Точечная очистка кэша прокси в server.ts (пункт 29, этап C) | Проверка отсутствия `gasCache.clear()` вне `invalidateCacheFor`, наличия 17 ключей в `CACHE_INVALIDATION`, сборка `compile_applet` | OK | 1) Выполнены 3 замены в `server.ts`. 2) Подтверждено: единственный `gasCache.clear()` находится внутри `invalidateCacheFor`, в карте `CACHE_INVALIDATION` ровно 17 ключей. 3) Сборка `compile_applet` выполнена успешно. |
| 2026-08-06 | Устранение дублирующего запроса getOzonClusters в OzonStocksTab.tsx (пункт 29, этап D) | Проверка 1 вхождения 'getOzonClusters' и 1 вхождения notifyCheckDone.current = true в OzonStocksTab.tsx, сборка compile_applet | OK | 1) Выполнены 2 замены в `src/components/OzonStocksTab.tsx`. 2) Подтверждено: ровно 1 вхождение `'getOzonClusters'`, ровно 1 вхождение `notifyCheckDone.current = true`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Обновление работы loginUser с листом «Сессии» в Code.gs (пункт 29, этап F) | Проверка отсутствия deleteRow и appendRow к листу «Сессии» в loginUser, проверка вызовов deleteRow (23) и сборка compile_applet | OK | 1) Выполнены 2 замены в `Code.gs`. 2) Подтверждено: в `loginUser` нет `deleteRow` и `appendRow`, перезапись листа атомарная. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Замер времени расчётов на вкладке «Остатки Озон» в OzonStocksTab.tsx (пункт 29, этап E) | Проверка 2 вхождений OZONPERF, 1 perfStart, 1 rowsPerfStart в OzonStocksTab.tsx, сборка compile_applet | OK | 1) Выполнены 3 замены в `src/components/OzonStocksTab.tsx`. 2) Подтверждено: ровно 2 вхождения `OZONPERF`, ровно 1 `perfStart`, ровно 1 `rowsPerfStart`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Исправление TS2339 в OzonStocksTab.tsx | Проверка 0 вхождений pendingSupplies.length, 2 вхождений OZONPERF, сборка compile_applet | OK | 1) Выполнена замена в `src/components/OzonStocksTab.tsx`. 2) Подтверждено: 0 вхождений `pendingSupplies.length`, 2 вхождения `OZONPERF`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Добавление параметра weeksLimit в getOzonSales в Code.gs | Проверка вызовов weeksLimit в Code.gs, 1 вхождения в объявлении функции, передача data && data.weeksLimit в switch, сборка compile_applet | OK | 1) Выполнены 3 замены в `Code.gs`. 2) Подтверждено: `getOzonSales(weeksLimit)` объявлена с параметром, в `switch` передаётся `data && data.weeksLimit`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Передача weeksLimit: 12 в getOzonSales в useWarehouseStore.ts | Проверка 1 вхождения weeksLimit в useWarehouseStore.ts, проверка сохранности других вызовов fetchGas, сборка compile_applet | OK | 1) Выполнена замена в `src/store/useWarehouseStore.ts`. 2) Подтверждено: ровно 1 вхождение `weeksLimit` в файле. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Передача weeksLimit внутри data в useWarehouseStore.ts | Проверка вызыва fetchGas в useWarehouseStore.ts на вхождение { data: { weeksLimit: 12 } }, сборка compile_applet | OK | 1) Выполнена замена в `src/store/useWarehouseStore.ts`. 2) Подтверждено: ровно 1 вхождение `weeksLimit` в виде `{ data: { weeksLimit: 12 } }`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Передача weeksLimit в блоке быстрого обхода в Code.gs | Проверка вызова getOzonSales с payload.data.weeksLimit в Code.gs, проверка отсутствия вызовов getOzonSales() без параметра, сборка compile_applet | OK | 1) Выполнена замена в `Code.gs`. 2) Подтверждено: 2 вызова `getOzonSales` с параметром (в блоке быстрого обхода и в switch), 0 вызовов без параметра. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Задержка расчёта покрытия до загрузки кластеров в OzonStocksTab.tsx | Проверка 3 вхождений clusterRefsLoaded, вызов setClusterRefsLoaded в finally, 10 элементов в зависимости useMemo coverage, сборка compile_applet | OK | 1) Выполнены 4 замены в `src/components/OzonStocksTab.tsx`. 2) Подтверждено: 3 вхождения `clusterRefsLoaded`, 1 вызов `setClusterRefsLoaded` в `finally`, 10 элементов в массиве зависимостей `coverage`. 3) Сборка `compile_applet` прошла успешно. |
| 2026-08-06 | Обновление статусов этапов в пункте 29 в OZON_PLAN.md | Проверка числа строк (184), байт (рост) и 29 пунктов по grep | OK | 1) Выполнена замена в `docs/OZON_PLAN.md`. 2) Подтверждено: ровно 184 строки, размер вырос, grep по пунктам даёт 29. |
| 2026-08-07 | Опрос состава неоформленных поставок Ozon и защита от пустого ответа в server.ts | Проверка размера server.ts (115682 -> 116185 байт), подсчёт вхождений shouldFetchBundle (2) и itemsAreEmpty (1), проверка tsc --noEmit, сборка compile_applet | OK | 1) Выполнены 2 замены в `server.ts`. 2) Подтверждено: размер вырос со 115682 до 116185 байт. 3) Подтверждено: 2 вхождения `shouldFetchBundle = true;`, 1 вхождение `const itemsAreEmpty = items.length === 0;`. 4) `npx tsc --noEmit` и `compile_applet` завершились успешно без ошибок. |
| 2026-08-07 | Удержание локального резерва Ozon до фактического списания в ozonPending.ts | Проверка размера ozonPending.ts (12368 -> 13446 байт), подсчёт вхождений isShipmentSettled (3) и isStockDeparted (0), подсчёт PENDING_ACTIVE_STATUSES (8), проверка tsc --noEmit, сборка compile_applet | OK | 1) Выполнены 4 замены в `src/lib/ozonPending.ts`. 2) Подтверждено: размер вырос с 12368 до 13446 байт. 3) Подтверждено: 3 вхождения `isShipmentSettled`, 0 вхождений `isStockDeparted`, 8 элементов в `PENDING_ACTIVE_STATUSES`. 4) `npx tsc --noEmit` и `compile_applet` завершились успешно без ошибок. |
| 2026-08-07 | Функция алертов о превышении резерва заявками в ozonAlerts.ts | Проверка размера ozonAlerts.ts (12597 -> 15283 байт), подсчёт вхождений buildReserveShortageAlerts (1), reserve_shortage (3), export function (3), import (3), проверка tsc --noEmit, сборка compile_applet | OK | 1) Выполнены 2 правки в `src/lib/ozonAlerts.ts`. 2) Подтверждено: размер вырос с 12597 до 15283 байт. 3) Подтверждено: 1 вхождение `buildReserveShortageAlerts`, 3 вхождения `reserve_shortage`, 3 вхождения `export function`, 3 строки `import`. 4) `npx tsc --noEmit` и `compile_applet` завершились успешно без ошибок. |
| 2026-08-07 | Подключение алертов резерва Ozon в Dashboard.tsx | Проверка размера Dashboard.tsx (55212 -> 57095 байт), подсчёт вхождений buildReserveShortageAlerts (2), reserveShortageAlerts (3), useMemo (12), проверка tsc --noEmit, сборка compile_applet | OK | 1) Выполнены 2 замены в `src/components/Dashboard.tsx`. 2) Подтверждено: размер вырос с 55212 до 57095 байт. 3) Подтверждено: 2 вхождения `buildReserveShortageAlerts`, 3 вхождения `reserveShortageAlerts`, 12 вхождений `useMemo(`. 4) `npx tsc --noEmit` и `compile_applet` завершились успешно без ошибок. |
| 2026-08-07 | Использование isActionableItem в OzonSuppliesTab.tsx | Проверка размера OzonSuppliesTab.tsx (88979 -> 89709 байт), подсчёт вхождений isActionableItem (6) и i.status === 'new' (1), проверка tsc --noEmit, сборка compile_applet | OK | 1) Выполнены 4 замены в `src/components/OzonSuppliesTab.tsx`. 2) Подтверждено: размер вырос с 88979 до 89709 байт. 3) Подтверждено: 6 вхождений `isActionableItem`, 1 вхождение `i.status === 'new'`. 4) `npx tsc --noEmit` и `compile_applet` завершились успешно без ошибок. |
| 2026-08-07 | Заморозка проверки остатков при записи в ConfirmModal.tsx | Проверка размера ConfirmModal.tsx (50499 -> 51608 байт), подсчёт вхождений isCommitting (5) и lastValidatedRef (3), проверка tsc --noEmit, сборка compile_applet | OK | 1) Выполнены 3 замены в `src/components/ConfirmModal.tsx`. 2) Подтверждено: размер вырос с 50499 до 51608 байт. 3) Подтверждено: 5 вхождений `isCommitting`, 3 вхождения `lastValidatedRef`. 4) `npx tsc --noEmit` и `compile_applet` завершились успешно без ошибок. |
| 2026-08-07 | Добавление метки версии сборки (__APP_BUILD__) | Проверка размера vite-env.d.ts (205 байт), подсчёт вхождений __APP_BUILD__ в vite.config.ts (1) и Sidebar.tsx (2), проверка tsc --noEmit, сборка compile_applet | OK | 1) Создан `src/vite-env.d.ts` (205 байт). 2) Выполнены замены в `vite.config.ts` и `src/components/Sidebar.tsx`. 3) Подтверждено: 1 вхождение `__APP_BUILD__` в `vite.config.ts`, 2 вхождения в `src/components/Sidebar.tsx`. 4) `npx tsc --noEmit` и `compile_applet` завершились успешно без ошибок. |
| 2026-08-07 | Фиксация пункта 30 в паспорте проекта OZON_PLAN.md | Проверка вхождения пункта 30 и хвостов в OZON_PLAN.md, проверка записей в DEVLOG.md, CHANGELOG.md, TEST_LOG.md | OK | 1) В docs/OZON_PLAN.md добавлен пункт 30 и три строки в известные хвосты. 2) Все три журнала обновлены строго добавлением новых строк в конец файлов. |
| 2026-08-07 | Фиксация пункта 31 в паспорте проекта OZON_PLAN.md | Проверка вхождения пункта 31 в OZON_PLAN.md, проверка grep и wc показателей, проверка записей в DEVLOG.md, CHANGELOG.md, TEST_LOG.md | OK | 1) В docs/OZON_PLAN.md внесены изменения по пункту 31. 2) Проверено вхождение 'Виртуальные заявки Ozon'. 3) Журналы обновлены дозаписью в конец файлов. |
| 2026-08-07 | Проброс isVirtual и originalSupplyId в server.ts | Проверка размера server.ts (116185 -> 117462 байт), строк (2671 -> 2687), подсчёт is_virtual (2) и isVirtual (3), проверка npx tsc --noEmit, сборка compile_applet | OK | 1) Выполнены 3 замены в `server.ts`. 2) Подтверждено: размер вырос со 116185 до 117462 байт, строки 2671 -> 2687. 3) Подтверждено: 2 вхождения `is_virtual`, 3 вхождения `isVirtual`. 4) `npx tsc --noEmit` прошёл без ошибок, сборка успешна. |
| 2026-08-07 | Добавление колонок Виртуальная и ИсходнаяПоставка в Code.gs | Проверка размера Code.gs (251999 -> 253687 байт), строк (6166 -> 6186), проверка grep function (121) и assertAdmin (33), проверка вхождений 'Виртуальная' (4), 'ИсходнаяПоставка' (3) и 'colIsVirtual' (5) | OK | 1) Выполнены 6 замен в `Code.gs`. 2) Подтверждено совпадение числа функций (121) и assertAdmin (33). 3) Подтверждены подсчёты 'Виртуальная' (4), 'ИсходнаяПоставка' (3), 'colIsVirtual' (5). |
| 2026-08-07 | Исключение виртуальных заявок из резерва и списания | Проверка размеров и строк src/ файлов, проверка grep counts (isVirtual, isVirtualShipment), проверка npx tsc --noEmit, сборка npx vite build и compile_applet | OK | 1) Выполнены 7 замен в 3 файлах. 2) Подтверждены показатели grep: isVirtual (types: 1, ozonPending: 3, ozonGroups: 52), isVirtualShipment (2). 3) `npx tsc --noEmit` и `npx vite build` и `compile_applet` завершились успешно. |
| 2026-08-07 | Отображение виртуальных заявок Ozon и обработка кнопкой Ок | Проверка размера OzonSuppliesTab.tsx (89709 -> 92142 байт), строк (1806 -> 1843), проверка grep isVirtual (8), handleAcknowledgeVirtualGroup (2), Игнорировать (2), Привязать как дубль (1), проверка npx tsc --noEmit, npx vite build, compile_applet | OK | 1) Выполнены 4 замены в `src/components/OzonSuppliesTab.tsx`. 2) Подтверждено совпадение вызовов Игнорировать (2) и Привязать как дубль (1). 3) Подтверждены показатели isVirtual (8) и handleAcknowledgeVirtualGroup (2). 4) Сборки tsc, vite и compile_applet прошли успешно. |
| 2026-08-08 | Отметка о выполнении пункта 31 в OZON_PLAN.md | Проверка размера docs/OZON_PLAN.md (155761 -> 155830 байт), количества строк (190), проверки grep (пункты, ⬜ 26, ✅ 31, ⬜) | OK | 1) Выполнены 6 дословных подстановок в `docs/OZON_PLAN.md`. 2) Количество строк осталась ровно 190. 3) Все проверочные команды grep вернули ожидаемые результаты. |
| 2026-08-08 | Внесение пункта 32 в OZON_PLAN.md | Проверка размера docs/OZON_PLAN.md (155830 -> 157297 байт), количества строк (190 -> 192), проверки grep (пункты, ⬜ 26, ✅ 31, 🟨 32, ## Известные хвосты вне плана) | OK | 1) Выполнена точечная подстановка в `docs/OZON_PLAN.md`. 2) Количество строк выросло со 190 до 192. 3) Все проверочные команды grep вернули ожидаемые результаты. |
| 2026-08-08 | Добавление markExternalShipmentsBatch в useWarehouseStore.ts | Проверка размера useWarehouseStore.ts, проверка grep показателей markExternalShipment: (2) и markExternalShipmentsBatch (2), проверка npx tsc --noEmit, npx vite build, compile_applet | OK | 1) Выполнены 2 дословные подстановки в `src/store/useWarehouseStore.ts`. 2) Подтверждено: `markExternalShipment:` встречается 2 раза, `markExternalShipmentsBatch` встречается 2 раза. 3) Проверки `npx tsc --noEmit`, `npx vite build` и `compile_applet` прошли успешно. |
| 2026-08-08 | Перевод OzonSuppliesTab.tsx на isSyncing и markExternalShipmentsBatch | Проверка размера OzonSuppliesTab.tsx, проверка grep (markExternalShipmentsBatch: 6, markExternalShipment(: 0, isSyncing: 4, disabled={isProcessing}: 10), проверка npx tsc --noEmit, npx vite build, compile_applet | OK | 1) Выполнены 7 дословных подстановок в `src/components/OzonSuppliesTab.tsx`. 2) Все проверочные показатели grep совпали с ожидаемыми. 3) `npx tsc --noEmit`, `npx vite build` и `compile_applet` завершились успешно. |
| 2026-08-08 | Отметка о выполнении пункта 32 в OZON_PLAN.md | Проверка размера docs/OZON_PLAN.md (157297 -> 157488 байт), количества строк (192), проверки grep (пункты, ⬜ 26, ✅ 31, ✅ 32, ## Известные хвосты вне плана) | OK | 1) Выполнены 4 дословные подстановки в `docs/OZON_PLAN.md`. 2) Количество строк осталась ровно 192. 3) Все проверочные команды grep вернули ожидаемые результаты. |
| 2026-08-08 | Документирование виртуальных заявок в OZON_API.md | Проверка байт и строк docs/OZON_API.md (74076 -> 76646 байт, 788 -> 831 строк), проверка grep ("## ", "is_virtual") | OK | 1) Выполнена 1 дословная подстановка в `docs/OZON_API.md`. 2) Добавлено 43 строки, удалено 0. 3) Количество заголовков `## ` выросло с 12 до 13, упоминаний `is_virtual` выросло с 3 до 8. |
| 2026-08-08 | Добавление правил 5–8 в OZON_PLAN.md | Проверка размера docs/OZON_PLAN.md (157488 -> 162804 байт), количества строк (192 -> 200), проверки grep (пункты: 32, ⬜ 26: 1, ##: 6, "ЛИМИТ ДЛИНЫ ПОДСТАНОВКИ": 1, "ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ АРХИТЕКТОРА": 1) | OK | 1) Выполнена 1 дословная подстановка в `docs/OZON_PLAN.md`. 2) В раздел «Правило границ промта» добавлены 4 новых правила (5–8). 3) Все проверочные команды grep вернули ожидаемые результаты. |
| 2026-08-09 | Внесение пунктов 33–39 в OZON_PLAN.md | Проверка размера docs/OZON_PLAN.md (162804 -> 185107 байт), количества строк (200 -> 214), проверка grep ("## Известные хвосты вне плана": 1) | OK | 1) Выполнена 1 дословная подстановка в `docs/OZON_PLAN.md`. 2) Добавлены 7 новых пунктов (33–39) со статусом ⬜. 3) Все проверочные команды вернули ожидаемые результаты. |
| 2026-08-09 | Блокировка операций «Приход» и «Корректировка» на виртуальные комплекты в useWarehouseStore.ts | Проверка размера useWarehouseStore.ts (1631 -> 1649 строк), проверка grep ("Пункт 33, этап A": 1, "virtualKitOnIncome": 3, "if (type === 'Расход') {": 1), compile_applet | OK | 1) Выполнена вставка блока проверки в commitTransaction. 2) Количество строк изменилось с 1631 до 1649. 3) Проверки grep полностью совпали с контрольными требованиями. 4) compile_applet успешно подтвердил сборку приложения. |
| 2026-08-09 | Предзаполнение компонентов виртуального комплекта в FactoryOrderModal.tsx | Проверка размера FactoryOrderModal.tsx (228 -> 237 строк), проверка grep ("Пункт 33, этап B": 1, "virtualKitOnReceive": 3, "setFactoryOrderReceived": 2), compile_applet | OK | 1) Выполнены 2 замена в `src/components/FactoryOrderModal.tsx`. 2) Количество строк изменилось с 228 до 237. 3) Проверки grep полностью совпали с контрольными требованиями. 4) compile_applet подтвердил успешную сборку. |
| 2026-08-09 | Завершение пункта 33 и заведение пункта 40 в OZON_PLAN.md | Проверка размера docs/OZON_PLAN.md (215 -> 216 строк), проверка grep ("- ✅ 33. Виртуальные комплекты": 1, "- ⬜ 40. Выбор адресата": 1, "- ⬜ 34. Контур поставок": 1) | OK | 1) Выполнены 3 механические замены в `docs/OZON_PLAN.md`. 2) Пункт 33 и этапы 0, A, B, C помечены ✅. 3) Добавлен пункт 40 со статусом ⬜. 4) Все проверочные команды grep вернули ожидаемые результаты. |
| 2026-08-09 | Уточнение критериев пункта 34 и приоритета пункта 40 в OZON_PLAN.md | Проверка размера docs/OZON_PLAN.md (216 -> 216 строк), проверка grep ("КРИТЕРИЙ ПРИЁМКИ": 1, "ПРИОРИТЕТ ИЗМЕНЁН РЕШЕНИЕМ ПОЛЬЗОВАТЕЛЯ": 1) | OK | 1) Выполнены 2 замены в `docs/OZON_PLAN.md`. 2) Количество строк осталось ровно 216. 3) Проверки grep полностью совпали с ожидаемыми фрагментами текста. |
| 2026-08-09 | Добавление настройки maxClusterDays в OZON_SETTINGS_DEFAULTS в Code.gs | Проверка размера Code.gs (6186 -> 6187 строк), проверка grep ("maxClusterDays": 1), compile_applet | OK | 1) Выполнена 1 вставка в `Code.gs`. 2) Количество строк увеличилось с 6186 до 6187. 3) В массиве OZON_SETTINGS_DEFAULTS стало 15 элементов. 4) grep подтвердил ровно 1 вхождение maxClusterDays. 5) compile_applet подтвердил успешную сборку. |
| 2026-08-09 | Расчёт целевого запаса без двойного счёта и отсекатель по макс. сроку продаж кластера в ozonCoverage.ts | Проверка размера ozonCoverage.ts (653 -> 657 строк), проверка grep ("maxClusterDays"?: 1, "calcCoverageDays": 2), compile_applet | OK | 1) Выполнены 2 замены в `src/lib/ozonCoverage.ts`. 2) Количество строк изменилось с 653 до 657. 3) Функция calcCoverageDays вызывается 2 раза во всём файле. 4) compile_applet подтвердил полную сборку и отсутствие ошибок типов. |
| 2026-08-09 | Прокидка maxClusterDays и обновление ColHint в OzonStocksTab.tsx и Dashboard.tsx | Проверка grep ("maxClusterDays": 1 в OzonStocksTab.tsx и Dashboard.tsx), compile_applet | OK | 1) Выполнены 2 замены в `src/components/OzonStocksTab.tsx` и 1 замена в `src/components/Dashboard.tsx`. 2) grep подтвердил по 1 вхождению maxClusterDays в обоих файлах. 3) compile_applet подтвердил успешную сборку без ошибок типов. |
| 2026-08-09 | Поддержка maxClusterDays и валидация целевого запаса в OzonSettingsModal.tsx | Проверка 5 замен, проверка grep ("maxClusterDays": 5 в OzonSettingsModal.tsx), compile_applet, lint_applet | OK | 1) Выполнены 5 точечных замен в `src/components/OzonSettingsModal.tsx`. 2) grep подтвердил ровно 5 вхождений `maxClusterDays`. 3) Проверка валидации checkTargetDays <= checkMinDays с выводом ошибки toast добавлена в handleSave. 4) compile_applet и lint_applet успешно подтвердили сборку без ошибок типов. |
| 2026-08-09 | Обновление разметки поля целевого запаса и добавление поля maxClusterDays в OzonSettingsModal.tsx | Проверка количества вхождений искомого блока (1), подсчёт maxClusterDays (6), подсчёт form.targetStockDays (3), compile_applet, lint_applet | OK | 1) Выполнена 1 точечная замена блока разметки. 2) Искомый блок встречался ровно 1 раз до правки. 3) Вхождений maxClusterDays во всём файле стало 6. 4) Вхождений form.targetStockDays осталось 3. 5) compile_applet и lint_applet прошли без ошибок. |
| 2026-08-09 | Внесение пункта 41 (География спроса) в OZON_PLAN.md | Проверка вхождений "## Известные хвосты вне плана" (1 до правки), проверка количества строк (217 -> 219), проверка "⬜ 41." (1), проверка порядка блоков | OK | 1) Выполнена 1 точечная замена перед "## Известные хвосты вне плана". 2) Строка-заголовок встречалась 1 раз. 3) Число строк в файле стало 219 (ровно на 2 больше). 4) Строка "⬜ 41." встречается ровно 1 раз. 5) Сразу после пункта 41 идёт пустая строка и заголовок "## Известные хвосты вне плана". |
| 2026-08-09 | Фиксация фактов разведки географии спроса в OZON_API.md | Проверка количества вхождений двух искомых строк (по 1 до правки), подсчёт строк (830 -> 837), проверка вхождений cluster_to (3) | OK | 1) Выполнены 2 точечные замены в `docs/OZON_API.md`. 2) Каждая искомая строка встречалась ровно 1 раз. 3) Количество строк в файле изменилось с 830 до 837 (ровно +7). 4) Строка `cluster_to` встречается в файле ровно 3 раза. |
| 2026-08-09 | Использование cluster_to как источника кластера продажи в server.ts | Проверка искомых фрагментов (по 1 до правки), подсчёт cluster_to (1) и analytics_data.warehouse_id (0), сохранность whClusterMap и loadWarehouseClusterMap, проверка строк (2688 -> 2692), compile_applet, lint_applet | OK | 1) Выполнены 2 замены в `server.ts`. 2) До правки каждый фрагмент встречался ровно 1 раз. 3) `cluster_to` встречается 1 раз, `analytics_data.warehouse_id` — 0 раз. 4) Объявление `whClusterMap` и функция `loadWarehouseClusterMap` сохранены. 5) Количество строк в файле стало 2692 (было 2688). 6) compile_applet и lint_applet подтвердили полную работоспособность. |
| 2026-08-09 | Логирование незнакомых кластеров SALESGEO в server.ts | Проверка искомого фрагмента (1 до правки), подсчёт SALESGEO (1) и knownClusterNames (1), проверка okCabinets.push(name) (2), проверка строк (2692 -> 2708), compile_applet, lint_applet | OK | 1) Выполнена 1 замена в `server.ts`. 2) Искомый фрагмент встречался ровно 1 раз. 3) `SALESGEO` и `knownClusterNames` встречаются по 1 разу. 4) `okCabinets.push(name)` встречается 2 раза (второй обработчик не тронут). 5) Число строк стало 2708 (было 2692). 6) compile_applet и lint_applet прошли без ошибок. |
| 2026-08-10 | Обновление эндпоинта /api/version в server.ts | Проверка искомого фрагмента (1 до правки), подсчёт K_REVISION (1) и salesClusterTo (1), compile_applet, lint_applet | OK | 1) Выполнена 1 замена в `server.ts`. 2) Искомый фрагмент встречался ровно 1 раз. 3) `K_REVISION` и `salesClusterTo` встречаются ровно по 1 разу во всём `server.ts`. 4) compile_applet и lint_applet прошли успешно без ошибок типов. |
| 2026-08-10 | Фиксация статуса выполнения пункта 41 в OZON_PLAN.md | Проверка вхождений "- ⬜ 41." (1 до правки, 0 после), проверка "- ✅ 41." (0 до, 1 после), проверка количества строк (219 -> 219) | OK | 1) Выполнены 2 текстовые замены в `docs/OZON_PLAN.md`. 2) Каждый искомый фрагмент встречался ровно 1 раз до правки. 3) Число строк в `docs/OZON_PLAN.md` составляет 219. 4) Строка "- ✅ 41." встречается ровно 1 раз, "- ⬜ 41." — 0 раз. |
| 2026-08-10 | Фиксация фактов про два кабинета и CIS-кластеры в OZON_API.md | Проверка искомой строки (1 до правки), проверка числа строк (837 -> 840), подсчёт Mercurius (3) и MaxiStore (2), проверка предшествующей строки | OK | 1) Выполнена вставка 3 новых строк сразу за целевой строкой в `docs/OZON_API.md`. 2) Искомая строка встречалась ровно 1 раз до правки. 3) Число строк в `docs/OZON_API.md` стало 840 (было 837). 4) `Mercurius` встречается 3 раза, `MaxiStore` — 2 раза. 5) Непосредственно перед первой вставленной строкой идет целевая строка про `financial_data.cluster_to`. |
| 2026-08-10 | Фиксация выполнения пункта 34 и решений пункта 35 в OZON_PLAN.md | Проверка замены фрагмента пункта 35 в OZON_PLAN.md (1 до правки), проверка строк (219), подсчёт «⬜ 35.» (1) и «A0» (1), проверка размера файла | OK | 1) Выполнена 1 текстовая замена в `docs/OZON_PLAN.md`. 2) Искомый фрагмент встречался ровно 1 раз до правки. 3) Число строк в `docs/OZON_PLAN.md` сохранилось (219). 4) «⬜ 35.» и «A0» встречаются по 1 разу. 5) Зафиксированы отметка выполнения пункта 34 и решения с эталоном пункта 35. |
| 2026-08-10 | Перевод сигнала «Заказ на фабрике» на модель ТРУБА в ozonCoverage.ts | Проверка вхождений фрагментов (по 1 до правки), проверка `pipelineQty` (4) и `factoryOnOrder` (2), проверка строк (661 -> 688), compile_applet, lint_applet | OK | 1) Выполнены 3 точечные замены в `src/lib/ozonCoverage.ts`. 2) Каждая из замен встретилась ровно по 1 разу. 3) `pipelineQty` встречается 4 раза, `factoryOnOrder` — 2 раза. 4) Количество строк выросло с 661 до 688. 5) compile_applet и lint_applet прошли успешно без ошибок типов. |
| 2026-08-10 | Передача активных заказов на фабрике в расчёт покрытия в OzonStocksTab.tsx | Проверка вхождений фрагментов (по 1 до правки), подсчёт вхождений factoryOnOrder и factoryOrdersByArticle, проверка сборки | OK | Заменяемые фрагменты встречались по 1 разу; правка выявила ошибку порядка объявлений, исправленную следующей задачей. |
| 2026-08-10 | Перенос factoryOrdersByArticle и factoryOnOrder выше coverage в OzonStocksTab.tsx | `npm run lint` (`tsc --noEmit`), проверка строк (353 < 368), проверка вхождений factoryOnOrder (3) и factoryOrdersByArticle (3), строк в файле (1468 -> 1468), compile_applet | OK | 1) Выполнены 2 замена в `src/components/OzonStocksTab.tsx`. 2) Заменяемые фрагменты встречались по 1 разу. 3) `factoryOnOrder` объявлена на строке 353, `coverage` на 368 (353 < 368). 4) `tsc --noEmit` и `lint_applet` подтвердили 0 ошибок типов (ошибки TS2448/TS2454 устранены). 5) compile_applet прошёл успешно. |
| 2026-08-10 | Передача активных заказов на фабрике в расчёт покрытия в Dashboard.tsx | `npm run lint` (`tsc --noEmit`), проверка строк (225 < 240), проверка factoryOnOrder (3), orderedArticles (2 до и 2 после), строк (1197 -> 1216), compile_applet | OK | 1) Выполнена 1 замена в `src/components/Dashboard.tsx`. 2) Заменяемый фрагмент встречался ровно 1 раз. 3) `factoryOnOrder` объявлен на строке 225, `ozonCoverage` на 240 (225 < 240). 4) `tsc --noEmit` подтвердил 0 ошибок типов TS2448/TS2454. 5) `factoryOnOrder` встречается 3 раза, `orderedArticles` 2 раза. 6) compile_applet прошёл успешно. |
| 2026-08-10 | Отображение пяти состояний ячейки «Заказ на фабрике» в OzonStocksTab.tsx | `npm run lint` (`tsc --noEmit`), проверка вхождений factoryWaitingQty (8), factoryOverdueList (4), factoryClusterOnly (2), строк (1468 -> 1491), compile_applet | OK | 1) Выполнены 4 точечные замены в `src/components/OzonStocksTab.tsx`. 2) Заменяемые фрагменты встречались по 1 разу. 3) `tsc --noEmit` и `lint_applet` подтвердили 0 ошибок типов. 4) Подсчитаны вхождения: `factoryWaitingQty` (8 вхождений), `factoryOverdueList` (4 вхождения), `factoryClusterOnly` (2 вхождения). 5) Число строк изменилось с 1468 до 1491. 6) compile_applet прошёл успешно. |
| 2026-08-10 | Сводная панель «Заказать на фабрике» под дозаказ и ТРУБУ в OzonStocksTab.tsx | `npm run lint` (`tsc --noEmit`), подсчёт clusterDeficitCount (7) и orderedCount (8), строк (1491 -> 1498), compile_applet | OK | 1) Выполнены 5 точечных замен в `src/components/OzonStocksTab.tsx`. 2) Заменяемые фрагменты встречались ровно по 1 разу. 3) `tsc --noEmit` и `lint_applet` подтвердили 0 ошибок типов. 4) Подсчитаны вхождения: `clusterDeficitCount` (7 вхождений), `orderedCount` (8 вхождений). 5) Число строк изменилось с 1491 до 1498. 6) compile_applet прошёл успешно. |
| 2026-08-10 | Перевод алертов «Пора заказать на фабрике» под дозаказ и ТРУБУ | `npm run lint` (`tsc --noEmit`), проверка orderedSet (0) и orderedArticles (0) в ozonAlerts.ts, строк в ozonAlerts.ts (383 -> 376) и Dashboard.tsx (1217 -> 1216), compile_applet | OK | 1) Выполнены 4 точечные замены (3 в `src/lib/ozonAlerts.ts` и 1 в `src/components/Dashboard.tsx`). 2) Все заменяемые фрагменты встречались ровно по 1 разу до правки. 3) `tsc --noEmit` и `lint_applet` подтвердили 0 ошибок типов. 4) Проверены вхождения: `orderedSet` (0) и `orderedArticles` (0) в `src/lib/ozonAlerts.ts`. 5) Изменение числа строк: `src/lib/ozonAlerts.ts` (383 -> 376), `src/components/Dashboard.tsx` (1217 -> 1216). 6) compile_applet прошёл успешно. |
| 2026-08-10 | Отображение размещенного заказа в ячейке при дефиците в кластерах | `npm run lint` (`tsc --noEmit`), подсчёт "дефицит в кластерах" (2 нижний регистр, 4 всего), строк (1499 -> 1511), compile_applet | OK | 1) Выполнена 1 точечная замена в `src/components/OzonStocksTab.tsx`. 2) Заменяемый фрагмент встречался ровно 1 раз. 3) `tsc --noEmit` и `lint_applet` подтвердили 0 ошибок типов. 4) Вхождения "дефицит в кластерах": 2 строки в ячейке (в нижнем регистре), 2 строки в сводной панели (с заглавной "Дефицит в кластерах:"). 5) Изменение числа строк в `src/components/OzonStocksTab.tsx`: 1499 -> 1511. 6) compile_applet прошёл успешно. |
| 2026-08-11 | Добавление колонки «Стоимость заказа, ₽» в OzonStocksTab.tsx | `npm run lint` (`tsc --noEmit`), подсчёт orderCost (6) и isColVisible('factory') (5 до и 5 после), строк (1511 -> 1535), compile_applet | OK | 1) Выполнены 7 точечных замен в `src/components/OzonStocksTab.tsx`. 2) Все заменяемые фрагменты встречались ровно по 1 разу до правки. 3) `tsc --noEmit` и `lint_applet` подтвердили 0 ошибок типов. 4) Подсчитаны вхождения: `orderCost` (6 вхождений), `isColVisible('factory')` (5 до и 5 после). 5) Изменение числа строк в `src/components/OzonStocksTab.tsx`: 1511 -> 1535. 6) compile_applet прошёл успешно. |
| 2026-08-11 | Добавление getLastPurchasePrices и регистрация действия в Code.gs | `npm run lint`, `compile_applet`, подсчёт getLastPurchasePrices (2), setValue в getLastPurchasePrices (0), строк (6188 -> 6245) | OK | 1) Выполнены 2 точечные замены в Code.gs. 2) Оба заменяемых фрагмента встречались ровно по 1 разу до правки. 3) `npm run lint` и `compile_applet` прошли успешно. 4) Подсчитаны вхождения: `getLastPurchasePrices` (2 вхождения: объявление функции и ветка switch), `setValue` внутри `getLastPurchasePrices` (0 вхождений). 5) Изменение числа строк в Code.gs: 6188 -> 6245. 6) compile_applet прошёл успешно. |
| 2026-08-11 | Добавление lastPurchasePrices и fetchLastPurchasePrices в useWarehouseStore.ts | `npm run lint` (`tsc --noEmit`), `compile_applet`, подсчёт lastPurchasePrices (3 exact / 5 substring), fetchLastPurchasePrices (2), isProcessing (0 в fetchLastPurchasePrices), строк (1650 -> 1664) | OK | 1) Выполнены 3 точечные замены в `src/store/useWarehouseStore.ts`. 2) Все заменяемые фрагменты встречались ровно по 1 разу до правки. 3) `npm run lint` и `compile_applet` подтвердили 0 ошибок типов. 4) Подсчитаны вхождения: exact word `lastPurchasePrices` (3 вхождения), `fetchLastPurchasePrices` (2 вхождения), `isProcessing` в `fetchLastPurchasePrices` (0 вхождений). 5) Изменение числа строк в `src/store/useWarehouseStore.ts`: 1650 -> 1664. 6) compile_applet прошёл успешно. |
| 2026-08-11 | Перевод колонки «Стоимость заказа, ₽» на цены последних поступлений | `npm run lint` (`tsc --noEmit`), `compile_applet`, подсчёт getOrderUnitCost (5), fetchLastPurchasePrices (3, вызов на строке 155), строк (1536 -> 1563) | OK | 1) Выполнены 4 точечные замены в `src/components/OzonStocksTab.tsx`. 2) Все заменяемые фрагменты встречались ровно по 1 разу до правки. 3) `npm run lint` и `compile_applet` подтвердили 0 ошибок типов. 4) Подсчитаны вхождения: `getOrderUnitCost` (5 вхождений), `fetchLastPurchasePrices` (3 вхождения, включая вызов на строке 155). 5) Изменение числа строк в `src/components/OzonStocksTab.tsx`: 1536 -> 1563. 6) compile_applet прошёл успешно. |
| 2026-08-11 | Добавление cancelFactoryOrder и регистрация действия в Code.gs | `npm run lint`, `compile_applet`, подсчёт cancelFactoryOrder (2), deleteRow (3, было 2), строк (6245 -> 6275) | OK | 1) Выполнены 2 точечные замены в Code.gs. 2) Оба заменяемых фрагмента встречались ровно по 1 разу до правки. 3) `npm run lint` и `compile_applet` прошли успешно. 4) Подсчитаны вхождения: `cancelFactoryOrder` (2 вхождения: объявление функции и ветка switch), `deleteRow` (3 вхождения во всём файле, выросло с 2 до 3). 5) Изменение числа строк в Code.gs: 6245 -> 6275. 6) compile_applet прошёл успешно. |
| 2026-08-11 | Перестановка блоков JSDoc комментариев в Code.gs | `npm run lint`, `compile_applet`, подсчёт cancelFactoryOrder (1), getLastPurchasePrices (1), deleteRow (3, без изменений), строк (6276 до и после) | OK | 1) Выполнена 1 точечная замена блоков JSDoc в Code.gs. 2) Заменяемый фрагмент встречался 1 раз. 3) `npm run lint` и `compile_applet` прошли успешно. 4) Проверена строка перед `function cancelFactoryOrder`: ` */`. 5) Вхождения `function cancelFactoryOrder` (1), `function getLastPurchasePrices` (1), `deleteRow` (3, не изменилось). 6) Число строк не изменилось (6276). |
| 2026-08-11 | Добавление cancelFactoryOrder в useWarehouseStore.ts | `npm run lint` (`tsc --noEmit`), `compile_applet`, подсчёт cancelFactoryOrder (3), fetchGas('cancelFactoryOrder' (1), строк (1665 -> 1685) | OK | 1) Выполнены 2 точечные замены в `src/store/useWarehouseStore.ts`. 2) Оба заменяемых фрагмента встречались ровно по 1 разу до правки. 3) `npm run lint` и `compile_applet` подтвердили 0 ошибок типов. 4) Подсчитаны вхождения: `cancelFactoryOrder` (3 вхождения: тип, действие и fetchGas), `fetchGas('cancelFactoryOrder'` (1 вхождение). 5) Изменение числа строк в `src/store/useWarehouseStore.ts`: 1665 -> 1685. 6) compile_applet прошёл успешно. |
| 2026-08-11 | Добавление отмены заказа и двухэтапного подтверждения в FactoryOrderModal.tsx | `npm run lint` (`tsc --noEmit`), `compile_applet`, подсчёт confirmCancel (6), handleCancel (2), Trash2 (3), первой строки, строк (238 -> 289) | OK | 1) Выполнены 4 точечные замены и добавлен импорт Trash2 в `src/components/FactoryOrderModal.tsx`. 2) Все заменяемые фрагменты встречались ровно по 1 разу до правки. 3) `npm run lint` (`tsc --noEmit`) и `compile_applet` подтвердили 0 ошибок сборки. 4) Вхождения: `confirmCancel` (6 вхождений), `handleCancel` (2 вхождения), `Trash2` (3 вхождения). 5) Первая строка: `import React, { useEffect, useState } from 'react';`. 6) Изменение числа строк: 238 -> 289. |
| 2026-08-11 | Фиксация выполнения пункта 35 в OZON_PLAN.md | `npm run lint`, `compile_applet`, проверка вхождений `⬜ 35.` (0), `✅ 35.` (1), `⬜ 42.` (1), количества строк (220 -> 220), байт (216 361 -> 219 627) | OK | 1) Выполнены 2 текстовые замены в `docs/OZON_PLAN.md`. 2) Оба заменяемых фрагмента встречались ровно по 1 разу до правки. 3) `npm run lint` и `compile_applet` прошлись без ошибок. 4) Вхождения: `⬜ 35.` (0), `✅ 35.` (1), `⬜ 42.` (1). 5) Количество строк сохранено (220), байт (216 361 -> 219 627). |
| 2026-08-11 | Добавление 5 настроек Ozon и правила валидации в Code.gs | `npm run lint`, `compile_applet`, подсчёт строк OZON_SETTINGS_DEFAULTS (15 -> 20), байт (258 403 -> 259 182), вхождений maxSpeedGrowth (1), trendWeeks (2) | OK | 1) Выполнены 2 точечные замены в `Code.gs`. 2) Заменяемые фрагменты встречались по 1 разу. 3) `npm run lint` и `compile_applet` прошли успешно. 4) Вхождения: maxSpeedGrowth (1), trendWeeks (2). 5) Количество строк OZON_SETTINGS_DEFAULTS: 15 -> 20. 6) Размер файла Code.gs: 258 403 -> 259 182 байт. |
| 2026-08-11 | Коррекция скорости продаж при дефиците в ozonCoverage.ts | `npm run lint` (`tsc --noEmit`), `compile_applet`, подсчёт строк (688 -> 857), байт (32 309 -> 41 652), вхождений applyDeficitSpeedCorrection (2), speedCorrection (3), MIN_WEEKS_WITH_SALES (4) | OK | 1) Выполнены 5 точечных замен в `src/lib/ozonCoverage.ts`. 2) Все заменяемые фрагменты встречались ровно по 1 разу до правки. 3) `npm run lint` (`tsc --noEmit`) подтвердил отсутствие ошибок типов ("ошибок нет"). 4) `compile_applet` прошёл успешно. 5) Подсчитаны строки (688 -> 857), байты (32 309 -> 41 652), вхождения: `applyDeficitSpeedCorrection` (2), `speedCorrection` (3), `MIN_WEEKS_WITH_SALES` (4). |
| 2026-08-11 | Передача 5 настроек дефицита из БД в OzonCoverageSettings | `npm run lint` (`tsc --noEmit`), `compile_applet`, подсчёт строк OzonStocksTab (1563 -> 1573), Dashboard (1216 -> 1226), вхождений deficitDays (по 2) и maxSpeedGrowth (по 2) | OK | 1) Выполнены по 2 точечные замены в `src/components/OzonStocksTab.tsx` и `src/components/Dashboard.tsx` (всего 4 замены). 2) Все заменяемые фрагменты встречались по 1 разу до правки. 3) `npm run lint` (`tsc --noEmit`) подтвердил отсутствие ошибок типов ("ошибок нет"). 4) `compile_applet` прошёл успешно. 5) Подсчитаны строки (OzonStocksTab: 1563 -> 1573, Dashboard: 1216 -> 1226), вхождения: `deficitDays` (по 2), `maxSpeedGrowth` (по 2). |
| 2026-08-11 | Добавление 5 полей ввода настроек дефицита в OzonSettingsModal.tsx | `npm run lint` (`tsc --noEmit`), `compile_applet`, подсчёт строк OzonSettingsModal (625 -> 729), вхождений deficitDays, trendWeeks, bestWeeks, minSalesForCorrection, maxSpeedGrowth (по 6), "Объём заказа на фабрике, дней" (1) | OK | 1) Выполнены 5 точечных замен в `src/components/OzonSettingsModal.tsx`. 2) Все заменяемые фрагменты встречались ровно по 1 разу до правки. 3) `npm run lint` (`tsc --noEmit`) подтвердил отсутствие ошибок типов ("ошибок нет"). 4) `compile_applet` прошёл успешно. 5) Число строк изменилось с 625 до 729. 6) Вхождения: `deficitDays`, `trendWeeks`, `bestWeeks`, `minSalesForCorrection`, `maxSpeedGrowth` — по 6 раз каждое, "Объём заказа на фабрике, дней" — 1 раз. |
| 2026-08-11 | Отображение плашки коррекции скорости при дефиците в OzonStocksTab.tsx | `npm run lint` (`tsc --noEmit`), `compile_applet`, подсчёт строк OzonStocksTab (1573 -> 1594), вхождений speedCorrection (7), TrendingUp (2), fmtSpeed(cls.perDay) (1) | OK | 1) Выполнены 2 точечные замены в `src/components/OzonStocksTab.tsx`. 2) Оба заменяемых фрагмента встречались по 1 разу до правки. 3) `npm run lint` (`tsc --noEmit`) подтвердил отсутствие ошибок типов ("ошибок нет"). 4) `compile_applet` прошёл успешно. 5) Подсчитаны строки (1573 -> 1594), вхождения: `speedCorrection` (7), `TrendingUp` (2), `fmtSpeed(cls.perDay)` (1). |
| 2026-08-18 | Инфраструктура Docker и переменная окружения PORT в server.ts | `npm ci` — 232 пакета, без ошибок; `npx tsc --noEmit` — 0 ошибок в файлах проекта; `npm run build` — 2122 модуля, сборка за 10,21 с, `dist/index.html` создан; запуск `PORT=8080 npx tsx server.ts` — в логе «Server running on http://localhost:8080», `curl /api/version` вернул HTTP 200 | OK | Все шаги Dockerfile проверены отдельно на машине разработчика. |
| 2026-08-18 | Модульные тесты на функции ozonCoverage.ts | `npx vitest run` — 29 тестов, 29 прошло, 0 упало, 0 пропущено, 530 мс; `npx tsc --noEmit` — 0 ошибок | OK | Тесты фиксируют текущее поведение. Расхождений между JSDoc-описанием и фактическим поведением не найдено. |
| 2026-08-18 | Сборка и запуск контейнера в Google Cloud Build (Docker локально недоступен) | Сборка образа по `Dockerfile` из дерева коммита `9aee3b6`; запуск контейнера с `PORT=8080`; запрос `/api/version` и главной страницы изнутри сети сборки | OK | 1) Сборка: все 10 шагов Dockerfile, `vite build` создал `dist`, образ собран за 34 с (build f922acc2). 2) Запуск (build 3460bd34, 51 с): в журнале контейнера «Server running on http://localhost:8080» — порт взят из переменной окружения, а не 3000. 3) `/api/version` → HTTP 200, тело `{"status":"success","version":"2026-07-31-cargoes"}`. 4) Главная страница → HTTP 200, значит `dist` раздаётся. 5) Образ НЕ публиковался, ревизия Cloud Run НЕ создавалась. 6) Строка «API ключ не найден» ожидаема: `GAS_URL`/`SERVER_SECRET` в проверке не задавались. Проверка воспроизводит отказ ревизии `service-00125-rgc` от 18.08.2026, которая не смогла занять порт 8080. |
| 2026-08-18 | Новый сервис Cloud Run `sklad` в europe-central2 (Варшава): развёртывание, связь с Apps Script, задержка | Развёртывание из ветки `work/cloud-run-and-tests` по `Dockerfile`; вход пользователя и работа с приложением по новому адресу; разбор журналов `GASDIAG`; замер задержки из Екатеринбурга | OK | 1) Ревизия `sklad-00002-5w7`, трафик 100 %, максимум экземпляров 1, старт с первой попытки TCP-пробы на порту 8080. 2) Все шесть переменных окружения на месте; в журнале «API ключ загружен в кэш» — прогрев ключа через Apps Script прошёл. 3) `GASDIAG`: 14 запросов, все `http=200`, повторных попыток 0. Среднее время Apps Script 4229 мс, максимум 9843 мс (`getOzonSales`, 182 КБ). 4) Одна заглушка `stub=true` на `getOzonSyncStatus` — известный незакрытый дефект редиректа POST→GET (раздел 14.4 плана), к переезду отношения не имеет, повтор не выполнялся. 5) Задержка по одному соединению: Орегон 277 мс (255–307), Варшава 127 мс (124–133), выигрыш 150 мс на запрос. Чистый сетевой оборот одинаков (99 против 103 мс). 6) Старый сервис `service` не тронут, работает. |
| 2026-08-18 | Логика истории остатков Ozon в этап E пункта 42 | Стенд на Node с заглушками Apps Script (`vm`, подмена `Utilities`, `Session`, `Logger`, `Date`, фальшивый лист), выполнен настоящий код из `Code.gs`. 27 проверок по 8 сценариям: первый запуск дня; повторный запуск в тот же день не меняет ничего; следующий день увеличивает счётчики на 1; регресс на даты-объекты из Sheets; новая неделя не трогает прошлую; срок хранения читается из настройки (проверено сравнением retention=2 против дефолта 15); новое сочетание среди недели получает «Дней наблюдений» = 1; склады одного кластера сворачиваются в одну строку. Дополнительно: мутационная проверка — при отключённой нормализации дат тест падает | OK | Провалов 0. Все 27 проверок прошли. Мутационная проверка: тест реально ловит класс дефектов нормализации дат. |
| 2026-08-18 | Сопутствующие правки (этап E пункта 42 и смена trendWeeks) | `node -e new Function(...)` по `Code.gs` — синтаксис OK; нулевых байтов 0; `npx tsc --noEmit` по `src/` — ошибок нет; `npx vitest run` — 29 тестов, 29 прошло | OK | Дополнительно: `git diff` по `Code.gs` показал ровно 3 удалённые строки, все три — намеренно заменённые (значение `trendWeeks` в константе, условие проверки настроек, `PROXY_URL`). |
| 2026-08-18 | Перенос стенда `Code.gs` в репозиторий | `npm run test:gas` из корня репозитория; контрольный прогон `npx vitest run` | OK | Стенд: 27 проверок, провалено 0, код возврата 0. Фронтенд-тесты не пострадали: 29 из 29. При переносе выявлены и устранены две помехи: абсолютный путь к `Code.gs` заменён на относительный; файлы переименованы в `.cjs`, потому что `"type": "module"` в `package.json` заставлял Node читать их как ES-модули. |
| 2026-08-18 | Пункт 36, этап A: ядро расчёта потребности на фабрике по компонентам | `npx tsc --noEmit`; `npx vitest run`; прогон скомпилированного модуля на боевых данных со сверкой с ручным расчётом | OK | `npx tsc --noEmit` — ошибок нет. `npx vitest run` — 39 тестов, 39 прошло (было 29, добавлено 10 на компоненты). Прогон на боевых данных: скорости и трубы всех четырёх компонентов сошлись до десятых с ручным расчётом — Миска серая 19,86 шт/д и труба 371,8; Миска бирюзовая 3,86 и 442,4; Бутылки 23,71 и 1320,2; Пакеты 23,71 и 1630,2; у обоих виртуальных комплектов `factory === null`. |
| 2026-08-18 | Пункт 36, этап B: интерфейс и алерты для потребности по компонентам | `npx tsc --noEmit`; `npx vitest run`; `npx vite build`; `git diff --numstat` | OK | `npx tsc --noEmit` — ошибок нет. `npx vitest run` — 39 из 39. `npx vite build` — сборка прошла за 6,05 с. `git diff --numstat` — затронуты ровно три разрешённых файла. Живой регресс в приложении (этап C пункта 36) ещё не проводился. |
| 2026-08-19 | Заказ на компонент виден в блоке компонентов | `npx tsc --noEmit -p tsconfig.json`; `npx vitest run`; `npm run build`; сверка `git diff` с файлом | OK | `tsc` — код возврата 0, вывода нет. Тесты — 39 из 39. Сборка — `built in 24.55s`, код возврата 0. Диагностика на боевых данных до правки: заказ «Миска серая» 1200 шт от 18.08.2026, ожидается 07.10.2026, статус active — в листе присутствует, в расчёт входит (запас 1572 шт, хватит на 79 дн), то есть дефект был чисто в отображении. |
| 2026-08-19 | Название товара Ozon в листе SKU | `npm run test:gas`; `npx vitest run`; `npx tsc --noEmit`; `npm run build`; мутационная проверка | OK | Стенд: 42 проверки, провалено 0 (было 27, добавлено 15 по 9 сценариям). Тесты фронтенда — 39 из 39. `tsc` — 0 ошибок. Сборка — код возврата 0. `Code.gs`: нулевых байтов 0, UTF-8 валиден. Мутационная проверка: снятие защиты `freshName && freshName !== currentName` роняет проверки П10 (распроданный товар теряет название) и П16 (пустое название затирает сохранённое) — провалено 4 из 42, после возврата кода снова 42 из 42. Отдельно проверено счётчиком вызовов `setValues`, что при отсутствии изменений запись в лист не выполняется ни разу. |
| 2026-08-19 | Пункт 38, этапы A–C: тренд продаж | `npx tsc --noEmit`; `npx vitest run`; `npm run test:gas`; `npm run build`; сверка с боевой базой скомпилированным модулем; сравнение старого и нового кода на одних данных; подсчёт колонок по уровням разметки | OK | Тестов 60 (было 39), провалено 0. Стенд Code.gs — 42 проверки, провалено 0. `tsc` — 0 ошибок. Сборка — код возврата 0. СВЕРКА С БОЕВОЙ БАЗОЙ: реализация даёт ровно контрольные числа замера 09.08.2026 — Набор_полок_выдв_бел 0,26; BowlGrayMini_01 0,78; Органайзер_2_пол_прозр 0,85; Миска_двойная 1,43; Этажерка_25_белая 1,81. ГЛАВНАЯ ЗАЩИТА: снимок контура поставок (скорости кластеров, покрытие, рекомендации, урезание по Моему складу, непокрытая потребность) у кода до тренда и после совпал БАЙТ В БАЙТ на боевых данных. Тренд изменил только заказ на фабрике: объёмы заказов не изменились ни у одного артикула, сдвинулось лишь покрытие — Миска серая 79 → 101 дн, Бутылки 56 → 67, Пакеты 69 → 82, Миска бирюзовая 116 → 106, Органайзер_2_пол_прозр 137 → 162. Выравнивание таблицы проверено отдельно: все 18 переключаемых колонок присутствуют в шапке и во всех четырёх уровнях строк. Первый контрольный прогон был негодным (сравнение через trendWeeks=1 меняет и окно коррекции скорости) и заменён сравнением двух версий кода. |
| 2026-08-19 | Замечания живого регресса пункта 38 | `npx tsc --noEmit`; `npx vitest run`; `npm run build`; сверка расхода комплекта и компонента скомпилированным модулем на боевых данных | OK | `tsc` — 0 ошибок, тесты 60 из 60, сборка — код возврата 0. Сверка: у BowlGrayMini_01 и «Миска серая» скорости совпадают и по факту (19,82), и по прогнозу (15,56); у BowlBlueMini_01 и «Миска бирюзовая» — 3,79 и 4,17. Тренд посчитан у всех 13 артикулов таблицы, без исключений. Раскраска в колонке «Скорость» стоит ровно у трёх артикулов со сработавшей коррекцией: Миска_двойная 3,43 → 8,39; Полка_выдв_27см 1,71 → 4,21; Набор_полок_выдв_бел 0,36 → 1,75. |
| 2026-08-19 | Пункт 39: три латентных дефекта расчёта | `npx tsc --noEmit`; `npx vitest run`; `npm run build`; сравнение старого и нового кода на боевых данных; прогон с окном 17 недель; проверка помощника numSetting таблицей значений | OK | Тестов 67 (было 60), провалено 0. `tsc` — 0 ошибок, сборка — код возврата 0. ГЛАВНАЯ ПРОВЕРКА: на боевых данных при текущих настройках снимок расчёта до и после правки совпал ПОЛНОСТЬЮ — артикулы, компоненты, узкие места, скорость. Дефект B воспроизведён и закрыт: при окне 17 недель, захватывающем архивный блок 20.04.2026, старый код брал знаменатель 119 дней и давал BowlGrayMini_01 22,87 шт/д, новый берёт 91 день и даёт 21,76; по Органайзер_2_пол_белый старый код наоборот занижал (3,01 против 3,09). Помощник numSetting проверен таблицей: ноль числом и строкой проходит как ноль, отсутствие значения и мусор дают умолчание. НАХОДКА при прогоне: 8 тестов пункта 36 упали не из-за дефекта, а из-за вырожденной фикстуры — все продажи лежали в одной неделе при окне в четыре, то есть тест опирался на подсчёт отсутствующих недель как нулевых. Продажи разложены по всем четырём неделям окна с сохранением сумм; ожидаемые числа тестов при этом НЕ менялись, что подтверждает сохранность их смысла. |
| 2026-08-19 | Резерв заявки в компонентах комплекта | `npx tsc --noEmit`; `npx vitest run`; `npm run build`; сравнение старого и нового кода на боевых данных с реальной заявкой | OK | Тестов 75 (было 67), провалено 0. `tsc` — 0 ошибок, сборка — код возврата 0. СВЕРКА НА БОЕВЫХ ДАННЫХ с резервом 104 BowlBlueMini_01: старый и новый код на одних входных данных дают ПОЛНОСТЬЮ одинаковый расчёт по артикулам и одинаковые поля компонентов; изменилось единственное — «собрать сейчас» у BowlBlueMini_01 упало с 1 до 0. Резерв развернулся только в компоненты бирюзового комплекта (Бутылки 104, Пакеты 104, Миска бирюзовая 104), «Миска серая» не затронута. ТРУБА у всех четырёх компонентов не изменилась — это и есть проверяемый инвариант. |
| 2026-08-19 | Пункт 40, этап E: плашка «долг себестоимости» | `npx tsc --noEmit`; `npx vitest run`; `npm run build`; прогон скомпилированного utils.ts по всем артикулам боевого склада | OK | `tsc` — 0 ошибок, тесты 75 из 75 (не затронуты), сборка — код возврата 0. ПРОГОН НА БОЕВЫХ ДАННЫХ, 17 артикулов склада: плашка загорается ровно на двух — «Миска серая» (13 шт, средняя 999,78 при последнем приходе 130,00, долг 11 307,14 руб) и «Полка_выдв_27см» (1 шт, средняя 6 195,66 при последнем приходе 517,00, долг 5 678,66 руб). У остальных пятнадцати долг нулевой или копеечный (максимум 0,39 руб от округления средней), плашка не загорается — ложных срабатываний нет. Суммы совпали с ручным разбором истории операций. |
| 2026-08-19 | Пункт 40, этапы B и G | `npx tsc --noEmit`; `npx vitest run`; `npm run test:gas`; `npm run build`; проверка Code.gs на порчу; мутационная проверка | OK | Стенд: 56 проверок, провалено 0 (было 42, добавлено 14 по пяти сценариям). Тесты фронтенда 75 из 75, `tsc` — 0 ошибок, сборка — код возврата 0, `Code.gs`: нулевых байтов 0, UTF-8 валиден. Стенд впервые выполняет настоящую commitTransaction: заглушка научилась листу «Остатки», реестру листов, appendRow и getUuid. Закреплено: списание в ноль сохраняет капитализацию (П18), списание с остатком сохраняет её же и пересчитывает среднюю (П19), обычный расход капитализацию по-прежнему УМЕНЬШАЕТ — правка не протекла в нормальное потребление (П20), приход на артикул с долгом впитывает долг в себестоимость партии (П21), компонент комплекта ведёт себя так же (П22). МУТАЦИОННАЯ ПРОВЕРКА: возврат прежнего обнуления роняет ровно П18, после отката кода снова 56 из 56. НЕ ПОКРЫТО: пути одиночного и массового восстановления — отдельный объём работы, отмечено в README. |
| 2026-08-19 | Пункт 40, этап A: выбор судьбы себестоимости | `npx tsc --noEmit`; `npx vitest run`; `npm run test:gas`; `npm run build`; проверка Code.gs на порчу; мутационная проверка | OK | Стенд: 70 проверок, провалено 0 (было 56, добавлено 14 по шести сценариям). Тесты фронтенда 75 из 75, `tsc` — 0 ошибок, сборка — код возврата 0, `Code.gs`: нулевых байтов 0, UTF-8 валиден. Закреплено: списание С МЕТКОЙ до нуля уменьшает капитализацию (П23), то же самое БЕЗ метки её сохраняет, и две проверки отличаются ТОЛЬКО строкой объекта (П24), частичное списание с меткой уменьшает капитализацию и не трогает среднюю (П25), метка НЕ влияет на обычный расход и на приход — двойной прогон даёт побайтово одинаковый результат (П26, П27), компонент комплекта ведёт себя так же (П28). МУТАЦИОННАЯ ПРОВЕРКА: снятие условия по метке роняет П23 и П25, после отката снова 70 из 70. НЕ ПОКРЫТО: пути удаления и восстановления — требуют модели листа «История» и архива, отмечено в README. |
| 2026-08-19 | Живой регресс пункта 40 | Проверка владельцем в боевом приложении, ревизия sklad-00010-hgp; сверка боевой базы выгрузкой после каждой операции | OK | Владелец подтвердил: выбор при списании работает, плашка долга появляется, ноль в корректировке сохраняется, долг переживает нулевой остаток. Выгрузка базы подтвердила, что «Полка_выдв_27см» после списания в ноль сохранила капитализацию 6195,66 руб — то есть правка бэкенда сработала, а видимая пропажа была дефектом показа. Итоговые числа проверок на конец дня: 75 тестов фронтенда, 70 проверок стенда Apps Script. |
| 2026-08-19 | Пункт 22, этап I: окно недель | `npx tsc --noEmit`; `npx vitest run`; `npm run test:gas`; `npm run build`; мутационная проверка; сверка боевых данных | OK | Стенд: 76 проверок, провалено 0 (было 70, добавлено 6). Тесты фронтенда 75 из 75, tsc 0 ошибок, сборка код возврата 0. Закреплено: без явного окна оно берётся из настроек и даёт 15 полных недель при trendWeeks 13 (П71); текущая незавершённая неделя отдаётся, но не занимает место полной (П71); рост настройки сразу расширяет окно (П72); явно переданное окно имеет приоритет (П73); архивные строки по 28 дней окно не съедают, строки старше окна не отдаются (П74). МУТАЦИОННАЯ ПРОВЕРКА: со старым кодом 4 из 6 новых проверок падают, после отката снова 76 из 76, Code.gs восстановлен побайтово (сверка sha). СВЕРКА С БОЕМ: независимый расчёт по выгрузке базы воспроизвёл экран по окну в 11 недель — 12 совпадений из 12 (9 множителей тренда и 3 коррекции скорости); по окну в 13 недель не совпадает ни одно. |
| 2026-08-19 | Item 22 stage I: production verification after deployment | Live check of revision `sklad-00011-rtq` in Chrome against an independent recalculation of the production database export | OK | 19 of 19 predictions matched: trend multipliers 0.78 / 1.00 / 0.85 / 1.10 / 1.00 / 0.90 / 1.04 / 0.88 / 1.00 / 1.16 / 0.89 / 1.00, corrected speeds 4.21 (Полка_выдв_27см) and 1.75 (Набор_полок_выдв_бел), factory orders 312 pcs / 52 boxes and 96 pcs / 24 boxes, order costs 161 061 and 130 112 RUB, component forecast speeds 19.72 / 19.72 / 15.56 / 4.17. Console reports sales=1389 rows (was 1192), confirming the window actually widened. No regression elsewhere: the other eleven articles kept their recommendations, cluster shares still total 1292 pcs. |
| 2026-08-19 | Items 42 stage F and 23 stage E: production verification | Independent recomputation of the production database export versus revision `sklad-00011-rtq` | OK | Item 42: three corrected speeds reproduced exactly (Миска_двойная 8.393, Полка_выдв_27см 4.214, Набор_полок_выдв_бел 1.750); growth factors 2.45x / 2.46x / 4.90x all below the ceiling of 5, so the ceiling is untested on live data. Item 23: local credit matched on all four positions — 13 pcs cluster 4036, 8 pcs cluster 4071, 6 pcs and 6 pcs cluster 4007; total «В заявках» 33 pcs confirms the «larger of the two, not the sum» rule; a supply created by hand inside Ozon Seller and absent from the journal is credited correctly. Untested on live data: 7-day safety timer, statuses REJECTED_AT_SUPPLY_WAREHOUSE / OVERDUE / ACCEPTED_AT_SUPPLY_WAREHOUSE, single-cluster cancellation. |
| 2026-08-20 | Item 26: start-up timing on production | Performance API on revisions sklad-00011-rtq through sklad-00016-zdf; cache-free A/B via cacheBust payloads; `npx tsc --noEmit`; `npx vitest run`; `npm run test:gas`; `npm run build` | OK | Baseline: 18 requests, 6 waves, last response 12.67 s. After the work: 14 requests, 4 waves, last response 8.57 s with 11 of 14 served from cache and the Ozon block finishing at 1.4 s instead of 12.3 s. Cache-free A/B, three rounds: five parallel calls 13.0/9.1/9.7 s versus composite 9.5/10.8/9.2 s. getOzonSales measured alone: 9.1-13.0 s uncached, 245-379 ms cached, 216 KB response. Regression suites unchanged and green: 75 frontend tests, 76 Apps Script stand checks, tsc clean, build exit 0. CAVEAT: a later reload missed the cache and took 10.8 s with 3 of 14 cached; cause not identified, recorded in the plan rather than glossed over. |
| 2026-08-20 | Item 26: sales sheet split | `npm run test:gas`; mutation check; Code.gs integrity | OK | Stand: 83 checks, 0 failed (was 76, seven added). Locked down: weekly rows stay in the main sheet and it contains NO 28-day rows (П75), compacted blocks land in the archive sheet (П75), an existing archive block is read back and not lost (П76), a weekly row of the same 28-day period is merged into the existing block, 100 + 7 = 107 (П77), retention prunes both sheets while leaving fresh weekly rows alone (П78). MUTATION CHECK: with the pre-split Code.gs 5 of the 7 new checks fail; after restoring, 83 of 83 again and Code.gs matches byte for byte by sha. Production database copied to «БД Склад — резерв перед разделением листа продаж (2026-08-20)» before deployment. |
| 2026-08-20 | Item 26: sales sheet split verified on production | Drive export of the production database before and after the sync; Performance API on revision `sklad-00016-zdf`; screen comparison | OK | Split confirmed: «Продажи Ozon» 1392 rows, all days=7, no archive rows left; «Продажи Ozon Архив» 1805 rows, all days=28; 3197 in total, nothing lost. Row-by-row comparison against the pre-split export: all 1805 archive blocks identical, only four weekly rows differ and all four are fresh Ozon data (three in the current unfinished week 2026-08-17, one back-dated correction on 2026-08-03). Timing: getOzonSales 5.1/7.8/6.1 s, mean 6.3 s, against 9.1/13.0/9.7 s, mean 10.6 s before; page load 7.75-9.1 s on a cold cache against a 12.67 s baseline. Calculations unchanged: factory orders 312/504/96 pcs, 13 alerts, credits 13 and 8 pcs. CORRECTION TO AN EARLIER INSTRUCTION: the «Обновить» button on the Ozon tab runs runOzonStocksSyncNow, which syncs STOCKS only — the sales sync that performs the split is Настройки → «Запустить проверку сейчас», or the 05:00/17:00 trigger. |
| 2026-08-20 | Item 26 stages A2 and C: start-up timing | Performance API on revisions sklad-00017-zxg and sklad-00018-dmw; GASDIAG action counts from Cloud Run logs; `npx tsc --noEmit`; `npx vitest run`; `npm run test:gas`; `npm run build` | OK | 13 requests in 3 waves ending at 6.58 s, against the morning baseline of 18 requests in 6 waves ending at 12.67 s. First request leaves at 635 ms instead of 1418 ms; 9 of 13 responses from cache; markup ready at 531 ms. Proxy logs confirm exactly one request of each kind per load — the duplicate getInitialData and getArchivedItems introduced by the first attempt at stage C are gone. Regression suites green: 75 frontend tests, 83 Apps Script stand checks, tsc clean, build exit 0. |
| 2026-08-20 | Day summary: full verification state | `npx tsc --noEmit`; `npx vitest run`; `npm run test:gas`; `npm run build`; mutation checks; production database exports; Performance API on production; Cloud Run GASDIAG logs | OK | End-of-day suites: 75 frontend tests, 83 Apps Script stand checks (was 70 in the morning), tsc clean, build exit 0. Two mutation checks ran and both bit: the pre-fix Code.gs fails 4 of 6 new week-window checks, and the pre-split Code.gs fails 5 of 7 new sheet-split checks; after each rollback the file matched byte for byte by sha. Production verification: 19 of 19 predicted values matched after the week-window fix; after the sheet split all 1805 archive blocks were identical to the pre-split export and every calculation was unchanged. Proxy logs confirm exactly one request of each kind per page load. |
| 2026-08-20 | Items 43, 44, 54 and 55 | `npx tsc --noEmit`; `npx vitest run`; `npm run build`; mutation check by reverting both logic changes; browser measurement of the sidebar widths against the built stylesheet | OK | 85 frontend tests pass, 10 of them new in `src/lib/ozonAlerts.test.ts`. MUTATION CHECK: reverting the item 55 guard and emptying FUNNEL_HIDDEN_STATUSES makes exactly 4 of the new tests fail, so they do detect the change rather than pass by construction. ITEM 54 MEASURED, NOT ASSUMED: the built stylesheet was served on localhost and the two class strings measured in the browser — the old string, with md:w-auto, gave the collapsed sidebar a computed width of 1280px; the new one gives 288px expanded and 80px collapsed, at a 1280px viewport. The three width rules were confirmed to sit in the same @media (min-width:48rem) block with w-auto emitted last, which is why it won. NOT COVERED: no test drives the funnel through React, so the wiring of isFunnelVisibleStatus into Dashboard rests on the type-check and the build; item 43 has no test at all — its logic lives inline in the component render. Nothing was checked on production: this batch is built but not deployed. |
| 2026-08-20 | Deployment of items 43, 44, 54 and 55 to production | `gcloud run deploy sklad` from a clean `git archive HEAD` export of commit 92bbbdc; HTTP and `/api/version` check; grep of the served bundles for each of the four changes | OK | Revision `sklad-00019-gzb` serves 100% of traffic, replacing `sklad-00018-dmw` (kept as the rollback target). Main page HTTP 200, `/api/version` reports the new revision. VERIFIED IN THE DEPLOYED BUNDLES, not just locally: 44 — «активных поставок» present in the Dashboard chunk and DATA_FILLING present in the hidden-status list; 55 — the guard is live as `S=u.some(f=>f.recommendation!==null&&f.recommendation.boxes>0);if(u.length>0&&S)`; 54 — the aside class string is now `md:relative md:z-auto`, the constant `md:w-auto` is gone from it, while `.md\:w-20`/`.md\:w-72`/`.md\:w-auto` still sit in that order in the stylesheet, which is why the collision existed; 43 — the «тариф с» hint is present in the DirectoryTab chunk. NOT VERIFIED: nothing was exercised on the live screen — that needs a login the assistant must not perform, so the owner's four-point check is still outstanding. Asset hashes differ from the local build because Cloud Run rebuilds inside the container and `__APP_BUILD__` changes. |
| 2026-08-20 | Items 54 and 43 after rework | `npx tsc --noEmit`; `npx vitest run`; `npm run test:gas`; `npm run build`; browser measurement of both sidebar states against the built stylesheet; mutation check on the rate module | OK | 101 frontend tests pass, 18 of them new in `src/lib/serviceRates.test.ts`. SIDEBAR MEASURED IN THE BROWSER, both states, at a 1280px viewport: collapsed — width 80px, all four labels display:none, button justify-content:center; expanded — width 288px, labels visible, justify-content:flex-start. The DEPLOYED markup was replayed in the same page and reproduced the owner's report exactly: panel 80px but label display:block with its right edge at 153px, 73px outside the panel, and justify-content:flex-start. MUTATION CHECK: restoring the old parsing (directory price plus recomputed quantity) and dropping the empty-ДействуетС guard fails exactly 4 of the new tests. NOT COVERED: no test drives the Отгрузка modal through React, so the wiring rests on the type-check; nothing verified on production yet. |
| 2026-08-20 | Deployment of the reworked items 54 and 43 | `gcloud run deploy sklad` from a clean `git archive HEAD` export of commit 09a2e06; `/api/version`; grep of the served bundles | OK | Revision `sklad-00020-g2p` serves 100% of traffic, replacing `sklad-00019-gzb`. CONFIRMED IN THE SERVED BUNDLES: the label class is now `${c===M.id?"":"hidden"} ${m?"md:hidden":"md:inline"}` — one utility per state, no collision; the constant `md:justify-start gap-0` is gone from the menu button and the pair is conditional; DirectoryTab carries «действует с» and no longer builds the base-price hint; ShipmentCostTab no longer contains the back-computed quantity expression. STILL PRESENT ON PURPOSE: «Тарифы по датам не заданы. Действует базовая цена: N ₽» inside the expanded tariff panel — that text predates this work and is accurate, since a service with no dated tariff really is billed at its base price. NOT VERIFIED: the services modal was not opened against a real shipment; that check belongs to the owner. |
| 2026-08-20 | Date format on screen | `npx tsc --noEmit`; `npx vitest run`; `npm run build` | OK | 106 frontend tests pass, 5 of them new for `formatDateRu`: a database date is flipped (2026-07-06 -> 06.07.2026), single-digit day and month are zero-padded, an already-flipped date survives a round trip, an empty value stays empty and an unparseable string is returned as is. Build and type-check clean. NOT CHECKED: nothing on production — this batch is built but not deployed. |
| 2026-08-20 | Token accounting behind rule 2.2a | Parsing of all 11 session transcripts in `~/.claude/projects/-Users-nikolajvyskrebencev-Ostatki-na-sklade/*.jsonl`: `usage` blocks per request, image blocks decoded from base64 with PNG dimensions read out of the IHDR header, tool_result sizes grouped by tool | OK | Five of the eleven transcript files are byte-identical copies of one session and were deduplicated before totalling. Image cost was computed from PIXEL AREA (width x height / 750), not from the base64 length — by characters in the log the screenshots look like 12.8% of the material, by actual tokens they are 0.3%, and quoting the character share would have been wrong by a factor of forty. Result for session `28af96ec`: 7 unique screenshots, 361 KB, 1 845 tokens on first send, 2.19 M tokens counting the re-read on each of the 1 570 requests, which is 0.49% of the 447.8 M input tokens. Composition of the same session by material: Bash 60.3%, assistant text 5.8%, user messages 3.3%. NOT MEASURED: how much an `Explore` launch actually costs in this setup — the 15–30 k figure in the rule is an estimate, not a measurement, and is written as such. |
| 2026-08-20 | Item 45 | `npx tsc --noEmit`; `npx vitest run`; `npm run build`; mutation check on the ceiling function | OK | 116 frontend tests pass, 10 of them new in `src/lib/ozonSupplyLines.test.ts`: one line takes the whole free stock; the same article in a second cluster lowers the ceiling for the first; another article does not; a new line counts every existing line; a fully consumed stock gives 0 and never a negative; lines asking for more than exists still give 0; an article missing from the stock figures gives Infinity, not 0; NaN and undefined quantities count as zero. MUTATION CHECK: dropping the unknown-article guard and the exceptKey filter fails exactly 4 of the new tests. NOT COVERED: the modal's rendering, the cluster and article pickers and the toast on an over-limit edit — the project has no React test harness, so those were verified only by the type-check and a clean build. Nothing was checked on production: this batch is built but not deployed. |
| 2026-08-20 | Deployment of item 45 | `gcloud run deploy sklad` from a clean `git archive HEAD` export of commit 5cc7207; `/api/version`; grep of the served bundle | OK | Revision `sklad-00022-bn7` serves 100% of traffic, replacing `sklad-00021-xx2`. CONFIRMED IN THE SERVED `OzonStocksTab` CHUNK: «Добавить позицию», «доступно для отгрузки», «свободно для отгрузки», the empty-cluster prompt and the «добавлено вручную» marker are all present, and the ceiling function is live in its minified form — the row key builds as `${article}|||${clusterId}` and the cap returns POSITIVE_INFINITY for an article missing from the stock figures, otherwise `Math.max(0, free - taken)`. NOT VERIFIED: nothing was exercised on the live screen — the pickers, the over-limit message and a real supply created with a hand-added line all need the owner, since the assistant must not log in. |
| 2026-08-20 | White screen on opening the supply window | `npx tsc --noEmit`; `npx vitest run`; `npm run build`; static scan of every .tsx for hooks below an early return | OK | 118 tests pass, 2 of them new in `src/lib/hooksOrder.test.ts`. The scan now reports ZERO offences across src/; before the fix it reported two — `OzonSupplyModal.tsx` (early return line 262, hook line 300) and `ConfirmModal.tsx` (early return 395, hook 407). The scanner is itself verified by a probe test that writes a deliberately broken component and asserts it is caught, so a scanner that silently matched nothing would fail the suite. DIAGNOSIS PROVED BY CODE, NOT BY OBSERVATION: an attempt to reproduce the crash in the owner's Chrome failed because the extension lacks host permission for `sklad-415081166309.europe-central2.run.app` — screenshot and page-text both returned «Cannot access contents of the page». The mechanism is nonetheless certain: hook counts differ between the closed and open render of a permanently mounted component, which is the documented cause of the exact symptom reported (blank page, cured only by reload). NOT VERIFIED: that the window now opens on production — that check belongs to the owner, or to me once the extension is granted access to the site. |
| 2026-08-20 | Deployment of the white-screen fix | `gcloud run deploy sklad` from a clean `git archive HEAD` export of commit e819fd9; `/api/version`; order check inside the served bundle | OK | Revision `sklad-00023-msl` serves 100% of traffic, replacing `sklad-00022-bn7`. Verified in the chunk the service actually serves: the picker memos now appear BEFORE the early `return null` of the supply window, which is the whole fix. NOT VERIFIED ON SCREEN: the Chrome extension still has no host permission for the production origin, so opening the window and clicking «Оформить поставку» is left to the owner. |
| 2026-08-20 | Supply window, live regression in the owner's Chrome | Real production session on revision `sklad-00024-jr5`, driven through the Chrome extension: opened the window, read the DOM, added lines, forced an over-limit quantity | OK | REPRODUCED FIRST: on `sklad-00023-msl` the window opened but was invisible — overlay top -2468px, panel top -780px, containing block traced to the `tab-enter` transform. AFTER THE FIX, all checked on the live screen: (1) the window opens centred on the screen, no scrolling; (2) the cluster picker offers 19 clusters and none of the 8 ticked in «Кластеры без поставок» — Алматы, Армения, Астана, Беларусь, Дальний Восток, Калининград, Махачкала, Узбекистан — verified by opening the settings window and reading its checkboxes; (3) the article picker offers 7 articles and every one of them reports a positive «Доступно для отгрузки» (80, 176, 18, 12, 18, …); (4) the hint reads exactly as dictated; (5) THREE lines were added by hand across TWO clusters — Казань twice and Воронеж once — giving «Итого: 4 строк, 3 кластеров, 36 шт», each marked «добавлено вручную», boxes computed (16 шт = 2 кор по 8); (6) the ceiling holds: typing 999 into the Казань line for Органайзер_3_пол_прозр snapped back to 12 with «свободно для отгрузки 12 шт, больше поставить нельзя», and 12 is right because the free stock is shared with the other line of that article. NOTHING WAS SENT TO OZON: the window was closed with «Отмена», no draft and no supply was created. 120 tests pass locally, 2 of them new for the CSS rule. |
| 2026-08-20 | Supply window rework | `npx tsc --noEmit`; `npx vitest run`; `npm run build`; live check in the owner's Chrome on revision `sklad-00025-pfm`; mutation check on the new helpers | OK | 132 tests pass, 12 of them new for `foldOzonVerdict`, `acceptedForLine` and `correctedQuantities`: accepted and rejected land in the right cluster and article, several entries for one article are summed, an item whose article cannot be restored is dropped, an empty or null answer does not throw, a line in a cluster Ozon never mentioned counts as fully not-accepted rather than silently accepted, correction is idempotent, and after correction nothing not-accepted remains. MUTATION CHECK: replacing `+=` with `=` in the fold, and defaulting an unknown cluster to «accepted in full», fails exactly 2 of the new tests. LIVE ON PRODUCTION: the window opens as ONE list grouped by cluster — «Уфа» and «Невинномысск» as headings with «Убрать кластер», rows showing the article with no Ozon SKU, «Добавить позицию» still in place, «Итого: 2 строк, 2 кластеров, 14 шт». NOT CHECKED LIVE, ON PURPOSE: the Ozon answer itself, the accepted/not-accepted figures and «Скорректировать поставку». Reaching them means pressing «Оформить заявку в Ozon», which runs the draft and creates a REAL supply as soon as Ozon confirms everything — that is the owner's call to make, not mine. |
| 2026-08-20 | Correction removes refused lines and clusters | `npx tsc --noEmit`; `npx vitest run`; `npm run build`; mutation check | OK | 137 tests pass, 8 of them rewritten around `applyOzonCorrection`: a partly accepted line keeps the accepted quantity and stays; an article Ozon takes none of is REMOVED rather than zeroed; a cluster Ozon takes nothing from is removed whole; a cluster keeping even one line stays; refusing everything empties the composition and removes every cluster; a cluster absent from the answer is removed rather than left silently; correction is idempotent on the survivors; and no not-accepted quantity remains afterwards. MUTATION CHECK: making the function zero a line instead of removing it, and never dropping a cluster — that is, the previous behaviour the owner rejected — fails exactly 5 of these tests. NOT CHECKED LIVE: same reason as the previous entry — the button that produces an Ozon answer also creates a real supply once Ozon confirms, so that check belongs to the owner. |






