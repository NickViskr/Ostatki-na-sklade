# Паспорт проекта «Интеграция с Ozon» (автопилот)

Статус-легенда: ⬜ не начат / 🟨 в работе / ✅ готов (+дата)

## Как работать с этим файлом
- Один пункт плана = один отдельный чат с архитектором (Claude).
- Новый чат начинается фразой: «Работаем по docs/OZON_PLAN.md из репозитория NickViskr/Ostatki-na-sklade, пункт N. Прочитай план и код».
- После завершения каждого пункта Gemini обновляет в этом файле статус пункта и раздел «Заметки для следующего чата».
- Этот файл — единственный источник правды по интеграции. При расхождении с чатами верить этому файлу.
- После завершения пункта архитектор (Claude) обязан: (а) выдать промт на отметку пункта ✅ с датой в этом файле, (б) напомнить пользователю закрыть текущий чат и открыть новый чат для следующего пункта стандартной фразой из этого файла.
- В начале каждого нового чата архитектор сверяет статусы пунктов в этом файле с фактическим кодом на GitHub; если пункт сделан, но не отмечен — сначала выдаётся промт на отметку, потом начинается работа.
- Все файлы репозитория создаются и изменяются только внутри рабочей среды AI Studio. Файлы, закоммиченные в GitHub извне (например, из Claude Code), при следующей публикации из AI Studio будут удалены — их содержимое нужно воссоздавать внутри проекта отдельным промтом.

## Реестр принятых решений (менять только по решению пользователя)
| Решение | Выбор |
|---|---|
| Дубли ручных и авто-заявок | Сопоставление по «отпечатку»: артикулы + количества + дата отгрузки (± окно) + назначение Ozon. Уверенное совпадение — автопривязка PostingID к ручной заявке без создания дубля; спорное — парковка на ручную сверку. Автопилот никогда не двигает остаток вслепую. С пункта 12 автопривязка уверенного дубля выполняется автоматически в момент обнаружения отгрузки (клиент, окно уведомлений), с тостом; спорные — только вручную на вкладке |
| Доп. услуги | Только «сдаточные» (забор груза, упаковка, тариф по дате) считаются ОДИН раз при сдаче поставки и входят в себестоимость. Хранение — отдельная метрика дашборда, в себестоимость поставки не входит |
| Момент ухода остатка со склада | Статус Ozon «принято на точке отгрузки» (ACCEPTED_AT_SUPPLY_WAREHOUSE). Позже данные поставки корректируются по приёмке на конечных складах (их может быть несколько). ИЗМЕНЕНИЕ 13.07.2026 (пункт 12): заявки, созданные в Ozon, но не отгруженные (DATA_FILLING, READY_TO_SUPPLY), оформить к списанию НЕЛЬЗЯ — кнопка заблокирована, остатки не трогаются; оформление доступно только после отгрузки. Проверка отгрузки — «принято на точке отгрузки ИЛИ дальше по воронке» (хелпер isStockDeparted), т.к. опрос 2 раза в сутки может проскочить статус ACCEPTED_AT_SUPPLY_WAREHOUSE. Отказ в приёмке, отмена и просрочка отгрузкой не считаются |
| Услуги в автопилоте | Автопилот обнаруживает ОТГРУЗКУ поставки (isStockDeparted) → при входе пользователя всплывающее окно с префиллом позиций и полем сдаточных услуг → пользователь проверяет/меняет услуги и утверждает. Без утверждения расход не создаётся |
| Опрос Ozon | Автоматически 2 раза в сутки — 05:00 и 17:00 МСК (изменено пользователем 12.07.2026 с изначальных 4 раз): триггеры Apps Script с inTimezone('Europe/Moscow') → прокси (Cloud Run: https://service-415081166309.us-west1.run.app/) → Ozon Seller API → лист «Внешние отгрузки». Cloud Run «засыпает» между запросами, поэтому будильник — именно триггер Apps Script. Плюс ручной запуск кнопкой в Настройках |
| Недостача при приёмке | Остатки склада НЕ меняет (товар физически уехал весь). Стоимость разницы «отгружено − принято» перераспределяется на себестоимость товаров этой поставки. Каждая корректировка оставляет след в Истории |
| Пересорт | Признак в акте приёмки: «заявлено X, принято меньше» + «заявлено 0, принято N». Проводится ТОЛЬКО после подтверждения пользователем. Ошибочно отправленный товар: списывается с остатков склада + включается в состав поставки + сдаточные услуги перераспределяются на новый состав. Заявленный, но не уехавший товар: возвращается на склад со своей базовой себестоимостью. Операция зеркалится во всех 6 путях учёта |
| Тестовая БД | Полная копия боевой Google-таблицы «БД Склад»; все листы и колонки создаются автоматически (идемпотентная инициализация getOrCreateSheet); лист «Пользователи» при копировании чистится от паролей |
| Режим разработки | Тумблер только у админа, действует ТОЛЬКО в браузере включившего (боевых пользователей не затрагивает); все запросы в этом режиме идут в тестовую БД; красная рамка по периметру приложения + плашка «РЕЖИМ РАЗРАБОТКИ — ТЕСТОВАЯ БД» |
| Правило внедрения | Пункты 7–18 разрабатываются только в режиме разработки на тестовой БД. Боевая база не участвует до пункта 20. Операции, двигающие остатки (пп. 12, 16), включаются только после просмотра «на показе» |

## План (один пункт = один чат)

### Фаза 0 — Предохранители
- ✅ 1. Паспорт проекта docs/OZON_PLAN.md (этот файл)
- ✅ 2. Кнопка «Резервная копия БД» в Настройках (админ): полная копия боевой таблицы на Диск, дата в названии (готово 07.07.2026; функция backupDatabase в Code.gs, кнопка в SettingsTab.tsx, копии складываются в папку Диска «Резервные копии БД Склад»; потребовалась разовая авторизация DriveApp через запуск backupDatabase в редакторе Apps Script)
- ✅ 3. Кнопка «Создать/обновить тестовую БД»: копия боевой, автосоздание листов/колонок, чистка листа «Пользователи»; id тестовой таблицы сохраняется в настройках (готово 07.07.2026; функция createOrUpdateTestDatabase в Code.gs, кнопка в SettingsTab.tsx с подтверждением через ConfirmDialog — window.confirm в песочнице фрейма заблокирован; копия кладётся в папку Диска «Тестовая БД Склад», листы «Пользователи» и «Сессии» очищаются, id и url сохраняются в Script Properties test_dbSpreadsheetId / test_dbSpreadsheetUrl; при обновлении все старые файлы в папке уходят в корзину — в папке всегда один актуальный файл, ничего не хранить там вручную)
- ✅ 4. Режим разработки: тумблер админа (только его браузер) + маршрутизация запросов в тестовую БД + красная рамка и плашка (готово 07.07.2026; серверная часть — глобальный флаг _devModeSpreadsheet в Code.gs, активация в doPost после verifySession только для админа, сессии и логин всегда по боевой БД, действия backupDatabase/createOrUpdateTestDatabase всегда по боевой, archiveTransactions в режиме разработки заблокирована; клиент — devMode в localStorage через useWarehouseStore, флаг devMode:true в теле запросов fetchGas кроме login/logout/verifySession, тумблер в SettingsTab, красная рамка и плашка «РЕЖИМ РАЗРАБОТКИ — ТЕСТОВАЯ БД» в App.tsx; переключение проверено вживую: данные читаются из тестовой БД, боевая не затрагивается)
- ✅ 5. Регресс стандартного режима: сервис работает ровно как раньше (готово 07.07.2026; полный прогон 21-шагового сценария через Claude в Chrome на боевой БД с выключенным режимом разработки: все числовые проверки пройдены, финальные остатки совпали с эталоном до копейки; dev-режим на боевой режим не влияет. Известный некритичный баг, НЕ связанный с dev-режимом: групповое восстановление отгрузки комплекта из корзины возвращает остатки корректно, но дробит запись на отдельные строки в Истории (теряется группировка groupId) — чинить отдельной задачей вне плана Ozon)

### Фаза 0.5 — Разведка API Ozon
- ✅ 6. Снять живые ответы Ozon через MCP: список заявок по статусам, детали, состав, акты приёмки (главный вопрос: отдаёт ли API данные пересорта), остатки складов → задокументировать реальные поля и идентификаторы в docs/OZON_API.md (готово 07.07.2026; разведка выполнена через MCP-сервер PCDCK/ozon-mcp в Claude Code, только читающие методы; результаты в docs/OZON_API.md — endpoint'ы, живые анонимизированные примеры, справочник статусов, пагинация. ГЛАВНЫЙ ОТВЕТ: API НЕ отдаёт данные приёмки и пересорта по FBO — в составе поставки только заявленное quantity, акты существуют лишь как PDF по схемам FBS/FBP; значит пункт 15 реализуется через форму ручного ввода акта. Ключевые находки для пунктов 7–16: учёт вести на уровне supply_id, а не заявки (вРЦ-заявка разворачивается в несколько поставок с разными статусами); допоставки склеивать по order_tags.original_supply_id; в /v3/supply-order/get параметр order_ids передавать строками; пагинация списка — last_id до пустого значения; остатки складов Ozon — /v1/analytics/stocks, premium, обновляется 2 раза в сутки)

### Фаза 1 — Связь с Ozon (read-only, тестовая БД)
- ✅ 7. Перевод /api/ozon/check на актуальный API /v3/supply-order/list (статусы states, пагинация last_id) по формам из docs/OZON_API.md; статус поставки пишется в «Внешние отгрузки» (готово 07.07.2026; учёт на уровне supply_id: одна вРЦ-заявка = несколько строк листа с разными складами хранения; цепочка list v3 → get v3 батчами по 50 строковых order_ids → bundle по одному bundle_id; лист «Внешние отгрузки» расширен до 13 колонок через ensureColumns, saveExternalShipments переведён в upsert: Ozon-колонки обновляются всегда, состав и дата отгрузки — только при внутреннем статусе new; отслеживаемые поставки с нефинальным статусом Ozon допрашиваются до финального; devMode прокидывается насквозь клиент → прокси → GAS, проверено: данные ушли в тестовую БД, боевая не изменилась; добавлена защита от лимита Ozon: пауза ≥400 мс между запросами + до 5 повторов при 429 с нарастающей паузой; контрольный повтор синхронизации: 61 поставка, добавлено 0, обновлено 61 — дубли не создаются)
- ✅ 8. Воронка поставок на дашборде: количество заявок по статусам (готово 08.07.2026; выбран вариант «по листу»: воронка считается из уже синхронизированного листа «Внешние отгрузки» без обращения к Ozon API — живой счётчик SupplyOrderStatusCounter отвергнут, т.к. тащит всю историю (770 COMPLETED) и не отдаёт OVERDUE; блок на дашборде только для админа, данные через fetchExternalShipments из стора; счёт по ПОСТАВКАМ (строкам листа) с подписью «N поставок в M заявках» (уникальные заявки по ключу orderId → orderNumber → postingId) и датой последнего статуса; словарь статусов и порядок воронки вынесены в общий модуль src/lib/ozonStatus.ts (STATUS_DICT, getStatusDetails, getStatusLabel, STATUS_FUNNEL_ORDER), OzonSuppliesTab переведён на импорт из него; карточки кликабельны (переход на вкладку Ozon), финальные статусы приглушены; известное некритичное допущение: строка без статуса Ozon считается как DATA_FILLING — после пункта 7 таких строк не бывает; Code.gs и прокси не менялись, деплой не требовался)
- ✅ 8а. Экран Ozon-поставок в приложении: список заявок, по клику заявка раскрывается по поставкам (склад хранения = направление: город/РЦ) с перечнем артикулов и количеств из ПозицииJSON; статусы Ozon по каждой поставке. Read-only, остатки не трогаются (готово 08.07.2026; вкладка «Ozon» только для админа: пункт меню в Sidebar, тип 'ozon' в useUIStore, guard в App.tsx; новый компонент OzonSuppliesTab.tsx — группировка по OrderID (fallback orderNumber → postingId), сортировка групп по последней дате статуса Ozon, сводка статусов, раскрытие заявка → поставки → состав из ПозицииJSON, словарь из 11 статусов Ozon по-русски с цветными бейджами, кнопка «Синхронизировать с Ozon»; в стор добавлен fetchExternalShipments — чтение листа без вызова Ozon API; добавлена зависимость motion для анимации раскрытия. Попутные фиксы прокси server.ts: (1) у вРЦ-поставок Ozon не отдаёт storage_warehouse — в «Склад хранения» теперь пишется название кластера-направления (город/регион) по macrolocal_cluster_id через справочник /v1/cluster/list, кэш на прокси 24 ч, два вызова CLUSTER_TYPE_OZON + CLUSTER_TYPE_CIS, fallback «Кластер N»; метод задокументирован в docs/OZON_API.md, раздел «ClusterList»; (2) защита от не-JSON ответа GAS — прокси возвращает понятную ошибку 502 с фрагментом ответа. Проверено вживую в режиме разработки на тестовой БД: группы раскрываются, статусы и состав корректны, в «Склад хранения» города/регионы)
- ✅ 8б. Оптимизация синхронизации: обновлять в листе «Внешние отгрузки» только реально изменившиеся строки (сравнение значений перед записью); для строк со статусом new не перезапрашивать состав, если статус Ozon не изменился (готово 08.07.2026; Code.gs saveExternalShipments: запись ячейки только при реальном изменении, хелпер normCell нормализует даты — дата с временем 00:00:00 сравнивается как yyyy-MM-dd, иначе строка «2026-07-10» от прокси вечно не совпадала с ячейкой-датой и давала ложные «обновлено: 25»; ПозицииJSON и «Дата отгрузки» у строк new перезаписываются только непустым значением — пустой itemsJSON от прокси означает «состав не запрашивался»; в ответ добавлен unchangedCount. server.ts /api/ozon/check: состав (/v1/supply-order/bundle) перезапрашивается только если статус Ozon изменился или состав в листе пуст (самовосстановление). Дополнительно — фильтр неактуальных поставок: OVERDUE убран из states запроса /v3/supply-order/list, OVERDUE добавлен в финальные статусы (уже отслеживаемые просроченные не допрашиваются повторно), новые поставки со статусом CANCELLED/OVERDUE, которых ещё нет в листе, пропускаются — они не отгружались; уже отслеживаемые обновляются как обычно, чтобы статус корректно закрылся. Проверено на пересозданной тестовой БД: было «найдено 60, обновлено 25 при каждом прогоне», стало «найдено 23, добавлено 23» и на повторном прогоне «добавлено 0, обновлено 0», данные совпали с личным кабинетом Ozon)
- ✅ 8в. (готово 09.07.2026, проверено вживую на двух кабинетах) Мультикабинет Ozon: сбор поставок с двух кабинетов (разные Client-Id/Api-Key, склад один). Решения: (1) хранение — Script Property OZON_CABINETS с JSON-массивом [{name, clientId, apiKey}], автомиграция старой пары OZON_CLIENT_ID/OZON_API_KEY в «Кабинет 1» (готово 08.07.2026); (2) Настройки — два блока «Кабинет 1»/«Кабинет 2» (название + Client-Id + Api-Key), второй опционален (готово 08.07.2026); (3) лист «Внешние отгрузки» — новая колонка «Кабинет», дописывается к существующему листу автоматически (готово 08.07.2026); (4) прокси опрашивает кабинеты последовательно (list → get → bundle для каждого), сливает в один список, сохраняет одной операцией; дедупликация по-прежнему по supply_id (глобально уникален в Ozon) (готово 08.07.2026); (5) вкладка Ozon — бейдж кабинета у заявки; воронка дашборда считает оба кабинета вместе (готово 08.07.2026); (6) отображение в Отгрузках и Истории — кабинет становится частью назначения: объект «Ozon (Название кабинета)»; в ConfirmModal при назначении Ozon появляется выбор кабинета (обязателен при двух кабинетах), префилл из вкладки Ozon подставляет кабинет автоматически; новых колонок в «Истории» не добавляется (готово 08.07.2026); (7) название кабинета автоподтягивается из Ozon: разведка 08.07.2026 подтвердила метод POST /v1/seller/info (поле company.name, см. docs/OZON_API.md раздел SellerInfo); в Настройках у кабинета кнопка «Проверить ключи и подтянуть название» — она же валидация ключей (403 code 7 = кабинет заблокирован); поле «Название» остаётся редактируемым как fallback (готово 08.07.2026); РЕАЛИЗОВАНО СВЕРХ ПЛАНА в этом же пункте: (8) названия кабинетов отдаются всем пользователям (getGlobalSettings.ozonCabinetNames, ключи — только админу); (9) /api/ozon/seller-info на прокси — проверка ключей + автоподтяжка названия (403 code 7 = кабинет заблокирован); (10) оформление поставок перенесено с вкладки «Ручная загрузка» на вкладку «Поставки Озон»: кнопки «Оформить»/«Игнорировать» на уровне заявки, позиции всех новых поставок заявки объединяются, одинаковые артикулы суммируются, после проведения все поставки заявки помечаются processed (pendingOzonPostingIds — массив); (11) себестоимость позиций при оформлении — через getEffectiveAvgCost (виртуальный комплект = сумма компонентов, обычный товар = средняя со склада), НЕ из справочной цены SKU; (12) при «Оформить» ранняя проверка наличия через getEffectiveAvailability с читаемым диалогом дефицита (ConfirmDialog принимает ReactNode) и подсказкой про дубли ручных заявок; (13) вкладка «Поставки Озон» по умолчанию показывает только актуальные заявки (есть поставка со статусом new), галочка «Показать обработанные»; воронка дашборда тоже считает только актуальные; (14) статус 'new' разрешён в updateExternalShipmentStatus (индексы колонок переведены на заголовки) + кнопка «Вернуть в новые» на обработанных заявках; (15) привязка заявки к транзакциям: при оформлении groupId главных транзакций пишутся JSON-массивом в TransGroupInfo, при удалении отгрузки (оба пути удаления) заявка автоматически возвращается в 'new'; ограничения привязки: работает для заявок, оформленных после внедрения, восстановление отгрузки из корзины статус заявки не меняет (защита от дубля — предупреждение о нехватке); (16) вкладки переименованы: «Загрузка» → «Ручная загрузка», «Ozon» → «Поставки Озон». Проверка вживую: ключи обоих кабинетов проверены кнопкой (названия подтянулись из /v1/seller/info), синхронизация собрала поставки двух магазинов с бейджами и фильтром, повторный прогон 0/0, полный цикл «оформить → провести → удалить из Истории → автовозврат в новые» отработал

### Фаза 2 — Сопоставление (read-only)
- ✅ 9. Сверка Ozon-поставок с ручными заявками по «отпечатку», показ «дубль/новая», спорные — на ручную сверку. Остатки не трогаются (готово 12.07.2026; реализация полностью на клиенте, Code.gs и server.ts не менялись, деплой не требовался. Модуль src/lib/ozonMatch.ts: отпечаток = точный состав (артикулы И количества 1:1, частичное совпадение по артикулам с другими количествами совпадением НЕ считается) + кабинет + дата; назначение транзакций нормализуется — хвост услуг в квадратных скобках «Ozon (Кабинет) [Упаковка… | Услуги…]» отрезается перед сравнением; уже привязанные ручные отгрузки (их ID в TransGroupInfo processed-заявок) из кандидатов исключаются. Вердикты: красный бейдж «Возможный дубль» — точный состав + кабинет + разница дат ≤ 5 дней; жёлтый «Похожа — проверьте» — точный состав при разнице ≤ 7 дней или несколько равных кандидатов; режим без даты — если у заявки нет даты отгрузки, но есть поставка READY_TO_SUPPLY, сверка идёт только по составу и кабинету, кандидаты ограничены свежими отгрузками (создание ≤ 7 дней назад). В раскрытии заявки — компактный блок совпадения (дата, объект, разница дат, состав в одну строку). Кнопка «Привязать как дубль» на заявках с совпадением: помечает поставки processed с TransGroupInfo = ID строк найденной ручной отгрузки, расход не создаётся, остатки не трогаются, автовозврат в новые при удалении той отгрузки работает. Проверено вживую на тестовой БД: полный цикл оформить → вернуть в новые → бейдж дубля → привязать → удалить отгрузку → автовозврат и исчезновение бейджа)

### Фаза 3 — Автопилот: обнаружение и расход
- ✅ 10. Автоопрос по расписанию: триггеры Apps Script → прокси → запись в «Внешние отгрузки» (готово 12.07.2026; по решению пользователя частота изменена с 4 до 2 раз в сутки — 05:00 и 17:00 МСК. Реализация: функция scheduledOzonCheck в Code.gs создаёт временную admin-сессию в боевом листе «Сессии» (срок 10 минут, удаление в finally) и вызывает прокси /api/ozon/check как обычный клиент — server.ts НЕ менялся, передеплой Cloud Run не потребовался. Триггеры: setupOzonSyncTriggers/removeOzonSyncTriggers, два триггера everyDays(1).atHour(5/17).inTimezone('Europe/Moscow'), срабатывание в окне часа после указанного времени. Цель записи — Script Property ozon_autoSyncTarget: отсутствует или 'test' → тестовая БД (devMode:true), 'prod' → боевая; переключение на 'prod' — в пункте 20. Результат последнего запуска — Script Property ozon_lastAutoSync. Управление — блок «Автоопрос Ozon» в Настройках (только админ): вкл/выкл, статус, последний запуск, кнопка «Запустить проверку сейчас» (action runOzonSyncNow; КРИТИЧНО: в doPost обрабатывается ДО захвата LockService, иначе дедлок с обратными запросами прокси saveExternalShipments). Грабли внедрения: UrlFetchApp впервые появился в Code.gs и требовал разрешение script.external_request; уже авторизованный скрипт НЕ запросил новое право сам — вызов молча падал; лечение: временная функция resetAuth c ScriptApp.invalidateAuth(), затем повторная авторизация запуском scheduledOzonCheck в редакторе. Проверено вживую на тестовой БД: прогон из редактора и кнопкой из приложения — найдено 38, добавлено 0, обновлено 0; временная сессия удаляется из листа «Сессии»)
- ✅ 11. Всплывающее окно при входе: «Обнаружена поставка №…», префилл позиций, проверка/правка сдаточных услуг, утверждение (готово 12.07.2026; реализация только на клиенте, Code.gs и server.ts не менялись, деплой не требовался. Общая логика вынесена в src/lib/ozonGroups.ts: buildOzonGroups (группировка заявок + сверка дублей) и хук useProcessOzonGroup (оформление: сопоставление SKU, себестоимость через getEffectiveAvgCost, проверка наличия, префилл ConfirmModal) — используются и вкладкой OzonSuppliesTab, и новым окном. Окно src/components/OzonNewSuppliesModal.tsx постоянно смонтировано в App.tsx (только админ), всплывает при загрузке страницы, если есть заявки с поставками в статусе new; различает «Новая» (зелёный бейдж — есть поставка, не показанная ранее) и «ожидает оформления»; заголовок меняется: «Обнаружены новые поставки Ozon» / «Заявки Ozon ждут оформления»; бейджи дублей из ozonMatch как на вкладке. Память показанных — localStorage ключ ozon_notifiedPostingIds (JSON-массив postingId, лимит 1000, привязан к браузеру), пополняется при любом закрытии окна: Позже / ✕ / Открыть вкладку / Оформить. Кнопка «Оформить» ведёт в стандартный ConfirmModal с проверкой/правкой сдаточных услуг и утверждением. Окно ничего не помечает и не двигает остатки; привязка дублей и игнор — только на вкладке. Проверено вживую на тестовой БД: первый вход — «новые», повторный — «ждут оформления», Оформить открывает ConfirmModal)
- ✅ 12. Создание расхода только после отгрузки на Ozon; дедуп по отпечатку с автопривязкой. ДВИГАЕТ ОСТАТКИ (готово 13.07.2026; по решению пользователя логика усилена: заявки до отгрузки оформить нельзя вовсе. Реализация только на клиенте (src/), Code.gs и server.ts не менялись, деплой не требовался. (1) src/lib/ozonStatus.ts: STOCK_DEPARTED_STATUSES + isStockDeparted — отгруженные статусы: ACCEPTED_AT_SUPPLY_WAREHOUSE, IN_TRANSIT, ACCEPTANCE_AT_STORAGE_WAREHOUSE, REPORTS_CONFIRMATION_AWAITING, REPORT_REJECTED, COMPLETED; сравнение «принято или дальше», т.к. опрос 2 р/сутки может проскочить статус. (2) src/lib/ozonGroups.ts: поле OzonGroup.needsExpense; useProcessOzonGroup оформляет только отгруженные new-поставки — при частичной отгрузке заявки неотгруженные поставки ждут и всплывут позже, списание всегда по факту. (3) Вкладка Поставки Озон: бейджи «Отгружена — оформите списание» (красный) / «Ожидает отгрузки на Ozon» (серый), кнопка «Оформить» disabled до отгрузки с подсказкой; Игнорировать/Привязать как дубль/Вернуть в новые доступны всегда. (4) Окно OzonNewSuppliesModal показывает ТОЛЬКО отгруженные заявки, тексты «Обнаружены отгрузки на Ozon», память ozon_notifiedPostingIds учитывает только отгруженные поставки. (5) Авто-привязка уверенных дублей в окне: verdict duplicate (ровно один кандидат) → все new-поставки заявки помечаются processed с TransGroupInfo кандидата, тост «расход не создаётся», такие заявки в окне не показываются; ref-защита от повторов, при ошибке тост с советом привязать вручную. (6) Фикс src/lib/ozonMatch.ts: режим без даты включается и при отгруженных статусах (isStockDeparted), не только READY_TO_SUPPLY — иначе заявка без таймслота/даты отгрузки на статусе ACCEPTED получала вердикт none и авто-привязка молчала; предохранитель прежний — кандидаты только из отгрузок, созданных ≤ 7 дней назад. Проверено вживую на тестовой БД: блокировка неотгруженной заявки, всплытие окна и списание после ACCEPTED, авто-привязка дубля заявки без даты отгрузки — второй расход не создан, остатки не менялись повторно)

### Фаза 4а — Приёмка: недостача
- ✅ 13. Обновление принятого количества по мере приёмки на конечных складах (готово 14.07.2026; API Ozon принятые количества не отдаёт (подтверждено разведкой docs/OZON_API.md) — реализован ручной ввод. Хранение: колонка «ПринятоJSON» листа «Внешние отгрузки», формат [{"offerId","accepted"}], по одной строке на поставку (supply_id = гранулярность приёмки по складам); пустая ячейка = «принято = заявлено» (умолчание, минимум ручного ввода). Code.gs: колонка в EXTERNAL_SHIPMENTS_HEADERS, acceptedJSON в getExternalShipments, action saveExternalShipmentAcceptance с валидацией (целые ≥ 0). Клиент: saveShipmentAcceptance в сторе; вкладка «Поставки Озон» — модалка AcceptanceModal (предзаполнение = заявлено, «Сбросить (= заявлено)» очищает ячейку), бейджи на поставке со статусов приёмки (ACCEPTANCE_STAGE_STATUSES/isAcceptanceStage в src/lib/ozonStatus.ts): «Приёмка: = заявлено» / «Принято полностью» / «Недостача X шт» / «Излишек Y шт», колонка «Принято» в составе поставки. Остатки и капитализация НЕ трогаются — это пункты 14–16. Проверено вживую на тестовой БД: ввод, недостача, излишек, сброс, запись в лист)
- ✅ 14. Недостача: перераспределение стоимости разницы на себестоимость товаров поставки, след в Истории; остатки склада не трогаются (готово 15.07.2026; перерасчёт хранится в колонке «ПерерасчётJSON» листа «Внешние отгрузки» (per-поставка); транзакции расхода и остатки склада не изменяются. Расчёт: src/lib/ozonShortage.ts (computeShortageRecalc — недостача ложится на принятые единицы того же артикула, при «принято 0» — пропорционально на остальные позиции; излишек блокирует перерасчёт статусом 'surplus'). Сервер: action saveShipmentShortageRecalc в Code.gs пишет перерасчёт и след-записи типа «Корректировка» (кол-во 0, суммы 0) в Историю напрямую, минуя пересчёт остатков. UI: предпросмотр и проведение в AcceptanceModal, колонки «Себест. базовая/скорр.» и бейдж «Перерасчёт ✓» в OzonSuppliesTab. Протестировано вживую на тестовой БД 15.07.2026: остатки не изменились, суммы сошлись до копейки)

### Фаза 4б — Приёмка: пересорт (самый строгий блок)
- ✅ 15. Обнаружение пересорта по признаку из актов (или форма ручного ввода акта, если API актов не отдаёт) + карточка подтверждения «вместо X уехал Y» (готово 17.07.2026, проверено вживую на тестовой БД). Реализация: (1) в AcceptanceModal ввод позиций не из заявки «заявлено 0, принято N» — блок «Позиции не из заявки (пересорт)», выбор артикула из SKU, хранение в ПринятоJSON с offerId = артикул SKU; (2) модуль src/lib/ozonPeresort.ts — detectPeresort(shipment, skus) сравнивает заявленное и принятое, отдаёт missing (недостающие) и extras (незаявленные), кандидат на пересорт = есть незаявленные позиции; (3) в computeShortageRecalc ранний выход со статусом 'peresort' ДО поиска транзакций — перерасчёт недостачи (п.14) блокируется, пока висит кандидат на пересорт; (4) бейджи на строке поставки: янтарный «Возможен пересорт (не из заявки: N шт)» (подавляет ложный «Принято полностью»), после подтверждения — индиго «Пересорт подтверждён — ожидает проведения»; (5) Code.gs: колонка «ПересортJSON» в «Внешние отгрузки» (создаётся автоматически через ensureColumns) + action saveShipmentPeresort с валидацией структуры (пары fromOfferId/fromArticle/toOfferId/toArticle/qty≥1, confirmedAt, confirmedBy), пустая строка = сброс; деплой выполнен; (6) карточка PeresortModal: списки «Не хватает»/«Лишнее» с остатками, конструктор пар «Вместо X уехал Y — N шт» по правилу «меньшего из двух», несопоставленные остатки помечаются как будущая обычная недостача/излишек, подтверждение через ConfirmDialog с предупреждением, сброс и повторное подтверждение работают; кнопка «Пересорт»/«Пересорт ✓» рядом с приёмкой. Остатки склада НЕ двигаются — проверено живым тестом (11 шагов, Claude в Chrome): остатки компонентов идентичны до единицы, ПересортJSON записан корректно. Формат ПересортJSON: {"pairs":[{fromOfferId,fromArticle,toOfferId,toArticle,qty}],confirmedAt,confirmedBy}. Движение остатков в обе стороны и перераспределение услуг — пункт 16
- ⬜ 16. Проведение пересорта после подтверждения: списание ошибочного со склада → включение в состав поставки → перераспределение услуг на новый состав → возврат не уехавшего со своей базовой себестоимостью. Зеркалирование во всех 6 путях + мини-регресс. ДВИГАЕТ ОСТАТКИ В 2 СТОРОНЫ

### Фаза 5 — Остатки Ozon + алерты
- ⬜ 17. Зеркало остатков складов Ozon: лист + карточка дашборда (справочно, на остатки своего склада не влияет)
- ⬜ 18. Алерты: просрочка поставки, отказ в приёмке, недостача, пересорт

### Фаза 6 — Боевой запуск
- ⬜ 19. Полный регресс на тестовой БД: 21 шаг + новые сценарии (дубль, недостача, пересорт) через Claude в Chrome
- ⬜ 20. Резервная копия боевой → поэтапное включение функций в бой по одной, с проверкой после каждой

## Заметки для следующего чата
- Пункты 1–15 ✅. Следующий пункт: 16 — проведение пересорта, ПЕРВЫЙ пункт Ozon с движением складских остатков. Подсказки: проводить только по подтверждённому ПересортJSON; ошибочно уехавший товар (to) списывается с остатков и включается в состав поставки, не уехавший (from) возвращается на склад со своей базовой себестоимостью; сдаточные услуги перераспределяются на новый состав; несопоставленный остаток обрабатывается как обычная недостача (перерасчёт п.14 разблокируется после проведения); как любая операция с остатками — зеркалирование во всех 6 путях учёта (провести / удалить одиночно / удалить массово / восстановить одиночно / восстановить массово / отредактировать); перед боем — полный регрессионный тест 21 шаг; тестовая поставка для отладки уже есть: 2000058621556 с подтверждённым пересортом BowlGrayMini_01 → Миска серая, 2 шт.
- Подсказки по пункту 15:
  - Точка входа уже готова: computeShortageRecalc (src/lib/ozonShortage.ts) возвращает status 'surplus' при излишке, AcceptanceModal показывает янтарное предупреждение «похоже на пересорт» — п.15 заменяет это предупреждение на полноценный процесс с подтверждением пользователя.
  - В отличие от п.14, пересорт ДВИГАЕТ остатки склада (списание ошибочно уехавшего товара, возврат не уехавшего по базовой себестоимости) — обязательное зеркалирование во всех 6 путях учёта.
  - Формат хранения перерасчёта (колонка «ПерерасчётJSON», ShortageRecalcItem) и механизм следов «Корректировка» переиспользовать.
- Связь приёмки с расходом: расход поставки ищется через TransGroupInfo (JSON-массив ID главных транзакций) у processed-строк заявки.
- Инфраструктура без изменений: кабинеты — OZON_CABINETS; автоопрос — scheduledOzonCheck, цель ozon_autoSyncTarget (сейчас 'test'); разработка только в режиме разработки на тестовой БД (красная рамка).
- Известный хвост вне плана: раздробление группы при восстановлении отгрузки комплекта из корзины (остатки корректны, страдает только группировка).
