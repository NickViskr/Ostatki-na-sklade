# История изменений

> Новые записи добавляются ТОЛЬКО новым блоком «## [ГГГГ-ММ-ДД]» в самом конце файла, даже если блок с такой датой уже есть выше. Сливать блоки с одинаковой датой запрещено. Существующие строки не редактируются и не удаляются. Полное правило: раздел «Правило ведения журналов» в `docs/OZON_PLAN.md`.

## [2026-07-31]

### Добавлено
- Пункт 24 «Грузоместа, этикетки и папка документов заявки» закрыт полностью: создание заявки в мастере автоматически раскладывает товар по грузоместам, отправляет их в Ozon, получает этикетки, собирает файлы состава по кластерам и складывает всё в папку «Озон <номер заявки>» на Google Диске вместе с этикетками ШК товаров из папки-библиотеки.
- Исправлена неверная заметка в `docs/OZON_PLAN.md`: пункт 25 был указан как следующий, хотя выполнен 30.07.2026. Порядок дальнейших работ переписан по открытым хвостам.
- План развития Ozon (`docs/OZON_PLAN.md`):
  - Переписан пункт 24 с учётом всех фактов Ozon API, решений пользователя по документам заявки и детализации этапов A–F.
- Документация Ozon API (`docs/OZON_API.md`):
  - Добавлена детальная секция о поведении API Ozon при отмене/удалении заявок (`CANCELLED`), эвристике и безопасных условиях автоматической очистки записей.
  - Добавлена секция «Разведка A2 пункта 24: документы заявки — чего в API НЕТ» и уточнено требование доработок в Code.gs для сохранения файлов заявки на Google Диск.

### Изменено
- Таб «Поставки Ozon» (`src/components/OzonSuppliesTab.tsx`):
  - Добавлена проверка `isFullyCancelled` для автоматического скрытия полностью отменённых в Ozon Seller заявок из списка актуальных и их учёта в обработанных.
- Серверный прокси (`server.ts`):
  - Передача списка отменённых заявок Ozon (`applyCancelledOzonOrders`) перед формированием записей отгрузок для снятия брони остатков и очистки устаревших отменённых заявок.
  - Добавлен дополнительный шаг опроса журнала «Заявки Ozon» (`getOzonSupplyRequests`) перед запросом деталей заявок для отслеживания статуса `CANCELLED` по заявкам, удалённым в Ozon Seller до первого опроса.
- Модальное окно поставки Ozon (`src/components/OzonSupplyModal.tsx`):
  - Показ предупреждений сборки (`warnings`, `problems`, `missingLabels`) перенесён ПОСЛЕ успешного уведомления о сборке папки на Google Диске, добавлены задержка вывода (400 мс) и увеличение длительности тостов до 15 секунд.
  - Автоматическая достройка поставки Ozon после создания заявки (финализация грузомест, этикеток и документов Google Диска).
  - Экран отображения прогресса достройки с информированием пользователя на каждом этапе.
  - Блокировка отправки с сообщением об ошибке, если у артикулов не задано «Штук в коробке» в SKU Базе.
- Обновление `Code.gs`:
  - Добавлена функция `syncOzonJournalFromShipments` для проставления терминального статуса («Отменена», «Просрочена», «Отказано») заявкам в «Заявки Ozon» по фактическому состоянию отгрузок.
  - Добавлена функция `applyCancelledOzonOrders` и константа `OZON_CANCELLED_KEEP_DAYS = 28` для пометки отменённых заявок Ozon и очистки необработанных отменённых записей старше 28 дней из таблицы.
  - Добавлена функция `normalizeLabelKey` с нормализацией NFC (устранение декомпозированной кириллицы с macOS) и регистронезависимой очисткой `.pdf`.
  - Поиск файлов этикеток переведён с `lib.getFilesByName` на однократное построение индекса `libIndex` по папке-библиотеке.
  - Обновлено описание настройки `supplyDocsLabelsFolder` на «Имя папки-библиотеки ШК товаров для генерации ярлыков коробок».
  - Заменена функция `saveSupplyDocsToDrive` на локальную реализацию обработки и сохранения документов поставки (акт приёма-передачи, ШК коробок) на Google Диск без использования прокси-сервера.

## [2026-07-30]

### Добавлено
- Модуль чистых функций грузомест Ozon (`src/lib/ozonCargo.ts`):
  - Добавлен модуль чистых функций для раскладки коробок («один артикул — одна коробка»), проверки лимита 30 коробок, разбора чек-листа готовности и сопоставления ключей коробок по полю key.
- Документация Ozon API (`docs/OZON_API.md`):
  - Добавлены 3 точечных уточнения по перезаписи единственного грузоместа, строгому сопоставлению по key и названию файла PDF-этикетки.
  - Добавлена секция с результатами разведки эндпоинтов Ozon API для пункта 24 (грузоместа и этикетки).
- Паспорт проекта (`docs/OZON_PLAN.md`):
  - В раздел «Известные хвосты вне плана» добавлены 6 пунктов живых наблюдений от 30.07.2026.
  - Пункт 25 («Ускорение приложения: снять глобальный замок с операций чтения») отмечен как выполненный (✅) с переносом регресса учётных путей на этап перед пунктом 24.
  - Бывшая волна 3 пункта 25 вынесена в отдельный необязательный пункт 27 («Освободить getArchivedItems от замка»).
- Индикатор статуса в свёрнутом сайдбаре (`src/components/Sidebar.tsx`):
  - Точка индикатора статуса в свёрнутом виде переведена на трёхуровневую цветовую схему (зеленый/желтый/красный).
- Трёхуровневый индикатор статуса системы и порог свежести 26ч (`src/components/Sidebar.tsx`, `src/store/useWarehouseStore.ts`):
  - Порог свежести прогона автоопроса увеличен до 26 часов для убирания ложных тревог при часовых колебаниях триггеров GAS.
  - Введён жёлтый уровень (`warning` / «Синхронизация с осечкой») для ситуаций с одиночными сбоями отдельных шагов.
  - В `fetchOzonSyncStatus` добавлена проверка наличия `sessionToken` до отправки запроса.
- Снятие LockService с читающих действий в `Code.gs` (волна 2 пункта 25):
  - Дополнительно 5 читающих действий (`getUsers`, `getGlobalSettings`, `getExternalShipments`, `getOzonSupplyRequests`, `checkSupplyAvailability`) переведены на выполнение без захвата `LockService.getScriptLock()`.
- Запись результатов аудита дерева вызовов и волн оптимизации LockService (`docs/OZON_PLAN.md`):
  - В пункте 25 зафиксированы результаты рекурсивного аудита 44 пишущих API-функций `Code.gs`.
  - Задокументирована классификация чистых чтений и чтений с ленивой инициализацией, сохранён запрет на удаление блока `assertAdmin` и план волн 1, 2 и 3.
- Снятие LockService с читающих действий в `Code.gs` (волна 1 пункта 25):
  - 6 читающих действий (`getInitialData`, `getStock`, `getTransactions`, `getSkus`, `getServices`, `getServiceRates`) переведены на выполнение без захвата глобального `LockService.getScriptLock()`.
  - Устранена последовательная очередь из 17 параллельных запросов при загрузке главной страницы.
- Автоматический повтор прогона автоопроса Ozon при сбоях (`Code.gs`):
  - Добавлена логика однократного повторного прогона через 2 минуты при неуспехе любого из шагов синхронизации (`scheduleOzonSyncRetryIfNeeded`).
  - Добавлен обработчик `runOzonSyncRetry` с автоочисткой одноразового триггера и защитой от зацикливания через `ozon_retryPending`.
- Отображение журнала прогонов автоопроса Ozon в сайдбаре (`src/store/useWarehouseStore.ts`, `src/components/Sidebar.tsx`):
  - Поле `history` добавлено в тип `ozonSyncStatus`.
  - В разметку блока «Статус системы» добавлена раскрывающаяся панель с журналом прогонов, статусами шагов и ошибками.
- Журнал последних 20 прогонов автоопроса Ozon (`Code.gs`):
  - Новая функция `saveOzonSyncResult(result)` ведает записью и ротацией до 20 записей истории прогонов в свойство `ozon_syncHistory`.
  - В `getOzonSyncStatusInfo` добавлена передача массива `history` на фронтенд.

## [2026-07-29]

### Добавлено
- Добавление пункта 25 в `docs/OZON_PLAN.md`:
  - В план работ включён пункт 25 по снятию глобального скриптового замка (`LockService`) с операций чтения для ускорения времени первого старта приложения.
- Отметка этапа 23.D в `docs/OZON_PLAN.md`:
  - Этап D пункта 23 отмечен как выполненный (`✅ готово 30.07.2026`).
  - Описаны итоги доработок интерфейса и алертов, подготовлена ориентировка на следующий этап E.
- Указание количества товара в заявках в алерте «Пора сделать поставку на Ozon» (`src/lib/ozonAlerts.ts`):
  - В текст алерта (`description`) добавлено пояснение `already in requests`, если `art.pendingTotal > 0`.
  - Предотвращено кажущееся противоречие между созданными заявками и новыми рекомендациями по поставкам.
- Модальное окно с расшифровкой зачёта по товару (`src/components/OzonStocksTab.tsx`):
  - Кликер по числу в колонке «Зачёт» таблицы остатков Ozon открывает подробное модальное окно `ozon-pending-modal`.
  - Отображение списка созданных заявок Ozon по данному артикулу со статусом (стилизованным плашкой `getStatusDetails`), датой создания, номером заказа, кластером и источником данных.
  - Итоговая строка суммы зачтённых штук в футере таблицы.
- Кнопка выбора видимых колонок таблицы остатков на вкладке «Склад» (`src/components/Dashboard.tsx`):
  - Кнопка «Колонки» (`Columns3`) с выпадающим списком переключения видимости колонок (`quantity`, `free`, `avgCost`, `capitalization`, `storage`, `turnover`).
  - Персональное сохранение скрытых колонок в `localStorage` по ключу `dashCols_${username}`.
  - Настройка по умолчанию скрывает учётный остаток `quantity`, оставляя видимым свободный остаток `free`.
  - Заголовки таблицы, ячейки загрузки skeleton и строки данных адаптируются под любой набор выбранных колонок.
- Добавление колонки «Свободно» на вкладке «Склад» (`src/components/Dashboard.tsx`):
  - Вызов `fetchExternalShipments()` включён для всех пользователей (не только админа) для корректного сбора данных о резервах.
  - Новая колонка «Свободно» в таблице остатков склада с поясняющей подсказкой `HelpCircle`.
  - Отображение свободного остатка артикула (`quantity - reserved`), а при наличии резерва — выделенной плашки с указанием размера резерва.
  - Колонка «Кол-во» сохранена в неизменном виде как учетный остаток для капитализации.
- Отображение свободного остатка Моего склада в таблице и панели рекомендаций (`src/components/OzonStocksTab.tsx`):
  - Подсказка `ColHint` для колонки «Мой склад» поясняет работу со свободным остатком и резервами.
  - В ячейках товаров колонка «Мой склад» показывает `freeMyStock` (при наличии резерва выводится детализация общего остатка и зарезервированного объёма).
  - В панели рекомендаций выведено «свободно X шт (из Y)» с расшифровкой по наведению.
- Отображение колонки «Зачёт» в таблице остатков Ozon (`src/components/OzonStocksTab.tsx`):
  - Новая переключаемая колонка «Зачёт» (`pending`) в списке скрываемых колонок.
  - Подсказка в шапке с разъяснением принципа зачёта и работы с «В заявках».
  - Отображение суммарного зачёта по товарам (`pendingTotal`), эффективного зачёта по кластерам (`pendingEffective`) и зачёта по заявкам без кластера.
- Обновление плана Ozon (`docs/OZON_PLAN.md`):
  - Перевод этапа C пункта 23 в статус завершённого («✅ C. Интеграция в расчёт покрытия»).
  - Назначение этапа D следующим рабочим шагом.
- Подключение расчёта зачёта потребности на главной (`src/components/Dashboard.tsx`):
  - Автоматическая подгрузка внешних отгрузок и заявок Ozon при старте дашборда администратором.
  - Расчёт `pendingSupplies` по всем кабинетам без разделения через `buildPendingSupplies`.
  - Передача `pending: pendingSupplies` в вызов `buildOzonCoverage` для синхронизации алертов с вкладкой «Остатки Озон».
- Подключение расчёта зачёта потребности на вкладке «Остатки Озон» (`src/components/OzonStocksTab.tsx`):
  - Автоматическая подгрузка внешних отгрузок и заявок Ozon при старте вкладки администратором.
  - Расчёт `pendingSupplies` с учётом выбранного фильтра по кабинету (`cabinetFilter`) через `buildPendingSupplies`.
  - Передача `pending: pendingSupplies` в вызов `buildOzonCoverage`.
- Интеграция чтения журнала «Заявки Ozon» в Zustand-стор (`src/store/useWarehouseStore.ts`):
  - Поля `ozonSupplyRequests` и `fetchOzonSupplyRequests` в состояние склада.
  - Асинхронный метод `fetchOzonSupplyRequests` для вызова экшена GAS `getOzonSupplyRequests`.
- Интеграция локального зачёта потребности в расчёт покрытия Ozon (`src/lib/ozonCoverage.ts`):
  - Добавлен интерфейс `OzonPendingLike` и опциональное поле `pending?: OzonPendingLike` в `OzonCoverageInput` (без кольцевого импорта `ozonPending.ts`).
  - Поля `pendingQty`, `requestedQty`, `pendingEffective` в `ClusterCoverageRow` и `pendingTotal`, `freeMyStock` в `ArticleCoverage`.
  - Учёт действенного зачёта (`pendingEffective = Math.max(pendingQty, requestedQty)`) при вычислении рекомендаций поставки по кластерам.
  - Резервирование остатка Моего склада под задействованный зачёт (`freeMyStock = Math.max(0, myStockAvailable - pendingTotal)`).
- Накопление колонки «В заявках» (`requested`) в кластерных агрегатах остатков Ozon (`src/lib/ozonCoverage.ts`):
  - Поле `requested: number` добавлено в интерфейс `ClusterStockAgg`.
  - В функции `buildClusterStocks` добавлено чтение и суммирование `requested` по кластерам без включения в расчётный остаток `estimated`.
- Модуль локального зачёта потребности по незавершённым заявкам Ozon (`src/lib/ozonPending.ts`):
  - Поле `clusterId?: string` в интерфейсе `ExternalShipment` (`src/types.ts`).
  - Чистые функции `buildPendingSupplies`, `isPendingCleared`, `isPendingActive`, `getPendingQty`, `mergePendingWithRequested`.
  - Учёт 7-дневного предохранителя (`PENDING_SAFETY_DAYS = 7`), обработка снятия зачёта по статусам Ozon (`CANCELLED`, `REJECTED_AT_SUPPLY_WAREHOUSE`, `OVERDUE` и уход остатка).
  - Подстраховка журналом «Заявки Ozon» до синхронизации данных от Ozon Seller API.
  - Объединение локального зачёта и колонки «В заявках» через выбор наибольшего значения (`mergePendingWithRequested`).
- Обновление правил и предохранителя в `docs/OZON_PLAN.md`:
  - Синхронизация предохранителя пункта 23 (7 дней).
  - Добавление правил работы с Ozon MCP, получением согласия пользователя на Claude в Chrome и обходом кэша gviz Google Таблиц.
- Обновление паспорта проекта `docs/OZON_PLAN.md`:
  - Пункт 23 переведён в статус `🟨 в работе`.
  - Зафиксированы решения по 7-дневному предохранителю и пересечению с «В заявках», отмечен завершённым этап A (29.07.2026), выделены подэтапы B–E.
- Резервный сопоставитель `clusterId` и статистика в `/api/ozon/check` (`server.ts`):
  - Построение обратного справочника `clusterIdByName` (название кластера → id) на базе `clusterMap`.
  - Определение `clusterId` по названию склада хранения, если Ozon не передал `macrolocal_cluster_id`.
  - Возврат статистики `clusterStats` (`fromApi`, `fromName`, `unresolved`) в JSON-ответе эндпоинта.
- Диагностический эндпоинт `GET /api/version` в `server.ts`:
  - Возвращает `{ status: "success", version: "2026-07-29-clusterid", features: { clusterIdInShipments: true } }`.
  - Размещён перед Vite middleware для доступности в продакшене без авторизации и раскрытия чувствительных данных.

## [2026-07-28]

### Добавлено
- Передача `clusterId` в эндпоинте `/api/ozon/check` (`server.ts`):
  - Сохранение `clusterId: macrolocalStr` в объекте `finalShipments`.
  - Передача `clusterId: s.clusterId || ''` в объект `shipmentsForGas` для отправки в Apps Script (`saveExternalShipments`).
- Колонка «КластерID» в листе «Внешние отгрузки» (`Code.gs`):
  - Добавлена новая колонка «КластерID» (macrolocal_cluster_id) в массив `EXTERNAL_SHIPMENTS_HEADERS`.
  - Извлечение `clusterId`/`cluster_id` строкой в `saveExternalShipments` и предотвращение затирания сохранённого значения пустым при обновлении.
  - Чтение и возврат `clusterId` в виде триммированной строки в `getExternalShipments`.
- Информация о коробках в блоке «Ответ Ozon» (`OzonSupplyModal.tsx`):
  - Хелпер `boxInfoForOzonItem` восстанавливает артикул приложения по offerId и Ozon-SKU через `resolveOzonArticle`.
  - Отображение количества коробок, нормы штук и неполных коробок в принятых и отклонённых позициях ответа Ozon.
- Отображение наполнения коробок и человекопонятные статусы в `OzonSupplyModal.tsx`:
  - Расчёт нормативного количества штук в коробке по `pcsPerBox` из SKU Базы.
  - Детальный вид коробок по каждой строке (количество коробок, норма штук, индикатор неполной коробки / отсутствия нормы).
  - Сводные неблокирующие предупреждения над составом о неполных коробках и не заданных нормах коробок.
  - Замена технических кодов статусов Ozon (`FULL_AVAILABLE`, `NOT_AVAILABLE_MATRIX` и т.д.) на ясные подписи на русском языке.
- Объединение экранов состава и вердикта Ozon в единый интерфейс `OzonSupplyModal.tsx`:
  - Состав заявки и ответ Ozon отображаются на одной странице без смены экранов.
  - Трёхрежимная логика кнопки управления: «Оформить заявку в Ozon» (первичный черновик + автосоздание при полном согласии Ozon), «Пересчитать в Ozon» (при изменениях состава после расчёта без автосоздания), «Создать заявку в этом составе» (финальное подтверждение).
  - Возможность редактирования штук и удаления отдельных строк/кластеров с автоматическим отслеживанием изменений (`dirty`).
- Обновлено и переписано модальное окно создания поставки Ozon `OzonSupplyModal.tsx`:
  - Таймаут запросов 60 сек с гильотинным завершением и отсчётом времени.
  - Автоматический сброс всех промежуточных состояний при повторном открытии модального окна.
  - Нормализация Ozon-SKU (`normalizeOzonSku`) и гибкое сопоставление через `ozonStocks` и `skus`.
  - Возможность удаления отдельной строки или всего кластера из заявки.
- Модальное окно создания поставки Ozon `OzonSupplyModal.tsx` и его интеграция с `OzonStocksTab.tsx`:
  - Проверка наличия ШК Ozon в SKU Базе для всех выбранных позиций.
  - Редактирование финального количества штук по каждой строке без изменения физических остатков.
  - Создание асинхронного черновика поставки через `/api/ozon/supply/draft` и отображение вердикта Ozon (принятые/отклонённые позиции по кластерам).
  - Подтверждение и создание заявки через `/api/ozon/supply/create` с серверной проверкой наличия товаров на Моём складе и записью в журнал GAS `saveOzonSupplyRequest`.
- Выбор строк рекомендаций и формирование плана поставки в `OzonStocksTab.tsx`:
  - Чекбоксы выбора конкретных строк «товар × кластер» в рекомендациях Ozon.
  - Панель выбранных позиций со статистикой по строкам, коробкам, штукам и кластерам.
  - Проверки мультикабинетности и превышения лимита коробок на кластер (`maxBoxesPerCluster`).
  - Плашка предупреждения при отсутствии выбранной точки отгрузки и кнопка «Оформить поставку» с предпросмотром состава заявки.
- Настройки точки отгрузки Ozon и лимита коробок в модальном окне `OzonSettingsModal.tsx`:
  - Поле ввода тарифного лимита «Максимум коробок на кластер» (`maxBoxesPerCluster`).
  - Поиск склада отгрузки Ozon FBO по наименованию (мин. 4 символа) через API `/api/ozon/dropoff/search` и выбор из списка с отображением адреса и типа.
  - Сохранение и загрузка настроек `dropOffWarehouseId`, `dropOffWarehouseName`, `dropOffWarehouseType` и `maxBoxesPerCluster`.
- Backend-эндпоинты прокси для оформления заявок на поставку Ozon (`server.ts`):
  - Поиск склада отгрузки `/api/ozon/dropoff/search` по подстроке наименования (мин. 4 символа).
  - Расчёт черновика поставки `/api/ozon/supply/draft` методом `/v1/draft/multi-cluster/create` с асинхронным опросом состояния и разбором принятых/отклонённых товаров.
  - Создание финальной заявки `/api/ozon/supply/create` с серверной перепроверкой остатков на Моём складе через GAS экшен `checkSupplyAvailability` перед вызовом `/v2/draft/supply/create`.
  - Вспомогательные хелперы `pickCabinet` (выбор кабинета Ozon) и `loadBundleItems` (пагинированное чтение бандла).
- Backend-структура и API-экшены для мастера создания поставок Ozon (`Code.gs`):
  - Новые настройки в `OZON_SETTINGS_DEFAULTS`: `maxBoxesPerCluster`, `dropOffWarehouseId`, `dropOffWarehouseName`, `dropOffWarehouseType`.
  - Валидация строковых параметров `dropOffWarehouseId` (цифровой ID) и `dropOffWarehouseType` (список `OZON_DROPOFF_TYPES`) в `saveOzonSettings`.
  - Лист «Заявки Ozon» (`OZON_SUPPLY_REQUESTS_HEADERS`) с полями ID, Дата, ОтгрузкаID, ТипОтгрузки, ОтгрузкаИмя, Статус, ПараметрыJSON, Создал, Создано.
  - Экшен `checkSupplyAvailability`: безопасная проверка остатков и виртуальных комплектов без внесения списаний.
  - Экшены `saveOzonSupplyRequest` и `getOzonSupplyRequests` для записи и чтения заявок Ozon FBO.
- Документирование результатов разведки этапа H в паспорте API (`docs/OZON_API.md`):
  - Добавлена новая секция «Разведка этапа H (28.07.2026): создание заявки на поставку — проверено вживую через Ozon MCP» с подробным описанием методов `/v1/cluster/list`, `/v1/draft/direct/create`, `/v2/draft/create/info`, `/v2/draft/timeslot/info`, `/v2/draft/supply/create` и особенностей работы с черновиками Ozon FBO.

## [2026-07-27]

### Добавлено
- Оформление и навигация для алертов покрытия Ozon на дашборде (`src/components/Dashboard.tsx`):
  - Добавлена визуализация в стиле Tailwind для тяжестей алертов 'sky' (голубая плашка) и 'orange' (оранжевая плашка).
  - Настроена навигация кнопки «Открыть»: переход на вкладку 'ozonStocks' для алертов типов 'supply_needed' и 'factory_order'.
- Алерты покрытия Ozon на дашборде (`src/components/Dashboard.tsx`):
  - Подключен фоновый расчёт покрытия через `buildOzonCoverage` и сбор алертов через `buildCoverageAlerts`.
  - Объединены алерты поставок Ozon с алертами потребности поставок и заказов на фабрике в общий список алертов.
- Вычисление алертов покрытия Ozon (`src/lib/ozonAlerts.ts`):
  - Расширены типы `OzonAlertType` ('supply_needed', 'factory_order') и `OzonAlert` (добавлены цвета 'sky', 'orange' и опциональное поле `article`).
  - Реализована функция `buildCoverageAlerts` для генерации алертов «Пора заказать на фабрике» (с проверкой активных заказов) и «Пора сделать поставку на Ozon» (с группировкой по кластерам и выводом до 3 наиболее критичных).

### Изменено
- Паспорт проекта `docs/OZON_PLAN.md`:
  - Отметка о выполнении этапа G (алерты на главной) в пункте 22, добавление пункта о двух независимых точках загрузки настроек Ozon в известные хвосты вне плана и актуализация заметок для следующего чата на этап H (мастер «Создать поставку в Ozon»).
- Паспорт проекта `docs/OZON_PLAN.md`:
  - Отметка о выполнении этапа F (блок рекомендаций) в пункте 22, обновление описания условий срабатывания заказа на фабрике, запись в известные хвосты вне плана и переключение заметок для следующего чата на этап G.
- Отображение количества коробок и контрастность подписей в модальном окне заказа на фабрике (`src/components/FactoryOrderModal.tsx`):
  - Расчёт коробок переведён на точное значение с выводом десятых долей и заменой точки на запятую для некратных значений.
  - Зелёная подсветка целого количества коробок (`font-bold text-emerald-600`) и красная подсветка некратных значений (`font-bold text-red-600`) с вычислением недостающих или лишних штук.
  - Увеличена контрастность и читаемость всех серых подписей, подзаголовка, подписей полей формы и поясняющего блока (`text-slate-600`, `text-slate-500`, `text-[12px]`).
- Алгоритм распределения остатка «Моего склада» между кластерами Ozon (`src/lib/ozonCoverage.ts`):
  - Потребность кластеров удовлетворяется по очереди из единого остатка товара на Моём складе.
  - Очередь распределения: сначала приоритетные кластеры (по убыванию коэффициента), далее остальные по возрастанию дней покрытия.
  - Рекомендации выделяются целыми коробками до исчерпания остатка, при недостаче выставляется `limitedByMyStock`.

### Исправлено
- Валидация настройки `priorityClusters` при сохранении настроек Ozon (`Code.gs`):
  - Исправлена ошибка сохранения, возникавшая из-за проверки двоеточий и десятичных точек универсальным правилом для чисто числовых ID.
  - В `saveOzonSettings` добавлена поддержка формата `КластерID:коэффициент` с валидацией числового ID и коэффициента не меньше 1.

### Добавлено
- Оформление прихода по кнопке «Партия пришла» (`src/components/FactoryOrderModal.tsx`):
  - При нажатии на «Партия пришла» заказ отмечается полученным, происходят автоматический выбор вкладки «Загрузка», установка операции «Приход», объекта «Склад» и предзаполнение текста накладной количеством из заказа.
- Запрет проведение прихода с нулевой себестоимостью (`src/components/ConfirmModal.tsx`):
  - Автоматическая проверка базовой цены позиций прихода, блокировка кнопки подтверждения при наличии нулевых цен, визуальная подсветка полей ввода красной рамкой и выведение информационного предупреждения.
- Блок «Рекомендации» во вкладке «Остатки Озон» (`src/components/OzonStocksTab.tsx`):
  - Интерактивный сворачиваемый блок перед панелью фильтров (свернут по умолчанию) с подсказками по направлениям «Отвезти на Ozon» и «Заказать на фабрике».
  - Отображение рекомендуемых коробок и штук с разбивкой по кластерам Ozon, индикация приоритетов и признаков лимита остатка на Моём складе.
  - Список сигналов заказа у производителя с кнопками быстрого открытия модалки `FactoryOrderModal` и отображением счетчика ранее размещенных заказов.
- Интеграция заказов на фабрике во вкладку «Остатки Озон» (`src/components/OzonStocksTab.tsx`):
  - Загрузка заказов при открытии вкладки методом `fetchFactoryOrders()`.
  - Отображение бейджей `заказ · дд.мм` (зеленый) и `ждали дд.мм` (янтарный, если дата просрочена) в колонке «Заказ на фабрике» вместо красной кнопки сигнала при наличии активного заказа.
  - Всплывающая карточка с детальной информацией о размещенном заказе (количество штук и коробок, даты размещения и ожидания, комментарий, автор отметки) при наведении курсора на бейдж.
  - Возможность открытия модального окна `FactoryOrderModal` по клику на бейдж активного заказа или красную кнопку сигнала.
- Модальное окно отметки заказа на фабрике (`src/components/FactoryOrderModal.tsx`):
  - Создан компонент `FactoryOrderModal` с формой ввода количества штук (с автоматическим перерасчетом коробок), ожидаемой даты прибытия и опционального комментария.
  - Автоматический расчет рекомендованного количества штук кратно коробкам и дефолтной даты прибытия на основе `leadTimeDays`.
  - Интеграция с методами стора `saveFactoryOrder` и `setFactoryOrderReceived` для сохранения заказа и отметки «Партия пришла».
  - Пояснительный информационный блок о правилах учета заказов у производителя (без влияния на текущие остатки и себестоимость).
- Подключение заказов на фабрике к клиенту (`src/types.ts`, `src/store/useWarehouseStore.ts`):
  - Добавлен интерфейс `FactoryOrder` в `src/types.ts`.
  - В Zustand-стор `useWarehouseStore` добавлены массив `factoryOrders: FactoryOrder[]` и асинхронные методы `fetchFactoryOrders()`, `saveFactoryOrder(order)` и `setFactoryOrderReceived(id)`.
  - Реализованы сетевые вызовы `fetchGas('getFactoryOrders')`, `fetchGas('saveFactoryOrder', { data: order })` и `fetchGas('setFactoryOrderReceived', { data: { id } })` с обработкой состояния `isProcessing` и всплывающими уведомлениями `toast`.
- Хранилище заказов на фабрике в Google Apps Script (`Code.gs`):
  - Создан лист «Заказы на фабрике» с заголовками `FACTORY_ORDERS_HEADERS` (`ID`, `Артикул`, `Дата заказа`, `Количество`, `Ожидаемое прибытие`, `Комментарий`, `Кто`, `Статус`, `Дата получения`).
  - Реализован быстрый читающий экшен `getFactoryOrders` без задержек `LockService`.
  - Реализованы экшены записи `saveFactoryOrder` (создание и редактирование заказов) и `setFactoryOrderReceived` (перевод заказа в статус `received` с фиксацией даты получения) с проверкой админ-прав.
  - Написаны вспомогательные функции чтения структуры листа `readFactoryOrdersSheet` и нормализации дат `normalizeFactoryDate`.
- Отображение причин сигнала «Заказ на фабрике» и самодиагностика колонки (`src/components/OzonStocksTab.tsx`):
  - Вывод меток причины сигнала в ячейке колонки: «нечем пополнить» (при локальном дефиците кластеров) и «кончается везде» (при исчерпании общего запаса).
  - Подробные всплывающие подсказки с указанием причин, объёма дефицита, текущих дней запаса и порога срабатывания.
  - Поясняющая надпись «срок не задан» для позиций без заполненной колонки «Срок поставки» в SKU Базе.
  - Расширенная подсказка `ColHint` в заголовке колонки с информацией о логике работы сигнала и условиях его срабатывания.
- Учёт дефицита кластеров в сигнале «Заказ на фабрике» (`src/lib/ozonCoverage.ts`):
  - Расширен `calcFactorySignal` для срабатывания по причине `clusterDeficit` при наличии непокрытой потребности кластеров (`unmetDeficitQty > 0`), даже если общих дней запаса формально хватает.
  - В `FactorySignal` добавлены поля `reason` ('total' | 'clusterDeficit') и `unmetDeficitQty`.
  - Объём заказа на фабрике выбирается как максимум из стандартного объёма по скорости и потребности дефицитных кластеров.
  - В `ClusterCoverageRow` и `ArticleCoverage` добавлены поля `unmetQty` и `unmetDeficitQty` для точного отслеживания нехватки остатка собственного склада.
- Отображение дефицита и потребностей кластеров при нулевом остатке на Моём складе (`src/components/OzonStocksTab.tsx`):
  - Красная индикация дефицита (`дефицит X шт` и `+ дефицит X шт`) в колонке «Рекомендация» для товаров первого уровня, если кластерам требуется поставка, но на собственном складе нет товара.
  - Красное отображение потребностей кластеров (`нужно X кор (Y шт) / нет на складе`) на втором уровне группировки.
  - Учёт `deficitQty` в фильтре «только с рекомендациями» для сохранения видимости товаров с дефицитом.
  - Обновлён текст всплывающей подсказки `ColHint` в заголовке колонки «Рекомендация» с расшифровкой цветовых обозначений (синий, оранжевый, красный).
- Колонка «Заказ на фабрике» в таблице «Остатки Озон» (`src/components/OzonStocksTab.tsx`):
  - Новая переключаемая колонка (видна по умолчанию) с информацией о необходимости размещения заказа у производителя.
  - Отображение требуемого количества штуках и коробках (`art.factory.orderQty` и `art.factory.orderBoxes`) ярко-розовым шрифтом (`text-rose-600`) с всплывающей подсказкой о днях покрытия и сроке поставки.
  - Плейсхолдеры `—` на уровнях кластеров, складов и агрегата без кластера для соблюдения сетки таблицы.
- Полноэкранный режим просмотра таблицы «Остатки Озон» (`src/components/OzonStocksTab.tsx`):
  - Кнопка «Развернуть» в панели управления таблицей для открытия во весь экран (`fixed inset-0 z-50`).
  - Шапка полноэкранного режима с названием таблицы, кнопкой «Свернуть (Esc)» и поддержкой выхода по клавише Escape.
  - Сохранение липкого позиционирования заголовков `<thead>` в полноэкранном режиме с заполнением доступной высоты экрана.
- Закрепление шапки таблицы «Остатки Озон» при вертикальной прокрутке (`src/components/OzonStocksTab.tsx`):
  - Ограничение высоты таблицы (`max-h-[70vh]`) с вертикальным и горизонтальным скроллом (`overflow-auto`).
  - Липкие заголовки `<thead> th` (`position: sticky`, `top: 0`, `z-index: 20`) с графитовой фоновой заливкой и разделителем.
- Индикация урезанной рекомендации в строке товара (`src/components/OzonStocksTab.tsx`):
  - Окрашивание итоговой рекомендации товара в янтарный цвет с всплывающей подсказкой, если ограничение остатка на Моём складе сработало хотя бы в одном из его кластеров (`recLimited`).
- Обозначение приоритета кластеров в сводке «Доли кластеров в продажах» (`src/components/OzonStocksTab.tsx`):
  - Показ коэффициента `×{c.priorityK}` янтарным бейджем рядом с названием кластера для приоритетных регионов.
- Всплывающие подсказки (ColHint) в заголовках колонок таблицы «Остатки Озон» (`src/components/OzonStocksTab.tsx`):
  - Значки иконки `HelpCircle` с выпадающим пояснением бизнес-смысла при наведении для всех 15 колонок таблицы.
- Индикация и использование настройки приоритетных кластеров во вкладке «Остатки Озон» (`src/components/OzonStocksTab.tsx`):
  - Загрузка и проброс поля `priorityClusters` из `getOzonSettings` в расчёт покрытия `buildOzonCoverage`.
  - Бейдж `приоритет ×{cls.priorityK}` янтарного цвета с всплывающей подсказкой в строке кластера.
- Выбор приоритетных кластеров в модальном окне настроек Ozon (`src/components/OzonSettingsModal.tsx`):
  - Блок «Приоритетные кластеры» с галочками и числовым полем индивидуального коэффициента запаса (по умолчанию 1,5).
  - Поддержка чтения, редакции и сохранения строки настройки `priorityClusters` («КластерID:коэффициент»).
  - Автоматическое взаимоисключение: исключение кластера («без поставок») сбрасывает его приоритет, а включение приоритета снимает галочку исключения.
- Учёт приоритетных кластеров в модуле планирования Ozon (`src/lib/ozonCoverage.ts`):
  - Поле `priorityClusters` в интерфейсе `OzonCoverageSettings` и функция `parsePriorityClusters` для разбора строки «КластерID:коэффициент».
  - Поля `priority` и `priorityK` в `ClusterCoverageRow`.
  - Увеличение `minStockDays` и `targetStockDays` на коэффициент `priorityK` при расчёте покрытия и рекомендаций для приоритетных кластеров.
- Настройка приоритетных кластеров `priorityClusters` в `Code.gs`:
  - Добавлена запись по умолчанию `{ key: 'priorityClusters', value: '', desc: 'Приоритетные кластеры в формате КластерID:коэффициент, через запятую' }` в `OZON_SETTINGS_DEFAULTS`.
  - Добавлен ключ `'priorityClusters'` в `OZON_SETTINGS_STRING_KEYS` для чтения значения настройки в виде строки.
- Постоянно развёрнутый вид секции «Остатки на складах Ozon» (`src/components/OzonStocksTab.tsx`):
  - Удалена кнопка сворачивания секции и состояние `isOzonStocksCollapsed`.
  - Время обновления, кнопки шапки («Колонки», «Настройки», «Обновить») и содержимое таблицы отображаются всегда.
- Оптимизация параллельной загрузки вкладки «Остатки Озон» в `Code.gs`:
  - Добавлена прямая обработка читающих экшенов (`getOzonStocks`, `getOzonSales`, `getOzonSyncStatus`, `getOzonSettings`, `getOzonClusters`) без захвата `LockService.getScriptLock()`.
  - Устранены таймауты при одновременной отправке читающих запросов клиентом во время работы фоновой синхронизации Ozon.
- Панель фильтров и сводка долей кластеров таблицы «Остатки Озон» (`src/components/OzonStocksTab.tsx`):
  - Панель фильтров (`#ozon-stocks-filters`): текстовый поиск по артикулу/названию (`#ozon-search-input`), селектор фильтрации по кабинету Ozon (`#ozon-cabinet-filter`), чекбокс «Только с рекомендациями» (`#ozon-only-rec-toggle`) и счётчик товаров.
  - Предварительная фильтрация остатков и продаж по кабинету в `filteredOzonStocks` и `filteredOzonSales` до расчёта покрытия `buildOzonCoverage`.
  - Сводный блок «Доли кластеров в продажах» (`#ozon-cluster-shares`) под таблицей с прогресс-барами процентных долей каждого кластера.
  - Обработчик закрытия меню колонок по клику вне элемента.
- Механизм скрытия колонок таблицы «Остатки Озон» (`src/components/OzonStocksTab.tsx`):
  - Выпадающее меню настроек колонок с вызовом по кнопке «Колонки» (`#btn-ozon-columns`).
  - Чекбоксы переключения видимости для 14 колонок с сохранением настроек в `localStorage` по ключу `ozon_stocks_hidden_cols`.
  - Значения по умолчанию для скрытых колонок: «Готовим», «В заявках», «Излишки», «Прочее».
  - Построчное обёртывание ячеек в `isColVisible` для <thead>, товаров, кластеров, складов и строки «Без кластера (агрегат)».
- Утверждённый макет 15 колонок для таблицы «Остатки Озон» (`src/components/OzonStocksTab.tsx`):
  - Приведение таблицы остатков Озон строго к 15 колонкам (Товар / Кластер / Склад, Продано, Скорость, Доля, Доступно, Готовим, В заявках, В пути, Излишки, Возвраты, Прочее, Расчётный, Покрытие, Мой склад, Рекомендация).
  - Внедрение точного расчёта агрегатов `sumBy` для всех статусов остатков и обработка остатков без кластера (`unboundRows` / «Без кластера (агрегат)»).
  - Точное совпадение функций форматирования чисел, скорости, дней и цвета покрытия.
- Трехуровневая таблица остатков Ozon во вкладке «Остатки Озон» (`src/components/OzonStocksTab.tsx`):
  - Перевод структуры с 2 уровней (артикул → склады) на 3 уровня (товар/артикул → кластеры → склады).
  - Подключение расчётных показателей покрытия, продаж, неснижаемого остатка, рекомендации к поставке и заказа на фабрику из `buildOzonCoverage`.
  - Реализация состояний независимого раскрытия товаров (`expandedArticles`) и кластеров (`expandedClusters`).
  - Добавление форматирования чисел (`fmtInt`), скорости продаж (`fmtSpeed`), покрытия в днях (`fmtDays`) и цветовых бейджей статусов (`coverageColor`).
- Подключение расчёта покрытия Ozon во вкладке «Остатки Озон» (`src/components/OzonStocksTab.tsx`):
  - Импортированы функции модуля `ozonCoverage`.
  - Добавлены селекторы стора `ozonSales`, `fetchOzonSales`, `skus`, `kits` и `getEffectiveAvailability`.
  - Реализована загрузка настроек и справочника кластеров Ozon через `fetchGas`.
  - Настроен расчёт объекта `coverage` через `useMemo` с вызовом `buildOzonCoverage`.
- Обновление статуса этапа D и заметок для следующего чата в паспорте проекта (`docs/OZON_PLAN.md`):
  - Этап D пункта 22 отмечен как выполненный ✅ (27.07.2026).
  - Заметки для следующего чата переведены на этап E (компонент таблицы с тремя уровнями детализации, фильтрами и скрытием колонок).
- Завершение модуля чистых функций планирования поставок Ozon (`src/lib/ozonCoverage.ts`):
  - Агрегация остатков Ozon по товарам и кластерам (`buildClusterStocks`).
  - Расчёт покрытия в днях (`calcCoverageDays`) с усмотрением неснижаемого остатка и исключённых кластеров.
  - Расчёт рекомендаций к поставке в кластер (`calcSupplyRecommendation`) с округлением до коробок и учетом остатков Моего склада.
  - Сигнал заказа на фабрику (`calcFactorySignal`).
  - Сборная функция расчета покрытия и рекомендаций по всем товарам (`buildOzonCoverage`).
- Создание модуля чистых функций планирования поставок Ozon (`src/lib/ozonCoverage.ts`):
  - Хелперы для работы с неделями по московскому времени (`getMskWeekMonday`, `getLastFullWeeks`).
  - Функция сопоставления артикула Ozon с внутренним SKU (`resolveOzonArticle`).
  - Расчёт общей и потоваровной скорости продаж, а также долей кластеров (`buildSalesSpeed`).
- Загрузка данных продаж Ozon на фронтенде (`src/types.ts`, `src/store/useWarehouseStore.ts`):
  - Добавлен интерфейс `OzonSalesRow` для представления записи продаж Ozon.
  - В Zustand-стор `useWarehouseStore` добавлены поле `ozonSales: OzonSalesRow[]` и асинхронный метод `fetchOzonSales()`.
  - Запрос данных через `fetchGas('getOzonSales')` с записью массива в состояние.
- Функция `getOzonSales()` и роут `getOzonSales` в Google Apps Script (`Code.gs`):
  - Чтение данных листа «Продажи Ozon» с возвратом массива объектов `{ week, cabinet, offerId, clusterName, qty, updatedAt, days }`.
  - Форматирование дат и защита от отсутствия обязательных колонок.
  - Регистрация действия `case 'getOzonSales'` в `doPost`.

## [2026-07-26]

### Добавлено
- Обновление описания этапа C в паспорте проекта (`docs/OZON_PLAN.md`):
  - Расширение итоговой сводки этапа C подробностями реализации справочника кластеров Ozon.
- Отображение полного справочника кластеров Ozon в модальном окне настроек и тосты о новых кластерах (`src/components/OzonSettingsModal.tsx`, `src/components/OzonStocksTab.tsx`):
  - Загрузка полного справочника кластеров из GAS через действие `getOzonClusters` при открытии модалки «Настройки Ozon».
  - Приоритетное построение списка чекбоксов «Кластеры без поставок» из справочника (с фоллбэком на остатки при пустом справочнике).
  - Автоматическая однократная проверка неотвеченных кластеров (`notified === false`) при монтировании вкладки «Остатки Озон».
  - Отображение информационного тоста `sonner` со списком названий новых кластеров и последующий сброс статуса через `markOzonClustersNotified`.
- Интеграция справочника кластеров Ozon в Google Apps Script (`Code.gs`):
  - Хранение справочника в листе «Кластеры Ozon» (`OZON_CLUSTERS_HEADERS`: КластерID, Название, Добавлен, Уведомлён).
  - Создание листа в `setupDatabase` и хелпер `getOzonClustersSheet`.
  - Функция `saveOzonClusters` для сохранения/обновления кластеров с автоматическим выставлением `Уведомлён = 1` и `newClusters: 0` при первичном наполнении.
  - Функции `getOzonClusters` и `markOzonClustersNotified` для считывания и сброса статуса уведомлений.
  - Роуты `saveOzonClusters`, `getOzonClusters`, `markOzonClustersNotified` в `doPost`.
  - Фоновая синхронизация `/api/ozon/clusters` в процедуре `scheduledOzonCheck`.
- Эндпоинт `POST /api/ozon/clusters` в `server.ts`:
  - Авторизация сессии через `verifyGasSession`.
  - Извлечение ключей Ozon через `fetchOzonKeys` с выбором первого кабинета.
  - Загрузка и обновление справочника кластеров через `loadClusterMap` (`/v1/cluster/list`).
  - Сохранение полученного списка кластеров `{ clusterId, clusterName }` в GAS через действие `saveOzonClusters`.
  - Формирование ответа со статистикой `totalClusters` и `newClusters`.
- Обновлён паспорт проекта (`docs/OZON_PLAN.md`):
  - Добавлено правило исключения кластеров без поставок из расчёта неснижаемого запаса и рекомендаций поставок.
  - Описание параметров «Настройки Ozon» дополнено ключом `excludedClusters`.
  - Этап C («Настройки») переведён в статус ✅ готов (27.07.2026).
- Блок «Кластеры без поставок» с чекбоксами в модальном окне «Настройки Ozon» (`src/types.ts`, `src/components/OzonSettingsModal.tsx`):
  - Поле `clusterId: string` добавлено в интерфейс `OzonStockRow`.
  - В интерфейс `OzonSettingsData` и локальное состояние формы добавлено поле `excludedClusters: string`.
  - Динамическое формирование отсортированного списка уникальных кластеров с непустым `clusterId` из `ozonStocks` через `useMemo` (с заглушкой «Кластер {clusterId}» при отсутствии имени).
  - Вертикальный список чекбоксов для выбора кластеров без поставок с подсказкой `FieldHint position="top"`.
  - Переключение чекбоксов обновляет `form.excludedClusters` (список ID через запятую), а в `handleSave` строка отправляется без числовых преобразований.
- Поддержка строкового параметра `excludedClusters` в листе «Настройки Ozon» и бэкенд-функциях `getOzonSettings` / `saveOzonSettings` (`Code.gs`):
  - Значение по умолчанию `{ key: 'excludedClusters', value: '', desc: 'КластерID без поставок, через запятую' }` добавлено в `OZON_SETTINGS_DEFAULTS`.
  - Зарегистрирован массив строковых ключей `OZON_SETTINGS_STRING_KEYS = ['excludedClusters']`.
  - В `getOzonSettings` для строковых ключей возвращается `String(rawVal || '').trim()` без преобразования в `Number` (сохраняя пустую строку как присутствующее значение).
  - В `saveOzonSettings` добавлена нормализация списка КластерID (split, trim, filter, проверка цифровых значений `/^\d+$/` и join) с исключением из числовых валидаций и возвратом ошибки при некорректном формате.
- Опциональный выбор направления всплывания тултипов в `FieldHint` (`src/components/OzonSettingsModal.tsx`):
  - Добавлен проп `position?: 'top' | 'bottom'` для управления направлением позиционирования подсказок.
  - Для первых трёх полей модального окна («Полных недель для скорости продаж», «Неснижаемый остаток, дней», «Целевой запас на Ozon, дней») включено позиционирование `position="bottom"`, что предотвращает визуальное обрезание верхней границей модального окна.
- Всплывающие подсказки с детальным описанием формул и логики планирования во все 6 полей модального окна «Настройки Ozon» (`src/components/OzonSettingsModal.tsx`):
  - Добавлен вспомогательный компонент `FieldHint` с иконкой `HelpCircle` из `lucide-react` и адаптивным всплывающим блоком при наведении.
  - Подсказки содержат подробные пояснения по расчёту скорости продаж, неснижаемого остатка, целевого запаса, объёма заказа на фабрике, процента возвратов и срока хранения продаж.
- Модальное окно и кнопка «Настройки» Ozon-планирования (`src/components/OzonSettingsModal.tsx`, `src/components/OzonStocksTab.tsx`):
  - Создан компонент `OzonSettingsModal.tsx` с шестью числовыми полями (скорость продаж в неделях, неснижаемый остаток, целевой запас, объем заказа на фабрике, % возвратов, срок хранения продаж).
  - Настроена загрузка значений через `getOzonSettings`, локальный `useState`, валидация диапазона/типов при сохранении через `saveOzonSettings` и вызов тостов `sonner`.
  - В тулбаре вкладки «Остатки Озон» (`OzonStocksTab.tsx`) перед кнопкой «Обновить» добавлена кнопка «Настройки» (`#btn-ozon-settings`) с иконкой `Settings` из `lucide-react`.
- Отображение и редактирование «Срок поставки, дн» (leadTimeDays) в пользовательском интерфейсе SKU (`src/types.ts`, `src/components/SkusTab.tsx`, `src/components/SkuModal.tsx`):
  - Поле `leadTimeDays: number` добавлено в клиентский тип `SKUItem`.
  - В таблице справочника SKU (`SkusTab.tsx`) добавлена сортируемая колонка «Срок поставки, дн» (ключ сортировки `leadTimeDays`) с выводом числа или прочерка «—».
  - В модальное окно создания/редактирования SKU (`SkuModal.tsx`) добавлено модальное поле ввода с ограничениями `min=0`, `max=365` и парсингом целых чисел `parseInt`.
- Расширение листа SKU колонкой «Срок поставки (дни)» (`Code.gs`):
  - В функциях `getSkus`, `addSku` и `updateSku` гарантировано наличие колонки `'Срок поставки (дни)'` в `ensureColumns`.
  - Поиск индекса колонки реализован динамически через `headers.indexOf` с фоллбэком на позицию 7.
  - В объектах SKU добавлено числовое поле `leadTimeDays` (дефолт `0`), поддерживаемое при чтении, создании и обновлении записей SKU.
- Добавлено хранение настроек Ozon-планирования и привязка срока хранения продаж (`Code.gs`):
  - Добавлены константы `OZON_SETTINGS_HEADERS` (['Ключ', 'Значение', 'Описание']) и `OZON_SETTINGS_DEFAULTS` с 6 ключевыми параметрами планирования (`speedWeeks`, `minStockDays`, `targetStockDays`, `factoryOrderDays`, `returnsToSalePct`, `salesRetentionWeeks`).
  - Создана функция `getOzonSettingsSheet()` для работы с листом «Настройки Ozon».
  - Создана функция `getOzonSettings()` с идемпотентным автозаполнением дефолтами при отсутствии ключей в листе.
  - Создана функция `saveOzonSettings(data)` с полной валидацией числовых диапазонов, целых чисел для недель и ограничением доступа администраторами (`assertAdmin`).
  - В `doPost` зарегистрированы экшены `getOzonSettings` и `saveOzonSettings`.
  - В `setupDatabase` включена автоинициализация листа «Настройки Ozon».
  - В `saveOzonSales` расчет ретенции продаж (`cutoffMs`) переведен на динамический параметр `salesRetentionWeeks` из настроек с безопасным фоллбэком на дефолтную константу `OZON_SALES_RETENTION_WEEKS`.

### Удалено
- Очистка устаревшего и мёртвого кода миграций в `Code.gs`:
  - Полностью удалены 6 функций (`recalculateStockFully`, `migrateKitCosts`, `cleanupZeroCostRows`, `migrateDatesToISO`, `migrateBowlKitsToVirtual`, `migrateComponentWriteOffCosts`), являвшихся однократными миграциями и рискованным полным пересчётом склада.

### Изменено
- Обновление статусов и замок следующего чата в паспорте проекта (`docs/OZON_PLAN.md`):
  - Статус этапа B в пункте 22 переведён в готов ✅ с подробным финишным описанием проверенного функционала продаж FBO, 28-дневных уплотнений архива, фоновой синхронизации и статусов системы.
  - Текущий рабочий этап пункта 22 в паспорте проекта переведён с B на C («Настройки»).
- Расширение блока «Статус системы» в Sidebar (`src/components/Sidebar.tsx`):
  - Добавлен автоматический периодический запрос `fetchOzonSyncStatus()` раз в 10 минут.
  - Реализован комплексный индикатор здоровья `allOk` (проверка соединения с GAS + свежесть и отсутствие ошибок в автоопросах Ozon за 14 часов).
  - Интегрирована кнопка-шеврон с разворачиванием подпанели подробностей по 4 подсистемам: «Связь с GAS», «Заявки Ozon», «Остатки Ozon» и «Продажи Ozon» с индивидуальной цветовой индикацией, всплывающими подсказками ошибок и временем последнего прогона.
- Детализация результатов последнего запуска в модуле «Автоопрос Ozon» (`src/components/SettingsTab.tsx`):
  - Перестроено содержимое блока `ozonSyncStatus.lastRun`: сохранена строка времени прогона и ниже добавлены 3 компактные секции («Заявки», «Остатки», «Продажи»).
  - Секция «Заявки» выводит метрики найдено/добавлено/обновлено (зелёным), сообщение об ошибке (красным) или «нет данных» (серым).
  - Секция «Остатки» выводит количество сохранённых строк (зелёным), ошибку (красным) или «нет данных» (серым).
  - Секция «Продажи» выводит режим работы (полная история / последние 3 недели), количество новых/удалённых строк, общую сумму в листе и число уплотнённых блоков (зелёным), ошибку (красным) или «нет данных» (серым).
- Доступность `getOzonSyncStatus` всем авторизованным пользователям и добавление метрик продаж (`Code.gs`):
  - Из `case 'getOzonSyncStatus'` в `doPost` удалён вызов `assertAdmin(currentUser)`, сделав информацию о статусе автоматической синхронизации Ozon доступной для всех пользователей.
  - В `scheduledOzonCheck` при успешном вызове сохранения продаж в итоговый объект результата добавлены метрики `salesTotal` (общее число итоговых строк в листе) и `salesCompacted` (число уплотнённых 28-дневных блоков).
- Уплотнение истории продаж Ozon (28-дневные блоки) в функции `saveOzonSales` (`Code.gs`):
  - В константу `OZON_SALES_HEADERS` добавлена колонка `'Дней'` в конец массива.
  - Объявлены константы `OZON_SALES_WEEKLY_ZONE_WEEKS` (13 недель), `OZON_SALES_PERIOD_ANCHOR_MS` (якорь 01.01.2024 MSK) и `OZON_SALES_PERIOD_MS` (28 дней).
  - В `saveOzonSales` внедрена автоматическая консолидация недельных записей продаж старше 13 недель в фиксированные 28-дневные блоки с 'Дней'=28 и идемпотентной агрегацией по `Map` (periodKey|cabinet|article|cluster).
- Обработка асинхронного ответа фонового запуска синхронизации Ozon (`src/store/useWarehouseStore.ts`):
  - В методе `runOzonSyncNow` добавлена ветка `if (data.async === true)` с выводом информационного уведомления toast.
  - Настроен 90-секундный таймер `setTimeout` для отложенной загрузки результатов синхронизации (статуса опроса и внешних отгрузок).
- Перевод вызова `runOzonSyncNow` в фоновый режим через одноразовый триггер (`Code.gs`):
  - В `doPost` обработчик `action === 'runOzonSyncNow'` переведён на мгновенный возврат с ответом `async: true` и запуск триггера `runOzonSyncOnce`.
  - Добавлена функция `runOzonSyncOnce()`, очищающая собственные одноразовые триггеры и вызывающая `scheduledOzonCheck()`.
  - Устранены клиенские таймауты при выполнении длинной цепочки запросов синхронизации (заявки, остатки, продажи).
- Уточнение ТЗ v3 пункта 22 в паспорте проекта (`docs/OZON_PLAN.md`):
  - Добавлено правило авто-уплотнения архива продаж (>13 недель) в 28-дневные блоки с фиксированным якорем 01.01.2024 и колонкой «Дней».
  - Зафиксированы правила учета позиций без КластерID/без кластера в общих продажах без привязки к конкретным кластерным рекомендациям.
  - Утвержден фоновый запуск ручной синхронизации Ozon через одноразовый триггер для устранения таймаутов на стороне клиенского приложения.
- Интеграция синхронизации продаж Ozon в автоматический опрос `scheduledOzonCheck` (`Code.gs`):
  - Сразу после блока остатков Ozon добавлен новый изолированный try/catch блок опроса продаж Ozon `/api/ozon/sales`.
  - Реализовано определение режима `salesMode`: `'full'` (если целевой лист «Продажи Ozon» пуст или отсутствует) или `'recent'` (при наличии существующих записей).
  - Формируется запрос с `mode: salesMode` к `/api/ozon/sales` и сохраняются поля результата (`salesOk`, `salesMode`, `salesRows`, `salesDeleted`, `salesMessage`) в итоговом `ozon_lastAutoSync`.
- Создание листа «Продажи Ozon» и функции `saveOzonSales` в Google Apps Script (`Code.gs`):
  - Добавлены константы `OZON_SALES_HEADERS` (['Неделя', 'Кабинет', 'Артикул', 'Кластер', 'Количество', 'Обновлено']) и `OZON_SALES_RETENTION_WEEKS` (78 недель).
  - В `setupDatabase` и функцию `getOzonSalesSheet` включено создание и проверка листа «Продажи Ozon».
  - В `doPost` добавлен обработчик `case 'saveOzonSales': result = saveOzonSales(data); break;`.
  - Реализована функция `saveOzonSales(payload)` для атомарного сохранения недельных агрегатов FBO-продаж с нормированием дат, очисткой устаревших строк (>78 недель) и удалением перезаписываемых недель в зависимости от режима `recent` / `full`.
- Поддержка колонки `КластерID` в листе «Остатки Ozon» Google Apps Script (`Code.gs`):
  - В константу `OZON_STOCKS_HEADERS` добавлена колонка `'КластерID'` в конец массива.
  - В функции `saveOzonStocks` добавлен поиск индекса `clusterIdIdx`, его включение в проверку обязательных колонок и сохранение `row[clusterIdIdx] = item.clusterId || ''`.
  - В функции `getOzonStocks` добавлен поиск индекса `clusterIdIdx`, его включение в проверку обязательных колонок и отдача `clusterId: String(row[clusterIdIdx] || '')` сразу после `clusterName`.
- Добавление поля `clusterId` в объект строки ответа `/api/ozon/stocks` (`server.ts`):
  - В `cabRows.push({...})` обработчика `POST /api/ozon/stocks` добавлено новое поле `clusterId: String(item.macrolocal_cluster_id || '')` сразу после `clusterName`.
- Добавление endpoint `/api/ozon/sales` и функции `getMskWeekMonday` (`server.ts`):
  - Реализован обработчик `POST /api/ozon/sales` по образцу `/api/ozon/stocks`.
  - Добавлена вспомогательная функция `getMskWeekMonday(dateStr)` для вычисления даты понедельника ISO-недели по MSK (+3h).
  - Реализовано разделение периода на 56-дневные окна для `mode === 'recent'` (текущая и 2 предыдущие недели) и `mode === 'full'` (380 дней).
  - Считывание и агрегация FBO-продаж из `POST /v2/posting/fbo/list`, исключение отменённых заказов, маппинг `warehouse_id` в кластер через `loadWarehouseClusterMap`, агрегирование по составному ключу `week|offerId|cluster`.
  - Передача агрегатов в GAS с действием `saveOzonSales` и форматированный ответ клиенту.
- Расширение кэша кластеров Ozon для построения маппинга складов в кластеры (`server.ts`):
  - Добавлена карта `cachedWarehouseClusterMap` (`warehouse_id` → `cluster.name`).
  - В `loadClusterMap` функция `processClusters` расширена обходом `cluster.logistic_clusters` и `warehouses` с валидацией `Array.isArray` и существующих полей для привязки складов к кластеру.
  - Добавлена вспомогательная функция `loadWarehouseClusterMap` для получения карты складов без изменения сигнатуры и вызовов `loadClusterMap`.
- Обновление пункта 22 и заметок для следующего чата в `docs/OZON_PLAN.md`:
  - Пункт 22 обновлён до состояния `🟨` с внесением всех утверждённых решений ТЗ v3 (скорость продаж по своим FBO-данным в разрезе кластеров, недельные агрегаты, покрытие, рекомендации поставок и заказов на фабрике, настройки, таблица, алерты, мастер создания поставок) и разбивкой на этапы A–I.
  - В разделе «Заметки для следующего чата» зафиксирована инструкция для старта следующего чата с этапа B.
- Дозапись результатов разведки Ozon API для пункта 22 в `docs/OZON_API.md`:
  - Добавлен раздел «Разведка для пункта 22 (26.07.2026): продажи FBO и черновики поставок».
  - Зафиксированы специфики метода `POST /v2/posting/fbo/list` (параметры, пагинация, `analytics_data.warehouse_id`, статусная логика и 14-дневное окно перезапроса).
  - Зафиксирован реестр методов создания и расчёта FBO-черновиков (`/v1/draft/...`, `/v2/draft/...`), 30-минутный лимит жизни черновика и необходимость полного мастера создания заявки на этапе H.
- Обновление реестра решений и списка известных хвостов в `docs/OZON_PLAN.md`:
  - В реестре принятых решений обновлено описание строки «Дубли ручных и авто-заявок» с фиксацией отключения молчаливой автопривязки дублей с 26.07.2026.
  - Добавлен отдельный раздел «Известные хвосты вне плана» с полным перечнем известных особенностей и нюансов системы.
  - В разделе «Заметки для следующего чата» обновлена ссылка на новый раздел известных хвостов вне плана.
- Обновление плана проекта Ozon (`docs/OZON_PLAN.md`):
  - Пункт 20 (Боевой запуск) отмечен как выполненный: зафиксированы результаты запускных работ, настройки фонового синхронизатора на боевую БД и наполнение зеркала остатков.
  - Добавлена «Фаза 7 — Доработки после запуска», включающая выполненный пункт 21 (вынос вкладки «Остатки Озон») и планируемый пункт 22 (доработка показателей и разрезов вкладки с обязательным предварительным опросом пользователя).
  - Заменён раздел «Заметки для следующего чата» на актуальную сводку по состоянию интеграции на боевом контуре.
- Вынос блока «Остатки на складах Ozon» в отдельную вкладку приложения (`src/components/OzonStocksTab.tsx`, `src/components/Dashboard.tsx`):
  - Создан новый компонент `OzonStocksTab.tsx`, содержащий полную функциональность просмотра остатков складов Ozon, карточки показателей, раскрытие строк по кластерам/складам и запуск синхронизации.
  - Из `Dashboard.tsx` удалены перенесенные состояния (`isOzonStocksCollapsed`, `expandedOfferKeys`), вычисления useMemo, подписки Zustand и элементы UI.
  - В `useUIStore.ts` добавлен идентификатор вкладки `'ozonStocks'`.
  - В `Sidebar.tsx` добавлена кнопка «Остатки Озон» с иконкой `Warehouse` (доступна только администраторам).
  - В `App.tsx` настроена ленивая загрузка `OzonStocksTab`, проверка прав доступа и условный рендеринг вкладки.
- Независимый опрос остатков складов Ozon при автоматической синхронизации (`Code.gs`):
  - В функции `scheduledOzonCheck` снято условие `if (result.ok)` перед вызовом `/api/ozon/stocks`.
  - Запрос остатков складов Ozon опрашивается при каждом срабатывании фонового триггера независмо от исхода опроса новых заявок Ozon `/api/ozon/check`.

## [2026-07-25]

### Изменено
- Завершение регресса и отметка пункта 19 в плане проекта (`docs/OZON_PLAN.md`):
  - Пункт 19 плана «Интеграция с Ozon» помечен как выполненный по результатам трехпроходного регрессионного тестирования на тестовой БД.
  - Обновлены заметки для следующего чата (пункт 20) и зафиксирован обновленный список известных хвостов вне плана.
- Отмена автоматической привязки дублей поставок Ozon (`src/components/OzonNewSuppliesModal.tsx`):
  - Удалена автоматическая молчаливая привязка совпавших заявок Ozon к ручным отгрузкам.
  - Очищены неиспользуемые импорты и ссылки (`useRef`, `toast`, `markExternalShipment`, `autoLinkedRef`).
  - Все заявки Ozon, совпавшие по составу с ручной отгрузкой, отображаются в модальном окне с меткой «Возможный дубль ручной отгрузки» для осознанной ручной привязки пользователем.
- Плюрализация наименований элементов в окнах подтверждения корзины (`src/components/DeletedItemsTab.tsx`):
  - Добавлена вспомогательная функция `pluralizeItems` для склонения слова «элемент» ("элемент", "элемента", "элементов") по числовым формам.
  - В диалогах массового удаления и восстановления раздела «Удаленное» заменено фиксированное слово «элементов» на динамически склоняемое значение.
- Уточнение формулировок в диалогах подтверждения удаления операций (`src/components/HistoryTab.tsx`, `src/components/ShipmentCostTab.tsx`):
  - Пугающая и неточная фраза «Действие нельзя отменить» заменена на корректную информацию о перемещении записей в раздел «Удаленное» с возможностью восстановления.
- Исключение внутренних операций с объектом «Склад» из отчёта «Себестоимость отгрузки» (`src/components/ShipmentCostTab.tsx`):
  - В хелпере `isNonShipmentDestination` добавлено отсечение объектов, нормализованное имя которых равно `"склад"` (с использованием `extractDestinationName`).
  - Операции Расхода с объектом «Склад» (включая помеченные суффиксами) более не засчитываются как коммерческие отгрузки и не отображаются в списке объектов.
- Пустое значение и визуальная подсказка для поля «Стоимость упаковки» (`src/components/ConfirmModal.tsx`):
  - Убран вводящий в заблуждение плейсхолдер `"0 ₽"`, заменён на пустую строку `""`.
  - Добавлена текстовая подсказка о том, что поле является обязательным и при отсутствии затрат на упаковку необходимо ввести `0`.
- Индикация неполных данных остатков Ozon при ошибках синхронизации кабинетов (`src/store/useWarehouseStore.ts`, `src/components/Dashboard.tsx`):
  - В Zustand-стор `useWarehouseStore` добавлено поле `ozonStocksSyncIssues` для хранения списка кабинетов с ошибками обновления.
  - На дашборде над картами остатков Ozon добавлена плашка-предупреждение при наличии проблем с синхронизацией отдельных кабинетов.
  - Добавлена информационная строка со списком всех кабинетов, данные по которым сейчас отображаются.
- Исключение операций корректировки остатка из отчёта «Себестоимость отгрузки» (`src/components/ShipmentCostTab.tsx`):
  - Добавлен helper `isNonShipmentDestination` для определения неторговых операций Расхода ("списание", "миграция", "корректировка остатка").
  - В `shipmentTransactions` добавленный хелпер исключает данные операции из расчёта показателей «Общая себестоимость», «Отгружено товаров» и «Всего отгрузок».
  - В `uniqueDestinations` хелпер исключает неторговые объекты из выпадающего списка фильтра объектов.
- Динамический перерасчет статусов остатков в модальном окне подтверждения `ConfirmModal.tsx` (hotfix вне плана Ozon):
  - Статусы позиций накладной и проверка достаточности на складе теперь пересчитываются на лету при изменении поля количества для операций «Расход».
  - При снижении количества до фактически доступного остатка статус ошибки автоматически снимается, разблокируя кнопку «Подтвердить и записать».
- Оптимизация производительности проведения отгрузок в Google Apps Script (`Code.gs`):
  - В функции `commitTransaction` запись строк в лист «История» переведена с построчного вызова `appendRow` на пакетную запись через `setValues(rowsToAppend)`. Ускорило обработку больших накладных (10–20 позиций) с 15–20 секунд до секундных задержек.
- Завершение пункта 18 плана интеграции Ozon («Алерты Ozon»):
  - Пункт 18 отмечен как полностью выполненный (✅) в паспорте проекта `docs/OZON_PLAN.md`.
  - Зафиксировано новое правило работы: файл `Code.gs` объявлен неприкосновенным для Gemini наравне с `OZON_PLAN.md`.
  - Восстановлено упоминание о подтверждении успешного срабатывания фонового триггера автообновления остатков Ozon (17:00 МСК 20.07.2026).

## [2026-07-22]

### Изменено
- Обновлен список направлений отгрузки по умолчанию и добавлена автоматическая миграция состояния в `src/store/useSettingsStore.ts`:
  - Направление «Склад» добавлено в массив `destinations` по умолчанию первым значением (`['Склад', 'Ozon', 'Wildberries']`).
  - Версия persistent-хранилища `useSettingsStore` повышена с 5 до 6.
  - В функцию `migrate` добавлен обработчик для `version <= 5`, автоматически внедряющий «Склад» в начало списка `destinations` локального хранилища у существующих пользователей.
- Исправлена форма ручной корректировки остатка в `src/components/ManualTab.tsx`:
  - Операция «Корректировка остатка» переведена с некорректного типа `'Корректировка'` на авторасчет дельты и проведение соответствующих операций `'Приход'` (при положительной разнице) и `'Расход'` (при отрицательной разнице) с сохранением суффикса `[Корректировка остатка]`.
  - Добавлена блокировка корректировки остатков для виртуальных комплектов с выводом информативного уведомления.
  - В интерфейсе добавлена серая подсказка с отображением текущего физического остатка выбранного артикула и снижен минимальный порог ввода количества до `min="0"`.
- Сужено условие автоматического разлогина в `src/store/useWarehouseStore.ts`:
  - Условие сброса сессионного токена и локального состояния пользователя в `fetchGas` изменено на строгую проверку сообщения `'Недействительная сессия'`. Это предотвращает случайное разлогинивание при спорадических сетевых сбоях или ответах сервера о недействительности токена из-за таймаута Google Apps Script.

## [2026-07-20]

### Добавлено
- Разработан чистый расчетный модуль алертов по поставкам Ozon (пункт 18 плана) в `src/lib/ozonAlerts.ts`:
  - Объявлен строго типизированный интерфейс `OzonAlert` и допустимые типы критических оповещений `OzonAlertType` ('overdue', 'rejected', 'dispute', 'shortage', 'peresort_confirm', 'peresort_commit').
  - Реализован внутренний хелпер `parsePeresortMeta` для безопасного разбора метаданных проведения пересорта из строки JSON-формата.
  - Написана чистая экспортируемая функция `buildOzonAlerts` для поштучной обработки и валидации внешних отгрузок (пропуск статусов 'ignored' и отмененных заказов 'CANCELLED').
  - Реализовано выявление критических Ozon-статусов (OVERDUE, REJECTED_AT_SUPPLY_WAREHOUSE, REPORT_REJECTED) с высшим приоритетом ('red').
  - Внедрена каскадная логика алертов пересорта: выявление потенциального пересорта (peresort_confirm — 'amber') и подтвержденного, но неоформленного пересорта (peresort_commit — 'violet').
  - Интегрирована интеллектуальная агрегация недостачи ('shortage' — 'amber') по позициям `itemsJSON` и `acceptedJSON` с исключением шума (алерт недостачи гасится, если по поставке уже активен алерт пересорта).
  - Настроен гарантированный порядок следования в итоговом массиве по приоритету важности (сначала red, затем violet, в конце amber), с сохранением исходного порядка внутри каждой группы.
- Интегрирован новый интерактивный визуальный блок «Алерты Ozon» на Дашборд (`src/components/Dashboard.tsx`):
  - Блок размещен самым верхним элементом (перед блоком воронки поставок Ozon) и доступен только пользователям с правами `isAdmin`.
  - Реализован показ только при наличии хотя бы одного нескрытого алерта.
  - Добавлена логика локального скрытия алертов (`dismissAlert`) с записью в `localStorage` по ключу `'ozon_dismissedAlerts'` (с защитой переполнения лимитом в последние 1000 записей).
  - Настроен расчет актуальных алертов через `useMemo`, зависящий от `externalShipments`, `skus` и списка скрытых ключей.
  - Реализован стильный адаптивный UI с поддержкой сворачивания/разворачивания состояния (`isAlertsCollapsed`) и цветовым выделением по уровню угрозы (`red` / `amber` / `violet`).
  - Добавлены кнопка перехода к деталям поставки («Открыть»), которая перенаправляет пользователя на вкладку «Поставки Озон», кнопка «Скрыть» (крестик `X`) и бейджи кабинетов для каждого уведомления.

### Изменено
- Добавлен бэкенд для зеркалирования остатков складов Ozon (пункт 17 плана) в `Code.gs`:
  - Добавлена новая константа `OZON_STOCKS_HEADERS` со спецификацией колонок листа «Остатки Ozon».
  - Реализованы создание листа в `setupDatabase` и хелпер `getOzonStocksSheet()` с автопроверкой и созданием колонок `ensureColumns`.
  - Добавлены функции сохранения `saveOzonStocks` (базовая валидация payload, очистка и перезапись снимков по кабинетам батчем без дедлоков) и чтения `getOzonStocks`.
  - В `doPost` добавлены роутинги для экшенов `runOzonStocksSyncNow` (ручной запуск опроса без захвата LockService), `saveOzonStocks` и `getOzonStocks`.
  - Интегрирован второй фоновый запрос к `/api/ozon/stocks` в автоматический опрос `scheduledOzonCheck`, защищённый изолированным `try/catch`.
- Добавлен прокси-серверный API endpoint для зеркалирования остатков складов Ozon в `server.ts`:
  - Добавлен маршрут `POST /api/ozon/stocks` с проверкой сессии и Ozon ключей.
  - Настроено пагинационное получение каталога SKU через `/v3/product/list` с лимитом до 20 страниц.
  - Реализовано получение остатков по пачкам из 100 SKU через `/v1/analytics/stocks`, фильтрация пустых строк и маппинг полей.
  - Добавлена обработка ошибок авторизации (HTTP 403) и батч-сохранение результатов в Google Sheets.
- Добавлен клиентский интерфейс и витрина зеркала остатков Ozon (пункт 17 плана):
  - В `src/types.ts` объявлен интерфейс `OzonStockRow` для строгого описания снимка остатков склада Ozon.
  - В `src/store/useWarehouseStore.ts` внедрено состояние `ozonStocks` и экшены `fetchOzonStocks` (тихий фоновый запрос) и `runOzonStocksSync` (полное ручное обновление остатков для админа с последовательным выводом тостов успехов/ошибок по кабинетам).
  - В `src/components/Dashboard.tsx` добавлен новый интерактивный сворачиваемый блок «Остатки на складах Ozon» сразу после блока воронки (доступный только администраторам). Блок содержит суммарные карточки-показатели, кнопку «Обновить» со спиннером анимации, группировку строк по артикулам (offerId + кабинет), раскрывающиеся списки с разбивкой по конкретным складам и кластерам при клике на строку, а также приглушение нулевых значений.
- Добавлена живая разведка API Ozon Seller для пункта 17 (каталог товаров и остатки на складах FBO) в файл документации `docs/OZON_API.md`, зафиксировав методы `/v3/product/list`, `/v3/product/info/list` и `/v1/analytics/stocks`, лимиты, пагинацию, нюансы ответа и готовую цепочку интеграции.
- Обновлена документация в плане проекта (`docs/OZON_PLAN.md`):
  - Пункт 17 («Зеркало остатков складов Ozon») отмечен как выполненный (✅ 20.07.2026) с фиксацией контракта, семантики снимков по кабинетам, циклом пагинации по 100 SKU и результатов живого теста (284 строки, 2 кабинета).
  - Раздел «Заметки для следующего чата» полностью обновлен: зафиксирован переход к пункту 18 (алерты), сырьё для них, инфраструктурные особенности (OZON_CABINETS, scheduledOzonCheck, ozon_autoSyncTarget) и правила разработки.

### Исправлено
- Исправлена избыточная автопривязка неотгруженных или отмененных поставок во всплывающем окне (`src/components/OzonNewSuppliesModal.tsx`):
  - В эффекте автоматической привязки дублей ручных отгрузок новые поставки (`newItems`) теперь фильтруются не только по статусу `'new'`, но и по условию фактического убытия товара со склада (`isStockDeparted(p.ozonStatus)`). Это предотвращает ошибочное занесение в статус обработанных тех отправлений Ozon, которые еще не были физически переданы в доставку (например, имеют статус `CANCELLED` или `IN_TRANSIT` до отгрузки).
- Исправлено двойное начисление дополнительных услуг/расходов на клиенте (`src/components/ConfirmModal.tsx`):
  - При подтверждении расходной операции (`opType === "Расход"`) на сервер теперь отправляются исходные `parsedItems` (с базовыми ценами) вместо `finalItems` (в которых цены уже увеличены на накладные расходы). Поскольку бэкенд (`commitTransaction` в `Code.gs`) теперь самостоятельно распределяет расходы, прописанные в `destination`, это исключает повторное начисление сдаточных услуг/упаковки на один и тот же товар.

## [2026-07-18]

### Изменено
- В функции `commitTransaction` (`Code.gs`) изменена логика распределения дополнительных расходов (сдаточных услуг): теперь доля доп. расходов начисляется пропорционально количеству на все позиции Расхода (как комплекты, так и обычные товары), предотвращая занижение себестоимости отгрузки.

### Исправлено
- Исправлено условие показа бейджа «Принято полностью» во вкладке поставок Ozon (`src/components/OzonSuppliesTab.tsx`):
  - Вызов вспомогательной функции `parsePeresortMeta` перенесён выше объявления массива бейджей `badges` в строке поставки.
  - Условие показа бейджа обновлено на `(!meta.confirmed || meta.committed)`. Это гарантирует, что бейдж «Принято полностью» корректно скрывается на этапе неподтвержденного пересорта и вновь отображается после проведения пересорта (коммита) поставки, когда состав фактически скорректирован.

## [2026-07-17]

### Добавлено
- Интегрирован пользовательский интерфейс проведения пересорта в `src/components/OzonSuppliesTab.tsx`:
  - Внедрена вспомогательная функция `parsePeresortMeta` для детального разбора состояния пересорта из JSON.
  - Реализовано динамическое отображение зеленого бейджа «Пересорт проведён» в строке отгрузки Ozon, если пересорт уже был проведен (`committed === true`), или прежнего индиго-бейжда «Пересорт подтверждён — ожидает проведения» в случае, если он только подтвержден, но не проведен.
  - В модальном окне сопоставления пересорта `PeresortModal` поддержан режим «проведено» (`meta.committed === true`): скрывается весь интерактивный конструктор пар (селекты, количества, кнопка добавления), кнопки удаления пар из списка, а также кнопки подтверждения сохранения и сброса. Показывается зеленая информационная плашка с датой/временем и именем пользователя, выполнившего проведение, и read-only список пар.
  - Для администраторов в `PeresortModal` добавлена кнопка «Провести пересорт» (отображается при подтвержденном, но не проведенном пересорте). По клику открывается `ConfirmDialog` с подробной детализацией пар, готовых к списанию, и предупреждением о движении остатков на складе. При согласии вызывается `commitShipmentPeresort`, обновляются остатки/история в Zustand-сторе и закрывается модалка.
- Внедрено вычислительное ядро пересорта Ozon на бэкенде в `Code.gs` для будущего перепроведения транзакций отгрузки:
  - `buildPeresortAdjustedItemsJSON`: Чистая функция для сборки нового состава поставки (itemsJSON) на основе подтвержденных пар пересорта с валидацией количеств и удаления обнуленных позиций.
  - `buildPeresortRecommitComposition`: Чистая функция для построения нового состава транзакций отгрузки. Пересчитывает количества исходных товаров со списанием targetQty по строкам, добавляет extra-товары (toArticle) с ценой из `stockAvgMap` (средняя себестоимость), и пересчитывает цены оставшихся исходных товаров по актуальной себестоимости.
  - `computePeresortNetDeltas`: Чистая функция для вычисления чистых изменений складских остатков по каждому SKU при перепроведении. Раскрывает комплекты (как legacy, так и virtual) до исходных компонентов/товаров с помощью `getKitComponents` и вычисляет разницу «вернулось на склад» минус «списалось в новом составе».
- Добавлена функция `commitShipmentPeresort(postingId, username)` для безопасного проведения пересорта Ozon:
  - Проводит предварительную сверку дельт остатков на складе по компонентам/комплектам с помощью `computePeresortNetDeltas`, блокируя проведение при нехватке остатка.
  - Удаляет старые транзакции отгрузки по поставке (`deleteTransaction` с `isUpdate = true`) и регистрирует новые транзакции (`commitTransaction`) на основе скорректированного фактического состава.
  - Обновляет `TransGroupInfo` с новыми ID транзакций для всех строк с тем же `OrderID` и статусом `processed`.
  - Изменяет состав поставки (`ПозицииJSON`) с учетом пересорта и фиксирует факт и дату проведения в колонке `ПересортJSON`.
  - Регистрирует транзакции типа `'Корректировка'` с нулевым количеством в истории транзакций для каждой пересортной пары для наглядности ведения учета.
  - Экшен `commitShipmentPeresort` зарегистрирован в switch `doPost` в `Code.gs`.
- Реализован клиентский метод проведения пересорта в Zustand-сторе `src/store/useWarehouseStore.ts`:
  - Метод `commitShipmentPeresort(postingId)` посылает соответствующий GAS action.
  - При успешном завершении операции обновляет состояние `stock` (применяя `normalizeStock`) и транзакций `transactions` в UI, после чего перезагружает актуальное состояние отгрузок `fetchExternalShipments` и выводит Sonner Toast-уведомление.

## [2026-07-15]

### Добавлено
- Реализован первый этап обработки пересорта Ozon при приёмке в `AcceptanceModal` (пункт 15 плана):
  - Добавлена возможность ввода позиций не из заявки («заявлено 0, принято N») с помощью динамической таблицы «Позиции не из заявки (пересорт)».
  - Внедрен интерактивный селектор выбора SKU для пересортных позиций, автоматически исключающий уже заявленные товары и другие ранее выбранные пересортные SKU во избежание дублирования строк.
  - Реализована попозиционная валидация пересорта (количество должно быть целым числом >= 1) с выводом тост-предупреждений об ошибках.
  - Изменена расчётная функция `computeShortageRecalc` в `src/lib/ozonShortage.ts`: добавлен превентивный выход `'peresort'` при обнаружении в принятом составе позиций, которых нет в заявке.
  - Добавлен янтарный предупреждающий баннер в `AcceptanceModal` для статуса `'peresort'`, который информирует пользователя о пересорте и временно блокирует перерасчет недостач до завершения ручного сопоставления.
- Разработан UI предпросмотра и проведения перераспределения себестоимости недостач Ozon в `AcceptanceModal` (пункт 14 плана):
  - Добавлена интерактивная таблица предпросмотра результатов перераспределения себестоимости (`recalcPreview`) в режиме реального времени.
  - Реализован вывод информативных предупреждающих блоков для состояний «Излишек/Пересорт» (блокировка перерасчета), «Ошибка валидации» и «Отмена перерасчета» (при сбросе приёмки к значению по умолчанию).
  - Реализован автоматический точечный запуск сохранения результатов перерасчета и корректировочной истории в БД Google Таблиц при сохранении приёмки.
  - Реализовано стильное удаление и отмена старых перерасчётов через `buildCancelNotes` при сбросе приёмки до заявленных значений.
- Внедрено отображение скорректированной себестоимости в составе поставки:
  - Функция `renderItemsTable` расширена отображением двух новых колонок («Себест. базовая» и «Себест. скорр.») при наличии сохраненных перерасчётов.
  - Скорректированная себестоимость, превышающая базовую, подсвечивается жирным красным шрифтом (`text-red-600 font-bold`).
- Добавлен цветной бейдж «Перерасчёт ✓» в строке отгрузки Ozon для наглядной идентификации позиций с проведенными корректировками недостач.
- Разработана клиентская расчётная логика перераспределения стоимости недостачи при приёмке поставок Ozon (пункт 14 плана `docs/OZON_PLAN.md`):
  - Создан новый вспомогательный модуль `src/lib/ozonShortage.ts`, реализующий функцию `computeShortageRecalc` для распределения стоимости недостачи (заявлено > принято) на принятые товары того же артикула. Если по артикулу принято 0 шт., его полная стоимость пропорционально распределяется на остальные принятые позиции.
  - Реализован строгий контроль сумм в виде инварианта «копейка-в-копейку» между суммарной базовой стоимостью поставки и итоговой стоимостью принятых позиций с погрешностью не более 1 копейки.
  - Реализована функция `parseRecalcJSON` для безопасного извлечения ранее сохранённых перерасчётов из JSON-поля БД.
  - Внедрена автоматическая генерация корректирующих записей истории (`historyNotes`) для отправки на сервер и последующего внесения в журнал транзакций как `Корректировка` с фиксацией сумм и номеров заявок/поставок.
  - В Zustand-стор `useWarehouseStore.ts` добавлен метод `saveShipmentShortageRecalc(postingId, recalcJSON, historyNotes)` для отправки результатов расчетов в Google Таблицу.

### Изменено
- Расширен интерфейс `ExternalShipment` в `src/types.ts` новым необязательным полем `recalcJSON?: string` для хранения и проброса перерасчёта.
- Обновлён план интеграции `docs/OZON_PLAN.md`: пункт 14 переведён в статус полностью выполненного (✅ 15.07.2026) с детальным описанием математических алгоритмов, принципов "orphanPool", инвариантов округления и структуры сгенерированных корректировок. В раздел заметок для следующей сессии добавлены подробные подсказки и архитектурные решения по пункту 15 (пересорт).

## [2026-07-14]

### Добавлено
- Реализована серверная часть сохранения перерасчёта стоимости недостачи Ozon (`Code.gs`):
  - Внедрена функция `saveShipmentShortageRecalc(postingId, recalcJSON, historyNotes, username)` для точечного обновления ячейки `'ПерерасчётJSON'` на листе «Внешние отгрузки» и атомарной записи корректировочных транзакций на лист «История».
  - В `doPost` добавлен новый экшен `saveShipmentShortageRecalc` с проверкой прав администратора (`assertAdmin`) и защитой транзакций через блокировщик `LockService`.
- Реализовано хранение принятых количеств по поставкам Ozon (`Code.gs`):
  - Добавлена новая колонка `'ПринятоJSON'` на листе «Внешние отгрузки» через расширение константы `EXTERNAL_SHIPMENTS_HEADERS`.
  - Внедрена функция `saveExternalShipmentAcceptance(postingId, acceptedJSON)` в Google Apps Script для атомарного сохранения расхождений/принятых количеств.
  - Реализована строгая валидация входящих данных: разрешены пустая строка (для сброса поля) или корректно сериализованный JSON-массив объектов с непустым строковым `offerId` и целочисленным `accepted` >= 0.
  - Интегрировано действие `saveExternalShipmentAcceptance` в обработчик `doPost` с использованием LockService для предотвращения гонок данных.
  - Обновлено чтение в `getExternalShipments` для сбора и отдачи поля `acceptedJSON` клиенту.
- Интегрирован проброс и сохранение принятых количеств на клиенте (`src/types.ts`, `src/store/useWarehouseStore.ts`):
  - Добавлено необязательное поле `acceptedJSON?: string` в интерфейс `ExternalShipment`.
  - В Zustand-стор `useWarehouseStore.ts` добавлен метод `saveShipmentAcceptance(postingId, acceptedJSON)` для сохранения приёмки с автоматической перезагрузкой списка поставок и выводом уведомлений.
- Реализована форма ввода принятых количеств по поставке Ozon (`src/lib/ozonStatus.ts`, `src/components/OzonSuppliesTab.tsx`):
  - Добавлены константа `ACCEPTANCE_STAGE_STATUSES` и функция `isAcceptanceStage` для определения этапов приёмки товара.
  - Добавлен парсинг принятых количеств и вывод интерактивных разноцветных бейджей результатов приёмки (недостача, излишек, полное соответствие).
  - Разработано модальное окно `AcceptanceModal` для ввода приёмки с попозиционной валидацией (целые числа >= 0), предупреждениями о возможном пересорте и быстрыми кнопками сброса и сохранения.
  - Обновлена таблица состава поставок с выводом колонки принятых количеств и визуальной подсветкой отклонений.
- Обновлена документация проекта (`docs/OZON_PLAN.md`):
  - Пункт 13 плана переведен в статус полностью выполненного (✅ 14.07.2026) с детальным описанием архитектурных решений, структуры данных, валидации, интерфейса AcceptanceModal и бейджей приёмки.
  - Раздел заметок для следующей сессии полностью актуализирован под требования и открытые вопросы пункта 14 (перераспределение разниц на себестоимость товаров).

## [2026-07-12]

### Добавлено
- Разработано и интегрировано новое модальное окно «Обнаружены новые поставки Ozon» (`src/components/OzonNewSuppliesModal.tsx`):
  - Автоматически информирует администратора при входе в приложение о наличии готовых к оформлению Ozon-заявок (тех, у которых внутренний статус `new`).
  - Выполняет однократный тихий запрос `fetchExternalShipments()` при монтировании компонента.
  - Отображает отфильтрованный список заявок со сводными параметрами: номер заявки, бейдж магазина (кабинета), плановая дата отгрузки, общее число новых поставок.
  - Поддерживает вывод цветовых бейджей сопоставления дубликатов («Возможный дубль ручной отгрузки» красным цветом и «Похожа на ручную — проверьте» янтарным) в полном соответствии со стилями вкладки поставок Ozon.
  - Позволяет запустить процесс оформления любой заявки в один клик через кнопку «Оформить» (использует хук `useProcessOzonGroup` и автоматически закрывает модалку во избежание визуального наложения окон).
  - Включает кнопку «Открыть Поставки Озон» для быстрого переключения активной вкладки приложения в раздел FBO поставок Ozon и кнопку «Позже» для закрытия.
  - Реализован плавный скроллинг длинных списков (`max-height` + `overflow-y-auto`).
- Подключена новая модалка в `src/App.tsx`: компонент постоянно смонтирован и рендерится в общем контейнере модальных окон только для администратора.
- Реализована серверная часть фонового автоопроса Ozon на стороне Google Apps Script (`Code.gs`):
  - Метод `scheduledOzonCheck()` создает временный токен сессии администратора («Автоопрос Ozon»), отправляет запрос к прокси, получает результат синхронизации и сохраняет его JSON-структурой в Script Property `ozon_lastAutoSync`. В конце процесса временная сессия безопасно удаляется из листа «Сессии».
  - Функции `setupOzonSyncTriggers()` и `removeOzonSyncTriggers()` для автоматической настройки триггеров по московскому времени (дважды в сутки: в 05:00 и в 17:00).
  - Функция `getOzonSyncStatusInfo()` возвращает статус активности триггеров, их количество, текущий целевой контур (`test` / `prod`) и подробности последнего автозапуска.
  - Обработка `action === 'runOzonSyncNow'` в `doPost` добавлена до LockService для исключения взаимоблокировок, так как прокси обращается к той же таблице в процессе синхронизации.
  - Интеграция команд `getOzonSyncStatus`, `setupOzonSyncTriggers` и `removeOzonSyncTriggers` в общий switch-case `doPost`.
- Реализована клиентская интеграция управления автоопросом Ozon:
  - В Zustand-стор `useWarehouseStore.ts` добавлены новые поля состояния `ozonSyncStatus` и действия `fetchOzonSyncStatus`, `setOzonSyncEnabled`, `runOzonSyncNow`.
  - В `SettingsTab.tsx` добавлена новая карточка настроек «Автоопрос Ozon» с выводом статуса, назначения БД, времени последнего автозапуска и лога результатов синхронизации.
  - Реализованы кнопки включения/выключения фонового триггера и немедленного ручного запуска опроса с индикацией состояния загрузки (`isProcessing`).

### Изменено
- Доработано модальное окно «Обнаружены новые поставки Ozon» (`src/components/OzonNewSuppliesModal.tsx`):
  - Добавлен механизм сохранения показанных поставок в локальное хранилище (`localStorage`, ключ `'ozon_notifiedPostingIds'`) для исключения навязчивости.
  - Реализовано разделение заявок на действительно «Новые» (содержат хотя бы один ранее не виденный `postingId`) и «Ожидающие» (уже были показаны при прошлых входах, но пока не оформлены и не отклонены).
  - Настроена автоматическая сортировка списка: заявки со статусом «Новая» отображаются первыми и помечаются специальным зеленым бейджем; заявки в статусе «Ожидает» следуют ниже.
  - Реализован вывод адаптивного заголовка и подзаголовка: при наличии новых заявок отображается заголовок о новых поставках и общее количество новых/ожидающих; при их отсутствии — заголовок о заявках, ждущих оформления, со счетчиком неоформленных.
  - Настроена гарантированная запись уникальных идентификаторов `postingId` всех `new`-поставок показанных заявок в реестр уведомлений при любом закрытии окна (кнопка «✕», кнопка «Позже», «Открыть Поставки Озон», «Оформить»). Ограничение объема истории установлено на последние 1000 записей.
- Внедрена проверка отгрузки со склада Ozon:
  - В `src/lib/ozonStatus.ts` добавлены константа `STOCK_DEPARTED_STATUSES` и проверочная функция `isStockDeparted` (для отслеживания статусов начиная с `ACCEPTED_AT_SUPPLY_WAREHOUSE`).
  - В интерфейс `OzonGroup` в `src/lib/ozonGroups.ts` добавлено свойство `needsExpense: boolean`, рассчитываемое в `buildOzonGroups` при обработке внешних поставок.
  - В хуке `useProcessOzonGroup` изменено формирование массива оформляемых позиций `newPostings` — теперь туда попадают только неотгруженные позиции, прошедшие проверку `isStockDeparted(ozonStatus)`. При пустом отфильтрованном результате выводится тост-сообщение о том, что заявка ещё не отгружена на Ozon.
- Отражение статуса отгрузки Ozon в интерфейсе:
  - В `src/components/OzonSuppliesTab.tsx` добавлен бейдж состояния отгрузки группы: красный «Отгружена — оформите списание» при `needsExpense === true`, серый «Ожидает отгрузки на Ozon» при `needsExpense === false` (отображается только для групп с новыми поставками).
  - На вкладке «Поставки Озон» кнопка «Оформить» блокируется (`disabled`) для неотгруженных групп, а при наведении выводится поясняющая всплывающая подсказка.
  - В `src/components/OzonNewSuppliesModal.tsx` модальное окно теперь показывает исключительно отгруженные заявки (`g.needsExpense === true`).
  - В логике окна введено разделение «отгруженных новых поставок» (использует фильтр `item.status === 'new' && isStockDeparted(item.ozonStatus)`), обновлены заголовки, подзаголовки со счетчиками и переименован бейдж в карточках заявок на «Отгружено поставок: N».
- Реализована автоматическая привязка уверенных Ozon-дублей:
  - В `src/components/OzonNewSuppliesModal.tsx` добавлен фоновый `useEffect` для автоматической привязки отгруженных Ozon-групп с вердиктом `duplicate` к соответствующей ручной отгрузке.
  - Привязка выполняется путем вызова `markExternalShipment` со статусом `'processed'` и сериализованными ID транзакций дублирующей ручной отгрузки.
  - При успешной привязке выводится тост-сообщение о сопоставлении, при ошибке — тост с рекомендацией сделать привязку вручную.
  - Автоматически привязанные группы отфильтрованы и скрываются из списка модального окна.
- Расширен режим сверки без даты для отгруженных заявок Ozon:
  - В `src/lib/ozonMatch.ts` изменена активация `noDateMode` в `matchOzonGroup` — теперь она срабатывает не только при READY_TO_SUPPLY, но и для любого отгруженного статуса (через `isStockDeparted`).
  - Это позволяет корректно сопоставлять отгруженные заявки Ozon без явной даты отгрузки с ручными списаниями за последние 7 дней, предотвращая ложные вердикты «none».
  - Обновлены комментарии к критериям сверки дубликатов.
- Вынесены переиспользуемые алгоритмы Ozon-поставок в отдельный модуль `src/lib/ozonGroups.ts`:
  - Функция `buildOzonGroups` осуществляет группировку по `orderId` -> `orderNumber` -> `postingId` и запускает сопоставление по отпечаткам (`matchOzonGroup`).
  - Кастомный хук `useProcessOzonGroup` инкапсулирует всю логику подготовки позиций, агрегации одинаковых SKU, сопоставления по штрихкоду/артикулу, расчета себестоимости через средневзвешенную со склада и превентивной проверки наличия на складе с выводом модального окна дефицита.
- Оптимизирован компонент `src/components/OzonSuppliesTab.tsx`: удален дублирующий код и заменен на импортированные вызовы `buildOzonGroups` и хука `useProcessOzonGroup`.
- Улучшена диагностика в `scheduledOzonCheck()` при ошибке вызова прокси: теперь во внутреннем catch-блоке `UrlFetchApp.fetch` возвращается оригинальный текст ошибки `fetchErr.toString()` для детального анализа причин сбоя в Script Properties.
- Обновлен паспорт проекта `docs/OZON_PLAN.md`: отмечен выполненным пункт 10 плана Ozon (интеграция автоопроса по расписанию, триггеров и блока управления), скорректирован реестр принятых решений и зафиксированы подробные заметки для следующего пункта (пункт 11).
- Обновлен паспорт проекта `docs/OZON_PLAN.md`: отмечен выполненным пункт 11 плана Ozon (интеграция всплывающего окна уведомлений о новых поставках Ozon с различением новых и ожидающих заявок, динамическими заголовками и localStorage), скорректированы заметки для следующего чата (переход к пункту 12: создание расхода при принятии на точке отгрузки).

## [2026-07-11]

### Изменено
- Обновлены правила и пороги сверки Ozon-заявок с ручными отгрузками:
  - Совпадением считается исключительно точный состав (одинаковые артикулы И одинаковые количества). Частичное совпадение по набору артикулов (`sameArticles`) больше не учитывается.
  - В режиме без даты (`noDateMode` при статусе `READY_TO_SUPPLY`) учитываются только свежие ручные отгрузки, созданные за последние 7 дней.
  - Окно уверенного совпадения по дате (`duplicate`) расширено до 5 дней (при наличии дат).
  - Сверка в обычном режиме без даты переведена на проверку `noDateMode` со свежестью ручной группы в 7 дней.
- Полностью переработан информационный блок совпадения с ручной отгрузкой в раскрытой Ozon-заявке:
  - Вместо громоздкой сетки характеристик и таблицы состава разработан компактный 3-строчный блок.
  - Строка 1 содержит заголовок («Возможный дубль...» для duplicate красным цветом или «Похожая ручная отгрузка...» для suspect янтарным).
  - Строка 2 содержит параметры поставки, объекта назначения и разницы дат в одну строку с разделителем ` · `.
  - Строка 3 содержит состав ручной отгрузки в одну строку в формате `Артикул ×Количество`.

### Исправлено
- Сбой сопоставления ручных отгрузок с Ozon-заявками из-за добавления к назначению транзакции сведений о дополнительных услугах (например, `[Упаковка: 300₽ ...]`). Реализована функция `normalizeDestination`, очищающая строку от квадратных скобок перед сопоставлением.

### Добавлено
- Успешно завершен и закрыт пункт 9 плана «Интеграция с Ozon» (Фаза 2: Сверка Ozon-поставок с ручными отгрузками по «отпечатку» и ручная привязка как дубль без повторного списания остатков).
- Реализована кнопка **«Привязать как дубль»** для Ozon-заявок с найденным совпадением ручной отгрузки:
  - Кнопка помечает заявку обработанной (`processed`) и связывает её с найденной ручной отгрузкой;
  - Списание со склада при этом не производится (остатки склада не меняются, расход уже списан в ручной транзакции);
  - Связь сохраняется в параметре `transGroupInfo` в виде JSON-строки с ID ручных транзакций, поэтому при удалении ручной отгрузки из Истории Ozon-заявка автоматически вернется в статус «Новые».
- Read-only механизм сверки Ozon-заявок с ручными отгрузками по «отпечатку» (состав SKU, даты и имя кабинета). Сверка выполняется автоматически на клиенте без модификации каких-либо данных или статусов.
- Цветовые бейджи-индикаторы на карточках вкладок «Поставки Озон»:
  - Красный бейдж **«Возможный дубль ручной отгрузки»** (при точном совпадении состава SKU, кабинета и близости дат до 2 дней);
  - Янтарный бейдж **«Похожа на ручную — проверьте»** (при точном совпадении состава в пределах 7 дней, либо совпадении набора артикулов с отличающимися количествами в пределах 2 дней).
- Информационный блок внутри раскрытой Ozon-заявки, выводящий детальное сопоставление с ручной отгрузкой: дату создания, дату планируемой поставки, объект назначения, разницу в днях, таблицу состава и вердикт о совпадении состава.
- Создан новый вспомогательный модуль `src/lib/ozonMatch.ts` для инкапсуляции алгоритмов сопоставления.

## [2026-07-10]

### Добавлено
- Кнопка «Вернуть в новые» на заголовок Ozon-заявки в `src/components/OzonSuppliesTab.tsx`. Кнопка отображается только для заявок, у которых нет новых поставок (`'new'`), но есть хотя бы одна оформленная (`'processed'`) или проигнорированная (`'ignored'`). По нажатию с модальным подтверждением все эти поставки массово сбрасываются обратно в статус `'new'`.
- Интегрирована проверка наличия остатков на складе при оформлении Ozon-заявок с детальным выводом дефицита в окне подтверждения.
- Скрытие обработанных Ozon-заявок (тех, у которых нет новых поставок) по умолчанию, чтобы не загромождать интерфейс. Добавлена галочка «Показать обработанные заявки (N)» для переключения видимости и возможности возврата их в статус «Новые».
- Поддержка полноценных JSX (ReactNode) в качестве описания подтверждения в `ConfirmDialog.tsx`. Это позволяет выводить дефицитные позиции красивым, структурированным списком с выделениями и переводами строк.
- Автоматическая привязка Ozon-поставок к созданным транзакциям через запись `groupId` в колонку `TransGroupInfo` листа «Внешние отгрузки» в Google Таблицах.
- Функция `returnLinkedOzonSupplies` в Zustand-сторе для отслеживания удалений транзакций и своевременного возврата Ozon-поставок в статус `'new'`.

### Исправлено
- Механизм привязки Ozon-заявок к транзакциям: привязка перенесена с `groupId` (который генерируется исключительно для комплектов и отсутствует у обычных товаров) на уникальные ID транзакций (`t.id`), гарантируя безотказный автовозврат любых оформленных поставок в статус «Новые» при удалении транзакций.
- Сигнатура метода `returnLinkedOzonSupplies`: убран избыточный второй параметр `transactionsBefore` благодаря прямому сопоставлению с сохранёнными ID транзакций.

### Изменено
- Обновлен паспорт проекта (`docs/OZON_PLAN.md`): пункт 8в («Мультикабинет Ozon») официально переведен в статус полностью завершенного и проверенного вживую пользователем на двух кабинетах (от 09.07.2026). Добавлены детальные архитектурные заметки, описание технических решений по привязке к транзакциям, логированию, сбросу статусов и оптимизации прокси-сервера.
- Изменены названия вкладок в боковом меню: «Загрузка» переименована в «Ручная загрузка», «Ozon» переименована в «Поставки Озон» для улучшения понятности интерфейса.
- Выравнены и обновлены соответствующие заголовки `h2` в `UploadTab.tsx` и `OzonSuppliesTab.tsx`.
- Расширен метод `updateExternalShipmentStatus` в `Code.gs` для поддержки сохранения поля `TransGroupInfo` и работы со статусом `'new'` с динамическим поиском колонок.
- Обновлен метод `commitTransaction` в Zustand-сторе для передачи привязанных `groupId` при оформлении Ozon-поставки.
- Интегрирована функция `returnLinkedOzonSupplies` в обработчики одиночного (`handleDeleteTransaction`) и группового (`handleDeleteMultipleTransactions`) удаления операций.
- Блок вывода дефицита в Ozon-оформлении переведен на структурированный JSX с подробным указанием нехватки по каждому товару и выделением недостающего количества красным цветом.
- Расширен тип поля `message` в состоянии диалога подтверждения в `useUIStore.ts` и `ConfirmDialog.tsx` с `string` до `React.ReactNode`.
- Воронка поставок Ozon на главном дашборде (`src/components/Dashboard.tsx`) переведена на отображение исключительно активных (новых) поставок со внутренним статусом `'new'` в соответствии с дефолтным отображением на вкладке Ozon.
- Определение себестоимости при подготовке Ozon-заявки переведено на стор-хелпер `getEffectiveAvgCost` (с поддержкой подсчета суммарной себестоимости для виртуальных комплектов).
- Расширен тип принимаемого статуса в методе `markExternalShipment` интерфейса `WarehouseState` в `src/store/useWarehouseStore.ts` до `'processed' | 'ignored' | 'new'` для поддержки сброса статуса в новые на уровне фронтенд-стора.

## [2026-07-09]

### Добавлено
- На вкладку «Ozon» (`src/components/OzonSuppliesTab.tsx`) выведен красивый и лаконичный небесно-голубой бейдж с названием кабинета Ozon (магазина), к которому относится каждая сгруппированная заявка. Название подгружается из поля `cabinet` отгрузок.
- Добавлена интерактивная панель фильтрации по Ozon-магазинам (кабинетам) на вкладке «Ozon» (`src/components/OzonSuppliesTab.tsx`). Панель автоматически отображается в случае, если в системе присутствуют 2 и более различных кабинетов, позволяя быстро отфильтровать список заявок в один клик.
- В модальное окно подтверждения операции `ConfirmModal.tsx` интегрирован селектор выбора магазина (кабинета) Ozon. Блок отображается и становится обязательным к заполнению только в случае, если в системе 2 или более кабинетов и оформляется расход на маркетплейс «Ozon».
- Добавлена валидация выбора магазина в `ConfirmModal.tsx`: если магазин не выбран, кнопка «Подтвердить» блокируется, над кнопками выводится красное сообщение об ошибке «Заполните обязательные поля: «Магазин Ozon»», а сам блок выбора подсвечивается красной рамкой. Ошибка и подсветка реактивно гаснут при клике на любой магазин.
- На вкладку «Ozon» (`src/components/OzonSuppliesTab.tsx`) для поставок с внутренним статусом `'new'` добавлены интерактивные кнопки «Оформить» (выполняет сопоставление SKU по штрихкоду/артикулу и открывает `ConfirmModal`) и «Игнорировать» (вызывает диалог подтверждения перед скрытием поставки).
- Добавлено групповое оформление и игнорирование на уровне заголовка всей Ozon заявки. Кнопки перенесены с индивидуальных поставок на заголовок группы, если в группе есть новые поставки. Оформление агрегирует и суммирует одинаковые артикулы по всей заявке за раз, а игнорирование обрабатывает все новые поставки заявки за одно подтверждение.
- Скорректирован источник себестоимости при оформлении групповой Ozon-заявки: теперь в первую очередь используется средняя себестоимость со склада (из листа Остатки), а базовая цена из SKU Базы применяется только в качестве резервного источника (если товара нет в наличии).
- В функции `updateExternalShipmentStatus` в `Code.gs` добавлено разрешение третьего допустимого статуса `'new'` (в дополнение к `'processed'` и `'ignored'`) для возможности сброса статуса FBO отгрузок Ozon назад в «Новые».

### Изменено
- Блок «Отгрузки Ozon Seller» полностью удален из вкладки «Загрузка» (`src/components/UploadTab.tsx`) вместе со всеми сопутствующими импортами, состояниями, хуками и колбэками. Вкладка стала чище, а все операции с FBO поставками теперь централизованы на вкладке «Ozon».
- Переработан стор `useWarehouseStore`: свойство `pendingOzonPostingId` заменено на массив `pendingOzonPostingIds` для массового перевода обработанных поставок в статус `'processed'`.
- Модифицирована функция `getGlobalSettings` in `Code.gs`: теперь список названий кабинетов Ozon (без API-ключей) отдается всем пользователям в поле `ozonCabinetNames`. Ключи и полные объекты кабинетов по-прежнему видит только администратор. Это необходимо для возможности выбора кабинета при оформлении отгрузок рядовыми пользователями.
- Расширен тип `ExternalShipment` in `src/types.ts` полем `cabinet` для типизации данных, приходящих из Google Таблиц.
- Исправлен статус пункта 8в («Мультикабинет Ozon») в `docs/OZON_PLAN.md` на «в работе» (⬜) для точного соответствия фактическому прогрессу (реализация фронтенда — бейджи кабинетов и выбор в ConfirmModal — еще впереди). Восстановлено детальное описание заметок для следующего чата и решений по пункту 8в.
- Обновлен Zustand-стор настроек (`src/store/useSettingsStore.ts`): добавлено поле `ozonCabinetNames` и экшен `setOzonCabinetNames` для безопасного хранения названий кабинетов на клиенте.
- Модифицированы обработчики `checkSession` и `handleLogin` в `src/store/useWarehouseStore.ts` для заполнения стора настроек списком `ozonCabinetNames` при получении глобальных настроек от Google Apps Script.
- Изменена логика обработки отгрузок в `src/components/UploadTab.tsx`: при обработке Ozon-отгрузок автоматически подставляется кабинет из параметров поставки, формируя назначение по маске `Ozon (Имя кабинета)`.
- Доработано модальное окно `ConfirmModal.tsx`: выбранное пользователем имя магазина (или единственный доступный кабинет Ozon) автоматически конкатенируется с назначением отгрузки в формате `Ozon (Название магазина)` для последующей записи в транзакцию.

### Исправлено
- Исправлена критическая ошибка инициализации во фронтенде: `Uncaught ReferenceError: Cannot access 'ozonCabinetNames' before initialization` в `ConfirmModal.tsx`, возникшая вследствие обращения к переменной `ozonCabinetNames` в инициализаторе `needCabinetChoice` выше строки её объявления. Переменная поднята наверх блока.

## [2026-07-08]

### Добавлено
- Интегрирована поддержка двух Ozon-кабинетов в Zustand-стор настроек (`src/store/useSettingsStore.ts`), добавлено новое поле состояния `ozonCabinets` (массив `OzonCabinet`) и экшен `setOzonCabinets` для глобального управления кабинетами.
- Во вкладке «Настройки» (`src/components/SettingsTab.tsx`) разработан полностью новый блок «Интеграция Ozon Seller» под два кабинета. Каждому кабинету предоставлены поля ввода «Название кабинета», «Client-Id», «Api-Key» и кнопка «Проверить ключи и подтянуть название», отправляющая запрос к прокси-серверу `/api/ozon/seller-info` и автоматически заполняющая имя кабинета.
- Разработан и интегрирован новый эндпоинт `/api/ozon/seller-info` в `server.ts` для проверки ключей Ozon и автоматического получения названия кабинета. Эндпоинт производит предварительную верификацию сессии, обращается к методу `/v1/seller/info` Ozon API и детально обрабатывает блокировки и ошибки авторизации, возвращая понятные ответы на русском языке.
- Внедрена поддержка новой колонки «Кабинет» в лист «Внешние отгрузки» Google Таблиц (`Code.gs`). Автоматически поддерживается её добавление, точечная запись и обновление (без затирания пустыми значениями), а также чтение и выдача клиенту.
- Реализована возможность сворачивания и разворачивания аналитического блока «Воронка поставок Ozon» на дашборде (`src/components/Dashboard.tsx`) с сохранением состояния разметки и плавной индикацией через поворот иконки-стрелки `ChevronDown`.

### Изменено
- Переведён эндпоинт `/api/ozon/check` в `server.ts` на параллельный/раздельный опрос нескольких кабинетов Ozon с автоматическим переиспользованием индивидуальных Client-Id и Api-Key каждого кабинета. Добавлена полная обратная совместимость со старым форматом настроек без массива кабинетов.
- Модифицирован стор `useWarehouseStore.ts`: при получении глобальных настроек (`getGlobalSettings`) в методах `checkSession` и `handleLogin` массив Ozon-кабинетов корректно извлекается и передается в Zustand-стор настроек.
- Обновлен паспорт проекта (`docs/OZON_PLAN.md`): пункт 8в («Мультикабинет Ozon») официально переведен в статус полностью завершенного.
- Задокументированы результаты живой разведки Seller API по методу `POST /v1/seller/info` (метод `SellerInfo` добавлен в `docs/OZON_API.md`). Подтверждена отдача названия кабинета в `company.name`, описаны ключевые поля, грабли с блокировками (403 code 7), типами подписок и обработкой потенциальных null-значений в рейтингах.
- Внедрена поддержка мультикабинетов в Google Apps Script (`Code.gs`): хранение ключей переведено на свойство `global_ozonCabinets` (JSON-массив). Добавлена автоматическая миграция старых ключей в первый элемент массива для сохранения обратной совместимости.
- Обновлен паспорт проекта (`docs/OZON_PLAN.md`): в пункт 8в добавлен автоматический сбор названия кабинета через `POST /v1/seller/info` в рамках кнопки валидации ключей в Настройках, с сохранением поля названия доступным для ручной корректировки как запасной вариант.
- Обновлен паспорт проекта (`docs/OZON_PLAN.md`): пункт 8б («Оптимизация синхронизации») официально отмечен как выполненный (✅ 08.07.2026). Подробно расписаны все технические решения: точечная запись ячеек в `Code.gs` с нормализацией дат в `normCell`, условная перезапись ПозицииJSON и даты отгрузки непустыми значениями, интеллектуальное кэширование состава в `server.ts` на основе статуса Ozon и наличия состава, а также умная фильтрация неактуальных поставок (`OVERDUE`/`CANCELLED`). В план добавлен новый пункт 8в («Мультикабинет Ozon») с детально зафиксированным пулом решений (Script Properties, HMAC-метод getOzonKeys, SettingsTab, ConfirmModal).

### Оптимизировано
- Оптимизирована функция `saveExternalShipments` in `Code.gs`: внедрено поколоночное сравнение (`setIfChanged`) значений перед записью, предотвращая лишнюю перезапись одинаковых данных в ячейках Google Таблиц и исключая ложные инкременты `updatedCount`.
- Добавлена строгая проверка непустых значений для даты отгрузки (`shipmentDate`) и состава (`itemsJSON`) при статусе `new`, защищая от случайного стирания ранее полученных данных пустыми значениями от прокси-сервера.
- Оптимизирован эндпоинт `/api/ozon/check` в `server.ts`: теперь запрашиваемый у Ozon состав (`/v1/supply-order/bundle`) кэшируется интеллектуально. Для отслеживаемых поставок со статусом `new` повторный запрос выполняется только тогда, когда статус Ozon изменился или сохраненные ранее данные состава пусты. Это кардинально снижает нагрузку на Ozon API и устраняет риск получения ошибок 429 (Too Many Requests).
- В эндпоинте `/api/ozon/check` оптимизирована фильтрация статусов: исключен статус `OVERDUE` из опроса активных поставок Ozon API (чтобы не тянуть просроченные поставки, которые не планируется отгружать), и добавлен в список финальных статусов для уже отслеживаемых поставок (исключая их повторный допрос). Новые не отслеживающиеся ранее поставки со статусами `CANCELLED` или `OVERDUE` теперь полностью игнорируются, разгружая базу данных и лист от лишних строк.

### Исправлено
- Исправлен баг сравнения дат в хелпере `normCell` функции `saveExternalShipments` (`Code.gs`): если дата в ячейке Google Таблицы имеет нулевое время (`00:00:00`), она форматируется в виде `yyyy-MM-dd` без часов, минут и секунд, благодаря чему строковые значения дат от прокси-сервера теперь корректно сопоставляются с ячейками без ложных перезаписей при синхронизации.

## [2026-07-07]

### Добавлено
- Добавлен интерактивный аналитический блок «Воронка поставок Ozon» на главный дашборд приложения (`src/components/Dashboard.tsx`). Блок доступен только администраторам, автоматически подгружает данные из Zustand-стора без вызова Ozon API, считает количество поставок по каждому статусу (с сортировкой по воронке), поддерживает кликабельность для быстрого перехода к Ozon-поставкам и выводит детальную сводку (общее число поставок, уникальные заявки, максимальная дата синхронизации).
- Разработан новый вспомогательный модуль `src/lib/ozonStatus.ts` для централизованной работы со справочником Ozon-статусов, цветовыми стилями бейджей и логикой воронки, на который переведены компоненты `Dashboard.tsx` и `OzonSuppliesTab.tsx`.
- Интегрирована автоматическая расшифровка `macrolocal_cluster_id` в понятные человекочитаемые названия направлений (город/регион) через Ozon API `POST /v1/cluster/list`.
- Внедрен in-memory кэш справочника кластеров Ozon в `server.ts` с TTL 24 часа и механизмом отказоустойчивости (при сбое API возвращается старый кэш, не прерывая общую синхронизацию).
- Документирован справочный endpoint Ozon API `POST /v1/cluster/list` (`ClusterList`) в файле `docs/OZON_API.md`: подробно описаны параметры, структура ответа с `macrolocal_cluster_id`, а также правила разбора и маппинга направлений поставок и типов складов.
- В паспорте проекта `docs/OZON_PLAN.md` пункт 8а («Экран Ozon-поставок в приложении») официально переведен в статус полностью выполненного (✅ 08.07.2026). Отражены все технические аспекты реализации (ролевой доступ, иерархический список, кнопка синхронизации, in-memory кэширование, обработка ошибок проксирования) и обновлены заметки на следующий этап (пункт 8).

### Изменено
- Исправлен баг извлечения склада хранения Ozon вРЦ (crossdock) в `server.ts`: для кросс-докинговых поставок Ozon не возвращает поле `storage_warehouse.name`. Реализован автоматический фолбек на поле `macrolocal_cluster_id` в виде текстового обозначения `"Кластер <ID>"`, что гарантирует заполненность и информативность колонки «Склад хранения» у всех типов поставок.
- Обновлен паспорт проекта (`docs/OZON_PLAN.md`):
  - Пункт 7 плана (Перевод синхронизации на Ozon API v3) отмечен как полностью выполненный с описанием технических деталей решения (учет по supply_id, пакетная выгрузка, upsert в GAS, поддержка devMode и защита от превышения лимитов 429).
  - Добавлены два новых пункта в Фазу 1: пункт 8а (Разработка экрана Ozon-поставок с развертыванием состава по складам направления) и пункт 8б (Оптимизация синхронизации с выборочным обновлением строк).
  - Актуализирован раздел «Заметки для следующего чата» для передачи фокуса разработки на интерфейс просмотра поставок (пункт 8а).
- В функции `fetchOzonApi` в `server.ts` реализована защита от превышения лимитов запросов (ошибка HTTP 429) и ограничение частоты:
  - Добавлен вспомогательный хелпер `sleep` и переменная `lastOzonCallAt` для отслеживания времени последнего запроса.
  - Установлен минимальный интервал в 400 мс между последовательными вызовами Ozon API для предотвращения пиковых нагрузок.
  - Добавлен автоматический повтор при получении статуса 429 (до 5 попыток) с экспоненциальным нарастанием паузы (1000 мс * номер попытки: 1с, 2с, 3с, 4с).
  - При сохранении статуса 429 после всех попыток генерируется структурированная ошибка с кодом 429 и понятным пользователю сообщением о необходимости повторить синхронизацию через минуту.
- Функция `checkOzonShipments` в Zustand-сторе (`src/store/useWarehouseStore.ts`) обновлена для вычисления и передачи флага `devMode: true` в POST-запрос к прокси-эндпоинту `/api/ozon/check` (если у текущего пользователя роль администратора и включен глобальный режим разработки).
- Текст успешного выполнения синхронизации (`toast.success`) в `checkOzonShipments` расширен для отображения количества обновленных записей (`updated`).
- Интерфейс `ExternalShipment` в `src/types.ts` расширен семью новыми необязательными полями (`orderId`, `orderNumber`, `ozonStatus`, `ozonStatusDate`, `dropOffWarehouse`, `storageWarehouse`, `timeslot`) для полноценной типизации Ozon-специфичных полей без изменения исходных полей.
- Обработчик `/api/ozon/check` в `server.ts` полностью переписан на актуальную цепочку Ozon API v3:
  - Шаг 2: Получение активных заявок через POST `/v3/supply-order/list` с постраничной пагинацией по `last_id` (лимит 100 на страницу, максимум 20 страниц).
  - Шаг 3: Слияние с ранее отслеживаемыми поставками из GAS (запрос `getExternalShipments`) и расширение набора заявок за счет поставок с незавершенными статусами для обновления финального статуса в таблице.
  - Шаг 4: Пакетный сбор подробностей по заявкам через POST `/v3/supply-order/get` порциями максимум по 50 `order_ids` (строкового типа).
  - Шаг 5: Формирование плоских записей на уровне конкретных поставок (`supply_id`) внутри заявок с разбором вРЦ и заполнением Ozon-специфичных полей и даты отгрузки.
  - Шаг 6: Постраничный сбор состава поставок через POST `/v1/supply-order/bundle` по одному `bundle_id` за раз только для новых записей или со статусом `'new'` (в целях экономии лимитов).
  - Шаг 7: Синхронизация пакета данных с GAS через `saveExternalShipments` с пробросом `devMode` и точной обработкой результатов.
- Функция `fetchOzonWithFallback` удалена из `server.ts` и заменена на `fetchOzonApi` для прямого вызова эндпоинта Ozon с сохранением stage и подробностей ошибок в формате JSON/тексте.
- Функция `saveExternalShipments` в `Code.gs` полностью переделана в форму upsert. Теперь при передаче поставок с существующими `PostingID` точечно обновляются новые Ozon-специфичные поля, а поля `'Дата отгрузки'` и `'ПозицииJSON'` обновляются только для строк с внутренним статусом `'new'`.
- Функция `getExternalShipments` в `Code.gs` расширена для считывания 7 новых Ozon-специфичных колонок по динамическим индексам с проверкой на отсутствие и безопасным форматированием дат.
- Метод создания листа `'Внешние отгрузки'` в `setupDatabase` переведен на использование глобальной константы `EXTERNAL_SHIPMENTS_HEADERS`.
- В паспорте проекта `/docs/OZON_PLAN.md` в раздел «Как работать с этим файлом» добавлено критически важное правило персистентности: все файлы репозитория должны создаваться и изменяться исключительно внутри рабочей среды AI Studio. Внешние коммиты (например, через Claude Code) будут перезаписаны при публикации.
- В паспорте проекта `/docs/OZON_PLAN.md` пункт 6 (Разведка API Ozon через MCP) переведен в завершенный статус ✅. Задокументированы живые примеры ответов, справочники статусов, особенности пагинации и работы со складами во внешнем файле `docs/OZON_API.md`. Сделан вывод об отсутствии актов приёмки с пересортом по API, пункт 15 плана будет реализовываться через ручной ввод актов. Обновлен раздел заметок для следующего чата.
- В паспорте проекта `/docs/OZON_PLAN.md` пункт 5 (Регресс стандартного режима) переведен в завершенный статус по результатам полного 21-шагового сценария регрессионного тестирования на боевой БД. Описаны результаты тестирования и выявленный некритичный баг дробления группы в Истории при восстановлении комплектов из корзины.
- Актуализирован статус пункта 4 в паспорте проекта `/docs/OZON_PLAN.md` (Режим разработки переведен в статус выполненного с описанием технических средств клиентской и серверной частей). Обновлен раздел заметок для следующего чата (вводные по проведению регрессионного тестирования стандартного боевого режима).

### Добавлено
- Новый интерактивный React-компонент `src/components/OzonSuppliesTab.tsx` для отображения поставок Ozon в режиме read-only с группировкой по `orderId` -> `orderNumber` -> `postingId`, иерархическим раскрытием состава и проверкой лимитов.
- Защищённый рендеринг и интеграция вкладки Ozon во все охранные механизмы в `src/App.tsx` (доступ только для администраторов, автоматический сброс не-админов).
- Установка NPM-пакета `motion` в `package.json` для поддержки плавных переходов при раскрытии иерархических списков.
- Повышена отказоустойчивость проксирования GAS запросов в `server.ts`: теперь сервер сперва читает сырой текстовый ответ, благодаря чему при сбоях развертывания веб-приложения Google Apps Script (когда возвращается HTML-страница ошибки или авторизации вместо JSON) клиент получает ясное описание проблемы вместо общего сбоя парсинга.
- В `Code.gs` добавлена глобальная константа `EXTERNAL_SHIPMENTS_HEADERS`, содержащая полный список из 13 заголовков для листа «Внешние отгрузки» (6 старых + 7 новых Ozon-специфичных колонок: `'OrderID'`, `'Номер заявки'`, `'Статус Ozon'`, `'Дата статуса Ozon'`, `'Пункт отгрузки'`, `'Склад хранения'`, `'Таймслот'`).
- В `getExternalShipmentsSheet` добавлена процедура автоматической дописи недостающих колонок через `ensureColumns` при инициализации листа.
- Воссоздан подробный файл технической документации `/docs/OZON_API.md` с разведкой Ozon API для FBO поставок (структура, лимиты, статусы, особенности пагинации).
- Добавлена клиентская часть «Режима разработки» в `src/store/useWarehouseStore.ts`, `src/components/SettingsTab.tsx` и `src/App.tsx`.
- Реализована интеграция стейта `devMode` (хранится в localStorage, не сохраняется в persist-конфигурации Zustand), который динамически прокидывается в тело запросов `fetchGas` для администраторов и принудительно перезагружает складские данные и архивные транзакции при переключении.
- Добавлен визуальный тумблер управления режимом разработки в административную панель вкладки «Настройки» с выводом информационных тостов (`toast.info`).
- Настроено контрастное визуальное оформление активного режима разработки: толстая красная рамка `border-4 border-red-600` по периметру экрана и фиксированная плашка в верхней части экрана.
- Добавлена серверная часть «Режима разработки» в `Code.gs`. Реализована условная маршрутизация входящих запросов администраторов на тестовую копию базы данных: если `payload.devMode === true` (и действие не входит в список исключенных служебных действий), централизованная таблица переключается на файл тестовой БД, определенный по свойству `test_dbSpreadsheetId`.
- Переведены на использование универсальной функции `getSpreadsheet()` 5 служебных функций миграций в `Code.gs` (`migrateKitCosts`, `cleanupZeroCostRows`, `migrateDatesToISO`, `migrateBowlKitsToVirtual`, `migrateComponentWriteOffCosts`) вместо прямого вызова `SpreadsheetApp.getActiveSpreadsheet()`.

## [2026-07-06]

### Изменено
- Исправлен баг обновления транзакций в Zustand-сторе (`src/store/useWarehouseStore.ts`): в функции `handleUpdateTransaction` при успешном изменении транзакции на сервере и получении актуального массива `payloadData.newTransactions` состояние транзакций теперь перезаписывается целиком вместо склеивания через Map. Это решает проблему фантомного отображения устаревших строк компонентов в пользовательском интерфейсе до перезагрузки страницы.
- Исправлен баг каскадного удаления строк на бэкенде (`Code.gs`): в функции `deleteTransaction` при каскадном удалении компонентов комплекта теперь динамически пересчитывается `rowIndex` главной транзакции, если удаляемый компонент находится выше по списку. Это предотвращает рассинхронизацию индексов и ошибочное удаление соседних строк или оставление главной строки как дубликата.
- Исправлен баг фильтрации старых версий транзакций в корзине (`Code.gs`): в функции `getArchivedItems` проверка типа `UpdatedVersion` перенесена с поля типа столбца `row[1]` на чтение поля `type` внутри JSON-колонки `row[3]`. Это гарантирует, что промежуточные версии обновленных транзакций больше не будут засорять корзину.
- Исправлен баг карточки «Всего отгрузок» на вкладке себестоимости в режиме «По месяцам» (`src/components/ShipmentCostTab.tsx`): вместо количества уникальных строк артикулов сводки (`monthlySummary.length`) теперь отображается корректное общее количество физических отгрузок в выбранном месяце (`monthlyShipmentCount`), которое рассчитывается через группировку по парам «дата создания + объект назначения».
- Реализована полноценная поддержка переключателя даты `dateMode` («По дате отгрузки / По дате создания») во вкладке себестоимости в режиме «По месяцам» (`src/components/ShipmentCostTab.tsx`). Теперь выбор режима даты корректно перестраивает список доступных месяцев в выпадающем списке `availableMonths`, а также фильтрует транзакции в `monthlySummary` и `monthlyShipmentCount` согласно выбранному критерию.

### Добавлено
- Создан паспорт проекта «Интеграция с Ozon» (автопилот) в `/docs/OZON_PLAN.md`, регламентирующий реестр принятых проектных решений, фазы разработки и правила взаимодействия между чатами при интеграции с Ozon Seller API.
- Добавлена кнопка «Резервная копия БД» во вкладку настроек (`src/components/SettingsTab.tsx`) и соответствующий бэкенд-обработчик `backupDatabase()` в `Code.gs`. Функция осуществляет копирование Google-таблицы в специальную папку «Резервные копии БД Склад» на Google Диске, присваивая копии уникальное имя с таймстампом даты и времени создания. Доступно только администраторам.
- Актуализированы статусы выполнения и добавлены новые правила работы с планом в паспорте проекта `/docs/OZON_PLAN.md`. Пункт 2 помечен как выполненный, добавлены требования для архитекторов перед открытием и закрытием каждого нового чата разработки.
- Добавлена кнопка «Создать/обновить тестовую БД» во вкладку настроек (`src/components/SettingsTab.tsx`) в теплых янтарных (amber) тонах и бэкенд-функция `createOrUpdateTestDatabase()` в `Code.gs`. Функция копирует боевую таблицу в папку «Тестовая БД Склад» на Диске, производит очистку списков пользователей и сессий с сохранением заголовков, запускает инициализацию структуры через `setupDatabase` и автоматически удаляет старую копию (если она существовала).
- Внедрен компонент подтверждения `ConfirmDialog` вместо стандартного `window.confirm` в кнопке создания/обновления тестовой базы данных в `src/components/SettingsTab.tsx`, так как стандартное всплывающее окно блокируется в песочнице iframe приложения.
- Переписан механизм удаления старых копий тестовых БД в `createOrUpdateTestDatabase()` в `Code.gs`. Теперь скрипт итерируется по всем файлам в целевой папке «Тестовая БД Склад» и отправляет в корзину все файлы кроме новосозданной тестовой копии, делая очистку папки максимально надежной.
- Актуализированы статусы выполнения в паспорте проекта `/docs/OZON_PLAN.md` (пункт 3 переведен в статус выполненного с описанием внедренного технического решения), а также дополнен раздел заметок для следующего чата разработки (вводные по Свойствам Скрипта, обращению к `getActiveSpreadsheet` и исключению использования `window.confirm`).

## [2026-07-05]

### Добавлено
- Добавлен переключатель режимов фильтрации и сортировки по дате («По дате отгрузки» / «По дате создания») на вкладке расчета себестоимости отгрузки (`src/components/ShipmentCostTab.tsx`), позволяющий гибко сопоставлять данные как по фактическим поставкам, так и по датам заведения документов в систему.
- Добавлена функция `getEffectiveAvgCost(article: string): number` в Zustand-стор (`src/store/useWarehouseStore.ts`) для расчета средней себестоимости виртуальных комплектов по сумме составляющих компонентов, умноженных на их нормы расхода.
- Добавлена «Панель администратора» на вкладке «Справочник» (`src/components/DirectoryTab.tsx`) с инструментом ремонта данных «Пересчитать капитализацию из себестоимости». Кнопка снабжена серым текстом-пояснением «Используйте после ручной правки себестоимости в таблице» и защищена подтверждающим диалогом.
- Добавлена функция `recalcCapitalizationFromAvg()` на бэкенде (`Code.gs`), которая вычисляет капитализацию каждого товара как произведение количества на среднюю себестоимость на листе «Остатки» и обновляет значения в ячейках. Возвращает сведения об изменённых строках и обновлённый список остатков.
- Добавлен обработчик экшена `'recalcCapFromAvg'` в `doPost` на бэкенде (`Code.gs`) с обязательной авторизационной проверкой прав администратора.

### Изменено
- Заблокировано ручное редактирование себестоимости для транзакций типа «Расход» в модальном окне редактирования операции (`src/components/EditTransModal.tsx`): поле ввода сделано недоступным для изменения, снабжено стилями `bg-slate-100 text-slate-400 cursor-not-allowed` и дополнено серой текстовой подсказкой. Для транзакций «Приход» цена остается полностью изменяемой.
- Обновлена фильтрация транзакций на вкладке расчета себестоимости отгрузки (`src/components/ShipmentCostTab.tsx`): теперь из списка `shipmentTransactions` исключаются записи, у которых в поле назначения (`destination`) содержатся слова "Списание" или "Миграция" (без учета регистра), чтобы не искажать коммерческие показатели отгрузок. При этом данные записи продолжают оставаться доступными и управляемыми на вкладке «История операций».
- Обновлена функция `getArchivedItems` на бэкенде (`Code.gs`): теперь при формировании списка удаленных транзакций для корзины автоматически пропускаются записи с типом `UpdatedVersion` (старые версии отредактированных транзакций). Сами записи остаются в листе «Удаленное» в качестве резервной истории изменений, но больше не отображаются в корзине и не приводят к дублированию при восстановлении.
- Обновлена валидация распознанных позиций ИИ при парсинге накладных: для виртуальных комплектов себестоимость теперь рассчитывается на основе компонентов склада через `getEffectiveAvgCost(article)` вместо обращения к собственному нулевому остатку на складе.
- Доработана модалка подтверждения операции `ConfirmModal.tsx` для отгрузок (Расход): в колонке «Себест.» теперь выводится итоговая себестоимость `item.price` (с учетом распределенных упаковок, прочих затрат и услуг подрядчиков), а если она отличается от базовой цены, снизу выводится серый мелкий текст «базовая: X ₽». Добавлено состояние `missingFieldsError`, благодаря которому при наличии незаполненных обязательных полей (дата поставки или стоимость упаковки) над кнопками отображается заметный статический красный блок-предупреждение с перечнем ошибок, а сами поля ввода подсвечиваются красной рамкой (`border-red-400 focus:ring-red-500`). Подсветка и красный блок динамически скрываются при заполнении соответствующих полей пользователем.
- Исправлено распределение накладных услуг в `getServicesExtraPerUnit` в случае `totalBaseValue === 0`: теперь услуги распределяются пропорционально количеству товаров (`доля = item.quantity / totalQuantity`, `extraPerUnit = totalServicesCost * доля / item.quantity`), а если общее количество товаров равно 0 — возвращается 0.
- В нижнюю панель модалки рядом с блоком «Общая сумма» добавлен блок «Доп. расходы», рассчитывающий суммарные расходы (упаковка, прочее и услуги) динамически «на лету» при любом изменении услуг, упаковки, прочих расходов или даты поставки.

## [2026-07-04]

### Добавлено
- Добавлено группирование удаленных позиций комплектов в одну строку на вкладке корзины (`src/components/DeletedItemsTab.tsx`). Записи с одинаковым `groupId` и `deletedAt` группируются и выводятся с пометкой «комплект: N компонентов» по данным главной строки. По клику на пометку строка плавно раскрывает список компонентов комплекта только для просмотра. Действия восстановления и выбор галочки для группы применяются одновременно ко всем ее элементам.
- Добавлен детализированный диалог-подтверждение перед массовым удалением транзакций на вкладке «История операций» (`HistoryTab.tsx`). Он отображает заголовок «Удалить N записей?», список удаляемых строк (тип, артикул, количество, дата) с лимитом до 15 записей и предупреждение красным цветом, если среди выбранных есть списания. При отмене выбора выделение строк сохраняется.
- Добавлена новая колонка «Литраж, л» в таблицу номенклатуры на вкладке «Справочник SKU» (`src/components/SkusTab.tsx`) после колонки «Комплект». Значение выводится из свойства `s.volumeLiters`, если оно больше нуля, иначе выводится серый прочерк («—»). Колонка поддерживает динамическую сортировку по объёму.
- Добавлено автоматическое предзаполнение количества услуг, содержащих в названии слово "забор" (без учёта регистра), значением 1 при открытии модального окна подтверждения отгрузки (`opType === 'Расход'`) в `ConfirmModal.tsx`. Пользователь может изменить это значение вручную (включая установку в 0). Правило не применяется для операций прихода.
- Добавлено глобальное поле «Коробок на паллете (среднее)» на вкладку справочников в `DirectoryTab.tsx`. Значение сохраняется в Script Properties на бэкенде под ключом `'global_boxesPerPallet'` и синхронизируется с глобальными настройками склада. Доступно для редактирования только администраторам.


### Изменено
- Исправлено массовое восстановление транзакций-списаний в `restoreMultipleArchivedItems` (`Code.gs`): при инкрементальном откате остатков для расходов-списаний (где назначение содержит "Списание") теперь изменяется только количество товара (`qtyDiff`), а капитализация (`capDiff`) остаётся неизменной, предотвращая ложное списание/изменение капитализации на листе «Остатки».
- Добавлена защита виртуальных комплектов в функцию `restoreTransaction` (`Code.gs`): при восстановлении из корзины строки расхода виртуального комплекта (где `writeOffCost === 0`), функция полностью игнорирует изменение остатка, капитализации и средней себестоимости данного артикула на листе «Остатки», при этом сама транзакция штатно вносится в лист истории транзакций.
- Исправлена функция `expandIdsWithCascade` (`src/store/useWarehouseStore.ts`): полностью удалена логика группировки по датам/назначениям и автоматического подтягивания транзакций с `total === 0`. Каскадное удаление теперь выполняется строго по `groupId` исключительно для связанных компонентов комплектов, исключая удаление несвязанных операций списания с совпадающими артикулами.
- Изменена логика расчёта паллет при отгрузке: вместо индивидуальных настроек `boxesPerPallet` у SKU теперь используется единое общее значение для всего склада (`boxesPerPalletGlobal`). Если в отгрузке суммарно 10 коробок и больше — отгрузка идёт на паллетах, количество паллет рассчитывается по формуле `Math.ceil(общее количество коробок / boxesPerPalletGlobal)`. Если коробок меньше 10 — паллеты не используются (0).
- Обновлено предзаполнение услуг со словом "паллет" в названии при подтверждении отгрузки — теперь значение по умолчанию равно расчетному числу паллет по новым правилам (включая 0, если коробок меньше 10).
- Из модального окна редактирования SKU (`SkuModal.tsx`) и инициализации добавления нового SKU (`SkusTab.tsx`) убрано поле «Коробок на паллете» за ненадобностью для редактирования конкретных позиций, при этом колонка и само свойство в бэкенде сохранены для совместимости с архивными данными.
- Исправлено проведение себестоимости при повторном применении восстановленной транзакции в `restoreTransaction` (`Code.gs`): для расходов-списаний (где объект получателя содержит "Списание") количество уменьшается как обычно (`newQty -= qty`), но капитализация товара не уменьшается (при остатке больше нуля, при обнулении остатка сбрасывается в 0). Средняя себестоимость динамически пересчитывается. Для обычных расходов (отгрузок) поведение осталось неизменным.
- Восстановленная строка в `restoreTransaction` теперь сохраняет метаданные `groupId` и `isComponent`, добавляя их значения в конец вызова `transSheet.appendRow`, предотвращая их утерю при перемещении транзакций из корзины.
- Согласован откат остатков при удалении транзакций-списаний в `deleteTransaction` и `deleteMultipleTransactions` (`Code.gs`): при удалении или массовом удалении расходов-списаний (где объект содержит "Списание") количество возвращается как обычно (+qty), но капитализация товара не увеличивается (так как при списании она не уменьшалась). Средняя себестоимость при этом пересчитывается на основе нового количества и прежней капитализации. Данное поведение также распространяется на каскадный откат компонентов комплектов. Для обычных расходов и отгрузок логика осталась без изменений.
- Изменён учёт себестоимости при списании товаров (`Code.gs`): если транзакция расхода направляется на списание (поле «Объект»/destination содержит слово "Списание"), количество товара уменьшается как обычно, но его капитализация не уменьшается (при остатке больше нуля). В результате себестоимость списанной партии перераспределяется на оставшиеся на складе единицы, динамически увеличивая среднюю себестоимость артикула. Если остаток падает до 0, капитализация обнуляется. При этом в строку транзакции корректно пишется вычисленный `writeOffCost` (средняя себестоимость до списания × списанное количество) для сохранения аналитических отчётов.
- Оптимизировано восстановление удаленных транзакций в `restoreMultipleArchivedItems` (`Code.gs`): вместо деструктивного полного пересчёта склада `recalculateStockFully` реализован инкрементальный откат остатков на базе накапливания изменений в `stockChanges` (с обратными знаками: +qty для 'Приход', -qty для 'Расход'). Добавлена проверка виртуальных комплектов, предотвращая ложное изменение остатков. В случае ухода остатка в минус, значение принудительно устанавливается в 0 с выводом предупреждения.

## [2026-07-03]

### Добавлено
- Поля "Ozon Client-Id" и "Ozon Api-Key" (типа password) на вкладке "Настройки" (`SettingsTab`), которые сохраняются в глобальные параметры Google Таблиц через экшен `saveGlobalSettings` и доступны для изменения исключительно администраторам.
- Состояние `externalShipments: ExternalShipment[]` и экшены `checkOzonShipments()` и `markExternalShipment()` в `useWarehouseStore.ts`.
- Кнопка "Проверить отгрузки Ozon" на вкладке "Загрузка накладных" (`UploadTab`) для запуска проверки новых FBO отправлений.
- Панель "Обнаружены новые отгрузки Ozon" со списком новых поставок (ID, дата, количество штук) и действиями "Оформить" и "Игнорировать" для каждого заказа.
- Интеллектуальный механизм маппинга товаров в отгрузке: для каждого товара ищется SKU по штрихкоду (`barcode == ozonBarcode`) из списка `skus`. В случае отсутствия совпадения, сопоставление выполняется по артикулу Ozon (`offerId == SKU`). Если SKU не найден, элементу присваивается статус `'unknown'`.
- Интеграция с `ConfirmModal`: при нажатии "Оформить" данные передаются в `parsedItems`, статус операции выставляется в `Расход` со складом назначения `Ozon`, а после успешной записи транзакции в Google Таблицу (`commitTransaction` возвращает true) автоматически запускается смена статуса отгрузки в листе "Внешние отгрузки" на `'processed'`.
- Поддержка двух типов комплектов в системе: `legacy` (списание собственного остатка + компонентов) и `virtual` (списание только компонентов).
- Переключатель "Тип комплекта" в модальном окне редактирования комплекта (`KitModal`).
- Отображение расчетного количества сборки для виртуальных комплектов на вкладке «Дашборд» с пометкой «сборка».
- Разовая миграционная функция `migrateBowlKitsToVirtual()` в `Code.gs` для безопасного переноса `BowlGrayMini_01` и `BowlBlueMini_01` на виртуальную схему со списанием остатков на новые компоненты («Миска серая» и «Миска бирюзовая»).
- Колонка `kitType` в листе «Комплекты» в Google Таблицах.
- Новая разовая миграционная функция `migrateComponentWriteOffCosts()` для ручного обновления исторических данных транзакций в листе «Транзакции», приводящая поле «Себестоимость списания» у строк-компонентов в соответствие с полем «Сумма».
- Поддержка поля «КОР/ПАЛ» (boxesPerPallet) для SKU: 6-я колонка в листе SKU в Google Таблицах, позволяющая задавать количество коробок на одном паллете.
- Поле ввода «Коробок на паллете» в модальном окне добавления/редактирования SKU (`SkuModal`) с адаптивной трехколоночной сеткой.
- Отображение количества коробок («X кор.») для каждой товарной позиции в таблице подтверждения отгрузки в `ConfirmModal` (только для операций «Расход»).
- Суммарный информационный блок под таблицей в `ConfirmModal` (для операций «Расход») с отображением итогового количества коробок (`totalBoxes`) и итогового количества паллет (`totalPallets`).
- Умное однократное автозаполнение (предзаполнение) количества дополнительных услуг: если название активной услуги содержит «короб», её количество автоматически устанавливается равным `totalBoxes`, если содержит «паллет» — `totalPallets`.
- Поддержка расчета стоимости хранения товаров на складе: добавлена колонка «Литраж (л)» (volumeLiters) в лист SKU и глобальный параметр стоимости хранения за 1 литр в сутки `storageRatePerLiterDay` (props `global_storageRate`).
- Поля `ozonClientId` и `ozonApiKey` в глобальных настройках (`getGlobalSettings` и `saveGlobalSettings`) с доступом только для администраторов.
- Защищенный по сигнатуре (`verifyServerSignature`) серверный action `'getOzonKeys'`, возвращающий API-ключи Ozon.
- Новый лист Google Таблиц «Внешние отгрузки» для накопления заказов с Ozon Seller API с колонками `PostingID`, `Дата обнаружения`, `Дата отгрузки`, `Статус` ('new', 'processed', 'ignored'), `ПозицииJSON`, `TransGroupInfo`.
- Три новых action-обработчика для работы с внешними отгрузками для авторизованных пользователей: `'saveExternalShipments'` (добавление новых уникальных отправлений), `'getExternalShipments'` (выборка всех записей в виде JSON-объектов) и `'updateExternalShipmentStatus'` (изменение статуса отправления).
- Новый эндпоинт POST `/api/ozon/check` в `server.ts` для запуска синхронизации заказов FBO Ozon за 30 дней со встроенной валидацией сессий и автоматическим пробросом в Google Sheets.
- Механизм `fetchOzonKeys` в `server.ts` для безопасного извлечения API-ключей Ozon из GAS с использованием HMAC SHA256 подписи и кэшированием на 1 час.
- Инвалидация кэша ключей Ozon в `server.ts` при сохранении глобальных настроек (`saveGlobalSettings`).
- Поддержка интеллектуальных фалбеков по версиям (v2 -> v1) для Ozon API методов получения списка поставок (`supply-order/list`), деталей (`supply-order/get`) и состава (`supply-order/bundle`) для отказоустойчивости при deprecation и 404 ошибках.
- Поле ввода «Литраж, л» (шаг 0.01, необязательное) в модальном окне добавления/редактирования SKU (`SkuModal`).
- Блок «Хранение товаров» на вкладке «Справочник» (`DirectoryTab`) для настройки тарифа хранения за 1 л/сутки (редактирование доступно только системным администраторам, сохранение через `saveGlobalSettings`, загрузка через `getGlobalSettings`).
- Новая карточка аналитики на дашборде «Стоимость хранения» с отображением расходов в сутки и прогнозируемых затрат за месяц (сутки x 30).
- Новая колонка «Хранение ₽/сут» в таблице остатков на дашборде с возможностью динамической сортировки по стоимости хранения остатка.

### Изменено
- Логика проведения транзакций (`commitTransaction`): для типа 'virtual' при Расходе себестоимость главной строки сбрасывается в 0, а собственный складской остаток артикула комплекта не изменяется (списываются только компоненты).
- Логика удаления транзакций (`deleteTransaction`): при откате главной строки Расхода виртуального комплекта (с нулевой себестоимостью и заполненным `groupId`), собственный остаток артикула не возвращается на склад, возвращаются только компоненты.
- Валидация остатков перед отгрузкой: для `legacy` проверяется собственный остаток комплекта и компонентов, для `virtual` — только компонентов.
- Исправлена формула расчета `nCap` при каскадном удалении транзакции комплекта: теперь прибавляется прямая сумма себестоимости списания из соответствующей колонки вместо умножения количества на цену за единицу, так как в `Себестоимость списания` теперь пишется итоговая себестоимость (сумма) списания.

### Исправлено
- Исправлено массовое удаление транзакций в `deleteMultipleTransactions` (`Code.gs`): теперь при откате расходов проверяется, является ли артикул виртуальным комплектом (`isVirtualKit && writeOffCost === 0`) на основе `getKits()`, предотвращая ложное начисление остатков на артикул комплекта. Также устранен баг с потерей полей `groupId` и `isComponent` при архивации удаленных транзакций; их индексы теперь вычисляются динамически по первой строке заголовков.
- Исправлена валидация наличия компонентов при проведении отгрузок ('Расход') в функции `commitTransaction` (`Code.gs`): теперь потребность компонентов предварительно суммируется в ассоциативный массив `componentDemand` по всем списываемым комплектам в одной транзакции. Это предотвращает ложное прохождение валидации, когда комплекты в отгрузке по отдельности укладываются в остатки, но суммарно уводят компоненты в дефицит.
- Исправлена проверка остатков для виртуальных комплектов (type === 'virtual') во время проведения отгрузки и валидации распознанных файлов в `useWarehouseStore.ts`. Ранее для них проверялся собственный физический остаток на складе (всегда равный 0), что блокировало отгрузку. Теперь остаток виртуального комплекта рассчитывается динамически по минимальной обеспеченности его компонентами на основе новой функции `getEffectiveAvailability`, добавлена агрегированная проверка нехватки компонентов при отгрузке, а также отключена ложная ошибка «Артикул не найден».
- Исправлен поиск листа транзакций в функции `migrateComponentWriteOffCosts` (`Code.gs`): жесткий вызов `ss.getSheetByName('Транзакции')` заменен на вызов `getTransactionSheet(ss)`, что предотвращает падение миграции с ошибкой «Лист :Транзакции не найден» на таблицах, где лист назван «История».
- Исправлено вычисление `shipmentTotalQty` в функции `commitTransaction` (`Code.gs`): теперь при подсчете общего количества отгрузки отсекаются позиции, имеющие статус, отличный от `'ok'` (например, аннулированные или неизвестные SKU). Это обеспечивает верное пропорциональное распределение накладных/транспортных расходов на фактически проведенные в системе товары.
- Исправлен баг в функции `setupDatabase` (`Code.gs`), когда при пересборке листа SKU терялась колонка «Литраж (л)», так как expectedHeaders содержал только 6 колонок, а лист полностью очищался. Теперь expectedHeaders содержит 7 колонок, индекс литража определяется динамически, значение сохраняется при миграции строк, а размер диапазона корректно увеличен до 7.
- Исправлен баг в `deleteTransaction` (Code.gs), где использовались жесткие индексы `13` (groupId) и `14` (isComponent). Теперь позиции колонок определяются динамически на основе первой строки заголовков. Это восстановило корректный откат транзакций списания комплектов и компонентов.
- Исправлен баг в `commitTransaction` (Code.gs), из-за которого в поле `Себестоимость списания` для строк-компонентов записывалось значение за единицу (`compAvg`) вместо общей себестоимости (`compTotal`). Это приводило к некорректной работе функции полного пересчета остатков и восстановления.
- Исправлена ошибка `TypeError: Assignment to constant variable` в функции `setupDatabase` (Code.gs). Переменная `transSheet` переведена на `let` для возможности корректного переопределения при инициализации пустой базы данных.
- Устранена уязвимость в `/api/parse-invoice` (server.ts): добавлена обязательная проверка сессии пользователя (`sessionToken`) с запросом к Google Apps Script для проверки сессий, отсутствующих в локальном in-memory кэше.
- Устранена уязвимость в `/api/gas` (server.ts): кэшированные ответы из `gasCache` теперь отдаются только при наличии валидного и закэшированного `sessionToken`, чтобы исключить обход аутентификации.
- Обновлен клиентский вызов `parseInvoiceWithGemini` (src/lib/gemini.ts, src/store/useWarehouseStore.ts) для корректной передачи `sessionToken` первым параметром.
- Унифицирована проверка прав администратора в `Code.gs` с добавлением вспомогательной функции `isAdminRole`.
- В `assertAdmin` удалена проверка по имени пользователя, осталась только проверка роли через `isAdminRole`.
- В `getGlobalSettings` заменено жесткое сравнение роли на вызов `isAdminRole`.
- Реализована детальная диагностика ошибок распознавания накладных: эндпоинт `/api/parse-invoice` теперь передает детальный объект ошибки (`details`) со свойствами `stage`, `model`, `httpStatus` и `rawError`. На клиенте эти детали обрабатываются, выводя информативные `toast.error` (об отсутствии ключа, ошибках модели 404, лимитах 429 и невалидном JSON от модели).
- Синхронизирована дефолтная модель Gemini: устаревшая модель по умолчанию `gemini-1.5-flash` и `gemini-3-flash-preview` заменена на актуальную `gemini-flash-latest` в `server.ts`, `Code.gs` (в `getGlobalSettings`) и в дефолтном значении параметра `parseInvoiceWithGemini` в `src/lib/gemini.ts`.
- Добавлена кнопка очистки загруженных накладных на вкладке «Загрузка»: в `useUIStore` внедрен метод `clearRecognitionData` для сброса истории, сырого текста, файлов и результатов парсинга. На вкладке добавлена кнопка «Очистить накладные» с предупреждающим подтверждением через `askConfirmation` и последующим алертом об успешной очистке.
- Реализована история тарифов услуг по датам в `Code.gs`: добавлен лист «Тарифы услуг» (ServiceID, Стоимость, ДействуетС), поддерживаемый в `setupDatabase` и `getOrCreateSheet`. Реализованы функции `getServiceRates`, `getServiceCostAt(serviceId, dateStr, ratesArr, services)` и экшены `addServiceRate` (для администраторов) и `getServiceRates` (для всех авторизованных). Функция `getServices` теперь возвращает дополнительное поле `currentCost` на сегодняшнюю дату.
- Подключена история тарифов услуг по датам на фронтенде: в `src/types.ts` добавлен интерфейс `ServiceRate`, в `useWarehouseStore` интегрирована загрузка тарифов в `fetchStock` через `getServiceRates` и добавлен экшен `handleAddServiceRate` для вызова GAS `addServiceRate`.
- На вкладке «Справочник» (`DirectoryTab`) для каждой услуги добавлена кнопка «Тарифы», раскрывающая панель со списком тарифов (стоимость + дата начала действия, отсортированные по убыванию даты) и формой добавления нового тарифа (доступной только системным администраторам).
- В модальном окне подтверждения (`ConfirmModal`) расчеты себестоимости отгрузки и вывод стоимости услуг полностью переписаны на динамический выбор тарифа через функцию `getServiceCostAt(serviceId, deliveryDate)` в зависимости от выбранной пользователем даты поставки на маркетплейс, с автоматическим пересчетом и обновлением цен в реальном времени при изменении даты.

## [2026-07-17]

### Добавлено
- Реализовано хранение подтверждённого пересорта для поставок Ozon на сервере (`Code.gs`):
  - Добавлена новая колонка `'ПересортJSON'` в `EXTERNAL_SHIPMENTS_HEADERS` листа «Внешние отгрузки».
  - Внедрена серверная функция `saveShipmentPeresort(postingId, peresortJSON)` с валидацией структуры JSON.
  - Структура подтверждённого пересорта валидируется по схеме `{"pairs":[{"fromOfferId":string,"fromArticle":string,"toOfferId":string,"toArticle":string,"qty":number}],"confirmedAt":string,"confirmedBy":string}`.
  - Добавлен экшен `'saveShipmentPeresort'` в `doPost` с вызовом `LockService.getScriptLock()`.
  - Обновлено чтение в `getExternalShipments` для извлечения и возврата поля `peresortJSON` клиенту.
- Реализована поддержка пересорта на стороне клиента (`src/types.ts`, `src/store/useWarehouseStore.ts`):
  - Добавлено свойство `peresortJSON?: string` в интерфейс `ExternalShipment` в `src/types.ts`.
  - В Zustand-сторе `useWarehouseStore.ts` объявлен и реализован асинхронный метод `saveShipmentPeresort(postingId, peresortJSON)` с поддержкой детекции `isProcessing`, вызовом `fetchGas('saveShipmentPeresort')`, обновлением списка отгрузок через `getExternalShipments` и тост-уведомлениями.

### Изменено
- Обновлена документация проекта (`docs/DEVLOG.md`, `docs/CHANGELOG.md`, `docs/TEST_LOG.md`).

## [2026-07-16]

### Добавлено
- Обнаружение пересорта для поставок Ozon в интерфейсе (`src/lib/ozonPeresort.ts`, `src/components/OzonSuppliesTab.tsx`):
  - Создан вспомогательный модуль обнаружения пересорта `detectPeresort` в `src/lib/ozonPeresort.ts`.
  - Модуль сравнивает заявленный и принятый состав поставок Ozon с учетом SKU.
  - Внедрен показ бейджа «Возможен пересорт (не из заявки: N шт)» во вкладке «Поставки Озон» при обнаружении лишних позиций.
  - Бейдж «Принято полностью» теперь скрывается при наличии незаявленных позиций.

## [2026-07-17]

### Добавлено
- Карточка подтверждения пересорта Ozon `PeresortModal` в `src/components/OzonSuppliesTab.tsx`. Карточка позволяет сопоставить недостающие позиции с принятыми лишними позициями, настроить количество в рамках допустимого лимита в конструкторе пар и подтвердить пересорт с сохранением в `peresortJSON` (без изменения остатков на складе на данном этапе).
- Интегрирована кнопка «Сбросить пересорт» в `PeresortModal`, стилизованная под требования и запрашивающая подтверждение у пользователя перед полной очисткой сохранённого пересорта.
- Кнопка «Пересорт» выводится в строке поставки на вкладке «Поставки Озон» (рядом с кнопкой «Ввести приёмку») с идентификатором `btn-open-peresort-{postingId}` при обнаружении пересорта или наличии уже подтверждённого пересорта.
- Фиолетовый/индиго бейдж «Пересорт подтверждён — ожидает проведения» для поставок, по которым подтверждён пересорт. Янтарный бейдж «Возможен пересорт» отображается только при наличии расхождений и отсутствии подтверждённого пересорта.

### Исправлено
- Исправлен баг дублирования SKU в выпадающем списке «Позиции не из заявки» модального окна приёмки `AcceptanceModal` путём автоматической фильтрации уже выбранной в текущей строке позиции из набора `getAvailableSkusForIndex(idx)`.
- Исправлено отсутствие уникального идентификатора для выпадающих списков артикулов в `AcceptanceModal`: теперь они имеют уникальный ID по маске `select-extra-article-{index}`.
- Настроен условный текст на кнопке `btn-open-peresort-{postingId}`: если `peresortJSON` заполнен, выводится текст «Пересорт ✓», в противном случае — «Пересорт».
- Исправлены два несуществующих класса в Tailwind CSS: класс `bg-indigo-150` изменен на `bg-indigo-100` для бейджа подтверждения пересорта, класс `hover:bg-emerald-105` изменен на `hover:bg-emerald-100` для кнопки сброса пересорта.
- Полностью закрыт пункт 15 интеграции Ozon (пересорт: обнаружение + подтверждение, без движения остатков). Успешно завершен сквозной цикл от приёмки не по заявке до детального сопоставления расхождений в парах «вместо X уехал Y» и записи подтверждённой структуры в `ПересортJSON`. Проверено живым сквозным тестом на тестовой БД.

### Восстановлено 29.07.2026 (запись была ошибочно удалена)
- Определение и нормализация Ozon-SKU в `OzonSupplyModal.tsx`:
  - Использование зеркала «Остатки Ozon» (`ozonStocks`) как главного источника чистых числовых SKU Ozon.
  - Функция `normalizeOzonSku` для очистки технических штрихкодов `OZN...` и дробных хвостов `.0`.
  - Отображение текущего Ozon-SKU под названием кластера в списке выбранных позиций.

## [2026-07-31]

### Добавлено
- Интеграция эндпоинтов финализации заявки Ozon и состояния грузомест в `server.ts`:
  - Подключен импорт библиотеки `xlsx`.
  - Добавлено поле `placementZone` в состав поставки.
  - Метка версии прокси обложена значением `2026-07-31-cargoes-state`.
  - Реализован эндпоинт POST `/api/ozon/supply/finalize` для завершения создания поставки в Ozon.
  - Реализован эндпоинт POST `/api/ozon/cargoes/state` для чтения статуса грузомест и готовности документов Ozon.
- Функция сохранения документов заявки Ozon на Google Диск в `Code.gs`:
  - В `OZON_SETTINGS_DEFAULTS` добавлены настройки `supplyDocsFolderId` (ID папки документов) и `supplyDocsLabelsFolder` (подпапка ШК).
  - Настройка `supplyDocsLabelsFolder` зарегистрирована в `OZON_SETTINGS_STRING_KEYS`.
  - В `doPost` зарегистрирован экшен `saveSupplyDocsToDrive`.
  - Реализована функция `saveSupplyDocsToDrive` и вспомогательные функции `sanitizeDriveName`, `getOrCreateChildFolder`, `replaceFileInFolder`, `nameFromDisposition`, `extFromContentType` для автоматической загрузки и распределения файлов этикеток, актов и накладных по папкам Google Диска.
- План развития Ozon (`docs/OZON_PLAN.md`):
  - Добавлен пункт 28 «Защита от двойного проведения операции» (приоритет №1, заведён по итогам боевой аварии 01.08.2026).
  - В раздел «Заметки для следующего чата» добавлена ориентировка на приоритетный старт работ по пункту 28.
- Идемпотентность и защита от дублей в `Code.gs` (Пункт 28, этап A):
  - В `ensureColumns` зафиксирована автоматическая инициализация колонки `OpID`.
  - Реализована функция `findTransactionsByOpId` и проверка совпадений `opId` с мгновенным возвратом успешного результата и флага `idempotentHit: true`.
  - В `commitTransaction` и `doPost` добавлен проброс и запись `payload.opId`.
- Генерация и передача `opId` на клиенте (Пункт 28, этап B):
  - Создана утилита `newOperationId` для генерации уникальных ключей операций (`UUID` / `random`).
  - В `useWarehouseStore.ts` расширен методы проведения операций с обработкой `idempotentHit`.
  - В `ConfirmModal.tsx` и `ManualTab.tsx` зафиксировано сохранение `opId` между повторными попытками до успешного завершения операции.

## [2026-08-01]

### Добавлено
- В `docs/OZON_PLAN.md` зафиксирован раздел «Правило ведения журналов» с регламентом дозаписи строго в конец файлов `docs/DEVLOG.md`, `docs/CHANGELOG.md` и `docs/TEST_LOG.md`.
- В заголовки `docs/DEVLOG.md`, `docs/CHANGELOG.md` и `docs/TEST_LOG.md` добавлены предупреждающие плашки с указанием правил обновления.

## [2026-08-01]

### Добавлено
- В `Code.gs` реализована функция `linkPostingsToCommit` для привязки поставок Ozon к проведённому расходу внутри операции `commitTransaction` (пункт 28, этап C).
- В `doPost` добавлены проброс `payload.postingIds` и автоматическая пометка поставок `processed` внутри серверного замка.

## [2026-08-01]

### Изменено
- В `useWarehouseStore.ts` пометка поставок Ozon перенесена внутрь атомарной операции `commitTransaction` (передача `postingIds` в payload) с обработкой результата `postingsLink` (пункт 28, этап C).
- В `ConfirmModal.tsx` добавлен сброс `pendingOzonPostingIds` при закрытии окна подтверждения для предотвращения привязки к чужим операциям.

## [2026-08-01]

### Изменено
- В `server.ts` реализовано распознавание заглушки `doGet`/HTML при редиректах 302, добавлен автоповтор до 3 попыток для read-only действий и `commit`, порог таймаута для пишущих действий поднят до 300 с, а очистка кэша ограничена только пишущими действиями (пункт 28, этап D).
- В `ConfirmModal.tsx` добавлено состояние `commitError`, отображение амбер-баннера «Ответ не получен, операция могла быть записана» при сетевых ошибках/таймаутах и замена кнопки «Подтвердить» на «Проверить результат» с перезагрузкой остатков и истории (пункт 28, этап D).

## [2026-08-01]

### Изменено
- В `docs/OZON_PLAN.md` восстановлены утраченные фрагменты: окончание пункта 22, полностью пункт 23 и статусы пункта 28 / этапа D (восстановление содержимого, утраченного предыдущим коммитом).

## [2026-08-01]

### Добавлено
- В `docs/OZON_PLAN.md` перед разделом «Правило ведения журналов» добавлен новый раздел «Правило границ промта», содержащий правила запрета правки кода при документационных задачах, запрета сжатия/удаления пунктов плана и обязательные проверки архитектора.

## [2026-08-01]

### Изменено
- В `server.ts` расширен список `READ_ONLY_ACTIONS`, ограничен автоповтор `commit` только случаями с наличием `opId`, а таймаут записи снижен с 300 до 90 секунд.
- В `src/components/ConfirmModal.tsx` добавлен таймер 20 секунд и информирующая плашка о длительном выполнении операции записи.

## [2026-08-01]

### Изменено
- В `docs/OZON_PLAN.md` официально закрыты этап D и пункт 28 «Защита от двойного проведения операции» (выполнен целиком 01.08.2026) с детальной фиксацией параметров исправлений, результатов замеров и регресса на тестовой БД.

## [2026-08-04]

### Изменено
- В `docs/OZON_PLAN.md` обновлена команда проверочного подсчёта пунктов плана с `grep -cE '^- [⬜🟨✅]'` на `grep -cE '^- (⬜|🟨|✅)'` и добавлены предупреждения о некорректном побайтовом разборе многобайтовых символов скобками `[]` в `grep -E`.

## [2026-08-04]

### Изменено
- В `server.ts` уменьшено максимальное число попыток повтора с 3 до 2 (`maxTries = canRetry ? 2 : 1`), установлена задержка перед повтором `retryDelayMs = 20_000`, снижение таймаута для читающих действий с 45_000 мс до 25_000 мс, заменено выражение задержки `1000 * attempt` на `retryDelayMs * attempt` в 4 местах прокси `/api/gas`.

## [2026-08-04]

### Изменено
- В `server.ts` добавлена ручная обработка 3xx редиректов Apps Script (`redirect: "manual"`) при POST-запросах, повторно отправляющая POST-запрос на полученный адрес `location` для сохранения метода POST.

## [2026-08-04]

### Изменено
- В `server.ts` отменена ручная обработка 3xx редиректов Apps Script, возвращено стандартное автоматическое перенаправление запросов fetch.

## [2026-08-04]

### Добавлено
- В `server.ts` добавлена диагностика запросов к Apps Script: счётчик параллельных запросов `gasInFlight`, записи `GASDIAG` при успешных ответах и ошибках с фиксацией таймаутов, длительности, наличия заглушки и счётчика параллельности.

## [2026-08-06]

### Добавлено
- В `server.ts` добавлено ограничение параллельных запросов к Google Apps Script (`GAS_MAX_PARALLEL = 3`) через встроенную асинхронную очередь `gasWaitQueue` с функциями `acquireGasSlot()` и `releaseGasSlot()`.
- В `Code.gs` список `LOCK_FREE_ACTIONS` расширен шестью читающими действиями (`getOzonSettings`, `getOzonClusters`, `getOzonStocks`, `getOzonSales`, `getFactoryOrders`, `getOzonSyncStatus`) для устранения задержек на ожиданиях LockService.
- В `server.ts` увеличен лимит параллельных запросов к Apps Script с 3 до 8 (`GAS_MAX_PARALLEL = 8`) и добавлена фиксация тела ответов в логах `GASBODY` для ошибочных и аномально коротких ответов.
- В `Code.gs` функция `verifySession` больше не удаляет строки просроченных сессий, исключая конфликт индексов строк при параллельном неблокирующем выполнении запросов.
- В `docs/OZON_PLAN.md` зафиксирован пункт 29 с детальным описанием причин 502 ошибок, выполненных шагов 1–5 и достижением Чекпоинта 1.
- В `server.ts` условие распознавания заглушки `doGet` ограничено ответами короче 300 байт, а определение HTML — префиксами `<!DOCTYPE` и `<html`. Это устраняет ложную отбраковку ответа `getOzonSyncStatus` с кодом 502.

## [2026-08-06]

### Изменено
- В `docs/OZON_PLAN.md` заменён текст пункта 29 на корректную формулировку: отражена истинная причина сбоев (удаление строк сессий в `verifySession`), зафиксированы результаты версии 138 Apps Script, отвергнутые гипотезы, инструменты диагностики и согласованный перечень этапов A–F.

## [2026-08-06]

### Изменено
- В `server.ts` реализован этап B пункта 29: введена функция `getCacheTtlMs` с раздельными сроками хранения ответов (справочники 10 мин, данные Ozon 5 мин, оперативные остатки 30 сек). В кэширование включены ответы по Ozon (`getOzonStocks`, `getOzonSales`, `getFactoryOrders`), устранены устаревшие `isCacheable` и `GAS_CACHE_TTL_MS`.

## [2026-08-06]

### Изменено
- В `server.ts` порог ограничителя частоты запросов `RATE_LIMIT` повышен с 60 до 300 запросов в минуту суммарно, чтобы исключить ложные ошибки HTTP 429 при общей работе через nginx прокси.

## [2026-08-06]

### Изменено
- В `server.ts` реализован этап C пункта 29: глобальная срезка кэша при записи заменена на точечную очистку по карте `CACHE_INVALIDATION` для 17 безопасных пишущих действий, сохраняющую тяжелые ответы (например, `getOzonSales`).

## [2026-08-06]

### Изменено
- В `src/components/OzonStocksTab.tsx` реализован этап D пункта 29: убран дублирующий вызов `getOzonClusters` при открытии вкладки «Остатки Озон». Проверка новых кластеров теперь использует уже полученный ответ единого запроса.

## [2026-08-06]

### Изменено
- В `Code.gs` реализован этап F пункта 29: в `loginUser` убрано удаление действующих сессий того же логина, а запись сессии переведена на единую атомарную операцию перезаписи листа (`clearContent` + `setValues`) без циклического вызова `deleteRow`.

## [2026-08-06]

### Изменено
- В `src/components/OzonStocksTab.tsx` реализован этап E пункта 29: добавлены замеры времени и логирование `OZONPERF` для функций `coverage` и `coverageRows` для диагностики производительности вкладки «Остатки Озон».

## [2026-08-06]

### Исправлено
- В `src/components/OzonStocksTab.tsx` удалено обращение к `pendingSupplies.length` из лога `OZONPERF`, устраняющее ошибку проверки типов TypeScript (TS2339).

## [2026-08-06]

### Добавлено
- В `Code.gs` добавлен необязательный параметр `weeksLimit` в функцию `getOzonSales`, позволяющий передавать ограничение по количеству выдаваемых последних недель истории продаж.

## [2026-08-06]

### Изменено
- В `src/store/useWarehouseStore.ts` при вызове `fetchGas('getOzonSales')` передаётся `{ weeksLimit: 12 }` для ограничения загрузки истории продаж Ozon последними 12 неделями.

## [2026-08-06]

### Изменено
- В `src/store/useWarehouseStore.ts` вызов `fetchGas('getOzonSales')` переведён на передачу `{ data: { weeksLimit: 12 } }` для успешной доставки параметра `weeksLimit` на сторону Apps Script.

## [2026-08-06]

### Изменено
- В `Code.gs` вызов `getOzonSales()` в блоке быстрых читающих экшенов заменён на `getOzonSales(payload.data && payload.data.weeksLimit)` для корректной передачи параметра `weeksLimit`.

## [2026-08-06]

### Изменено
- В `src/components/OzonStocksTab.tsx` расчёт покрытия товаров `coverage` отложен до получения ответа по справочнику кластеров (`clusterRefsLoaded`).

## [2026-08-06]

### Изменено
- В `docs/OZON_PLAN.md` зафиксировано завершение всех этапов A–F в пункте 29 с описанием полученных результатов и эксплуатационных заметок.

## [2026-08-07]

### Изменено
- В `server.ts` включён перезапрос состава для всех неоформленных поставок Ozon (`status === 'new'`) на каждом опросе, а также добавлена защита от сброса ранее сохранённого состава при пустом ответе Ozon API.
- В `src/lib/ozonPending.ts` локальный резерв под заявку Ozon сохранён до фактического списания со склада или явной отмены/отказа заявки (снято удаление зачёта по `isStockDeparted`, зачёт снимается по `isShipmentSettled` или `PENDING_CLEARED_STATUSES`).
- В `src/lib/ozonAlerts.ts` добавлен тип алерта `'reserve_shortage'`, интерфейс `ReserveShortageInput` и чистая функция `buildReserveShortageAlerts` для предупреждения о превышении резерва под заявки над доступными остатками.
- В `src/components/Dashboard.tsx` подключено вычисление алертов `reserveShortageAlerts` через `buildReserveShortageAlerts` для предупреждения пользователя на главной об элементах заявок Ozon, превышающих остатки.
- В `src/components/OzonSuppliesTab.tsx` введён хелпер `isActionableItem` для исключения отменённых кластеров Ozon из проверок актуальности поставок.
- В `src/components/ConfirmModal.tsx` заморожена валидация остатков во время записи операции через `isCommitting` и `lastValidatedRef` для исключения кратковременного отображения ложной ошибки о нехватке товара.
- Добавлена автоформируемая метка версии сборки (`__APP_BUILD__`) внизу бокового меню (`Sidebar.tsx`), с подстановкой времени сборки (МСК) в `vite.config.ts` и объявлением глобального типа в `src/vite-env.d.ts`.

## [2026-08-07]

### Изменено
- Паспорт проекта (`docs/OZON_PLAN.md`): добавлен пункт 30, описывающий решения по резервированию под заявки Ozon, непрерывному опросу состава, алерту нехватки и метке версии сборки; в раздел «Известные хвосты вне плана» внесены три новых пункта наблюдений.

## [2026-08-07]

### Изменено
- Паспорт проекта (`docs/OZON_PLAN.md`): добавлен пункт 31, описывающий виртуальные заявки Ozon (`order_tags.is_virtual`), запрет их резервирования и списания, обработку кнопкой «Ок» без записей в историю и этапы A–E.

## [2026-08-07]

### Изменено
- Серверную часть (`server.ts`): реализовано извлечение `is_virtual` и `original_supply_id` из `order_tags` в ответе Ozon API и их передача в структурах поставок для Apps Script (этап A пункта 31).

## [2026-08-07]

### Изменено
- Apps Script (`Code.gs`): добавлены колонки «Виртуальная» и «ИсходнаяПоставка» в заголовок листа «Внешние отгрузки», их занесение при обработке поставок от прокси и считывание при выгрузке (этап B пункта 31).

## [2026-08-07]

### Изменено
- Логику работы с виртуальными заявками Ozon (`src/types.ts`, `src/lib/ozonPending.ts`, `src/lib/ozonGroups.ts`): добавлены поля `isVirtual` и `originalSupplyId` в `ExternalShipment`, запрещено попадание виртуальных заявок в локальный резерв, отключено автоматическое флагование `needsExpense` и добавлен блокирующий toast-предохранитель при обработке группы (этап C пункта 31).

## [2026-08-07]

### Изменено
- Интерфейс работы с виртуальными заявками Ozon (`src/components/OzonSuppliesTab.tsx`): добавлены визуальные бейджи и описания виртуальных заявок, замена кнопок оформления и дублей на единую кнопку «Ок», перебирающую новые поставки и помечающую их как принятые к сведению без списания со склада (этап D пункта 31).

## [2026-08-08]

### Изменено
- Паспорт проекта (`docs/OZON_PLAN.md`): отмечены выполненными пункт 31 (Виртуальные заявки Ozon) и все его этапы (0, A, B, C, D, E).

## [2026-08-08]

### Добавлено
- Паспорт проекта (`docs/OZON_PLAN.md`): заведён новый пункт 32 (Ложный спиннер синхронизации и медленная массовая пометка поставок) с описанием дефектов, решений пользователя, ограничений и этапов 0, A, B, C.

## [2026-08-08]

### Добавлено
- Стор склада (`src/store/useWarehouseStore.ts`): заведено пакетное действие `markExternalShipmentsBatch` для последовательной записи статуса N поставок с единственным финальным вызовом `getExternalShipments` (этап A пункта 32).

## [2026-08-08]

### Изменено
- Вкладка поставок Ozon (`src/components/OzonSuppliesTab.tsx`): переведен спиннер кнопки синхронизации на локальное состояние `isSyncing` и переведены 4 обработчика на пакетное действие `markExternalShipmentsBatch` (этап B пункта 32).

## [2026-08-08]

### Изменено
- Паспорт проекта (`docs/OZON_PLAN.md`): отмечен выполненным пункт 32 (Ложный спиннер синхронизации и медленная массовая пометка поставок) и его этапы (0, A, B, C).

## [2026-08-08]

### Добавлено
- Документация API Ozon (`docs/OZON_API.md`): добавлен раздел «Виртуальные заявки на поставку (`order_tags.is_virtual`)» с описанием бизнес-причин, формата API, данных замера и описанием ловушек.








