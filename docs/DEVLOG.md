# Журнал разработки

| Дата | Задача | Изменённые файлы | Ключевые решения | Известные ограничения |
| :--- | :--- | :--- | :--- | :--- |
| 2026-07-03 | Внедрение типов комплектов (legacy/virtual) и миграция Bowl-комплектов | `Code.gs`, `src/types.ts`, `src/store/useWarehouseStore.ts`, `src/components/KitModal.tsx`, `src/components/Dashboard.tsx` | Добавление колонки `kitType` в лист «Комплекты»; пропуск списания самого товара для виртуальных комплектов; расчёт виртуального остатка на основе компонентов на клиенте; автоматическая функция `migrateBowlKitsToVirtual` для безопасного переноса BowlGrayMini_01 and BowlBlueMini_01 | Виртуальные остатки рассчитываются на клиенте на основе текущих остатков компонентов, что может незначительно грузить интерфейс при очень больших объемах данных. |
| 2026-07-03 | Исправление бага в `deleteTransaction` (динамический поиск индексов колонок) | `Code.gs` | Определение индексов колонок `groupId` и `isComponent` по заголовкам листа (вместо жестких индексов `13` и `14`), исправление условий отката и каскадного удаления для виртуальных комплектов. | Нет |
| 2026-07-03 | Исправление записи `writeOffCost` за единицу вместо суммы и каскадного отката | `Code.gs` | Изменение значения `writeOffCost` для строк-компонентов с `compAvg` на `compTotal` в `commitTransaction`; изменение формулы расчета `nCap` в `deleteTransaction` (сложение вместо умножения на количество); создание функции миграции `migrateComponentWriteOffCosts` для исправления исторических данных. | Нет |
| 2026-07-03 | Исправление TypeError в `setupDatabase` | `Code.gs` | Замена `const transSheet` на `let transSheet` во избежание ошибки повторного присваивания (TypeError) при создании нового листа «Транзакции» на пустой базе. | Нет |
| 2026-07-03 | Закрытие уязвимостей в `server.ts` (сессии и кэш) | `server.ts`, `src/lib/gemini.ts`, `src/store/useWarehouseStore.ts` | Добавлена обязательная проверка и валидация `sessionToken` через GAS в `/api/parse-invoice` с сохранением в кэш; в `/api/gas` кэшированные ответы возвращаются только при валидном (`isTokenCached`) токене. | Нет |
| 2026-07-03 | Унификация проверки прав администратора | `Code.gs` | Добавлена вспомогательная функция `isAdminRole` для проверки прав; `assertAdmin` теперь проверяет только роль через `isAdminRole`, а в `getGlobalSettings` аналогично используется `isAdminRole`. | Нет |
| 2026-07-03 | Диагностика ошибок распознавания накладных | `server.ts`, `src/lib/gemini.ts`, `src/store/useWarehouseStore.ts` | Формирование структурированного объекта ошибки (`details` с `stage`, `model`, `httpStatus`, `rawError`) в `/api/parse-invoice`, проброс `details` через класс `Error` на клиенте и вывод расшифрованных информативных `toast.error` в сторе. | Нет |
| 2026-07-03 | Синхронизация дефолтной модели Gemini | `server.ts`, `Code.gs`, `src/lib/gemini.ts` | Заменена устаревшая дефолтная модель `gemini-1.5-flash` / `gemini-3-flash-preview` на актуальную `gemini-flash-latest` во всех местах инициализации по умолчанию. | Нет |
| 2026-07-03 | Кнопка очистки загруженных накладных | `src/store/useUIStore.ts`, `src/components/UploadTab.tsx` | Внедрен action `clearRecognitionData` в UI-стор. На вкладке загрузки добавлена кнопка «Очистить накладные» с иконкой `Trash2`, отображающаяся при наличии текста или истории, и запрашивающая подтверждение через `askConfirmation`. | Нет |
| 2026-07-03 | История тарифов услуг по датам | `Code.gs` | Добавление листа «Тарифы услуг»; реализация функций `getServiceRates`, `getServiceCostAt`, `addServiceRate`; интеграция новых экшенов `addServiceRate` и `getServiceRates` в `doPost`; расширение `getServices` полем `currentCost` на сегодня. | Нет |
| 2026-07-03 | Подключение тарифов услуг на фронтенде | `src/types.ts`, `src/store/useWarehouseStore.ts`, `src/components/DirectoryTab.tsx`, `src/components/ConfirmModal.tsx` | Внедрены интерфейс `ServiceRate`, состояние `serviceRates` в `useWarehouseStore`, загрузка через `fetchStock`, действие `handleAddServiceRate`; на вкладке «Справочник» добавлена раскрывающаяся панель тарифов с историей и формой добавления для админа; в `ConfirmModal` расчеты себестоимости и отображаемой цены услуг переписаны на динамический выбор тарифа по дате доставки. | Нет |
| 2026-07-03 | Расчёт коробок и паллет при отгрузке и предзаполнение услуг | `Code.gs`, `src/types.ts`, `src/components/SkuModal.tsx`, `src/components/SkusTab.tsx`, `src/store/useUIStore.ts`, `src/components/ConfirmModal.tsx` | Добавление 6-й колонки «КОР/ПАЛ» (boxesPerPallet) в лист SKU; поддержка CRUD и инициализации в `ensureSkuExists`, `addSku`, `updateSku`, `SkuModal` and `SkusTab`; в `ConfirmModal` для операций «Расход» реализован расчет коробок на строку («X кор.») и вывод итогов по коробкам и паллетам; добавлено умное однократное предзаполнение услуг со словами «короб» и «паллет» при открытии модального окна. | Нет |
| 2026-07-03 | Внедрение расчёта стоимости хранения товаров на складе | `Code.gs`, `src/types.ts`, `src/components/SkuModal.tsx`, `src/store/useUIStore.ts`, `src/components/SkusTab.tsx`, `src/store/useSettingsStore.ts`, `src/store/useWarehouseStore.ts`, `src/components/DirectoryTab.tsx`, `src/components/Dashboard.tsx` | Внедрение колонки «Литраж (л)» (volumeLiters) в лист SKU с динамическим определением индекса колонки; добавление глобального параметра `storageRatePerLiterDay` (props `global_storageRate`); добавление поля «Литраж, л» (шаг 0.01) в `SkuModal`; добавление блока «Хранение товаров» на вкладку «Справочник» (`DirectoryTab`) с возможностью сохранения тарифа (только для администраторов); создание новой карточки аналитики «Стоимость хранения» на дашборде (сутки / месяц); добавление колонки «Хранение ₽/сут» в таблицу остатков с поддержкой динамической сортировки по стоимости хранения. | Нет |
| 2026-07-03 | Backend-подготовка для интеграции с Ozon Seller API | `Code.gs` | Добавление полей `ozonClientId` и `ozonApiKey` в global settings; новый защищенный action `getOzonKeys` с подписью; создание таблицы «Внешние отгрузки» и добавление трех action-обработчиков (`saveExternalShipments`, `getExternalShipments`, `updateExternalShipmentStatus`) для авторизованных пользователей. | Нет |
| 2026-07-03 | Интеграция эндпоинта POST `/api/ozon/check` и `fetchOzonKeys` | `server.ts` | Разработка функции `fetchOzonKeys` с кешированием на 1 час и проверкой HMAC; интеграция `/api/ozon/check` с защитой сессии; получение поставок FBO за 30 дней; извлечение состава и деталей с поддержкой fallback версий (v2 -> v1) для API Ozon; проксирование в GAS `saveExternalShipments`. | Нет |
| 2026-07-03 | Фронтенд интеграции Ozon Seller API для отслеживания и оформления отгрузок | `src/types.ts`, `src/store/useSettingsStore.ts`, `src/store/useWarehouseStore.ts`, `src/components/SettingsTab.tsx`, `src/components/UploadTab.tsx` | Добавление полей Ozon Client-Id и Ozon Api-Key (password) на вкладку настроек (только админ), сохранение глобально; добавление состояния `externalShipments` и экшена `checkOzonShipments` / `markExternalShipment`; добавление блока "Отгрузки Ozon Seller" на вкладке загрузки; интеллектуальное сопоставление штрихкодов (`barcode == ozonBarcode`) и артикулов Ozon (`offerId == SKU`); интеграция с `ConfirmModal` через хук `pendingOzonPostingId`. | Нет |
| 2026-07-03 | Сохранение литража в setupDatabase при пересборке SKU | `Code.gs` | Добавление 'Литраж (л)' 7-й колонкой в expectedHeaders листа SKU; определение индекса литража через headers.findIndex; чтение литража в цикле миграции строк и запись 7-м элементом; увеличение размера диапазона и очистки до 7 колонок и A1:G1 для заголовков; обновление блока создания нового листа SKU. | Нет |
| 2026-07-03 | Фильтрация позиций в вычислении shipmentTotalQty | `Code.gs` | Замена вычисления `shipmentTotalQty` в `commitTransaction` так, чтобы исключались позиции с невалидным статусом (`item.status && item.status !== 'ok'`). Это предотвращает неверное распределение накладных расходов на отменённые или неизвестные позиции. | Нет |
| 2026-07-03 | Исправление поиска листа транзакций в migrateComponentWriteOffCosts | `Code.gs` | Замена жесткого поиска листа `ss.getSheetByName('Транзакции')` на вызов существующей функции `getTransactionSheet(ss)`, которая автоматически поддерживает оба варианта наименования («Транзакции» и «История»). Это устраняет падение миграции с ошибкой отсутствия листа. | Нет |
| 2026-07-03 | Проверка и валидация остатков для виртуальных комплектов | `src/store/useWarehouseStore.ts` | Добавление метода `getEffectiveAvailability` для вычисления остатков виртуальных комплектов по минимальному остатку их компонентов; использование его в `commitTransaction` и валидации распознанных инвойсов; добавление агрегированной проверки компонентов виртуальных комплектов при отгрузке. | Нет |
| 2026-07-03 | Агрегированная валидация наличия компонентов в commitTransaction | `Code.gs` | Добавление сбора суммарной потребности компонентов по всем списываемым комплектам в `componentDemand` перед проверкой доступности. Теперь остатки компонентов проверяются один раз по сумме требований всех комплектов в одной транзакции, предотвращая уход компонентов в минус. | Нет |
| 2026-07-04 | Исправление багов в deleteMultipleTransactions | `Code.gs` | Устранено некорректное изменение остатков виртуальных комплектов (с нулевой себестоимостью) при откате остатков путем добавления проверки через `getKits()`. Также исправлено теряние полей `groupId` и `isComponent` при архивации удаленных транзакций путем определения их индексов по первой строке заголовков. | Нет |
| 2026-07-04 | Вывод колонки литража в справочнике SKU | `src/components/SkusTab.tsx` | Внедрение новой колонки «Литраж, л» в таблицу номенклатуры после колонки «Комплект» (так как количество коробок на паллете в таблице отсутствует). Значение `s.volumeLiters` выводится, если оно больше нуля, иначе выводится серый прочерк. Добавлена возможность сортировки по данной колонке. | Нет |
| 2026-07-04 | Инкрементальный откат остатков при восстановлении транзакций | `Code.gs` | Замена вызова `recalculateStockFully(ss)` на инкрементальный откат остатков в `restoreMultipleArchivedItems(archiveIds)`. Накапливаются изменения в `stockChanges` с обратным знаком (Приход: +qty, +total; Расход: -qty, -writeOffCost), не затрагивая виртуальные комплекты с нулевой себестоимостью. Изменения записываются одним пакетным обновлением в лист «Остатки». При уходе остатка в минус он приравнивается к 0, а в ответ добавляется предупреждение. | Нет |
| 2026-07-04 | Особый учёт себестоимости при списании товара | `Code.gs` | Добавлена функция `isWriteOffDestination` для детекции списаний (когда "Объект" содержит "Списание"). В `commitTransaction` при списании (тип "Расход") количество уменьшается, но капитализация товара не списывается (если остаток не равен 0, при котором капитализация обнуляется), за счет чего оставшаяся себестоимость раскидывается на остаток артикула, а средняя себестоимость динамически возрастает. | Нет |
| 2026-07-04 | Согласование отката остатков при удалении транзакций-списаний | `Code.gs` | При удалении списания (проверка через `isWriteOffDestination`) капитализация товара не должна восстанавливаться (увеличиваться), так как при проведении списания она не списывалась. В `deleteTransaction` и `deleteMultipleTransactions` для расходов-списаний сделано сохранение капитализации неизменной и динамический пересчет средней себестоимости на основе нового количества и прежней капитализации. Для обычных расходов и отгрузок логика возврата себестоимости сохранена без изменений. | Нет |
| 2026-07-04 | Согласование отката остатков при восстановлении списаний в restoreTransaction | `Code.gs` | В `restoreTransaction` для расходов-списаний количество уменьшается (`newQty -= qty`), капитализация не меняется (и обнуляется при `newQty === 0`), а средняя себестоимость динамически пересчитывается. При обычной отгрузке поведение осталось прежним. Также в `transSheet.appendRow` добавлены значения `payload.groupId || ''` и `payload.isComponent || false` в конец строки, чтобы избежать потери метаданных при восстановлении транзакции из корзины. | Нет |
| 2026-07-04 | Предзаполнение количества услуг по умолчанию для «забор» | `src/components/ConfirmModal.tsx` | В `ConfirmModal` при открытии модального окна для отгрузки (`opType === 'Расход'`), если активная услуга содержит в названии слово "забор" (без учета регистра), её количество автоматически предзаполняется значением 1. Пользователь может скорректировать это значение вручную, в том числе обнулить. На приходные операции правило не распространяется. | Нет |
| 2026-07-04 | Новая логика расчета паллет при отгрузке (глобальное среднее) | `Code.gs`, `src/store/useSettingsStore.ts`, `src/store/useWarehouseStore.ts`, `src/components/DirectoryTab.tsx`, `src/components/ConfirmModal.tsx`, `src/components/SkuModal.tsx`, `src/components/SkusTab.tsx` | Внедрено поле `boxesPerPalletGlobal` во всем приложении вместо индивидуальных `boxesPerPallet` у SKU. Логика отгрузки на паллетах теперь срабатывает, если суммарное число коробок >= 10, количество паллет рассчитывается делением общего числа коробок на `boxesPerPalletGlobal` с округлением вверх. Сделано поле редактирования настройки на вкладке справочника, убрано поле из SkuModal и его инициализации. | Нет |
| 2026-07-04 | Подтверждение массового удаления операций с детализацией | `src/components/HistoryTab.tsx` | Изменен диалог массового удаления операций (bulkDeleteConfirm). Теперь выводится заголовок "Удалить N записей?", список выбранных строк (тип, артикул, количество, дата) с лимитом до 15 позиций и счетчиком оставшихся, а также предупреждение красным цветом, если удаляются списания. Сохраняется выделение при отмене. | Нет |
| 2026-07-04 | Ограничение каскада удаления только по groupId комплектов | `src/store/useWarehouseStore.ts` | Упрощена функция `expandIdsWithCascade` для исключения логики группировки по датам/назначениям и автоматического удаления нулевых строк с тем же артикулом. Теперь каскадное удаление строго ограничено связью компонентов комплекта через `groupId`. | Нет |
| 2026-07-04 | Защита виртуальных комплектов при восстановлении транзакций | `Code.gs` | Добавлена проверка в `restoreTransaction` для типа 'Расход'. Если восстанавливаемый артикул является виртуальным комплектом (`kitExists && kitData.type === 'virtual'`) и `writeOffCost === 0`, то изменения остатков, капитализации и средней себестоимости в листе «Остатки» полностью пропускаются, но запись транзакции добавляется в историю в стандартном режиме. | Нет |
| 2026-07-04 | Согласование капитализации при массовом восстановлении списаний | `Code.gs` | В функции `restoreMultipleArchivedItems` при инкрементальном откате остатков для расходов-списаний (где назначение содержит "Списание") теперь изменяется только количество товара (`qtyDiff`), а капитализация (`capDiff`) остаётся неизменной, как и при их удалении/проведении. | Нет |
| 2026-07-04 | Группировка удаленных элементов комплектов в корзине | `src/components/DeletedItemsTab.tsx` | Реализована группировка удаленных транзакций комплектов (где совпадают `groupId` и время удаления `deletedAt`). Для каждой группы выводится одна сводная строка по данным главного комплекта с интерактивным бейджем состава. По клику раскрывается вложенная панель для просмотра компонентов. Чекбоксы и действия восстановления у группы применяют массовое восстановление ко всем входящим записям. | Нет |
| 2026-07-05 | Добавление инструмента ремонта капитализации для администратора | `Code.gs`, `src/components/DirectoryTab.tsx` | Добавление функции `recalcCapitalizationFromAvg()` для пересчета капитализации по формуле `quantity * avgCost` и обновления листа «Остатки»; добавление action-обработчика `recalcCapFromAvg` в `doPost` с защитой `assertAdmin`; добавление кнопки ремонта в «Панель администратора» на вкладке справочника с подтверждением действия и уведомлением. | Нет |
| 2026-07-05 | Расчет себестоимости виртуального комплекта из компонентов при валидации | `src/store/useWarehouseStore.ts` | Добавлена функция `getEffectiveAvgCost(article)` в Zustand-стор. При валидации распознанных позиций в ИИ-обработчике счетов-фактур себестоимость виртуального комплекта теперь рассчитывается динамически как сумма себестоимостей его компонентов на складе, умноженных на их нормы, вместо использования собственного нулевого остатка. | Нет |
| 2026-07-05 | Доработка модалки подтверждения отгрузки и распределения расходов | `src/components/ConfirmModal.tsx` | В `ConfirmModal` для расходов (отгрузок) в колонке «Себест.» теперь выводится итоговая цена с учетом доп. расходов, а базовая цена выводится серым мелким шрифтом ниже. Исправлено распределение в `getServicesExtraPerUnit` при `totalBaseValue === 0` для распределения пропорционально количеству. В нижней панели добавлен блок «Доп. расходы», пересчитываемый динамически при изменении стоимости упаковки, прочих расходов, выбранных услуг и даты поставки. | Нет |
| 2026-07-05 | Исключение 'UpdatedVersion' из списка корзины | `Code.gs` | В функции `getArchivedItems` добавлен пропуск записей с типом `UpdatedVersion` в цикле разбора строк листа «Удаленное». Это предотвращает вывод старых версий отредактированных транзакций в корзине (что вызывало создание дублей при их восстановлении), но сохраняет сами записи в таблице как историю изменений. | Нет |
| 2026-07-05 | Исключение списаний и миграций комплектов из вкладки себестоимости отгрузок | `src/components/ShipmentCostTab.tsx` | В фильтр `shipmentTransactions` добавлено условие исключения транзакций, содержащих слова "Списание" или "Миграция" в поле назначения (destination) без учёта регистра. Это предотвращает ошибочное отображение технологических и внутренних операций как коммерческих отгрузок, при этом они остаются доступными на вкладке истории. | Нет |
| 2026-07-05 | Блокировка редактирования себестоимости расхода в форме редактирования транзакции | `src/components/EditTransModal.tsx` | Для транзакций типа «Расход» в модальном окне редактирования `EditTransModal` поле ввода цены/себестоимости сделано доступным только для чтения (disabled, с серым фоном и курсором-запретом), а также добавлена серая текстовая подсказка о том, что себестоимость расхода пересчитывается автоматически по средневзвешенным показателям склада. Для транзакций «Приход» поле осталось редактируемым. | Нет |
| 2026-07-05 | Добавление переключателя режима даты для отгрузок (фактическая отгрузка vs создание) | `src/components/ShipmentCostTab.tsx` | Реализовано новое состояние `dateMode` ('delivery' | 'created') и визуальный переключатель в шапке вкладки «Себестоимость отгрузки». В `shipmentTransactions` фильтрация по диапазону дат и сортировка динамически адаптируются к выбранному режиму: используют `deliveryDate || date` в режиме 'delivery' или `date` (дата создания) в режиме 'created'. | Нет |
| 2026-07-05 | Повышение видимости ошибок валидации обязательных полей при отгрузке | `src/components/ConfirmModal.tsx` | Внедрено состояние `missingFieldsError` и реактивный `useEffect` для очистки подсветок при заполнении. При обнаружении пропущенных обязательных полей («Дата поставки на маркетплейс» и/или «Стоимость упаковки») в `ConfirmModal` при нажатии на подтверждение, помимо toast-уведомления, выводится красный статический блок с предупреждением и список незаполненных полей над нижней панелью кнопок. Соответствующие поля ввода динамически подсвечиваются красной границей `border-red-400 focus:ring-red-500`. | Нет |
| 2026-07-06 | Исправление бага склеивания транзакций при их обновлении | `src/store/useWarehouseStore.ts` | В функции `handleUpdateTransaction` при получении успешного ответа сервера с полным актуальным списком транзакций в `payloadData.newTransactions` теперь происходит его полная замена в состоянии, а не частичное подмешивание через Map. Это исключает сохранение устаревших/фантомных строк компонентов в интерфейсе до ручной перезагрузки страницы. Механизм склеивания сохранен как запасной fallback-вариант на случай отсутствия `newTransactions`. | Нет |
| 2026-07-06 | Корректировка rowIndex при каскадном удалении компонентов комплекта | `Code.gs` | Исправлен баг в функции `deleteTransaction`: при удалении строк компонентов комплекта, расположенных в таблице выше главной строки транзакции, текущий `rowIndex` главной транзакции теперь динамически уменьшается на 1 при каждом удалении строки с индексом `< rowIndex`. Это гарантирует, что итоговый `transSheet.deleteRow(rowIndex)` удалит именно главную строку, а не строку под ней, предотвращая появление неубираемых дубликатов на листе. | Нет |
| 2026-07-06 | Исправление фильтрации старых версий транзакций в корзине | `Code.gs` | В функции `getArchivedItems` исправлена фильтрация строк типа `UpdatedVersion`. Ранее фильтр проверял колонку `type` (`row[1]`), где всегда записано значение `'Transaction'`. Теперь условие фильтрации анализирует поле `type` внутри упакованного JSON-объекта в колонке `dataJSON` (`row[3]`). Это полностью исключает попадание промежуточных версий отредактированных записей в корзину. | Нет |
| 2026-07-06 | Исправление карточки «Всего отгрузок» на вкладке себестоимости в режиме «По месяцам» | `src/components/ShipmentCostTab.tsx` | В режиме просмотра `viewMode === 'monthly'` заменен вывод количества уникальных артикулов (`monthlySummary.length`) на точный подсчет числа физических поставок (`monthlyShipmentCount`). Для этого добавлен хук `useMemo`, который группирует главные транзакции выбранного месяца в уникальный `Set` ключей вида `formatDateStr(t.date) + '|' + extractDestinationName(t.destination)`. | Нет |
| 2026-07-06 | Поддержка тумблера режима дат в помесячной сводке себестоимости | `src/components/ShipmentCostTab.tsx` | Внедрена поддержка переключателя `dateMode` ('delivery' | 'created') для режима просмотра «По месяцам». Добавлен вспомогательный хелпер `getTxMonthKey(t)`, динамически возвращающий месяц по дате отгрузки (`t.deliveryDate || t.date`) или по дате создания (`t.date`). Данный хелпер применен в `availableMonths`, `monthlySummary` и `monthlyShipmentCount` с добавлением его в массивы зависимостей `useMemo`. | Нет |
| 2026-07-06 | Создание паспорта проекта «Интеграция с Ozon» (автопилот) | `docs/OZON_PLAN.md` | Создан подробный паспорт проекта интеграции с Ozon, содержащий реестр принятых решений, описание фаз разработки от Фазы 0 (Предохранители) до Фазы 6 (Боевой запуск) и текущие заметки. | Нет |
| 2026-07-06 | Добавление кнопки «Резервная копия БД» | `Code.gs`, `src/components/SettingsTab.tsx`, `docs/OZON_PLAN.md` | В Apps Script добавлена функция `backupDatabase()`, копирующая Google-таблицу в отдельную папку «Резервные копии БД Склад» на Диске с таймстампом в названии. Во вкладке «Настройки» для администраторов добавлена кнопка «Резервная копия БД» с анимацией загрузки, обработкой ошибок и информационным описанием. Статус пункта 2 в `OZON_PLAN.md` переведен в завершенный. | Нет |
| 2026-07-06 | Актуализация статусов и правил в паспорте проекта Ozon | `docs/OZON_PLAN.md` | В паспорте проекта `OZON_PLAN.md` пункт 2 (Резервная копия БД) отмечен как полностью выполненный с детализацией решения. Также обновлен раздел «Заметки для следующего чата» и добавлены 2 новых правила работы с планом. | Нет |
| 2026-07-06 | Добавление кнопки «Создать/обновить тестовую БД» | `Code.gs`, `src/components/SettingsTab.tsx`, `docs/OZON_PLAN.md` | Изменена сигнатура `setupDatabase` для работы с произвольными объектами таблиц. Реализована функция `createOrUpdateTestDatabase()` в `Code.gs`, которая делает копию боевой таблицы в папку Диска «Тестовая БД Склад», очищает листы «Пользователи» и «Сессии» с записью заголовков, прогоняет `setupDatabase(testSs)` и удаляет старую копию. Во вкладку настроек администратора внедрена янтарная (amber) кнопка с иконкой и описанием. Статус пункта 3 в `OZON_PLAN.md` переведен в завершенный. | Нет |
| 2026-07-06 | Перевод кнопки создания тестовой БД на ConfirmDialog | `src/components/SettingsTab.tsx` | Заменен заблокированный в песочнице фрейма вызов `window.confirm` в обработчике `handleCreateTestDb` на модальное окно `ConfirmDialog`. Добавлен стейт `showTestDbConfirm` и подключен компонент диалога с подробным предупреждением. | Нет |
| 2026-07-06 | Повышение надежности очистки старых тестовых БД | `Code.gs` | Переписан механизм удаления старых копий тестовых БД в `createOrUpdateTestDatabase()`. Вместо поиска по сохраненному ID, функция теперь итерируется по всем файлам в папке «Тестовая БД Склад» и отправляет в корзину (`setTrashed(true)`) все копии кроме только что созданной. Возвращаемый результат дополнен полем `trashedOld`. | Нет |
| 2026-07-06 | Актуализация статуса и заметок в паспорте Ozon (пункт 3) | `docs/OZON_PLAN.md` | В паспорте проекта `OZON_PLAN.md` пункт 3 отмечен как выполненный, подробно расписано техническое решение (использование ConfirmDialog вместо window.confirm, параметры Script Properties, очистка папки Диска). Также обновлен раздел «Заметки для следующего чата» с указаниями для Фазы 0.5/Фазы 1. | Нет |
| 2026-07-07 | Добавление серверной маршрутизации для режима разработки | `Code.gs` | Добавлена глобальная переменная `_devModeSpreadsheet` и ее инициализация/очистка в `doPost()`. Внедрена условная маршрутизация: если `payload.devMode === true` (для администраторов, кроме исключенных публичных/сервисных действий), getSpreadsheet() переопределяет активную таблицу на тестовую БД с ID из Свойств Скрипта. 5 вспомогательных функций миграций переведены на использование `getSpreadsheet()`. | Нет |
| 2026-07-07 | Клиентская часть «Режима разработки» | `src/store/useWarehouseStore.ts`, `src/components/SettingsTab.tsx`, `src/App.tsx` | В интерфейс и состояние стора добавлен флаг `devMode` (инициализируется из localStorage) и его экшен-мутатор `setDevMode`, который принудительно перезагружает остатки и архивные транзакции после переключения. Метод `fetchGas` дополнен автоматическим расчетом и включением в payload значения `devMode: true` для всех непользовательских запросов администраторов. В `SettingsTab` в секцию администратора добавлен красивый тумблер с вызовом `setDevMode` и выводом информационных тостов. В `App.tsx` настроено отображение красной рамки толщиной 4px по контуру экрана и плавающего предупреждающего баннера в шапке при активном режиме. | Нет |
| 2026-07-07 | Актуализация статуса и заметок в паспорте Ozon (пункт 4) | `docs/OZON_PLAN.md` | В паспорте проекта `OZON_PLAN.md` пункт 4 (Режим разработки) отмечен как полностью выполненный с детализацией серверных и клиентских решений. Также обновлен раздел «Заметки для следующего чата» с подробными указаниями для Фазы 0.5 (регресс стандартного режима) и напоминанием о запрете использования window.confirm. | Нет |
| 2026-07-07 | Успешное регрессионное тестирование стандартного режима и закрытие Фазы 0 | `docs/OZON_PLAN.md` | В паспорте проекта `OZON_PLAN.md` пункт 5 (Регресс стандартного режима) переведен в завершенный статус по итогам прогона полного 21-шагового сценария тестирования на боевой БД. Зафиксирован некритичный баг дробления группы в Истории при восстановлении комплектов из корзины. Актуализирован раздел заметок для следующего чата разработки, Фаза 0 официально объявлена полностью завершенной. | Нет |
| 2026-07-07 | Разведка API Ozon и закрытие Фазы 0.5 | `docs/OZON_PLAN.md` | В паспорте проекта `OZON_PLAN.md` пункт 6 (Разведка API Ozon через MCP) переведен в статус выполненного. Разведка выполнена на реальных данных через MCP-сервер, результаты задокументированы во внешнем файле `docs/OZON_API.md`. Получены ответы по структуре поставок, складам и пагинации, а также сделан ключевой вывод об отсутствии актов приёмки по API FBO. Обновлен раздел заметок для перехода к Фазе 1. | Нет |
| 2026-07-07 | Воссоздание документации по Ozon API в рабочей среде | `docs/OZON_API.md`, `docs/OZON_PLAN.md` | Воссоздан подробный технический файл `docs/OZON_API.md` с разведкой Ozon API для FBO (статусы, пагинация, лимиты, структуры). Добавлено правило в `docs/OZON_PLAN.md` о необходимости изменения файлов репозитория строго в рабочей среде AI Studio во избежание их утери при следующей публикации. | Нет |
| 2026-07-07 | Подготовка листа «Внешние отгрузки» к синхронизации с Ozon API v3 | `Code.gs` | Добавлена новая константа `EXTERNAL_SHIPMENTS_HEADERS` с 13 колонками. Обновлены `getExternalShipmentsSheet` и `setupDatabase` для динамического дополнения колонок через `ensureColumns`. Переписана `saveExternalShipments` в форму upsert с точечным обновлением Ozon-специфичных полей и сохранением внутренних статусов. Функция `getExternalShipments` расширена для чтения всех новых полей через `indexOf` с корректной обработкой типов. | Нет |
| 2026-07-07 | Интеграция прокси-обработчика Ozon API v3 | `server.ts` | Переписан обработчик `/api/ozon/check`: устаревшие `/v1`/`/v2` фильтры по дате заменены на современный v3-поток. Реализован Шаг 2 (получение активных заявок `/v3/supply-order/list` с постраничной пагинацией по `last_id`), Шаг 3 (слияние со списком уже отслеживаемых поставок из GAS и расширение опрашиваемого множества заявок), Шаг 4 (пакетный сбор деталей `/v3/supply-order/get` порциями по 50), Шаги 5-6 (формирование данных на уровне поставки-supply, постраничное чтение состава `/v1/supply-order/bundle` только для новых или `'new'` поставок в целях экономии лимитов), и Шаги 7-8 (сохранение в GAS с поддержкой devMode). Старая функция `fetchOzonWithFallback` заменена на `fetchOzonApi` для прямого вызова с валидацией HTTP-статуса и передачей stage. | Нет |
| 2026-07-07 | Клиентское управление Ozon API v3 и расширение типов | `src/store/useWarehouseStore.ts`, `src/types.ts` | Добавлена логика вычисления флага `devMode` перед вызовом `/api/ozon/check` в функции `checkOzonShipments`. Тело запроса расширено для передачи `devMode: true` (если включен devMode и роль административная). Информационное всплывающее сообщение `toast.success` обновлено для отображения количества обновленных записей (`updated`). Расширен интерфейс `ExternalShipment` 7 новыми необязательными полями: `orderId`, `orderNumber`, `ozonStatus`, `ozonStatusDate`, `dropOffWarehouse`, `storageWarehouse` и `timeslot`. | Нет |
| 2026-07-07 | Защита от превышения лимитов Ozon API (ограничение частоты) | `server.ts` | Реализована защита от ошибок `429` в функции `fetchOzonApi`. Добавлен вспомогательный хелпер `sleep` и трекер `lastOzonCallAt`. Обеспечен обязательный интервал 400 мс между последовательными запросами к Ozon. Реализован цикл до 5 попыток с экспоненциальным нарастанием времени ожидания (1с, 2с, 3с, 4с) при получении статуса 429. При исчерпании попыток генерируется понятная ошибка с httpStatus 429 и рекомендацией повторить позже. | Нет |
| 2026-07-07 | Актуализация статуса Фазы 1 в паспорте Ozon (пункт 7) | `docs/OZON_PLAN.md` | В паспорте проекта `OZON_PLAN.md` пункт 7 (Синхронизация по Ozon API v3) отмечен как полностью завершенный с детальным техническим обоснованием. Добавлены два новых подпункта 8а (интерфейс просмотра поставок с раскрытием состава) и 8б (оптимизация синхронизации/избежание дублирования состава при неизменном статусе Ozon). Обновлен раздел «Заметки для следующего чата» для плавной передачи фокуса разработки на следующий этап. | Нет |
| 2026-07-07 | Разработка read-only вкладки поставок Ozon | `src/components/OzonSuppliesTab.tsx`, `src/App.tsx`, `package.json` | Создан новый интерактивный React-компонент `OzonSuppliesTab.tsx` с группировкой по `orderId` -> `orderNumber` -> `postingId`, иерархическим раскрытием состава поставок и табличным просмотром SKU. Интегрирована кнопка синхронизации с защитой от повторного нажатия. Вкладка добавлена во все системные охранные механизмы `App.tsx`, ограничив доступ только для администраторов. Установлен пакет `motion` для плавных анимаций переходов и раскрытия панелей. | Нет |
| 2026-07-07 | Улучшение обработки ошибок проксирования к GAS | `server.ts` | Реализована безопасная обработка ответов от Google Apps Script: ответ сперва считывается как текст. При ошибке парсинга JSON (например, если возвращается HTML-страница ошибки или авторизации Google) сервер больше не падает по `SyntaxError`, а возвращает корректный HTTP-статус `502` с информативным сниппетом ответа для легкой отладки. | Нет |
| 2026-07-07 | Исправление извлечения склада хранения вРЦ (crossdock) | `server.ts` | Исправлен баг в обработчике `/api/ozon/check`: для crossdock поставок Ozon не возвращает `storage_warehouse.name`. Теперь при отсутствии этого поля корректно используется `macrolocal_cluster_id` в виде "Кластер ID". | Нет |
| 2026-07-07 | Документирование метода Ozon ClusterList v1 | `docs/OZON_API.md` | Добавлен новый раздел с описанием endpoint `POST /v1/cluster/list` для сопоставления ID макрокластеров с человекочитаемыми названиями направлений поставок и складов. Обновлена таблица соответствия колонок. | Нет |
| 2026-07-07 | Интеграция сопоставления кластеров Ozon через ClusterList | `server.ts` | На уровне модуля добавлен in-memory кэш сопоставления `macrolocal_cluster_id` в человекочитаемые названия направлений (TTL 24ч). Добавлена функция `loadClusterMap(keys)` для последовательного вызова `POST /v1/cluster/list` (для OZON и CIS типов) и заполнения карты соответствий. Интегрирована проверка кэша и фолбеки при ошибках загрузки. Изменен расчет `storageWarehouse` для выстраивания приоритета (оригинальное имя -> имя из справочника кластеров -> статический fallback "Кластер ID" -> пустая строка). | Нет |
| 2026-07-07 | Актуализация статуса Фазы 1 в паспорте Ozon (пункт 8а) | `docs/OZON_PLAN.md` | В паспорте проекта `OZON_PLAN.md` пункт 8а отмечен как полностью выполненный с перечислением всех технических деталей, включая защиту в `App.tsx`, группировку/сортировку, цветные бейджи статусов, in-memory кэш на прокси и обработку ошибок. Обновлен раздел заметок для следующего чата. | Нет |
| 2026-07-07 | Добавление блока «Воронка поставок Ozon» на дашборд | `src/lib/ozonStatus.ts`, `src/components/OzonSuppliesTab.tsx`, `src/components/Dashboard.tsx` | Создан новый модуль `ozonStatus.ts` с экспортируемыми STATUS_DICT, getStatusDetails, getStatusLabel и константой сортировки STATUS_FUNNEL_ORDER. Выполнена замена локальных переменных в OzonSuppliesTab.tsx на импорты. На дашборде реализован новый аналитический блок «Воронка поставок Ozon», доступный только администраторам. Блок подгружает данные из Zustand-стора (fetchExternalShipments), считает поставки по каждому статусу, отображает компактные карточки с цветовыми бейджами и динамической подписью с количеством уникальных заявок и временем синхронизации, а также переводит на вкладку Ozon по клику. | Нет |
| 2026-07-08 | Актуализация статуса Фазы 1 в паспорте Ozon (пункт 8) | `docs/OZON_PLAN.md` | В паспорте проекта `OZON_PLAN.md` пункт 8 (Воронка поставок на дашборде) официально отмечен как полностью выполненный (✅ 08.07.2026). Отражен выбор варианта «по листу» без живых запросов к Ozon API, использование общего модуля `ozonStatus.ts`, кликабельность карточек и ограничение видимости только для администраторов. Обновлен раздел заметок для следующего чата на пункт 8б. | Нет |
| 2026-07-08 | Оптимизация сохранения поставок saveExternalShipments | `Code.gs` | Функция `saveExternalShipments` переписана с поколоночным сравнением значений перед перезаписью (`setIfChanged`). Это исключает лишнюю перезапись одинаковых значений и накрутку `updatedCount`. Также добавлена строгая защита: при статусе `new` дата отгрузки и состав (`itemsJSON`) обновляются только при наличии непустых значений, предотвращая затирание состава пустыми данными от прокси при отсутствии запроса состава. | Нет |
| 2026-07-08 | Исправление бага сравнения дат в saveExternalShipments | `Code.gs` | В хелпере `normCell` исправлен баг сопоставления дат. Поскольку Google Таблицы автоматически парсят строку вида "yyyy-MM-dd" в ячейке как полноценный объект даты с временем 00:00:00, прежний вывод `yyyy-MM-dd HH:mm:ss` не совпадал с чистой строкой даты от прокси-сервера. Добавлено условие: если дата в ячейке имеет время ровно `00:00:00`, она форматируется без часов/минут/секунд (`yyyy-MM-dd`), предотвращая бесконечные перезаписи и ложные отчеты об обновлениях. | Нет |
| 2026-07-08 | Оптимизация эндпоинта /api/ozon/check | `server.ts` | В обработчике `/api/ozon/check` оптимизирован опрос Ozon API. Теперь при составлении существующей карты поставок отслеживается наличие состава (`itemsJSON`). Запрос состава (`shouldFetchBundle`) для поставок с внутренним статусом `new` выполняется только в двух случаях: статус Ozon изменился по сравнению с сохраненным или состав в листе пуст (для самовосстановления). Это кратно уменьшает количество запросов `/v1/supply-order/bundle` при каждой синхронизации и исключает риск упереться в лимиты Ozon. | Нет |
| 2026-07-08 | Оптимизация фильтрации статусов Ozon в /api/ozon/check | `server.ts` | В эндпоинте `/api/ozon/check` убран статус `OVERDUE` из запроса списка активных заявок Ozon API, а также статус `OVERDUE` добавлен в список финальных статусов для уже отслеживаемых поставок (чтобы исключить их повторный допрос). Новые поставки со статусом `CANCELLED` или `OVERDUE`, которых ещё нет в листе, теперь игнорируются (не вносятся в базу), так как они фактически не отгружались. | Нет |
| 2026-07-08 | Актуализация статуса Фазы 1 в паспорте Ozon (пункт 8б) | `docs/OZON_PLAN.md` | В паспорте проекта `docs/OZON_PLAN.md` пункт 8б («Оптимизация синхронизации») официально отмечен как полностью выполненный (✅ 08.07.2026). Подробно задокументированы решения по точечной записи ячеек при изменении (setIfChanged), учету дат в хелпере normCell, условной перезаписи состава и даты отгрузки только непустыми значениями. Зафиксированы доработки эндпоинта `/api/ozon/check` (интеллектуальный запрос состава, исключение OVERDUE/CANCELLED) и результаты живого тестирования. Сформулирован детальный план реализации по новому пункту 8в «Мультикабинет Ozon» и актуализированы заметки для следующего чата. | Нет |
| 2026-07-08 | Разведка API SellerInfo и фиксация в планах | `docs/OZON_API.md`, `docs/OZON_PLAN.md` | Проведена разведка метода Ozon Seller API `POST /v1/seller/info` (вызов без параметров). Метод подтвердил отдачу имени магазина через `company.name`. Зафиксирована подробная структура ответа, грабли и ограничения в `docs/OZON_API.md`. Описание пункта 8в в паспорте проекта `docs/OZON_PLAN.md` уточнено — теперь вместо ручного ввода имени кабинета будет использоваться автоподгрузка через кнопку проверки ключей с возможностью ручной корректировки. | Нет |
| 2026-07-08 | Поддержка мультикабинетов Ozon в Apps Script | `Code.gs` | Переведена логика хранения ключей Ozon в Script Properties на JSON-массив кабинетов `global_ozonCabinets` [{name, clientId, apiKey}]. Реализована полная обратная совместимость: если свойства нет, старые `global_ozonClientId`/`global_ozonApiKey` автоматически мигрируют в «Кабинет 1» (функция `getOzonCabinets`). Обновлена выдача ключей прокси в действии `getOzonKeys` (добавлено поле `cabinets`, а также сохранены старые поля первого кабинета). Модифицированы функции `getGlobalSettings` и `saveGlobalSettings` для полной поддержки массива кабинетов и их синхронизации с индивидуальными ключами. | Нет |
| 2026-07-08 | Сворачиваемая воронка поставок Ozon | `src/components/Dashboard.tsx` | В блок «Воронка поставок Ozon» добавлен интерактивный механизм сворачивания. Создано состояние `isFunnelCollapsed` (по умолчанию `false`). Добавлена анимированная кнопка `ChevronDown` (вращается на 90 градусов при скрытии). Клики по кнопке или по всему заголовку сворачивают/разворачивают блок карточек и подпись со статистикой, сохраняя драгоценное вертикальное пространство для пользователей. | Нет |
| 2026-07-08 | Добавление колонки «Кабинет» во «Внешние отгрузки» в Apps Script | `Code.gs` | В листе «Внешние отгрузки» добавлена поддержка новой колонки «Кабинет». В константу `EXTERNAL_SHIPMENTS_HEADERS` дописано имя колонки. В функции `saveExternalShipments` реализована инициализация индекса колонки, запись значения только при непустом значении кабинета для обновления существующих записей и базовая запись для новых записей. Функция `getExternalShipments` теперь вычитывает значение колонки и отдает его клиенту в поле `cabinet` для вывода и бейджинга. | Нет |
| 2026-07-08 | Создание эндпоинта /api/ozon/seller-info | `server.ts` | Разработан и внедрен новый POST-эндпоинт `/api/ozon/seller-info`. Он принимает `clientId` и `apiKey` в теле запроса, валидирует сессию пользователя через GAS-проверку (`verifySession`) с кэшированием токенов, а затем выполняет прямой запрос к Ozon API по адресу `/v1/seller/info`. Эндпоинт корректно обрабатывает специфичные ошибки Ozon, включая ошибку блокировки кабинета (HTTP 403 с кодом 7) и неверные авторизационные данные (HTTP 401), возвращая клиенту понятное русскоязычное сообщение. При успехе возвращает `company.name` для автоматического подтягивания названия кабинета. | Нет |
| 2026-07-08 | Мультикабинетная интеграция Ozon Seller в Настройках | `src/store/useSettingsStore.ts`, `src/store/useWarehouseStore.ts`, `src/components/SettingsTab.tsx` | Внедрение поддержки двух Ozon-кабинетов; добавление интерфейса `OzonCabinet`, полей `ozonCabinets` и экшенов `setOzonCabinets` in Zustand-стор настроек; синхронизация их получения в `checkSession`/`handleLogin` из `getGlobalSettings` и сохранение через `saveGlobalSettings`; переработка вкладки «Настройки» с выводом блоков для двух кабинетов, кнопки проверки ключей через прокси `/api/ozon/seller-info` с автоподгрузкой названия и общей кнопкой сохранения. | Нет |
| 2026-07-08 | Мультикабинетный опрос Ozon API на бэкенде (/api/ozon/check) | `server.ts` | Переведен эндпоинт проверки поставок `/api/ozon/check` на опрос нескольких кабинетов Ozon с полной обратной совместимостью (при отсутствии мультикабинетов опрашивается единственный кабинет из старой конфигурации). Активные заявки запрашиваются отдельно ключами каждого кабинета, а уже отслеживаемые поставки опрашиваются ключами кабинета, название которого прописано в строке. При сохранении в Google Таблицы каждая отгрузка помечается названием своего кабинета. | Нет |
| 2026-07-09 | Восстановление статуса пункта 8в в паспорте Ozon | `docs/OZON_PLAN.md` | Исправлен статус пункта 8в («Мультикабинет Ozon») в `docs/OZON_PLAN.md` с ошибочного «выполнено» назад на «в работе» (⬜). Восстановлено подробное описание предстоящего фронтенд-объема работ (бейдж кабинета, выбор в ConfirmModal) и заметок для следующего чата, включая описание справочника кластеров и правил разработки на тестовой БД. | Нет |
| 2026-07-09 | Бейджирование Ozon кабинетов на вкладке поставок | `src/types.ts`, `src/components/OzonSuppliesTab.tsx` | В интерфейс `ExternalShipment` добавлено опциональное поле `cabinet`. На вкладке «Ozon» в группировку заявок `groupedShipments` добавлено поле `cabinet` из первой отгрузки в группе. В заголовке каждой группы заявок добавлен компактный красивый бейдж цвета `sky` со значением кабинета Ozon для наглядной идентификации принадлежности к конкретному магазину. | Нет |
| 2026-07-09 | Фильтрация Ozon поставок по кабинетам | `src/components/OzonSuppliesTab.tsx` | На вкладку «Ozon» добавлено состояние `cabinetFilter` и панель с кнопками для быстрой фильтрации по каждому из доступных Ozon магазинов (плюс опция «Все магазины»). Панель динамически отображается только в случае, если в системе присутствуют 2 и более различных кабинетов. Список заявок корректно фильтруется в реальном времени. | Нет |
| 2026-07-09 | Доступ к названиям Ozon кабинетов всем ролям | `Code.gs` | В функции `getGlobalSettings` добавлено поле `settings.ozonCabinetNames`, содержащее массив названий кабинетов из `getOzonCabinets()`. Значение отдается всем пользователям без ограничений по роли, тогда как полные данные кабинетов `settings.ozonCabinets` с секретными API-ключами по-прежнему скрыты и возвращаются только администраторам. Это позволяет рядовым пользователям выбирать кабинеты Ozon при создании отгрузок без ущерба безопасности ключей. | Нет |
| 2026-07-09 | Интеграция выбора Ozon-кабинета во фронтенде | `src/store/useSettingsStore.ts`, `src/store/useWarehouseStore.ts`, `src/components/UploadTab.tsx`, `src/components/ConfirmModal.tsx` | Внедрен выбор Ozon кабинета при оформлении отгрузки с назначением «Ozon». В `useSettingsStore` добавлено поле `ozonCabinetNames` с экшеном `setOzonCabinetNames`, которые заполняются при авторизации/проверке сессии в `useWarehouseStore`. В `UploadTab` кнопка автоматического оформления FBO отгрузок теперь подставляет имя кабинета в формат `Ozon (Кабинет)`. В `ConfirmModal` добавлена валидация («Магазин Ozon» становится обязательным, если кабинетов два или более), реактивное снятие ошибки подсветки и селектор выбора кабинета, а само имя кабинета автоматически конкатенируется с назначением. | Нет |
| 2026-07-09 | Исправление ReferenceError при инициализации `ConfirmModal` | `src/components/ConfirmModal.tsx` | Исправлена ошибка `Cannot access 'ozonCabinetNames' before initialization` путем переноса объявления переменной `ozonCabinetNames` перед строкой её первого использования в переменной `needCabinetChoice`. Лишнее дублирующее объявление в конце того же блока удалено. | Нет |
| 2026-07-09 | Перенос оформления Ozon-поставок с вкладки Загрузка на Ozon | `src/components/UploadTab.tsx`, `src/components/OzonSuppliesTab.tsx` | Полностью удален блок «Отгрузки Ozon Seller» из вкладки «Загрузка» (`UploadTab.tsx`) вместе со всеми неиспользуемыми импортами, хуками и обработчиками. Логика оформления и игнорирования поставок перенесена на вкладку «Поставки Ozon» (`OzonSuppliesTab.tsx`). Для поставок со статусом `'new'` добавлены адаптивные кнопки «Оформить» и «Игнорировать», которые подготавливают позиции на основе штрихкодов или SKU и открывают окно подтверждения. Код оптимизирован, отсутствуют неиспользуемые переменные. | Нет |
| 2026-07-09 | Групповое оформление Ozon-заявок с уровня группы | `src/store/useWarehouseStore.ts`, `src/components/OzonSuppliesTab.tsx` | Перенесены кнопки «Оформить» и «Игнорировать» со строчек отдельных поставок на заголовок всей заявки (группы) Ozon. Стор `useWarehouseStore` переведен с одиночного `pendingOzonPostingId` на массив `pendingOzonPostingIds` для одновременной обработки. Кнопка «Оформить» на заголовке заявки собирает все новые (`'new'`) поставки в группе, суммирует (агрегирует) одинаковые артикулы в единый список товаров, и готовит модальное окно. При успешном завершении транзакции все эти поставки массово переводятся в статус `'processed'`. «Игнорировать» массово помечает все новые поставки группы как `'ignored'` с одним подтверждением. | Нет |
| 2026-07-09 | Корректировка источника себестоимости Ozon-расходов | `src/components/OzonSuppliesTab.tsx` | Исправлен источник себестоимости при оформлении групповой FBO Ozon-заявки. Вместо справочной цены из SKU Базы (`matchedSku.price`) теперь в первую очередь запрашивается средняя себестоимость со склада из листа Остатки (`avgCost` соответствующего товара в `stock`). Если товара на складе нет в наличии, справочная цена из SKU Базы используется в качестве надежного резервного варианта. | Нет |
| 2026-07-09 | Разрешение перевода Ozon-поставок в статус 'new' | `Code.gs` | В функции `updateExternalShipmentStatus` добавлено разрешение третьего допустимого статуса `'new'`. Это необходимо для корректной работы кнопки возврата отгрузки в статус «Новые», если пользователь удаляет транзакции из истории отгрузок и хочет переоформить данную поставку Ozon заново. | Нет |
| 2026-07-10 | Функция возврата Ozon-заявок в статус «Новые» | `src/store/useWarehouseStore.ts`, `src/components/OzonSuppliesTab.tsx` | Расширен тип `status` метода `markExternalShipment` в `useWarehouseStore.ts` до поддержки статуса `'new'`. В `OzonSuppliesTab.tsx` добавлен callback `handleReturnGroupToNew` с вызовом диалога подтверждения `askConfirmation`. Добавлена кнопка «Вернуть в новые» на заголовке заявки, которая отображается, если в заявке нет новых поставок, но имеются оформленные или проигнорированные поставки. | Нет |
| 2026-07-10 | Себестоимость из getEffectiveAvgCost и проверка остатков в Ozon | `src/components/OzonSuppliesTab.tsx` | Переведено определение себестоимости при подготовке Ozon-заявки на стор-хелпер `getEffectiveAvgCost` (для комплектов суммируются компоненты, для обычных товаров берётся `avgCost` со склада). Также добавлена превентивная проверка наличия всех товаров через `getEffectiveAvailability` при нажатии «Оформить». При дефиците выводится модальное окно с подробным списком недостающих товаров и предупреждением о возможном дублировании отгрузки. | Нет |
| 2026-07-10 | Скрытие обработанных Ozon-заявок и тумблер показа | `src/components/OzonSuppliesTab.tsx` | На вкладке «Ozon» реализовано скрытие оформленных или проигнорированных заявок по умолчанию. Добавлено локальное состояние `showProcessed` и вычисление деривативного массива `displayedGroups` и счетчика `processedGroupsCount`. Добавлен интерактивный чекбокс «Показать обработанные заявки (N)», отображаемый при наличии таковых. Список групп и проверка пустого состояния переведены на использование отфильтрованного `displayedGroups`. | Нет |
| 2026-07-10 | Актуализация воронки Ozon на дашборде | `src/components/Dashboard.tsx` | Воронка «Воронка поставок Ozon» переведена на подсчет исключительно активных отгрузок со внутренним статусом `'new'` (как отображается в Ozon-вкладке по умолчанию). Внутри `useMemo` для `funnelData` отфильтрован массив `actualShipments` по условию `status === 'new'`. Все последующие метрики воронки, включая уникальные заказы (`totalOrders`), общее число отгрузок (`totalShipments`) и время последнего обновления (`lastUpdatedStr`), обновлены для использования этого отфильтрованного массива. | Нет |
| 2026-07-10 | Поддержка JSX (ReactNode) в ConfirmDialog для красивого вывода дефицита | `src/components/ConfirmDialog.tsx`, `src/store/useUIStore.ts`, `src/components/OzonSuppliesTab.tsx` | Изменен тип свойства `message` в интерфейсе `ConfirmDialogProps` с `string` на `React.ReactNode`. В `src/store/useUIStore.ts` тип свойства `message` в состоянии `confirmDialog` и сигнатура действия `askConfirmation` также обновлены до `React.ReactNode`. В `src/components/OzonSuppliesTab.tsx` логика проверки дефицита переведена на структурированный вывод со списком недостающих артикулов, выделением их жирным шрифтом и переводами строк в виде JSX-фрагмента, передаваемого в диалог подтверждения. | Нет |
| 2026-07-10 | Связывание Ozon-поставок с транзакциями и автоматический возврат в статус «Новые» | `Code.gs`, `src/store/useWarehouseStore.ts` | При оформлении Ozon-заявки массив groupId главных созданных транзакций сохраняется JSON-строкой в колонку TransGroupInfo листа «Внешние отгрузки». При удалении транзакций в handleDeleteTransaction и handleDeleteMultipleTransactions вызывается returnLinkedOzonSupplies, которая ищет связанные поставки и возвращает их в статус 'new' в GAS и во фронтенде. | Нет |
| 2026-07-10 | Переименование вкладок и выравнивание заголовков | `src/components/Sidebar.tsx`, `src/components/UploadTab.tsx`, `src/components/OzonSuppliesTab.tsx` | Вкладки переименованы: «Загрузка» -> «Ручная загрузка», «Ozon» -> «Поставки Озон». Также выравнены соответствующие заголовки в контентных панелях (`h2`) для стилистической целостности интерфейса. | Нет |
| 2026-07-10 | Завершение интеграции и закрытие пункта 8в плана Ozon | `docs/OZON_PLAN.md` | Пункт 8в плана по мультикабинетам Ozon официально переведен в статус полностью выполненного (✅ 09.07.2026). Архитектурный план актуализирован, добавлены финальные заметки по структуре связанных транзакций, автовозврату статусов, кэшированию запросов к Ozon API и поколоночному сравнению ячеек. Все решения верифицированы пользователем в боевой среде. | Нет |
| 2026-07-10 | Исправление связывания Ozon-заявок и автовозврата в «новые» | `src/store/useWarehouseStore.ts` | Исправлен механизм автовозврата Ozon-заявок при удалении транзакций из Истории. Привязка переведена с `groupId` на уникальные ID транзакций (`t.id`), так как у обычных (некомплектных) товаров `groupId` отсутствует. Метод `returnLinkedOzonSupplies` адаптирован под новые привязки и больше не требует передачи исходного массива транзакций вторым параметром. | Нет |
| 2026-07-11 | Сверка Ozon-заявок с ручными отгрузками по «отпечатку» | `src/lib/ozonMatch.ts`, `src/components/OzonSuppliesTab.tsx` | Разработана read-only сверка Ozon-заявок с ручными отгрузками по составу SKU, дате и кабинету без изменения данных. На вкладке «Поставки Озон» выводятся индикаторы-бейджы: «Возможный дубль» (точное совпадение по составу и кабинету в пределах 2 дней) или «Похожа на ручную» (точное совпадение до 7 дней либо частичное до 2 дней). При раскрытии заявки отображаются детальные характеристики лучшего кандидата. | Нет |
