# Паспорт проекта «Интеграция с Ozon» (автопилот)

Статус-легенда: ⬜ не начат / 🟨 в работе / ✅ готов (+дата)

## Как работать с этим файлом
- Один пункт плана = одна задача.
- ПРОЦЕСС ОБНОВЛЁН 18.08.2026. Прежняя схема «архитектор пишет промт → Gemini в Google AI Studio пишет код → пользователь публикует из AI Studio» ОТМЕНЕНА. Код пишут агенты Claude Code (planner, coder, tester, logger) в локальном клоне репозитория; решения принимает только оркестратор — главная сессия Claude Code. Публиковать из AI Studio ЗАПРЕЩЕНО: публикация затрёт работу, сделанную через GitHub.
- Этот файл — единственный источник правды по интеграции. При расхождении с чатами верить этому файлу.
- ПРАВИЛА ВЕДЕНИЯ ФАЙЛА (введены 18.07.2026 после третьего повреждения):
  - Выполненный пункт описывается в плане максимум 2–3 строками (дата, суть, ключевые решения). Все детали реализации — только в docs/DEVLOG.md дозаписью в конец.
  - Этот файл изменяется ТОЛЬКО отдельной документационной задачей (правка статуса пункта + раздел «Заметки для следующего чата»). В любой задаче на код этот файл указывается в ограничениях как неприкосновенный — его запрещено изменять даже при восстановлении после ошибок. Перед правкой снимается резервная копия, после правки обязательна проверка целостности: grep -cE '^- (⬜|🟨|✅) [0-9]+\.' должен вернуть прежнее число пунктов.
  - Журнальные файлы (DEVLOG, CHANGELOG, TEST_LOG) — только дозапись новых строк в конец.
- Полная детальная версия плана до 17.07.2026 сохранена в git-истории репозитория, коммит 50a8b64.
- После завершения пункта оркестратор отдельной документационной задачей ставит отметку ✅.
- В начале работы оркестратор сверяет статусы пунктов с ФАКТИЧЕСКИМ кодом в рабочей копии, а не по памяти. Отчётам агентов не доверять: проверять git diff по файлам.
- Путь кода в бой: локальная рабочая копия → коммит в ветку work/<пункт> → слияние в main с разрешения пользователя → push → Cloud Build собирает по Dockerfile → Cloud Run. Файлы репозитория изменяются в локальном клоне, а не в AI Studio.
- Code.gs развёртывается отдельно и вручную через редактор Apps Script: GitHub на него не влияет.

## Реестр принятых решений (менять только по решению пользователя)
| Решение | Выбор |
|---|---|
| Дубли ручных и авто-заявок | Сопоставление по «отпечатку»: артикулы + количества + дата отгрузки (± окно) + назначение Ozon. С 26.07.2026 молчаливая автопривязка ОТКЛЮЧЕНА: заявка с совпавшим составом показывается в баннере новых поставок с меткой «Возможный дубль ручной отгрузки», привязка выполняется только вручную кнопкой «Привязать как дубль» на вкладке «Поставки Озон». Причина — критерий совпадения (точный состав + кабинет + даты ±5 дней) при типовых поставках мог привязать заявку к чужой похожей отгрузке, и остатки не списались бы |
| Доп. услуги | Только «сдаточные» (забор, упаковка, тариф по дате) считаются один раз при сдаче и входят в себестоимость. Хранение — отдельная метрика, в себестоимость поставки не входит. Распределение (с 20.07.2026): сервер (commitTransaction) начисляет долю доп. расходов пропорционально количеству на ВСЕ позиции Расхода — и комплекты, и обычные товары; клиент (ConfirmModal) для Расхода отправляет базовые parsedItems, чтобы не задваивать, итоговую себестоимость собирает сервер |
| Момент ухода остатка | Статус Ozon ACCEPTED_AT_SUPPLY_WAREHOUSE. Заявки до отгрузки (DATA_FILLING, READY_TO_SUPPLY) оформить нельзя; проверка — хелпер isStockDeparted («принято на точке отгрузки или дальше»), т.к. опрос 2 раза в сутки может проскочить статус. Отказ, отмена и просрочка отгрузкой не считаются |
| Услуги в автопилоте | Автопилот обнаруживает отгрузку → всплывающее окно с префиллом позиций и сдаточных услуг → без утверждения пользователем расход не создаётся |
| Опрос Ozon | 2 раза в сутки, 05:00 и 17:00 МСК: триггеры Apps Script (Europe/Moscow) → прокси Cloud Run (https://service-415081166309.us-west1.run.app/) → Ozon Seller API → лист «Внешние отгрузки». Будильник — именно триггер Apps Script (Cloud Run засыпает). Плюс ручной запуск из Настроек |
| Недостача при приёмке | Остатки склада НЕ меняет. Стоимость разницы «отгружено − принято» перераспределяется на себестоимость товаров поставки. Каждая корректировка оставляет след в Истории |
| Пересорт | Признак: «заявлено X, принято меньше» + «заявлено 0, принято N». Проводится только после подтверждения пользователем. Ошибочный товар: списывается со склада + включается в состав поставки + услуги перераспределяются. Не уехавший: возвращается со своей базовой себестоимостью. Зеркалится во всех 6 путях учёта |
| Тестовая БД | Полная копия боевой; листы/колонки создаются идемпотентно (getOrCreateSheet); «Пользователи» чистится от паролей |
| Режим разработки | Тумблер только у админа, действует только в его браузере; запросы идут в тестовую БД; красная рамка + плашка «РЕЖИМ РАЗРАБОТКИ — ТЕСТОВАЯ БД» |
| Правило внедрения | Пункты 7–18 — только в режиме разработки на тестовой БД. Боевая база не участвует до п.20. Операции с остатками (пп. 12, 16) включаются только после просмотра «на показе» |

## План (один пункт = один чат)

### Фаза 0 — Предохранители
- ✅ 1. Паспорт проекта docs/OZON_PLAN.md (этот файл)
- ✅ 2. Кнопка «Резервная копия БД» (админ, готово 07.07.2026): backupDatabase в Code.gs, копии в папке Диска «Резервные копии БД Склад»
- ✅ 3. Кнопка «Создать/обновить тестовую БД» (готово 07.07.2026): createOrUpdateTestDatabase, id/url в Script Properties test_dbSpreadsheetId / test_dbSpreadsheetUrl, в папке всегда один актуальный файл
- ✅ 4. Режим разработки (готово 07.07.2026): флаг _devModeSpreadsheet в doPost (только админ), devMode в fetchGas, сессии/логин всегда по боевой, красная рамка
- ✅ 5. Регресс стандартного режима (готово 07.07.2026): 21/21 шагов на боевой БД. Известный хвост вне плана: восстановление отгрузки комплекта из корзины дробит группировку в Истории (остатки корректны)

### Фаза 0.5 — Разведка API Ozon
- ✅ 6. Живые ответы Ozon в docs/OZON_API.md (готово 07.07.2026). Главное: API НЕ отдаёт приёмку/пересорт по FBO (акты — только PDF в FBS/FBP) → пп. 13–15 через ручной ввод. Учёт на уровне supply_id (вРЦ-заявка = несколько поставок); допоставки — по order_tags.original_supply_id; order_ids в /v3/supply-order/get — строками; пагинация — last_id до пустого; остатки складов Ozon — /v1/analytics/stocks (premium, обновление 2 раза в сутки)

### Фаза 1 — Связь с Ozon (read-only, тестовая БД)
- ✅ 7. /api/ozon/check на /v3/supply-order/list (готово 07.07.2026): цепочка list → get (батчи 50) → bundle; upsert по supply_id; защита от 429 (пауза ≥400 мс, до 5 повторов)
- ✅ 8. Воронка поставок на дашборде (готово 08.07.2026): счёт по листу без обращения к API; словарь статусов в src/lib/ozonStatus.ts
- ✅ 8а. Вкладка Ozon-поставок (готово 08.07.2026): OzonSuppliesTab.tsx, группировка по OrderID, состав из ПозицииJSON; склад хранения вРЦ = кластер по /v1/cluster/list (кэш прокси 24 ч)
- ✅ 8б. Оптимизация синхронизации (готово 08.07.2026): запись только изменившихся ячеек (normCell для дат), состав перезапрашивается только при смене статуса; новые CANCELLED/OVERDUE пропускаются
- ✅ 8в. Мультикабинет (готово 09.07.2026): Script Property OZON_CABINETS [{name, clientId, apiKey}], колонка «Кабинет», объект «Ozon (Кабинет)»; оформление перенесено на вкладку «Поставки Озон» (позиции заявки объединяются), себестоимость через getEffectiveAvgCost, ранняя проверка наличия; привязка заявки: ID главных транзакций JSON-массивом в TransGroupInfo, при удалении отгрузки автовозврат заявки в 'new'; /api/ozon/seller-info — проверка ключей и автоназвание кабинета

### Фаза 2 — Сопоставление (read-only)
- ✅ 9. Сверка с ручными заявками по «отпечатку» (готово 12.07.2026): src/lib/ozonMatch.ts, точный состав 1:1 + кабинет + дата (≤5 дней «Возможный дубль», ≤7 «Похожа»); режим без даты для READY_TO_SUPPLY (создание ≤7 дней); кнопка «Привязать как дубль» — processed + TransGroupInfo без создания расхода

### Фаза 3 — Автопилот: обнаружение и расход
- ✅ 10. Автоопрос по расписанию (готово 12.07.2026): scheduledOzonCheck создаёт временную admin-сессию (10 мин) и зовёт прокси; триггеры setupOzonSyncTriggers 05:00/17:00 МСК; цель — Script Property ozon_autoSyncTarget ('test'/'prod', сейчас 'test'); управление в Настройках. КРИТИЧНО: action runOzonSyncNow в doPost обрабатывается ДО захвата LockService (иначе дедлок с обратными запросами прокси)
- ✅ 11. Окно «Обнаружена поставка» при входе (готово 13.07.2026): префилл позиций, проверка сдаточных услуг, утверждение пользователем
- ✅ 12. Расход только после отгрузки, дедуп с автопривязкой (готово 13.07.2026): isStockDeparted блокирует оформление до отгрузки; уверенные дубли привязываются автоматически с тостом. ДВИГАЕТ ОСТАТКИ
- ✅ 8г/12а. Попутные фиксы: см. DEVLOG

### Фаза 4а — Приёмка: недостача
- ✅ 13. Ручной ввод принятого количества (готово 14.07.2026): колонка «ПринятоJSON» [{"offerId","accepted"}] по одной строке на поставку; пустая ячейка = «принято = заявлено»; AcceptanceModal, бейджи приёмки (isAcceptanceStage в ozonStatus.ts). Остатки не трогаются
- ✅ 14. Перерасчёт недостачи (готово 15.07.2026): колонка «ПерерасчётJSON», расчёт в src/lib/ozonShortage.ts (computeShortageRecalc: недостача на принятые единицы того же артикула, «принято 0» — пропорционально; излишек блокирует статусом 'surplus'); action saveShipmentShortageRecalc пишет след-записи «Корректировка» (кол-во 0) в Историю. Остатки не трогаются

### Фаза 4б — Приёмка: пересорт (самый строгий блок)
- ✅ 15. Обнаружение и подтверждение пересорта (готово 17.07.2026): ввод незаявленных позиций в AcceptanceModal (offerId = артикул SKU); detectPeresort в src/lib/ozonPeresort.ts (missing/extras); computeShortageRecalc возвращает 'peresort' и блокирует п.14 до проведения; PeresortModal — пары «вместо X уехал Y» по правилу «меньшего из двух»; ПересортJSON: {"pairs":[{fromOfferId,fromArticle,toOfferId,toArticle,qty}],confirmedAt,confirmedBy}. Остатки не трогаются (проверено вживую)
- ✅ 16. Проведение пересорта (готово 20.07.2026). ДВИГАЕТ ОСТАТКИ В 2 СТОРОНЫ: commitShipmentPeresort в Code.gs — пере-проведение расхода заявки (deleteTransaction(isUpdate) + commitTransaction с новым составом, предпроверка нетто-дельт до записей), обновление ПозицииJSON (исходник в ПересортJSON.originalItemsJSON) и TransGroupInfo по OrderID, committedAt/committedBy, след «Корректировка» в Истории; UI: кнопка «Провести пересорт» (только админ), бейдж «Пересорт проведён», модалка read-only. Живой тест через Claude в Chrome + сверка БД до копейки. Попутные фиксы: услуги теперь распределяются сервером на все позиции Расхода (устранено задвоение из ConfirmModal), авто-привязка дублей помечает только отгруженные поставки. Детали в DEVLOG

### Фаза 5 — Остатки Ozon + алерты
- ✅ 17. Зеркало остатков складов Ozon (готово 20.07.2026): прокси /api/ozon/stocks (/v3/product/list, sku из живого ответа, пагинация last_id → /v1/analytics/stocks пачками по 100; PREMIUM_LITE достаточно) → лист «Остатки Ozon» (снимок по кабинетам: пропущенный кабинет сохраняет старое зеркало) → блок на дашборде (карточки-итоги + таблица артикул→склады, кнопка «Обновить»); автообновление вторым вызовом в scheduledOzonCheck (собственный try/catch). Строки warehouse_id=0 → «Без склада (агрегат кластера)», полностью нулевые строки не пишутся. Остатки своего склада не затронуты. Живой тест 20.07: 284 строки, 2 кабинета, сверка лист↔дашборд до единицы, боевая БД чиста. Детали в DEVLOG
- ✅ 18. Алерты Ozon (готово 25.07.2026): модуль src/lib/ozonAlerts.ts (buildOzonAlerts) + блок «Алерты Ozon» на главной странице (только админ, над воронкой). Шесть типов: просрочка/отказ/спор (red по статусу), недостача (amber, гасится при пересорте), пересорт «нужно подтвердить» (amber) и «нужно провести» (violet). Скрытие карточек — localStorage ozon_dismissedAlerts (лимит 1000). Read-only, остатки не трогает.
  Тесты (Claude в Chrome): 20.07 — 9/9, сверены отказ в приёмке и недостача (заявлено 54/принято 44 → «−10»), сворачивание, переход, скрытие и его персистентность. 25.07 — дотест на подсаженных заявках TEST-ALERT-01…04: подтверждены оставшиеся ветки (просрочка, спор, пересорт «подтвердить» и «провести»), порядок групп и гашение недостачи при пересорте. Данные не искажены.
  Замечание для п.19: при ручной подсадке дублей строк в лист ключ алерта (postingId:тип) совпадает у копий — «скрыть» уберёт обе. В бою невозможно (upsert по PostingID).

### Фаза 6 — Боевой запуск
- ✅ 19. Полный регресс на тестовой БД (готово 26.07.2026): три прогона через Claude в Chrome — А (ядро учёта, 25/25), Б (восстановление из корзины + Ozon read-only), В (оформление по заявке, дубль, недостача, пересорт на подсаженных заявках TEST-V-01…04). Все шесть учётных путей подтверждены, склад сошёлся с эталоном до копейки.
  По итогам исправлено 7 дефектов (детали в DEVLOG): из отчёта «Себестоимость отгрузки» исключены корректировки остатка и объект «Склад»; плашка о неполных данных остатков Ozon; плейсхолдер стоимости упаковки; формулировки удаления и склонение в корзине; отключена молчаливая автопривязка дублей.
  Ложные тревоги прогона Б: пустые воронка и алерты — следствие того, что после пересоздания тестовой БД все поставки скопировались обработанными, а не дефект кода.
- ✅ 20. Боевой запуск (готово 26.07.2026): резервная копия боевой БД, автоопрос переключён на боевую (Script Property ozon_autoSyncTarget = 'prod' — свойство создаётся вручную, код его не создаёт и при отсутствии считает целью тестовую БД), зеркало остатков Ozon наполнено в бою (283 строки, 2 кабинета, 42 склада). Попутный фикс Code.gs: вызов /api/ozon/stocks развязан с результатом опроса заявок — раньше при сбое опроса заявок остатки не обновлялись вовсе. Причина ошибки HTTP 400 «invalid value for int64 field order_ids: OrderID» — подсаженные тестовые строки, созданные копированием строки-заголовка; удалены. Сверка после включения: «Внешние отгрузки» без новых строк, «История» 165 строк без изменений, остатки склада не сдвинулись. Детали в DEVLOG

### Фаза 7 — Доработки после запуска
- ✅ 21. Отдельная вкладка «Остатки Озон» (готово 26.07.2026): блок «Остатки на складах Ozon» перенесён с вкладки «Склад» в самостоятельный компонент src/components/OzonStocksTab.tsx, пункт меню только для админа сразу после «Поставки Озон». Перенос без изменения логики и вёрстки
- ✅ 22. Доработка вкладки «Остатки Озон»: из зеркала — в инструмент планирования поставок. ТЗ v3 утверждено пользователем 26.07.2026 (опрос проведён). Правило: один этап = один отдельный чат.
  Утверждённые решения ТЗ v3 (менять только по решению пользователя):
  - Скорость продаж — по СВОИМ данным продаж Ozon (НЕ по ads Ozon), в разрезе кластеров; доли кластеров в продажах в % (по всем товарам и по каждому).
  - Хранение продаж — один лист «Продажи Ozon»: «Неделя (дата понедельника, ISO) × Кабинет × Артикул × Кластер × Количество × Дней», по складам НЕ детализируем. Источник /v2/posting/fbo/list с with.analytics_data:true (склад → кластер через справочник кластеров прокси); продажа = любой статус кроме cancelled, по дате created_at. Свежая зона (~13 недель) хранится НЕДЕЛЬНЫМИ строками (Дней=7); всё старше автоматически уплотняется при синхронизации в фиксированные 28-дневные блоки (Дней=28) с якорем 01.01.2024 (понедельник): складываются только ПОЛНЫЕ блоки из 4 недель, ключ Период × Кабинет × Артикул × Кластер; границы блоков не зависят от текущей даты, уплотнение детерминировано и идемпотентно (уточнение ТЗ v3 по решению пользователя от 26.07.2026). Синхронизация перезапрашивает последние 3 недели и ПОЛНОСТЬЮ пересобирает эти недельные строки (полная замена, идемпотентно — подхват отмен задним числом, обрыв не приводит к задвоению). Ретенция: строки старше 78 недель (18 месяцев, настройка) удаляются автоматически при синхронизации. Первичная загрузка — вся доступная история API (~10–11 месяцев, окнами ≤ 1 года; запускается автоматически, когда лист пуст). Автообновление тем же прогоном, что и остатки. Средненедельные продажи = Количество ÷ (Дней/7).
  - Привязка к кластеру — только при заполненном идентификаторе: строки остатков без «КластерID» (агрегаты «Без склада (агрегат кластера)») и строки продаж «Без кластера» участвуют в ОБЩИХ итогах и общей скорости продаж, но не привязываются к конкретному кластеру и не входят в кластерные расчёты покрытия и рекомендаций (решение пользователя от 26.07.2026).
  - Ручной запуск синхронизации («Запустить проверку сейчас») выполняется в ФОНЕ через одноразовый триггер (по образцу архивации транзакций): кнопка мгновенно получает ответ «запущено», статус подтягивается позже. Устраняет таймаут «Ответ не является json» при длинной цепочке заявки + остатки + продажи (решение этапа B, 26.07.2026).
  - Скорость продаж = сумма последних 4 ПОЛНЫХ недель ÷ 28 дней; текущая незавершённая неделя в расчёт не входит. Прогноз по сезонности на исторических неделях — не в п.22, отдельный будущий пункт (данные для него копятся с этапа B).
  - Расчётный остаток кластера = Доступно + В пути + Возвраты × (настройка «% возвратов, возвращающихся в продажу»); «Готовим к продаже» в расчёт не входит. Покрытие, дней = (расчётный остаток − скорость × неснижаемые дни) ÷ скорость.
  - Неснижаемый остаток — в ДНЯХ продаж, одна глобальная настройка; в штуках применяется по каждому кластеру отдельно (скорость кластера × дни). Рекомендация поставки — ПО КЛАСТЕРАМ, при покрытии ниже целевого: нужно = скорость × (целевой запас + неснижаемые дни) − расчётный остаток, округление ВВЕРХ до целых коробок (ШТ/КОР из SKU Базы), формат «N коробок (M шт)»; ограничение остатком Моего склада (виртуальные комплекты — по компонентам, min(остаток ÷ норма)). ИЗМЕНЕНИЕ ТЗ от 27.07.2026 (утверждено пользователем): остаток Моего склада ограничивает НЕ каждый кластер по отдельности, а распределяется между кластерами по очереди — сначала приоритетные кластеры (по убыванию коэффициента), затем остальные по возрастанию покрытия; каждому выдаётся целое число коробок, пока остаток не исчерпан, кому не хватило — урезанная рекомендация с признаком limitedByMyStock и оранжевой подсветкой. Прежнее правило «ограничение по каждому кластеру отдельно» давало неисполнимую сумму рекомендаций (пример с живой проверки: на складе 96 шт, рекомендации по кластерам суммарно 198 шт). Исключённые кластеры (решение пользователя от 27.07.2026): кластеры, отмеченные в настройках как «без поставок», не получают рекомендаций поставок и неснижаемый запас к ним не применяется; их остатки и продажи продолжают учитываться в общих итогах, долях кластеров и сигнале «пора заказать на фабрике». Обязательное требование к этапам D и F. Приоритетные кластеры (решение пользователя от 27.07.2026): в настройках Ozon кластер отмечается галочкой «Приоритетный» и получает индивидуальный коэффициент (по умолчанию 1,5); для такого кластера целевой и неснижаемый запас умножаются на коэффициент, за счёт чего рекомендация загорается раньше и объём выше, а в очереди распределения остатка он идёт первым. Хранится строкой priorityClusters в формате «КластерID:коэффициент» через запятую. Кластер не может быть одновременно приоритетным и «без поставок»: отметка одного снимает другую.
  - Заказ на фабрике: сигнал «пора заказывать» при (расчётный остаток всех кластеров + Мой склад) ÷ скорость < срок поставки товара + неснижаемые дни; объём = скорость × настройка «объём заказа в днях»; вывод в ШТУКАХ + рядом пересчёт в коробки. Срок поставки от фабрики до маркетплейса — новая колонка в SKU Базе (свой у каждого товара: Китай/Россия). ИЗМЕНЕНИЕ ТЗ от 27.07.2026 (утверждено пользователем): сигнал срабатывает по ЛЮБОЙ из двух причин — reason 'total' (общего остатка хватит меньше, чем на срок поставки + неснижаемые дни) или reason 'clusterDeficit' (у кластеров есть непокрытая потребность в поставке, а Мой склад её не закрывает). Причина: остаток между кластерами Ozon не перебрасывается, поэтому здоровый суммарный остаток скрывал голодающие регионы — на живой проверке товар с дефицитом 96 шт и пустым складом не давал сигнала. Объём заказа = больший из «скорость × дни заказа» и непокрытой потребности кластеров; непокрытая потребность считается по каждому кластеру как (нужно коробок − выдано коробок) × ШТ/КОР. ОТМЕТКА ЗАКАЗА (решение пользователя от 27.07.2026): факт размещения заказа хранится в листе «Заказы на фабрике» (ID, Артикул, Дата заказа, Количество, Ожидаемое прибытие, Комментарий, Кто, Статус, Дата получения), одна активная запись на артикул, экшены getFactoryOrders / saveFactoryOrder / setFactoryOrderReceived. На остатки, себестоимость и капитализацию отметка НЕ влияет — заказанный товар физически не лежит ни на Ozon, ни на своём складе. Отметка гасит красный сигнал зелёным бейджем с датой прибытия (после этой даты бейдж оранжевый), детали — во всплывающей карточке при наведении, ввод и правка — в модалке по клику. Кнопка «Партия пришла» закрывает заказ и открывает вкладку «Загрузка» с предзаполненным приходом (тип «Приход», объект «Склад», текст накладной с артикулом и количеством): остатки поднимаются штатным маршрутом, потребность и сигналы пересчитываются сами. ПРАВИЛО УЧЁТА от 27.07.2026: приход нельзя провести с себестоимостью ≤ 0 — кнопка подтверждения в ConfirmModal блокируется, поле цены подсвечивается красным (иначе количество выросло бы без капитализации и средняя себестоимость обрушилась бы); на «Расход» правило не распространяется.
  - Настройки вкладки (шестерёнка, только админ; глобальные — новый лист «Настройки Ozon» ключ-значение, идемпотентно): число полных недель для скорости (по умолчанию 4), неснижаемые дни, целевой запас на Ozon в днях, объём заказа на фабрике в днях, % возвратов в продажу, срок хранения продаж в неделях (78), кластеры без поставок (строковый ключ excludedClusters — список КластерID через запятую).
  - Таблица: товар → кластеры → склады; скрытие колонок галочками (localStorage); фильтры: кабинет, поиск по артикулу, «только с рекомендациями»; сводный блок долей кластеров; блок «Рекомендации» (поставки и заказы на фабрику). Расчёты — чистые функции в src/lib/ozonCoverage.ts. Два новых типа алертов на главной в ozonAlerts.ts: «пора сделать поставку», «пора заказать на фабрике».
  - Кнопка «Создать поставку в Ozon» (последний этап, первая ПИШУЩАЯ операция в Ozon API): полный мастер — черновик /v1/draft/direct/create (живёт 30 минут, отдельный шаг «только черновик» бессмыслен) → расчёт /v2/draft/create/info → таймслот → заявка /v2/draft/supply/create; состав только Ozon-SKU + quantity; кластер = macrolocal_cluster_id (для этого в лист «Остатки Ozon» добавляется колонка «КластерID»). Количество редактируется, наличие на Моём складе проверяется ДВАЖДЫ: вживую в модалке и повторно на сервере перед отправкой в Ozon. Учёт приложения кнопка не трогает — созданную заявку подхватит штатный опрос. Включение в бой — только «на показе». ИЗМЕНЕНИЕ ТЗ от 28.07.2026 (утверждено пользователем по итогам живой разведки через Ozon MCP): поставки идут НЕ напрямую на РФЦ, а на точку отгрузки (drop-off, склад ЕКАТЕРИНБУРГ_ХАБ_ПЫШМА, warehouse_id 1020002118674000, тип SORTING_CENTER) с развозом Ozon по кластерам. Поэтому вместо /v1/draft/direct/create используется /v1/draft/multi-cluster/create с delivery_info.type = "DROPOFF" и supply_type MULTI_CLUSTER; склад размещения пользователь НЕ выбирает — при MULTI_CLUSTER Ozon возвращает storage_warehouse: null и назначает склад сам. Шаг выбора таймслота из мастера ИСКЛЮЧЁН: поле timeslot в /v2/draft/supply/create необязательно, а таймслот и данные водителя пользователь заполняет вручную в Ozon Seller (решение пользователя от 28.07.2026). Точка отгрузки хранится в настройках Ozon (dropOffWarehouseId / dropOffWarehouseName / dropOffWarehouseType) и ищется через /v1/warehouse/fbo/list — Ozon может её сменить. Тарифный лимит бесплатной отгрузки в кластер (настройка maxBoxesPerCluster, по умолчанию 30) в API не отражён вовсе, поэтому проверяется на стороне приложения и только предупреждает, не блокируя. Логика главной кнопки мастера: первое нажатие создаёт черновик и, если Ozon подтвердил состав полностью, сразу создаёт заявку; после любой правки состава кнопка становится «Пересчитать в Ozon» и заявку не создаёт.
  Детали API — docs/OZON_API.md, секция «Разведка для пункта 22 (26.07.2026)».
  Этапы: ✅ A. Разведка API (26.07.2026) → ✅ B. Продажи FBO и КластерID (готово 26.07.2026): прокси /api/ozon/sales (/v2/posting/fbo/list, недели по МСК, окна ≤56 дней, кластер по warehouse_id через справочник прокси) → лист «Продажи Ozon»: свежие ~13 недель по 7 дней, старше — 28-дневные блоки от якоря 01.01.2024 (колонка «Дней» 7/28), пересборка последних 3 недель, ретенция 78 недель, первичная загрузка автоматически при пустом листе; «КластерID» в «Остатках Ozon»; ручной запуск синхронизации переведён в фоновый триггер (устранён таймаут «Ответ не является json»); модуль «Статус системы» в сайдбаре — общая лампочка + детали GAS/заявки/остатки/продажи, доступен всем; секции трёх частей прогона в «Автоопрос Ozon». Идемпотентность и компакция проверены вживую на тестовой БД (4875 → 3030 строк), в бою с 26.07.2026. Детали в DEVLOG → ✅ C. Настройки (готово 27.07.2026): лист «Настройки Ozon» — 6 числовых ключей + строковый excludedClusters, идемпотентное самозаполнение дефолтами (4/7/30/60/80/78), ретенция продаж читает лист с fallback на константу; колонка «Срок поставки (дни)» в SKU (leadTimeDays: Code.gs + SKU База UI); модалка «Настройки Ozon» с шестерёнкой на вкладке «Остатки Озон», подсказки к полям, чекбоксы «Кластеры без поставок»; справочник кластеров: лист «Кластеры Ozon» (КластерID/Название/Добавлен/Уведомлён), прокси-endpoint /api/ozon/clusters (полный список из /v1/cluster/list, обе группы), четвёртый шаг в scheduledOzonCheck, модалка читает справочник (fallback — кластеры из остатков), тост о новых кластерах при открытии вкладки (первичное наполнение — без тоста); попутно из Code.gs удалены 6 мёртвых разовых функций, включая recalculateStockFully. Детали в DEVLOG → ✅ D. Модуль ozonCoverage.ts (готово 27.07.2026): экшен getOzonSales в Code.gs (чтение листа «Продажи Ozon»), тип OzonSalesRow + ozonSales/fetchOzonSales в сторе, модуль чистых функций src/lib/ozonCoverage.ts — недели по МСК (последние N полных недель, текущая исключается), resolveOzonArticle (ШК Ozon → offer_id), buildSalesSpeed (скорость и доли кластеров), buildClusterStocks (расчётный остаток = Доступно + В пути + Возвраты × %, без «Готовим» и «В заявках»; строки без КластерID — только в общие итоги), calcCoverageDays (исключённые кластеры — без неснижаемого), calcSupplyRecommendation (округление вверх до коробок, ограничение Моим складом по каждому кластеру отдельно, признак limitedByMyStock), calcFactorySignal, сборный buildOzonCoverage. Проверено прогоном на синтетических данных: 42/42 совпадений с ручным расчётом, tsc без ошибок. Детали в DEVLOG → ✅ E. Таблица (готово 27.07.2026): три уровня «товар → кластер → склад», 15 колонок со скрытием галочками и сохранением выбора в браузере, подсказки по каждой колонке, фильтры (поиск по артикулу и названию, кабинет, «только с рекомендациями»), сводный блок долей кластеров, приоритетные кластеры с индивидуальным коэффициентом запаса, распределение остатка Моего склада между кластерами. Секция вкладки больше не сворачивается. Детали в DEVLOG → ✅ F. Блок рекомендаций (готово 27.07.2026): колонка «Заказ на фабрике» с причиной сигнала и подсказкой «хватит на N дн. при пороге M дн.» даже при молчащем сигнале; дефицит кластеров при пустом Моём складе показывается красным в колонке «Рекомендация» и пропускается фильтром «только с рекомендациями»; блок «Рекомендации» над фильтрами (свёрнут по умолчанию, два списка: поставки по кластерам и заказы на фабрике); лист «Заказы на фабрике» с отметкой размещённого заказа, всплывающей карточкой и модалкой; «Партия пришла» передаёт приход в штатный маршрут «Загрузка»; запрет прихода с нулевой себестоимостью. Живой прогон на тестовой БД пройден полностью. Детали в DEVLOG → ✅ G. Алерты на главной (готово 28.07.2026): в src/lib/ozonAlerts.ts добавлена функция buildCoverageAlerts — типы алертов supply_needed и factory_order, цвета severity sky и orange, поле article, postingId стал необязательным; дашборд фоном (через 1,2 с после отрисовки, только для админа) подгружает остатки Ozon, продажи, заказы на фабрике, настройки Ozon и справочник кластеров, считает buildOzonCoverage по всем кабинетам и добавляет алерты покрытия в конец блока «Алерты Ozon»; товар с активным заказом на фабрике сигнала «пора заказать на фабрике» не даёт; ключ скрытия включает объём, поэтому скрытая карточка возвращается при изменении рекомендованных цифр; кнопка «Открыть» на новых карточках ведёт на вкладку «Остатки Озон». Загрузка данных сделана локально в Dashboard.tsx, вкладка «Остатки Озон» не тронута. Живой прогон на тестовой БД пройден. Детали в DEVLOG → ✅ H. Мастер «Создать поставку» (готово 29.07.2026): галочки у рекомендаций по кластерам и панель выбора с лимитом коробок в OzonStocksTab; настройки точки отгрузки с поиском через /api/ozon/dropoff/search и ключ maxBoxesPerCluster; три эндпоинта прокси (dropoff/search, supply/draft, supply/create); экшены Code.gs checkSupplyAvailability, saveOzonSupplyRequest, getOzonSupplyRequests и лист «Заявки Ozon»; единый экран мастера OzonSupplyModal.tsx — вердикт Ozon и редактируемый состав вместе, правка количеств и удаление кластеров с обязательным пересчётом, наполнение коробок и предупреждение о неполных (не блокирующее), таймер ожидания 60 с. Учёт не затронут. Детали в DEVLOG → ✅ I. Live regression via Claude in Chrome with number-by-number verification (2026-08-19): DONE, ONE DEFECT FOUND, FIXED, DEPLOYED AND VERIFIED ON PRODUCTION. Frontend revision sklad-00011-rtq, Code.gs deployed by the owner. After deployment all 19 predicted values matched the screen exactly: trend multipliers for all 13 articles, corrected speeds 4.21 and 1.75, factory orders 312 and 96 pcs, component forecast speeds 19.72 / 19.72 / 15.56 / 4.17, order costs 161 061 and 130 112 RUB. Sales payload grew from 1192 to 1389 rows (+16.5%), coverage build time unchanged at 6-11 ms. A third factory-order alert appeared, exactly the one the truncated window had been hiding. Метод: числа экрана сверены с независимым расчётом по выгрузке боевой базы (не теми же функциями, иначе проверка была бы кругом). СОШЛОСЬ: скорость продаж по 13 товарам, расчётный остаток 13 из 13, покрытие, порог, объём дозаказа и коробки, множитель тренда 9 из 9, коррекция скорости 3 из 3, доли кластеров (всего 1292 шт), стоимость заказа по ценам последних приходов, разворот резерва заявки в компоненты. Подтверждено правило «остатки исключённых кластеров входят в ТРУБУ»: расчёт без них расходился, код прав. ДЕФЕКТ: фронтенд просил у Code.gs последние 12 недель продаж, а окно тренда настроено на 13; счётчик считал различные значения колонки «Неделя» вместе с текущей незавершённой, поэтому полных недель доходило 11. Тренд и коррекция скорости при дефиците считались по укороченному ряду. Доказано воспроизведением: по окну в 11 недель совпали все 12 контрольных чисел, по окну в 13 — ни одного. ЦЕНА В ШТУКАХ: «Полка_выдв_27см» дозаказ 264 вместо 312; «Набор_полок_выдв_бел» сигнал перевёрнут — «товар есть, лежит не там» вместо «дозаказать 96 шт». ПРАВКА: отбор недель в getOzonSales переведён на ДАТУ, размер окна вычисляется в Code.gs по «Настройкам Ozon» (max(trendWeeks, speedWeeks) + 2), жёсткое число 12 из фронтенда убрано. Стенд 76 проверок (было 70), мутационная проверка со старым кодом роняет 4 из 6 новых. ПОСЛЕ РАЗВЁРТЫВАНИЯ ИЗМЕНИТСЯ: у «Органайзер_полка_черн» тренд 1,22 → 1,00 (в расширенном окне есть неделя без продаж, срабатывает фильтр нулевых недель); у остальных тренд сдвинется на сотые; скорость «Полка_выдв_27см» 3,75 → 4,21 и «Набор_полок_выдв_бел» 1,43 → 1,75

- ✅ 23. Локальный зачёт потребности после создания заявки. Проблема: после создания заявки товар продолжает висеть в рекомендациях, потому что колонку «В заявках» заполняет сам Ozon с задержкой в часы — есть риск отправить тот же товар повторно. Решение (утверждено пользователем 29.07.2026): количества из созданных заявок вычитаются из потребности сразу, а зачёт снимается ПО ФАКТУ, а не по таймеру — при статусе заявки CANCELLED, REJECTED_AT_SUPPLY_WAREHOUSE или OVERDUE, при отмене отдельного кластера (supplies[].state = CANCELLED), при переходе в ACCEPTED_AT_SUPPLY_WAREHOUSE и далее, либо когда Ozon показал товар в «В заявках». Предохранитель: 7 дней — если статус получить не удалось, зачёт истекает сам. Источник данных — соединение листов «Заявки Ozon» и «Внешние отгрузки» по OrderID, новых вызовов Ozon API не требуется. Побочная выгода: зачёт подхватит и заявки, созданные вручную в Ozon Seller. ПРОВЕРИТЬ ПЕРВЫМ ДЕЛОМ: сохраняется ли во «Внешних отгрузках» кластер по каждой позиции — при MULTI_CLUSTER состав лежит отдельным bundle_id на каждый кластер, а «Склад хранения» приходит пустым; если кластера нет, зачёт получится только суммарный по артикулу, и объём работ изменится. Решения (утверждены пользователем 29.07.2026): предохранитель зачёта — 7 дней (подрядчики собирают поставку долго, всё это время заявка должна учитываться); при пересечении локального зачёта и колонки «В заявках» из «Остатков Ozon» берётся НАИБОЛЬШЕЕ из двух, а не сумма — оба числа описывают одни и те же заявки. Этапы: ✅ A. Колонка «КластерID» во «Внешних отгрузках» (29.07.2026): прокси передаёт macrolocal_cluster_id из supplies[], в Code.gs колонка пишется только непустым значением и читается строкой; запасной путь — поиск идентификатора по названию кластера в справочнике; счётчики clusterStats в ответе /api/ozon/check; диагностический эндпоинт GET /api/version. Проверено на боевой БД: 28 из 28 поставок получили кластер из API, заявка в статусе DATA_FILLING уже разложена по кластерам → зачёт возможен покластерно с момента создания заявки → ✅ B. Модуль зачёта (готово 29.07.2026): src/lib/ozonPending.ts — чистые функции buildPendingSupplies (источник правды «Внешние отгрузки» покластерно; журнал «Заявки Ozon» подстраховывает только разрыв до ближайшего опроса Ozon и не задваивает зачёт), isPendingCleared / isPendingActive, mergePendingWithRequested («наибольшее из двух» с колонкой «В заявках»); предохранитель 7 дней применяется только при неизвестном статусе, при DATA_FILLING и READY_TO_SUPPLY зачёт держится сколько угодно долго; поле clusterId добавлено в тип ExternalShipment. Модуль ни к чему не подключён, деплой не требовался. Детали в DEVLOG → ✅ C. Интеграция в расчёт покрытия (готово 29.07.2026): buildOzonCoverage принимает готовый зачёт параметром pending, структурный тип OzonPendingLike объявлен прямо в ozonCoverage.ts — импорт ozonPending.ts оттуда запрещён навсегда, это кольцевая зависимость; зачёт прибавляется к остатку ТОЛЬКО при расчёте потребности, покрытие в днях считается по чистому остатку Ozon (товар в заявке ещё не приехал); по каждому кластеру берётся наибольшее из локального зачёта и колонки «В заявках»; товар из заявок резервируется на Моём складе (решение пользователя 29.07.2026) — свободный остаток при распределении уменьшается на зачёт, иначе одна и та же партия будет рекомендована второму кластеру; в calcFactorySignal по-прежнему передаётся полный остаток Моего склада, а не урезанный; журнал «Заявки Ozon» загружается действием getOzonSupplyRequests, Code.gs не менялся. Проверено в бою 29.07.2026 после деплоя приложения. Детали в DEVLOG → ✅ D. Интерфейс и алерты (готово 30.07.2026): на вкладке «Остатки Озон» — колонка «Зачёт» (по товару весь зачёт, по кластеру наибольшее из локального зачёта и «В заявках»), «Мой склад» показывает свободный остаток с подписью «из N · резерв M», модалка «Расшифровка зачёта» по клику на цифру; на вкладке «Склад» — колонка «Свободно» и переключатель видимости колонок с сохранением в localStorage по логину, «Кол-во» скрыта по умолчанию и остаётся неизменной учётной величиной; в алерте «Пора сделать поставку» добавлена пометка об уже созданных заявках. Проверено в бою 30.07.2026: локальный зачёт 118 шт совпал с колонкой Ozon «В заявках». Детали в DEVLOG → ✅ E. Live regression (2026-08-19) on production revision sklad-00011-rtq. Method: the credit was recomputed independently from the «Заявки Ozon» journal and «Внешние отгрузки» sheets and compared with the screen. RESULT — four credited positions, all matching exactly: BowlGrayMini_01 13 pcs in cluster 4036 Воронеж, Органайзер_2_пол_PureWhite 8 pcs in 4071 Ростов, Органайзер_3_пол_прозр 6 pcs and Органайзер_3_пол_PureWhite 6 pcs in 4007 Санкт-Петербург. WHAT THIS PROVES. (1) The credit is built only from live supplies: the two READY_TO_SUPPLY orders 123849090 and 123851723 produce it, while every COMPLETED and CANCELLED order — including the partially cancelled 121166050 — produces none. (2) The promised side benefit works: order 123851723 was created by hand inside Ozon Seller and does not exist in the journal at all, yet its 13 pcs are credited. (3) The «take the LARGER of the two, never the sum» rule holds: Ozon's own «В заявках» column reports exactly the same four positions with the same quantities, and the screen shows 33 pcs in total — summing would have shown 66 and halved the computed need. (4) DECISIVE DEMONSTRATION that the credit actually suppresses the recommendation in the right cluster: for Органайзер_2_пол_PureWhite, Ростов has 17 days of cover and a credit of 8 and gets NO recommendation, while Екатеринбург has WORSE cover at 18 days, no credit, and gets a recommendation of 1 box / 8 pcs. Two clusters with nearly identical cover and opposite outcomes; the credit is the only difference. NOT EXERCISED BY CURRENT LIVE DATA, stated honestly: the 7-day safety timer (every journal entry older than 7 days is already settled by status, so the timer never had to fire), the statuses REJECTED_AT_SUPPLY_WAREHOUSE and OVERDUE, the transition to ACCEPTED_AT_SUPPLY_WAREHOUSE, and single-cluster cancellation while the remaining clusters stay active. These paths are covered by the unit tests of stages A-D, not by production observation.
- ✅ 24. Грузоместа, этикетки и папка документов заявки. ВЫПОЛНЕН 31.07.2026, вся цепочка проверена вживую на боевых заявках. Проблема: заявка создаётся в статусе DATA_FILLING без грузомест, коробки заполняются вручную в Ozon Seller, а три типа файлов вручную скачиваются и раскладываются по папкам Google Диска. Разведка завершена, все формы запросов и ответов записаны в docs/OZON_API.md, секции «Разведка пункта 24 (30.07.2026)» и «Разведка A2 пункта 24 (31.07.2026)» — при работе над пунктом читать их обязательно. ФАКТЫ API, проверены живыми вызовами: ключ всей цепочки грузомест — supply_id (в приложении это PostingID во «Внешних отгрузках»), order_id методы грузомест не принимают и отвечают «Order snapshot with orderId … not exist»; цепочка /v1/cargoes/create (обязательно value.type BOX, не более 30 коробок, delete_current_version: true заменяет раскладку целиком и обходит запрет CANT_DELETE_ALL_CARGOES) → /v2/cargoes/create/info (cargo_id по нашему key, порядок элементов произвольный, сопоставлять только по key) → /v1/cargoes-label/create и /v1/cargoes-label/get (pre-signed ссылка на файл, живёт 24 часа, открывается без ключей) → /v1/cargoes/rules/get (шесть правил готовности, is_valid_distribution_rule бинарное по артикулам); /v1/cargoes/get в любой момент отдаёт текущие грузоместа поставки, поэтому хранить их в Google Sheets НЕ нужно; этикетки грузомест генерируются ТОЛЬКО покластерно — для заявки на N кластеров нужно N вызовов и получится N файлов; методов для «Состава через xls» и «Этикеток всех товаров» в API нет вовсе. РЕШЕНИЯ ПОЛЬЗОВАТЕЛЯ (30–31.07.2026, менять только по его решению): раскладка по коробкам — один артикул одна коробка, остаток уходит в отдельную неполную коробку, смешанных коробок не бывает; отправка раскладки ВСЕГДА перезаписывает состав целиком, метод /v1/cargoes/delete приложением не используется никогда; готовые файлы складываются на Google Диск в родительскую папку с идентификатором 1hTJPqJrkV7qC4YuUi2qm_-_9y9iDjnFe, сам идентификатор хранится в листе «Настройки Ozon» строковым ключом supplyDocsFolderId; на каждую заявку создаётся папка с именем «Озон <order_number>», у тестовой заявки это «Озон 2000061478681», исторические папки назывались по order_id и переименованию не подлежат; в папку заявки кладутся три вида файлов — состав по одному файлу на кластер с именем кластера в том виде, в каком его отдаёт Ozon; этикетки грузомест сохраняются ровно как пришли от Ozon, их НЕ переименовывают и НЕ распаковывают, потому что кластер напечатан на самой этикетке, а Ozon отдаёт то PDF, то архив; этикетки товаров через API недоступны в принципе, поэтому ведётся библиотека PDF по Ozon-SKU в отдельной папке внутри родительской, приложение копирует нужные файлы в папку заявки и сообщает, для каких артикулов этикетки не хватает; файл состава приложение собирает само по структуре из docs/OZON_API.md, все семь колонок берутся из уже имеющихся данных; складывание файлов на Диск выполняет Apps Script через UrlFetchApp и DriveApp — разрешение на Google Диск у скрипта уже выдано и используется в backupDatabase, новых OAuth-scope и сервисного аккаунта не требуется; хранить грузоместа, номера коробок и ссылки на этикетки в Google Sheets не нужно, источник правды — Ozon. Этапы: ✅ A. Разведка цепочки грузомест и этикеток грузомест (30.07.2026), проверена вживую на заявке 120366939 с созданием и перезаписью грузомест → ✅ B. Модуль раскладки src/lib/ozonCargo.ts (30.07.2026): чистые функции buildCargoPlan, buildBoxesPayload, parseCargoRules, matchCargoKeys, compareCargoWithPlan, parseSupplyItems; модуль ни к чему не подключён, деплой не требовался → ✅ A2. Разведка документов заявки (31.07.2026), результат в основном отрицательный, записан в docs/OZON_API.md → ✅ A3. Документирование разведки и переписывание этого пункта (31.07.2026) → ✅ C. Прокси server.ts (31.07.2026): состояние грузомест, сохранение раскладки, этикетки по каждому кластеру, сборка файла состава; требует редеплоя Cloud Run → ✅ D. Code.gs (31.07.2026): ключ supplyDocsFolderId в «Настройках Ozon» и действие сборки папки заявки на Google Диске; требует ручного деплоя Apps Script → ✅ E. Интерфейс, сделано иначе, чем планировалось: по решению пользователя дополнительных кнопок нет, вся достройка запускается автоматически сразу после подтверждения создания заявки в мастере поставки, с экраном прогресса по шагам: кнопка «Грузоместа» с раскладкой и чек-листом готовности, кнопка «Собрать папку поставки»; требует редеплоя Cloud Run → ✅ F. Живой регресс пройден 31.07.2026: заявка создана, грузоместа разложены и приняты Ozon, этикетки получены, папка «Озон <номер>» собрана на Google Диске с файлами состава по кластерам, этикетками грузомест и этикетками ШК товаров. По ходу регресса вскрыты и закрыты три дефекта: имена файлов на Google Диске, созданные на macOS, хранят кириллическую «й» в декомпозированной форме и не находились побайтовым поиском — теперь библиотека индексируется по нормализованному имени (NFC, без расширения, без учёта регистра); заявка, удалённая в Ozon Seller до первого опроса, навсегда удерживала локальный резерв — теперь опрос берёт идентификаторы ещё и из журнала «Заявки Ozon», а отменённым заявкам проставляется статус «Отменена», что мгновенно возвращает остаток на «Мой склад»; заявка, отменённая уже после начала отслеживания, закрывается локально функцией syncOzonJournalFromShipments по состоянию строк «Внешних отгрузок». Полностью отменённые заявки скрыты со вкладки «Поставки Озон» и видны только под галочкой «Показать обработанные». Отменённые записи хранятся 28 дней и удаляются; строка «Внешних отгрузок» удаляется только при выполнении семи условий сразу — подробности в docs/OZON_API.md, секция «Отменённые заявки на поставку»
- ✅ 25. Ускорение приложения: снять глобальный замок с операций чтения. ВЫПОЛНЕНО 30.07.2026, развёрнуто версией Apps Script 131. Волны 1 и 2 закрыты, замок снят с одиннадцати читающих действий, под замком осознанно оставлен только getArchivedItems. ОГОВОРКА ПО ПРОВЕРКЕ: условие этого пункта требовало регресс всех шести учётных путей на тестовой БД, и он НЕ проводился. Обоснование закрытия без него: правка не тронула ни одного пишущего пути, замок для операций записи остался прежним, число функций в Code.gs 110 до и 110 после, число вызовов assertAdmin 31 до и 31 после, блок обхода switch не тронут. Вместо регресса записи выполнена живая проверка чтения на боевых данных 30.07.2026: сверены 211 строк расчётов вкладки «Остатки Озон» до штуки, открыты и проверены вкладки Склад, История, SKU База, Справочник, Поставки Озон. Замер до и после сделан: очередь по замку доказанно разобрана, шесть освобождённых действий выполняются за 4,78 с против 22,5 с суммой поодиночке. РЕШЕНИЕ: полный регресс шести учётных путей провести перед пунктом 24, когда работа коснётся операций записи. Попытка провести смоук-тест 30.07.2026 сорвалась по внешней причине — конфликт сессий при работе двух клиентов под одной учётной записью, см. раздел «Известные хвосты вне плана». Проблема замерена вживую 30.07.2026: при загрузке главной клиент делает 17 запросов к Apps Script, и они выстраиваются в очередь — 1,9 / 2,6 / 3,8 / 4,8 / 5,7 / 6,3 / 7,4 / 8,0 / 8,4 с, полная загрузка около 8,4 с. Причина НЕ в объёме данных: вся боевая БД это примерно 3 700 строк и 400 КБ, меньше 0,5% лимита Google Sheets в 10 млн ячеек; крупнейший лист «Продажи Ozon» (3 099 строк) имеет собственный потолок около 8–9 тыс. строк за счёт ретенции 78 недель и компакции старых недель в 28-дневные блоки. Причина в том, что doPost берёт LockService.getScriptLock() с waitLock(10000) на КАЖДЫЙ запрос, включая чтение, поэтому семнадцать параллельных запросов исполняются последовательно. Решение: замок берётся только для действий, изменяющих данные; читающие действия выполняются без него. Ожидаемый эффект — старт с 8,4 с до примерно 2 с. ОГРАНИЧЕНИЯ ЭТОГО ПУНКТА: список читающих действий составляется поимённо по фактическому коду, а не по префиксу «get» — любое действие, которое пишет хоть одну ячейку, включая автосоздание листов и идемпотентное самозаполнение дефолтами, остаётся под замком; отдельно выявить действия, которые читают и попутно пишут. Помнить про уже известный дедлок: runOzonSyncNow в doPost должен выполняться до waitLock. Проверка: замер времени загрузки главной до и после, затем регресс всех шести учётных путей на тестовой БД. Решение об отказе от разделения базы на две Google-таблицы (утверждено пользователем 30.07.2026): разделение прироста скорости не даёт, ломает целостность операций между листами, которые сейчас защищены одним замком, требует открывать второй файл через openById на каждый запрос и перебрать 53 вызова getDataRange() — признано нецелесообразным; вернуться к вопросу имеет смысл только для архивного файла «Истории» и только через несколько лет. РЕЗУЛЬТАТ АУДИТА ДЕРЕВА ВЫЗОВОВ (30.07.2026, рекурсивный обход по 44 пишущим API): полностью чистые чтения — getStock, getServiceRates, getUsers, getGlobalSettings, getServices (найденный в нём sort относится к JavaScript-массиву, а не к Range.sort); чтения с ленивой инициализацией — getTransactions и getSkus через ensureColumns дописывают колонку-заголовок, getExternalShipments, getOzonSupplyRequests и checkSupplyAvailability через getOrCreateSheet создают отсутствующий лист, getInitialData наследует и то и другое; худший случай этих операций безвреден, потому что два параллельных ensureColumns пишут одно значение в одну ячейку, а второй insertSheet просто вернёт ошибку одного запроса без порчи данных. Под замком остаётся getArchivedItems: он вызывает cleanOldArchivedItems, которая физически удаляет строки старше 60 дней через deleteRow в цикле. ВАЖНО ДЛЯ ВОЛНЫ 2: блок обхода switch на строке «if (action === 'getOzonStocks' || ...» удалять нельзя — у getFactoryOrders нет ветки в switch, а пять веток Ozon-чтений внутри switch содержат assertAdmin, которого блок обхода не выполняет, поэтому маршрутизация этих действий через switch отберёт доступ у неадминистраторов. Этапы: ✅ волна 1 (30.07.2026) — замок снят с шести действий загрузки главной: getInitialData, getStock, getTransactions, getSkus, getServices, getServiceRates; ✅ волна 2 (30.07.2026) — в список LOCK_FREE_ACTIONS добавлены getUsers, getGlobalSettings, getExternalShipments, getOzonSupplyRequests, checkSupplyAvailability; выполнена сразу после волны 1, потому что живой замер показал, что участок кода под замком короткий и риск минимален; ⬜ волна 3 (необязательная) — вынести cleanOldArchivedItems из getArchivedItems в отдельный суточный триггер, после чего getArchivedItems тоже можно освободить от замка. РЕЗУЛЬТАТ ВОЛНЫ 1, ЗАМЕРЕНО ЖИВЬЁМ 30.07.2026 через Performance API браузера: очередь по замку разобрана — шесть освобождённых действий, запущенные одновременно, отработали за 4,78 с против 22,5 с суммой поодиночке; старая лестница 1,9 / 2,6 / 3,8 / 4,8 / 5,7 / 6,3 / 7,4 / 8,0 / 8,4 с больше не воспроизводится. Однако общее время загрузки главной не улучшилось и составляет около 13 с — причина оказалась другой, см. пункт 26.

- ✅ 26. Сократить число обращений к Apps Script при старте. ПРИОРИТЕТ НИЗКИЙ, решение пользователя 30.07.2026: скорость загрузки сейчас не главная задача, пункт выполняется последним. Замеры 30.07.2026 через Performance API на боевом адресе: при загрузке главной клиент делает 17 обращений к Apps Script, вёрстка готова за 0,77 с, последний ответ приходит на 13,4 с; обращения идут шестью зависимыми волнами со стартами на 768, 1617, 3281, 3998, 5885 и 10099 мс, внутри волны запросы честно параллельны, но каждая волна ждёт предыдущую. Стоимость слоёв замерена отдельно: круг браузер → прокси → браузер около 280 мс, круг прокси → Apps Script → прокси от 2 до 4 с, чтение самих данных стоит практически ноль. Доказательство того, что дело не в объёме данных: чтение листа «Остатки» из 17 строк занимает 2,2 с, а проверка сессии по листу «Сессии» из 1 строки — 3,9 с. Фактические размеры листов боевой БД на 30.07.2026: Остатки 17, История 165, SKU 19, Комплекты 6, Услуги 7, Тарифы услуг 4, Пользователи 3, Сессии 1, Удаленное 9, Внешние отгрузки 36, Остатки Ozon 278, Продажи Ozon 3110, Заявки Ozon 1, Кластеры Ozon 27, Настройки Ozon 12. ОТВЕРГНУТАЯ ГИПОТЕЗА: кэшировать проверку сессии бессмысленно, в листе «Сессии» одна строка. Решение: платим за сам вызов, а не за данные, поэтому единственный рычаг — сократить число вызовов. ЗАМЕР 2026-08-20 И ПЕРЕСМОТР ПОСЫЛКИ. The item was built on the premise that we pay for the call, not for the data, so the only lever is fewer calls. Measurement on production disproved that. The whole start-up is ONE request: getOzonSales takes 9-13 s on its own, while the other four Ozon reads take 2.3-5.2 s each and run in parallel. A clean cache-free A/B over three rounds: five parallel calls 10.6 s average, one composite call 9.8 s average — the difference is inside the noise. The reason getOzonSales is expensive is the sheet, not the call: «Продажи Ozon» holds 3194 rows and is read whole every time, yet 1805 of them (57%) are 28-day archive blocks that the date window always discards. Served from the proxy cache the same call takes 250 ms, so KEEPING THE CACHE ALIVE is worth far more than bundling calls. Этапы: ✅ A. Объединить чтения Ozon (getOzonStocks, getOzonSales, getOzonSettings, getOzonClusters, getOzonSyncStatus, getFactoryOrders) в один составной вызов по образцу getInitialData, сохранив текущее поведение блока обхода замка и не добавляя assertAdmin туда, где его сейчас нет; ❌ B. Добавить в тот же составной вызов getUsers, getGlobalSettings, getExternalShipments — ВЫЧЕРКНУТ 2026-08-20 по итогам замера. The stage rests on the same premise the measurement disproved: bundling reads does not make them faster, it makes them SERIAL inside one Apps Script execution, while separate reads run in parallel and are cached individually by the proxy. The cache-free A/B was 10.6 s for five parallel calls against 9.8 s for one composite — inside the noise. Adding three more reads to the composite would lengthen the single longest request on the page instead of shortening the page. What actually worked was the opposite: making the sales sheet smaller and keeping the cache alive; ✅ C. Разобрать в сторе цепочку зависимых волн (2026-08-20, revision sklad-00018-dmw). Everything used to wait for verifySession — about 3 seconds — before the first read was even sent. The token is already in the browser and every read verifies the session inside Apps Script anyway, so the reads now start immediately, alongside the check. The user role is persisted next to the token, so an admin also starts the composite Ozon read — the longest request on the page — at once. An in-flight guard on fetchOzonInitialData keeps two screens from sending the same read twice. TRAP HIT AND FIXED ALONG THE WAY: the boot flag was first read through a store subscription, so the effect closed over the value from the render that happened BEFORE checkSession ran and sent a second getInitialData anyway — and having the flag in the dependency list re-ran the effect and fetched the archive twice too. The flag is now read imperatively via getState() and is not a dependency. RESULT: 13 requests in 3 waves ending at 6.58 s, against 18 requests in 6 waves ending at 12.67 s in the morning; the first request now leaves at 635 ms instead of 1418 ms, and 9 of 13 responses come from cache. Stage A2 landed together with this: Ozon settings and the cluster reference book moved from the local state of both the dashboard and the «Остатки Озон» tab into the store, so the same two reads are no longer made twice per session; saving settings re-reads them explicitly, because the tab no longer refetches on mount. RESIDUAL, still not explained: an occasional page load misses the cache entirely — after the 2026-08-20 work the chain is 4 waves deep, not 6, and the tail is now the last wave starting at ~4.6 s. ЧТО СДЕЛАНО 2026-08-20 (stage A1 plus cache repairs, deployed as sklad-00016-zdf): getOzonInitialData returns stocks, sales, settings, clusters and factory orders in one call, served from the existing lock-free fast path so lock behaviour and permissions are unchanged; the 1200 ms artificial delay in front of the Ozon block is gone — it had arrived with the alerts feature, was never explained, and simply postponed the LONGEST request on the page; the Ozon cache lifetime went from 5 to 60 minutes. THREE CACHE BUGS FOUND AND FIXED, two of them introduced by this very change: the proxy tracks actions in three separate lists and a new action must be added to all of them. (1) getCacheTtlMs returns 0 for an unknown action, so the composite was not cached at all — five cached reads had been replaced by one uncached one and start-up went 12.7 -> 14.5 s. (2) READ_ONLY_ACTIONS drives cache invalidation, retry and the write path; the composite was missing, so every cache miss on it wiped the ENTIRE cache. (3) LONG-STANDING, not ours: getLastPurchasePrices was never in READ_ONLY_ACTIONS although the dashboard fires it on EVERY load — so every single load wiped the whole cache. That is why caching had never helped at start-up. RESULT MEASURED: 18 requests in 6 waves ending at 12.67 s became 14 requests in 4 waves ending at 8.57 s on a warm cache, with 11 of 14 served from cache and the Ozon data on screen at 1.4 s instead of 12.3 s. HONEST CAVEAT: a later reload still missed the cache and took 10.8 s with only 3 of 14 cached; no start-up write was found to explain it and the cause is NOT identified. SALES SHEET SPLIT — DONE AND VERIFIED ON PRODUCTION 2026-08-20. The 28-day blocks moved to «Продажи Ozon Архив»; getOzonSales now reads 1392 rows instead of 3197. DESIGN NOTE: the archive is not frozen history — saveOzonSales rebuilds it on every sync by compacting weekly rows that age out of the 13-week weekly zone and by re-aggregating existing blocks, so the write path reads BOTH sheets and splits again at write time. No manual migration was needed: the legacy rows were read out of the main sheet on the first sync and written back to the archive sheet. MEASURED: getOzonSales alone 5.1/7.8/6.1 s, mean 6.3 s, against 9.1/13.0/9.7 s, mean 10.6 s before — about -40%, in line with reading 43% of the rows. Page load on a cold cache 7.75-9.1 s against the 12.67 s baseline. DATA VERIFIED: 1392 weekly rows and 1805 archive blocks, all 1805 blocks identical to the pre-split export; the only four row differences are in the weekly zone and are fresh sales pulled from Ozon by the same sync. CALCULATIONS UNCHANGED: factory orders still 312 / 504 / 96 pcs, 13 alerts, credits of 13 and 8 pcs intact — and since the order quantity is computed from the forecast speed, an unchanged order proves an unchanged trend. TRAP FOUND IN PASSING: the weekly zone is 13 weeks and trendWeeks is also 13, so the trend has ZERO margin — raising trendWeeks above 13 would silently give it fewer full weeks, because week 14 is already compacted and dropped by the days !== 7 filter. NEXT LEVERS: unchain the start-up waves — everything waits for checkSession to return (~3 s) even though the token is already in the browser and the reads could run alongside it; this is the same area where item 29 hit heavy session bugs, so it needs care. Then stage A2 — the «Остатки Озон» tab still re-fetches settings and clusters that the dashboard already loaded. Also still open: the intermittent cache miss recorded above. Ожидаемый эффект: 17 обращений превращаются в 3–4, время старта с 13 с до примерно 4 с. Наблюдение на будущее: под искусственной нагрузкой из шести одновременных запросов дважды зафиксирован отказ ровно через 30,3 и 30,4 с — где-то стоит таймаут в 30 секунд; при обычной работе не проявляется, но по механике похоже на разовые осечки автоопроса Ozon.
- ⬜ 27. НЕОБЯЗАТЕЛЬНЫЙ. Освободить getArchivedItems от замка. Это бывшая волна 3 пункта 25, вынесена отдельным пунктом 30.07.2026. Сейчас getArchivedItems — единственное читающее действие, оставшееся под LockService, и снять замок нельзя, потому что внутри чтения спрятана уборка: getArchivedItems вызывает cleanOldArchivedItems, которая физически удаляет из листа «Удаленное» строки старше 60 дней через deleteRow в цикле со сдвигом индексов. То есть при каждом открытии вкладки удалённых записей происходит запись. Решение: вынести cleanOldArchivedItems из getArchivedItems, повесить её на отдельный суточный триггер рядом с recalculateDailyAnalytics, после чего getArchivedItems становится чистым чтением и добавляется в список LOCK_FREE_ACTIONS. ПОЧЕМУ НЕОБЯЗАТЕЛЬНЫЙ: замер 30.07.2026 показал, что участок кода под замком короткий, поэтому выигрыш — доли секунды на редко открываемой вкладке, а цена — новый триггер, за которым надо следить. Делать только если появится свободное время или если вкладка удалённых записей начнёт заметно тормозить.
- ✅ 28. Защита от двойного проведения операции. ВЫПОЛНЕН ЦЕЛИКОМ 01.08.2026: все этапы 0, A, B, C, D и E закрыты, развёрнуты на бою и проверены живым регрессом на тестовой БД. Заведён как ПРИОРИТЕТ №1, пункт заведён 01.08.2026 по итогам боевой аварии. ЧТО СЛУЧИЛОСЬ: 01.08.2026 расход по заявке №120361763-1 (OrderID 120044532, кабинет Mercurius, 4 поставки по кластерам, состав 48+30+40 = 118 шт) записался в лист «История» ДВАЖДЫ — в 05:11:14 и в 05:12:26. Со склада ушло 236 шт вместо 118, капитализация занижена на 66 604,38 ₽ (Органайзер_2_пол_белый 48 шт / 24 120,48 ₽, Органайзер_3_пол_PureWhite 30 шт / 21 609,90 ₽, Органайзер_2_пол_PureWhite 40 шт / 20 874,00 ₽). Дублирующие транзакции: 4c7ad462…, 6c6966cf…, 30719393…. Третья попытка проведения была остановлена серверной проверкой наличия в commitTransaction — это единственный рубеж, который сработал. МЕХАНИКА, разобрана по коду и фактическим данным боевой БД: (1) прокси /api/gas обрывает запрос к Apps Script через 30 с через AbortController и отдаёт клиенту HTTP 504, но abort() рвёт только канал прокси → Apps Script, а сам скрипт Google выполнение не прекращает и дописывает все строки до конца; (2) идемпотентности нет — ни клиент, ни Code.gs не отличают повтор той же самой операции от новой такой же; (3) ConfirmModal.tsx закрывает окно только при success === true, поэтому после ошибки окно остаётся открытым с тем же составом и активной кнопкой и провоцирует повторное нажатие; (4) пометка поставок processed идёт отдельными запросами markExternalShipment уже ПОСЛЕ успешного ответа по расходу, поэтому при обрыве все 4 строки «Внешних отгрузок» остались в статусе new с пустым TransGroupInfo, и заявка продолжила предлагаться к оформлению. Замер 01.08.2026 через Performance API на боевом адресе: при обычной загрузке главной самое долгое ЧТЕНИЕ занимает 19,1 с при пороге обрыва 30 с; расход тяжелее любого чтения (захват LockService, запись строк в «Историю» и «Остатки», распределение доп. услуг, возврат getStock и getSkus в ответе), поэтому выход за 30 с штатно ожидаем. Это тот же 30-секундный таймаут, что уже зафиксирован наблюдением в пункте 26. РЕШЕНИЕ УТВЕРЖДЕНО ПОЛЬЗОВАТЕЛЕМ 01.08.2026 — связка A + B + C: A. Ключ операции (идемпотентность): клиент генерирует opId один раз при открытии окна подтверждения и передаёт его с каждым commit; в лист «История» добавляется колонка «OpID»; Code.gs ВНУТРИ захваченного замка проверяет наличие строк с таким opId и при совпадении не пишет ничего повторно, а возвращает прежний результат как успех. B. Пометка поставок внутри той же серверной операции: pendingOzonPostingIds передаётся прямо в action commit, Apps Script одним заходом пишет расход и проставляет processed + TransGroupInfo, отдельные вызовы markExternalShipment из этого пути убираются. C. Честный таймаут: порог обрыва в server.ts для пишущих действий поднимается (Cloud Run допускает до 300 с), текст ошибки заменяется на «Ответ не получен, операция могла быть записана», кнопка «Подтвердить» после ошибки заменяется на «Проверить результат» с перезагрузкой остатков и истории. ОТВЕРГНУТО: вариант D (эвристический поиск уже записанной операции по «отпечатку» на стороне клиента) — при наличии A он не нужен и может ошибиться на двух одинаковых отгрузках подряд; пункт 26 как лекарство от этой аварии не годится, он снижает вероятность, но не устраняет причину. ОГРАНИЧЕНИЯ ЭТОГО ПУНКТА: проверка идемпотентности обязана выполняться ВНУТРИ захваченного замка, иначе два параллельных запроса проскочат мимо неё; opId генерируется один раз на открытие окна подтверждения, а не на каждое нажатие кнопки; правки не должны затрагивать остальные пять учётных путей (удаление одиночное и массовое, восстановление одиночное и массовое, редактирование). Этапы: ✅ 0. Внесение этого пункта в план (01.08.2026) → ✅ A. Code.gs: колонка «OpID» и проверка идемпотентности (01.08.2026, развёрнуто) → ✅ B. Клиент: генерация и передача opId (01.08.2026, развёрнуто) → ✅ C. Code.gs и стор: пометка поставок внутри commit (01.08.2026, развёрнуто) → ✅ D. server.ts и ConfirmModal.tsx (готово 01.08.2026, развёрнуто и проверено вживую). Черновик был написан вне промта в коммите c7fcd39 и содержал три дефекта, исправленные коммитом 66d0eb4: повтор действия commit теперь разрешён ТОЛЬКО при непустом opId (переменные opIdRaw и hasOpId в server.ts) — без ключа повтор запрещён навсегда, иначе возвращается авария с двойным списанием; в READ_ONLY_ACTIONS добавлены пять читающих действий, ошибочно считавшихся записью (getStock, getServiceRates, getOzonStocks, getOzonSales, checkSupplyAvailability), список сверен с LOCK_FREE_ACTIONS в Code.gs; порог обрыва для пишущих действий снижен с 300 до 90 секунд, что даёт трёхкратный запас над самой долгой наблюдавшейся записью в 30 с; в ConfirmModal.tsx через 20 секунд ожидания появляется плашка «Операция идёт дольше обычного», снимающая главную причину аварии — человек не понимал, живо ли приложение, и жал кнопку второй раз. Сохранены без изменений распознавание заглушки doGet, механизм автоповтора и кнопка «Проверить результат». Заодно оставлена правка условия сброса кэша в прокси (решение пользователя 01.08.2026): раньше gasCache.clear() вызывало любое некэшируемое действие, теперь только пишущее — медиана ответа на загрузке упала примерно с 4 до 2,6 с. ЗАМЕРЫ ПОСЛЕ РАЗВЁРТЫВАНИЯ, боевой контур 01.08.2026: две загрузки подряд дали 15 и 16 запросов с максимумом 4 523 и 6 478 мс и НУЛЁМ отказов, тогда как до правки каждая загрузка стабильно давала один запрос ровно на 30 300 мс с ошибкой; пачка из восьми одновременных чтений — 3 066 мс, ноль отказов; пять переведённых в правильную ветку чтений отвечают за 2,1–10,5 с. Сообщение «Ошибка gas» при входе исчезло. Проверено также, что обычная запись без ключа и с ключом по-прежнему проходит и боевых данных не меняет. СТРОГИЙ РЕГРЕСС ЗАПРЕТА ПОВТОРА, тестовая БД 01.08.2026, три сценария с искусственно тяжёлой записью: 1) 60 позиций БЕЗ ключа — ответ пришёл ровно на 90 613 мс с текстом timeout (90s), в лист легло РОВНО 60 строк, то есть попытка была одна; будь повтор разрешён без ключа, ожидание составило бы около 273 с, а строк было бы 120 или 180; 2) 60 позиций с ключом уложились в порог за 42 369 мс, 60 строк; 3) 150 позиций с ключом вышли за порог, автоповтор сработал, строк ровно 150, дублей нет. После тестов тестовая БД возвращена в исходное состояние: 208 транзакций, остаток 66 шт / 47 545,56 ₽. НАЙДЕНО ПРИ РЕГРЕССЕ, завести отдельным пунктом: при обрыве по таймауту Apps Script продолжает выполнение и ДЕРЖИТ ЗАМОК, поэтому автоповтор для commit приходит через секунду, ждёт замок отведённые waitLock 10 секунд и возвращает пользователю «Exception: Lock timeout: another process was holding the lock» вместо понятного текста; данным это не вредит и дублей не создаёт, что подтверждено сценарием 3, но сообщение непонятное. Лечится увеличением паузы перед повтором примерно до 30 с либо увеличением ожидания замка. КОСМЕТИКА, подобрать при следующем касании этих файлов: в server.ts над блоком остался устаревший комментарий «3) Порог обрыва 300 с для пишущих действий», в ConfirmModal.tsx в комментарии слиплось «ижал» вместо «и жал» → ✅ E. Живой регресс на тестовой БД (01.08.2026, пройден). ВОССТАНОВЛЕНИЕ ДАННЫХ АВАРИИ выполняется пользователем вручную до начала работ: удаление трёх дублирующих транзакций от 05:12:26 через вкладку «История» и привязка заявки кнопкой «Привязать как дубль» на вкладке «Поставки Озон». Отдельно подтверждено пользователем 01.08.2026: состав заявки менялся вручную в Ozon Seller (приложение отправило 62 шт, Ozon отдал 118 шт), приложение обязано списывать по фактической отгрузке из листа «Внешние отгрузки», расхождение с журналом «Заявки Ozon» дефектом не является. РЕЗУЛЬТАТ ЭТАПА E (01.08.2026, тестовая БД, четыре сценария): 1) расход 5 шт с новым ключом и привязкой поставки — остаток 66→61, одна строка, postingsLink linked=1; 2) ВОСПРОИЗВЕДЕНИЕ АВАРИИ: поставка принудительно возвращена в статус new, повторена та же операция с тем же ключом — вернулся idempotentHit: true, дубль НЕ создан (остаток 61, транзакций столько же), при этом привязка поставки была доделана повтором, то есть ровно тот сбой, что случился 01.08.2026, теперь самолечится; 3) контроль на ложные срабатывания: тот же состав с новым ключом дал настоящую вторую запись, защита легитимные операции не глушит; 4) две одинаковые операции с одним ключом, отправленные ОДНОВРЕМЕННО — записала только одна, что доказывает работу проверки внутри замка. После тестов данные тестовой БД возвращены в исходное состояние до копейки. Вывод: авария 01.08.2026 на текущем коде технически невозможна. НАХОДКИ ДЛЯ ЭТАПА D, обнаружены замерами через Claude в Chrome 01.08.2026: при загрузке приложения делается 16 запросов к /api/gas, из них 15 укладываются в 2–10 с, а один спорадически упирается ровно в 30 300 мс и падает — это и есть постоянная «Ошибка gas» при каждом входе; поодиночке и малыми пачками те же действия отвечают за 2–4 с, то есть это не очередь и не перегрузка, а редкое залипание отдельного выполнения Apps Script. Вторая причина той же ошибки: иногда POST-запрос прокси доезжает до Apps Script как GET и попадает в заглушку doGet, которая возвращает status 'ok' и текст «Google Apps Script Web App is operational. Use POST for API requests.»; приложение показывает это как ошибку и при этом считает запрос завершённым. Причина в том, что /exec отвечает редиректом, а fetch в server.ts идёт с настройками по умолчанию, и при редиректе 302 метод POST превращается в GET. Состав этапа D: 1) прокси распознаёт заглушку doGet по её тексту и не отдаёт её клиенту как данные; 2) автоповтор в прокси до двух попыток при таймауте и при заглушке — ТОЛЬКО для read-only действий и для commit, потому что у commit есть ключ операции и повтор ему безвреден, остальные пишущие действия повторять запрещено; 3) поднять порог обрыва для пишущих действий, Cloud Run допускает до 300 с; 4) честный текст ошибки и кнопка «Проверить результат» вместо активной кнопки «Подтвердить». ПОБОЧНАЯ НАХОДКА, в этап D не входит, завести отдельным пунктом: в server.ts любое некэшируемое действие вызывает gasCache.clear(), а кэшируются всего шесть действий, поэтому кэш ответов практически не работает. ФАКТЫ О ТЕСТОВОЙ БД, проверены 01.08.2026: маршрутизация в тестовую БД управляется флагом devMode, который клиент кладёт В КАЖДЫЙ ОТДЕЛЬНЫЙ ЗАПРОС (localStorage devMode + роль администратора), а не глобальной настройкой; поэтому любой ручной запрос к /api/gas без этого флага уходит в БОЕВУЮ базу, и перед любыми тестовыми записями обязательно доказывать фактом, куда именно попадают данные. Актуальная тестовая БД на 01.08.2026 — файл «БД Склад — ТЕСТОВАЯ (2026-07-29 11-39)», идентификатор 1FhCEoe954fRbFtDN8Xy4sKzCAJMqaFGTPTLPpQMrpYk; более старая копия «ТЕСТОВАЯ (2026-07-11 19-19)» с идентификатором 1vL4wH9-Bc6JNDZMb8LVDw61s945XiH4jeVqNsis9zHg лежит рядом и в работе НЕ участвует — не перепутать. Действующий идентификатор всегда лежит в Script Property test_dbSpreadsheetId, ни одно read-only действие его не возвращает, поэтому проверять принадлежность базы надо сверкой цифр из приложения с содержимым файла.

- ✅ 29. Массовые сбои связи с Apps Script 04–06.08.2026: диагностика и устранение. СИМПТОМЫ: постоянная «Ошибка GAS», ложное сообщение «Ключи Ozon не подходят» при исправных ключах, вечный спиннер «Загрузка статуса…» и красная надпись «Автоопрос выключен» при работающих триггерах, вылеты на экран входа, сохранение поставки по 3–4 минуты с последующим ложным предупреждением о дубле. Работал один пользователь под одной учётной записью. НАСТОЯЩАЯ ПРИЧИНА, найдена 06.08.2026: функция verifySession, выполняемая при КАЖДОМ запросе, удаляла строку листа «Сессии» при встрече истёкшей сессии через sheet.deleteRow. После снятия глобального замка с читающих действий эти удаления пошли параллельно на одном листе, нумерация строк смещалась в процессе, и вместо истёкших стирались живые сессии. Приложение теряло токен через секунды после входа, получало «Unauthorized: Недействительная сессия», входило заново — цикл замыкался. В логах Cloud Run это выглядело как шесть входов за четыре минуты и 26 ответов об ошибке сессии. Все прочие симптомы были следствиями: ошибка сессии на пути получения ключей превращалась в «ключи не подходят», ошибка сессии в getOzonSyncStatus давала вечный спиннер, повторные входы и повторы запросов создавали всплески нагрузки, на которые Google отвечал страницами 404 и заглушками doGet. ИСПРАВЛЕНО 06.08.2026 удалением одной строки sheet.deleteRow из verifySession, развёрнуто версией Apps Script 138. Результат по логам: ошибок сессии ноль, страниц 404 ноль, заглушек doGet ноль, вход один раз вместо шести, вылеты прекратились. ОТВЕРГНУТЫЕ ГИПОТЕЗЫ, повторно НЕ проверять: (1) переход с getActiveSpreadsheet на openById — сбой был не в доступе к таблице; (2) ручная обработка редиректа с повтором POST по адресу из заголовка Location — ПРОВЕРЕНО И ВРЕДНО, Apps Script выполняет doPost на первом запросе к /exec и отдаёт результат по адресу script.googleusercontent.com только методом GET, повтор POST туда ломает авторизацию с HTTP 502, правка откачена в тот же день; (3) глобальный замок LockService как причина медленных чтений — замок был снят волной 3 с шести Ozon-действий, улучшения это не дало, значит причиной он не был; (4) размер листа «Сессии» — в нём одна строка данных, число 615 было ошибочно прочитано архитектором как количество записей, тогда как это разметка листа. ГЛАВНЫЙ ВЫВОД: три подряд гипотезы, построенные на чтении кода без измерений, оказались неверны, и одна из них сломала авторизацию в бою; причина нашлась за один цикл, как только в прокси появилось журналирование. ПРАВИЛО НА БУДУЩЕЕ: при непонятном сбое связи сначала включается журнал и снимаются факты, и только потом пишется лечащая правка; правка по догадке из кода без подтверждающего замера запрещена. ИНСТРУМЕНТЫ ДИАГНОСТИКИ, добавлены в server.ts 06.08.2026 и остаются в коде: запись GASDIAG по каждому запросу к Apps Script (действие, номер попытки, код HTTP, длительность, счётчик одновременных запросов inflight, признак заглушки, длина ответа) и запись GASBODY с первыми 300 символами ответа, если код не 200 или длина меньше 300; читаются в консоли Cloud Run: сервис → вкладка «Журналы» → фильтр GASDIAG или GASBODY. Второй незаменимый источник — панель «Выполнения» в редакторе Apps Script: она показывает фактическую длительность doPost со стороны Google. Сверка двух источников доказала, что скрипт отрабатывает за 1–6 секунд, а браузер ждёт до 67 секунд, то есть потери происходят вне скрипта. СОПУТСТВУЮЩИЕ ПРАВКИ 06.08.2026, развёрнуты: число попыток для читающих действий снижено с 3 до 2, пауза перед повтором поднята с 1 до 20 секунд (повтор через секунду упирался в замок, который держало прерванное по таймауту, но продолжающее выполняться обращение — дефект был записан в пункте 28 и здесь закрыт), таймаут чтения снижен с 45 до 25 секунд, введено ограничение одновременных обращений к Apps Script GAS_MAX_PARALLEL: значение 3 оказалось слишком жёстким и растянуло вход до двух минут, рабочее значение 8. Настройка Cloud Run: максимум экземпляров сервиса выставлен в 1, чтобы второй холодный экземпляр не поднимался с пустым кэшем ключей Ozon; ВНИМАНИЕ, развёртывание из AI Studio может сбрасывать эту настройку, проверять после каждого деплоя. ЭТАПЫ, все закрыты 06.08.2026: ✅ A. Ложное распознавание заглушки doGet устранено — прокси считал заглушкой любой ответ, содержащий текст «Use POST for API requests» в любом месте и любой длины, а getOzonSyncStatus возвращает журнал прошлых прогонов, куда этот текст попал как запись о старой ошибке, поэтому корректный ответ на 4835 байт постоянно отбрасывался с кодом 502 и виджет автоопроса вечно показывал спиннер; теперь заглушка распознаётся только в ответе короче 300 байт, а HTML — только если ответ с него начинается; дополнительно вручную удалено Script Property ozon_syncHistory; проверено вживую, спиннер исчез; ✅ B. Раздельные сроки жизни кэша вместо общих 30 секунд: справочники 10 минут, данные Ozon 5 минут, оперативные чтения 30 секунд. Побочно закрыта находка пункта 28: кэшировались всего шесть действий, и данные Ozon в их число не входили, поэтому getOzonSales на 498 КБ загружался заново при каждом обращении. Проверено по логам: повторные getOzonSales, getOzonStocks, getOzonClusters исчезли; ✅ C. Точечная очистка кэша: карта CACHE_INVALIDATION на 17 пишущих действий, каждое сбрасывает только затронутые им чтения. Список намеренно узкий — commit, все операции с транзакциями, SKU, комплектами, пересортом, недостачей и setFactoryOrderReceived по-прежнему очищают кэш ЦЕЛИКОМ, потому что показать устаревшие остатки после операции опаснее лишнего запроса; ✅ D. Двойная загрузка getOzonClusters на вкладке «Остатки Озон» убрана: проверка новых кластеров использует ответ, который уже получает загрузчик справочника; ✅ E. Замер расчёта рекомендаций выполнен и ОПРОВЕРГ гипотезу о тяжёлом расчёте: buildOzonCoverage занимает 2–7 мс, сборка строк таблицы 10–23 мс, то есть расчёт не виноват вовсе. Настоящая причина медленного открытия вкладки — объём передаваемых данных: getOzonSales отдавал всю историю продаж, 3207 строк и 498 КБ, ожидание 11–22 секунды. Введён необязательный параметр weeksLimit, клиент запрашивает 12 недель, ответ упал до 185 КБ и 1196 строк, время до 7–9 секунд. Расчёт скорости продаж использует 4 полные недели (настройка speedWeeks), 12 недель дают тройной запас; без параметра функция по-прежнему отдаёт всю историю, поэтому отложенная фича прогнозирования не сломана. Дополнительно устранены холостые пересчёты: расчёт запускался семь раз, первые четыре — без справочника кластеров (clusters=0), из-за чего таблица рисовалась без названий и переписывалась. Введён признак clusterRefsLoaded, расчётов стало пять и все с полным справочником. Замеры OZONPERF оставлены в коде для будущих проверок, читаются в консоли браузера по фильтру OZONPERF; ✅ F. Вход больше не удаляет прежние сессии того же логина — именно из-за этого вторая вкладка выбивала первую. Истёкшие сессии по-прежнему отсеиваются, но лист «Сессии» переписывается одной операцией setValues вместо цикла deleteRow со смещением индексов. Проверено вживую: две вкладки под одним логином работают одновременно. ГРАБЛИ, НАЙДЕННЫЕ НА ЭТАПЕ E, обязательны к учёту при любой правке параметров действий: (1) fetchGas в useWarehouseStore.ts раскладывает extraPayload на ВЕРХНИЙ уровень тела запроса, а doPost читает параметры действий из payload.data, поэтому передавать надо в виде fetchGas('action', { data: { param: value } }), иначе параметр молча теряется; (2) читающие Ozon-действия getOzonStocks, getOzonSales, getOzonSyncStatus, getOzonSettings, getOzonClusters и getFactoryOrders обслуживаются блоком быстрого обхода switch, который возвращает результат ДО оператора switch, поэтому правка ветки в switch на них НЕ действует — менять надо оба места. Из-за этих двух граблей ограничение выдачи продаж пришлось доводить тремя правками подряд. ЭКСПЛУАТАЦИОННЫЕ ЗАМЕТКИ: развёртывание из AI Studio создаёт новую версию Cloud Run и может сбросить настройку «Максимум экземпляров» обратно с 1 на 11 — проверять после КАЖДОГО деплоя, иначе несколько экземпляров множат нагрузку на Apps Script и возвращают заглушки; порог ограничителя частоты в server.ts поднят с 60 до 300 запросов в минуту, потому что перед Node.js стоит nginx и доверие прокси не настроено, из-за чего req.ip одинаков для всех и лимит действует на всё приложение сразу, а одна загрузка делает около 17 запросов; сразу после развёртывания первый запрос уходит в холодный старт и может занять более 30 секунд, поэтому приложение стоит открывать через 1–2 минуты после деплоя, иначе на поднимающийся контейнер обрушивается вся пачка запросов и возникает лавина ошибок; ручной запуск обновления Ozon через runOzonSyncNow — тяжёлая пишущая операция под глобальным замком, во время неё приложение ощутимо вязнет, это ожидаемо. ОСТАВЛЕНО БЕЗ ЛЕЧЕНИЯ: изредка отдельное выполнение Apps Script залипает и падает по таймауту при почти пустой очереди (inflight 1–2), рядом те же действия отвечают за 2–9 секунд; явление уже описано в пункте 28, автоповтор его отрабатывает, причина на стороне Google. ЗАКРЫТ 19.08.2026: все шесть этапов A–F выполнены и проверены вживую ещё 06.08.2026, маркер «частично» у заголовка остался по недосмотру. С 06.08.2026 по 19.08.2026 симптомы не повторялись. ОСТАЁТСЯ БЕЗ ЛЕЧЕНИЯ, осознанно: изредка отдельное выполнение Apps Script залипает и падает по таймауту при почти пустой очереди — причина на стороне Google, автоповтор пункта 28 её отрабатывает.

- ✅ 30. Резерв под заявки, сверка состава и метка версии. ВЫПОЛНЕН ЦЕЛИКОМ 07.08.2026, все правки развёрнуты на бою и проверены живьём. ПРОБЛЕМЫ, вскрытые в бою: (1) состав заявки можно изменить в Ozon Seller, но прокси перезапрашивал его только при смене статуса Ozon, поэтому локальный резерв неделями оставался устаревшим; (2) резерв снимался в момент отгрузки на Ozon (isStockDeparted), хотя списание со склада проводится вручную позже — в этом окне товар не был ни зарезервирован, ни списан и мог быть повторно распределён в другой кластер; (3) у многокластерной заявки отменённые кластеры навсегда оставались в локальном статусе 'new', из-за чего полностью проведённая заявка вечно висела на вкладке «Поставки Озон» с надписью «Ожидает отгрузки на Ozon»; (4) после успешного сохранения расхода позиции на долю секунды подсвечивались красным с ложным «Недостаточно на складе»; (5) пользователь не мог определить, какая версия приложения задеплоена. РЕШЕНИЯ ПОЛЬЗОВАТЕЛЯ (07.08.2026, менять только по его решению): состав всех неоформленных поставок перезапрашивается при каждом опросе Ozon, включая ручной запуск кнопкой «Синхронизировать с Ozon» — отдельной сверки состава в момент нажатия «Оформить списание» НЕ делать, момент опроса и есть момент сверки; резерв держится до фактического списания либо до отмены заявки в Ozon Seller, отгрузка на Ozon резерв больше НЕ снимает; если после изменения состава резерв превысил остаток склада, приложение показывает алерт, но заявку правит пользователь в Ozon Seller — автоматически приложение её не меняет; метка версии формируется автоматически при сборке, а не ведётся вручную. ЭТАПЫ: ✅ A. server.ts (07.08.2026): в блоке Step 6 условие shouldFetchBundle развязано со сменой статуса Ozon — состав тянется для всех строк с локальным статусом 'new' на каждом опросе; добавлена защита от затирания непустого состава пустым ответом Ozon (пустой ответ означает сбой запроса, а не «в поставке нет товара»); требует редеплоя Cloud Run → ✅ B. src/lib/ozonPending.ts (07.08.2026): isPendingCleared больше НЕ вызывает isStockDeparted, импорт удалён; PENDING_ACTIVE_STATUSES расширен с 2 до 8 статусов (DATA_FILLING, READY_TO_SUPPLY, ACCEPTED_AT_SUPPLY_WAREHOUSE, IN_TRANSIT, ACCEPTANCE_AT_STORAGE_WAREHOUSE, REPORTS_CONFIRMATION_AWAITING, REPORT_REJECTED, COMPLETED), поэтому предохранитель 7 дней применяется только при пустом или неизвестном статусе; добавлен хелпер isShipmentSettled (локальный статус 'processed' или 'ignored'), по которому зачёт снимается, и он же учитывается при подстраховке журналом «Заявки Ozon» → ✅ C. src/lib/ozonAlerts.ts (07.08.2026): тип OzonAlertType расширен значением reserve_shortage, добавлена чистая функция buildReserveShortageAlerts и интерфейс ReserveShortageInput; функция сравнивает два справочника «артикул → шт» и сортирует алерты по величине нехватки → ✅ D. src/components/Dashboard.tsx (07.08.2026): подключение алерта в блок «Алерты Ozon»; в сравнение идут только артикулы из справочника SKU, потому что нераспознанные позиции заявки дали бы ложную нехватку; доступность берётся через getEffectiveAvailability, названия — из зеркала «Остатки Ozon» → ✅ E. src/components/OzonSuppliesTab.tsx (07.08.2026): введён хелпер isActionableItem (локальный статус 'new' И статус Ozon не CANCELLED), применён в четырёх местах — счётчик обработанных заявок, фильтр видимых заявок, бейдж состояния и блок кнопок; правило isFullyCancelled сохранено без изменений → ✅ F. src/components/ConfirmModal.tsx, vite.config.ts, src/components/Sidebar.tsx, новый src/vite-env.d.ts (07.08.2026): проверка остатков замораживается на время записи операции через состояние isCommitting и lastValidatedRef, иначе после успешной записи обновлённые остатки сравниваются со старыми количествами окна; метка версии сборки подставляется Vite через define как __APP_BUILD__ в формате «ГГГГ-ММ-ДД ЧЧ:ММ МСК», считается без Intl, чтобы не зависеть от локалей среды сборки, и выводится строкой «Сборка …» внизу бокового меню. ЖИВОЙ РЕГРЕСС 07.08.2026 на боевой БД: заявка № 121225716-1 (8 поставок: 2 «Принята на точке отгрузки», 2 «В пути», 4 «Отменена») подтвердила диагноз — все 4 отгруженные поставки были проведены корректно, заявку держали в списке только 4 отменённых кластера; после правки заявка ушла из актуальных в обработанные. Резерв проверен на цифрах: Органайзер_2_пол_PureWhite 184 шт остаток / 168 свободно / 16 резерв, Органайзер_3_пол_PureWhite 18 / 6 / 12; ложных алертов «Заявка больше остатка» не возникло. Метка версии подтверждена в собранном бандле до деплоя и в интерфейсе после деплоя. КРИТИЧЕСКОЕ ДЛЯ БУДУЩИХ ПРАВОК: резерв под заявку теперь снимается ТОЛЬКО тремя способами — списание проведено (локальный статус 'processed'), поставка проигнорирована ('ignored'), либо заявка получила статус CANCELLED, REJECTED_AT_SUPPLY_WAREHOUSE или OVERDUE; возвращать снятие резерва по факту отгрузки запрещено. Проверка остатков в окне подтверждения идёт по getEffectiveAvailability и резерв НЕ учитывает — это осознанное решение, иначе оформление собственной заявки блокировало бы себя же.
- ✅ 31. Виртуальные заявки Ozon: не резервировать и не списывать. ВЫПОЛНЕН ЦЕЛИКОМ 08.08.2026. ПРОБЛЕМА, обнаружена в бою 07.08.2026: на вкладке «Поставки Озон» висят 4 заявки, созданные не продавцом, а самим Ozon. Ozon создаёт такие заявки, когда находит потерянный товар и оприходует его на свой склад, а также когда после отказа в приёмке формирует допоставку по уже отгруженному товару. В обоих случаях товар физически УЖЕ находится у Ozon, а наш склад в операции не участвует. Приложение об этом не знает: под такие заявки строится локальный резерв на «Моём складе» и предлагается кнопка «Оформить», то есть один и тот же товар был бы списан со склада второй раз. ПРИЗНАК В API: order_tags.is_virtual (boolean) в ответе POST /v3/supply-order/get; сопутствующее поле order_tags.original_supply_id указывает на исходную поставку. Ни одно из этих полей прокси сейчас не читает — в блоке Step 5 обработчика /api/ozon/check разбираются только state, timeslot, drop_off_warehouse и supplies[]. ФАКТЫ, СНЯТЫЕ ЖИВЫМИ ВЫЗОВАМИ на выборке из 6 заявок: у двух заявок is_virtual = true, у обеих original_supply_id отличен от нуля и указывает на исходную поставку, причём у одной из них исходная поставка имеет статус REJECTED_AT_SUPPLY_WAREHOUSE; у четырёх невиртуальных заявок original_supply_id равен нулю. ЛОЖНАЯ ЗАЦЕПКА, не использовать: числовой order_number вида 2000058667455 виртуальности НЕ означает, такой же номер встречается и у обычной заявки. ОТКРЫТЫЙ ВОПРОС, закрывается замером на этапе B: выборка из 6 заявок мала, а полный опрос через Ozon MCP 07.08.2026 провести не удалось — TCP-соединение до api-seller.ozon.ru обрывалось по таймауту при работающем интернете и открывающемся сайте ozon.ru; поэтому пока не доказано, что все 4 висящие заявки помечены именно флагом is_virtual. Замер выполняется не через MCP, а самим приложением: этапы A и B нейтральны к поведению, они лишь доводят флаг из API до листа «Внешние отгрузки», после чего колонка «Виртуальная» на боевых данных прямо показывает, какие заявки виртуальные. Только после этого выполняются этапы C и D. РЕШЕНИЯ ПОЛЬЗОВАТЕЛЯ (07.08.2026, менять только по его решению): единственный признак виртуальности — order_tags.is_virtual равно true, поле original_supply_id в логику не закладывается и хранится только для справки; под виртуальную заявку локальный резерв НЕ строится никогда; списание со склада по виртуальной заявке НЕ проводится никогда; виртуальная заявка показывается на вкладке «Поставки Озон» с пометкой «Виртуальная», её состав раскрывается как у обычной заявки, и вместо кнопок «Оформить» и «Игнорировать» показывается одна кнопка «Ок», которая помечает все поставки заявки локальным статусом ignored и убирает заявку из актуальных; никаких дополнительных подтверждений и никаких записей в «Историю» кнопка «Ок» не делает. ОГРАНИЧЕНИЯ ЭТОГО ПУНКТА: правило пункта 30 «резерв снимается только тремя способами» не отменяется и не переписывается — виртуальные заявки просто НЕ ПОПАДАЮТ в расчёт резерва с самого начала; шесть учётных путей не затрагиваются вовсе, потому что ни одна правка этого пункта не пишет в «Историю» и не двигает остатки; колонка «Виртуальная» добавляется в КОНЕЦ массива EXTERNAL_SHIPMENTS_HEADERS, существующие колонки не переставляются и не переименовываются. Этапы: ✅ 0. Внесение этого пункта в план → ✅ A. server.ts: признак виртуальности передаётся в GAS → ✅ B. Code.gs: колонка «Виртуальная» в листе «Внешние отгрузки», после развёртывания и синхронизации снимается замер по 4 висящим заявкам → ✅ C. src/types.ts, src/lib/ozonPending.ts, src/lib/ozonGroups.ts: виртуальные заявки исключаются из резерва и из оформления списания → ✅ D. src/components/OzonSuppliesTab.tsx: пометка «Виртуальная» и кнопка «Ок» → ✅ E. Живой регресс на боевой БД 08.08.2026 ПРОЙДЕН: резерв уменьшился на состав виртуальных заявок, все 4 заявки закрыты кнопкой «Ок», обычные заявки работают как раньше

- ✅ 32. Ложный спиннер синхронизации и медленная массовая пометка поставок. ВЫПОЛНЕН ЦЕЛИКОМ 08.08.2026. ПРОБЛЕМА, найдена при живом регрессе пункта 31 08.08.2026; оба дефекта существовали в коде ДО пункта 31 и им не порождены. ДЕФЕКТ 1: действие markExternalShipment в src/store/useWarehouseStore.ts ставит ГЛОБАЛЬНЫЙ флаг isProcessing, а кнопка «Синхронизировать с Ozon» рисует спиннер по этому же флагу, поэтому любая запись через стор выглядит как запущенная синхронизация с Ozon; к Ozon API при этом не уходит ни одного запроса. Касается кнопок «Ок», «Игнорировать», «Привязать как дубль» и «Вернуть в новые» одинаково. ДЕФЕКТ 2: markExternalShipment на КАЖДУЮ поставку делает два обращения к Apps Script — запись updateExternalShipmentStatus и полную перезагрузку листа getExternalShipments, — а все четыре обработчика вкладки перебирают поставки последовательно через await; на четырёх виртуальных заявках это 8 обращений подряд, ожидание около минуты, поскольку getExternalShipments по хвостам плана занимает 8–20 секунд. РЕШЕНИЯ ПОЛЬЗОВАТЕЛЯ 08.08.2026: спиннер кнопки синхронизации переводится на локальное состояние компонента, блокировка кнопки на время любой записи сохраняется; в сторе заводится пакетное действие markExternalShipmentsBatch — N записей подряд, перезагрузка листа ОДНА в конце; на него переводятся все четыре обработчика. ОГРАНИЧЕНИЯ: действие markExternalShipment НЕ удаляется и НЕ переписывается, оно остаётся для одиночных вызовов; перезагрузка листа обязана выполняться даже при ошибке на середине пачки; шесть учётных путей не затрагиваются, ни одна правка не пишет в «Историю» и не двигает остатки. Этапы: ✅ 0. Внесение этого пункта в план → ✅ A. src/store/useWarehouseStore.ts: пакетное действие markExternalShipmentsBatch → ✅ B. src/components/OzonSuppliesTab.tsx: локальный спиннер синхронизации и перевод четырёх обработчиков на пакетное действие → ✅ C. Живой регресс 08.08.2026 ПРОЙДЕН: «Ок» и «Игнорировать» отрабатывают заметно быстрее, спиннер синхронизации при них не крутится, статусы в листе проставляются верно

- ✅ 33. Виртуальные комплекты: безопасный приход партии с фабрики. ВЫПОЛНЕН ЦЕЛИКОМ 09.08.2026, живой регресс пройден на боевой БД. ПРОБЛЕМА, найдена архитектором 09.08.2026 при аудите расчёта потребности. BowlGrayMini_01 и BowlBlueMini_01 — виртуальные комплекты (лист «Комплекты», kitType = virtual). У виртуального комплекта собственного остатка нет, доступность считается функцией getEffectiveAvailability в src/store/useWarehouseStore.ts как минимум по компонентам, делённый на норму. Кнопка «Партия пришла» в src/components/FactoryOrderModal.tsx подставляет в поле распознавания строку вида «Приход партии с фабрики / артикул комплекта количество N шт», то есть оформляет приход на сам комплект. Блокировка операций над виртуальным комплектом существует только для операции «Корректировка остатка» в src/components/ManualTab.tsx, для операции «Приход» её нет. ПОСЛЕДСТВИЯ при проведении такого прихода: N штук лягут на строку комплекта в листе «Остатки», доступность останется равной минимуму по компонентам и не изменится, сигнал «Заказ на фабрике» не погаснет, капитализация уйдёт на артикул, который в расходе не участвует. СРОЧНОСТЬ: активный заказ на 1200 шт BowlGrayMini_01 ожидается 15.08.2026. РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 09.08.2026: операции «Приход» и «Корректировка остатка» на виртуальный комплект блокируются одинаково; кнопка «Партия пришла» подставляет не комплект, а его компоненты, по одной строке на компонент, с количеством, умноженным на норму компонента; для обычного товара и для legacy-комплекта поведение не меняется. ОГРАНИЧЕНИЯ: расход виртуального комплекта не трогается и по-прежнему списывает компоненты; getEffectiveAvailability не меняется; шесть учётных путей не затрагиваются; лист «Заказы на фабрике» продолжает хранить артикул комплекта. Этапы: ✅ 0. Внесение этого пункта в план → ✅ A. Блокировка операции «Приход» на виртуальный комплект: проверка добавлена в commitTransaction файла src/store/useWarehouseStore.ts, поэтому закрывает оба пути — ручной ввод и распознавание накладной → ✅ B. Кнопка «Партия пришла» подставляет компоненты: правка в src/components/FactoryOrderModal.tsx, количество умножается на норму компонента → ✅ C. Живой регресс 09.08.2026 ПРОЙДЕН: приход на BowlGrayMini_01 заблокирован с подсказкой про компоненты, приход на «Миска серая» проходит нормально, кнопка «Партия пришла» подставляет три строки компонентов. ЗАМЕЧАНИЕ ПО ИТОГАМ РЕГРЕССА: компоненты виртуальных комплектов не отображаются на вкладке «Остатки Озон», потому что не продаются на Ozon, поэтому завести заказ на фабрике на компонент пока невозможно — это закрывается пунктом 36.

- ✅ 34. Контур поставок на Ozon: целевой запас без двойного счёта и отсекатель по сроку продаж кластера. ВЫПОЛНЕН ЦЕЛИКОМ 10.08.2026, развёрнут на бою. ПРОБЛЕМА, найдена архитектором 09.08.2026. ДЕФЕКТ 1: в src/lib/ozonCoverage.ts функция calcSupplyRecommendation считает целевой уровень как perDay умножить на сумму targetStockDays и minStockDays, то есть неснижаемый запас прибавляется к целевому, хотя по определению пользователя он входит внутрь целевого. При настройках 30 и 10 фактический уровень равен 40 дням вместо 30, и порог включения рекомендации тоже 40 дней вместо 30. Замер на боевой БД 09.08.2026 при окне скорости 4 недели: 56 позиций и 1480 шт к отправке вместо 53 позиций и 1272 шт. ДЕФЕКТ 2: потребность каждого кластера округляется вверх до целой коробки, поэтому кластер со скоростью 0,07 шт в день получает коробку на 42 шт, то есть запас на 588 дней; по товару Миска_двойная чистая потребность 220 шт превращается в 588 шт. РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 09.08.2026: целевой уровень равен perDay умножить на targetStockDays умножить на коэффициент приоритета, без слагаемого minStockDays; вводится настройка «Максимальный срок продаж кластера, дней» со значением по умолчанию 100 — если после поставки расчётный запас кластера превысит это число дней, кластер исключается из рекомендации; исключённые по этому правилу кластеры НЕ создают непокрытой потребности и не влияют на заказ на фабрике; в окне настроек Ozon добавляется проверка «Целевой запас должен быть больше Неснижаемого остатка». ДОПУЩЕНИЕ, зафиксировано пользователем 09.08.2026: отдельный параметр «Срок доставки до Ozon» не вводится, он считается заложенным в срок поставки с фабрики. Фактический минимум в кластере равен целевому запасу минус реальный срок приёмки Ozon; при целевом 30 и приёмке около 15 дней пол составляет примерно 15 дней, что выше неснижаемого 10 дней. Если целевой запас опустить ниже 25 дней, неснижаемый остаток перестанет держаться. ОГРАНИЧЕНИЯ: функция calcCoverageDays не меняется; список исключённых кластеров excludedClusters работает как раньше; коэффициенты приоритетных кластеров не меняются; очередь раздачи остатка Моего склада не меняется; Code.gs правится только в части значения по умолчанию новой настройки. КРИТЕРИЙ ПРИЁМКИ, замер на боевой БД 09.08.2026 при окне скорости 4 недели: сейчас, при уровне 40 дней и без отсекателя — 56 позиций и 1480 шт к отправке; при уровне 30 дней без отсекателя — 53 позиции и 1272 шт; при уровне 30 дней с отсекателем 100 дней — 40 позиций и 906 шт. Пункт считается выполненным верно, если после развёртывания цифры сойдутся с третьей строкой. РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 09.08.2026 по отсекателю: коэффициент приоритетного кластера к максимальному сроку продаж НЕ применяется, это абсолютный защитный потолок; значение 0 в этой настройке выключает отсекатель полностью. Этапы: ✅ 0. Внесение этого пункта в план (09.08.2026) → ✅ A. Code.gs: новая настройка maxClusterDays со значением 100 (10.08.2026) → ✅ B. src/lib/ozonCoverage.ts: целевой уровень без двойного счёта и отсекатель (10.08.2026) → ✅ C. Прокидка новой настройки в src/components/OzonStocksTab.tsx и src/components/Dashboard.tsx (10.08.2026) → ✅ D. Окно настроек Ozon: поле и проверка (10.08.2026) → ✅ E. Живой регресс (10.08.2026). РЕЗУЛЬТАТ ЗАМЕРА на боевой БД 10.08.2026: 40 строк и 898 шт к отправке против 57 строк и 1476 шт до правки, что соответствует критерию приёмки.

- ✅ 35. Контур заказа на фабрике: модель «труба». ВЫПОЛНЕН ЦЕЛИКОМ 11.08.2026, развёрнут на бою и проверен пользователем. ПРОБЛЕМА, найдена архитектором 09.08.2026 в функции calcFactorySignal файла src/lib/ozonCoverage.ts. ДЕФЕКТ 1: порог срабатывания считается от остатка без учёта уже заказанного на фабрике, поэтому сигнал приходится гасить в интерфейсе самим фактом наличия активного заказа; если заказано мало, приложение об этом молчит. ДЕФЕКТ 2: объём заказа считается как наибольшее из произведения скорости на factoryOrderDays и непокрытой потребности кластеров, он не вычитает имеющийся остаток и не зависит от срока поставки. ДЕФЕКТ 3: порог 60 дней равен сумме объёма заказа 50 и неснижаемого 10, поэтому после прихода партии сигнал загорается сразу. ДЕФЕКТ 4: просроченный заказ гасит сигнал бессрочно, меняется только цвет плашки. Замеры на боевой БД 09.08.2026: Органайзер_3_пол_прозр при запасе на 121 день и дефиците 2 шт в одном кластере требует заказать 100 шт; Этажерка_25_белая при запасе на 114 дней и дефиците 2 шт требует 25 шт. РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 09.08.2026: вводится величина ТРУБА, равная сумме расчётного остатка на всех кластерах Ozon, остатка на Моём складе и заказанного на фабрике и ещё не полученного; ПОРОГ равен скорости, умноженной на сумму срока поставки и неснижаемого запаса; ОБЪЁМ равен скорости, умноженной на сумму срока поставки, неснижаемого запаса и объёма заказа в днях, минус ТРУБА, с округлением вверх до целых коробок один раз по товару; сигнал больше не гасится наличием заказа, он пересчитывается с его учётом и показывает «дозаказать N шт, уже заказано M шт, ожидается ДД.ММ»; заказ с прошедшей датой ожидания в ТРУБУ не входит и подсвечивается; добавляется колонка «Стоимость заказа, ₽», равная количеству, умноженному на среднюю себестоимость, скрываемая штатным меню «Колонки». РЕШЕНИЯ ПОЛЬЗОВАТЕЛЯ 10.08.2026, менять только по его решению. (1) Если ТРУБА выше порога, но у кластеров есть непокрытая потребность, заказ на фабрике НЕ предлагается: вместо цифры показывается серая пометка «дефицит в кластерах N шт — товар есть, но лежит не там». (2) В ТРУБУ входит СУММА всех активных заказов по артикулу, в плашке показываются все заказы с датами, модалка по клику открывает самый ранний по дате ожидания. (3) Просроченный заказ в ТРУБУ не входит, его плашка выделяется жёлтым, и пользователю доступны ровно два действия: «Изменить дату прибытия» и «Отменить заказ»; отмена ФИЗИЧЕСКИ УДАЛЯЕТ строку заказа из листа «Заказы на фабрике», после чего потребность пересчитывается заново. (4) Стоимость заказа считается через getEffectiveAvgCost, чтобы у виртуального комплекта сложилась себестоимость компонентов; при нулевой себестоимости в ячейке прочерк, а не ноль. ОГРАНИЧЕНИЯ: непокрытая потребность кластеров больше НЕ поднимает объём заказа до объёма в днях, она показывается отдельным признаком; структура листа «Заказы на фабрике» и функция saveFactoryOrder не меняются; в Code.gs добавляется ТОЛЬКО новое действие удаления заказа, существующие функции заказов на фабрике не переписываются; src/lib/ozonPending.ts не трогается; прямой импорт ozonPending.ts в ozonCoverage.ts остаётся запрещён навсегда; шесть учётных путей не затрагиваются, ни одна правка пункта не пишет в «Историю» и не двигает остатки. КРИТЕРИЙ ПРИЁМКИ, эталон снят архитектором 10.08.2026 прогоном обеих версий расчёта на выгрузке боевой БД при настройках speedWeeks 4, minStockDays 10, targetStockDays 30, factoryOrderDays 50: до правки сигнал горит по 7 позициям на 1654 шт, после правки должен гореть по 1 позиции BowlGrayMini_01 на 1512 шт, а шесть позиций с активными заказами должны показать «дефицит в кластерах» без цифры заказа. НАБЛЮДЕНИЕ 10.08.2026, в объём пункта не входит: заказ на 1200 шт BowlGrayMini_01 помечен полученным 09.08.2026, но приход на компоненты ещё не проведён, поэтому партия уже вышла из ТРУБЫ, а на склад ещё не вошла, и расчётная цифра дозаказа завышена; лечится пунктом 36. Этапы: ✅ 0. Внесение этого пункта в план и фиксация решений (10.08.2026) → ✅ A0. Эталон «до» на боевых данных снят и сохранён (10.08.2026) → ✅ A. src/lib/ozonCoverage.ts: труба, порог, объём (10.08.2026) → ✅ B1. Прокидка активных заказов в расчёт: src/components/OzonStocksTab.tsx (10.08.2026, потребовала правки порядка объявлений: переменные объявлялись ниже расчёта, который их использует, ошибки TS2448 и TS2454) → ✅ B2. То же в src/components/Dashboard.tsx (10.08.2026) → ✅ C1. Пять состояний ячейки «Заказ на фабрике» и отбор в сводной панели по объёму дозаказа, а не по факту наличия заказа (11.08.2026) → ✅ C2. src/lib/ozonAlerts.ts: алерт с учётом заказанного, параметр orderedArticles удалён из сигнатуры buildCoverageAlerts (11.08.2026) → ✅ D. Колонка «Стоимость заказа, ₽» по цене последнего поступления (11.08.2026) → ✅ E. Отмена заказа: новое действие cancelFactoryOrder в Code.gs, действие в хранилище, кнопка с подтверждением в два нажатия в FactoryOrderModal.tsx (11.08.2026) → ✅ F. Живой регресс на боевой БД, пользователь подтвердил (11.08.2026). РЕЗУЛЬТАТ, сверено с эталоном, снятым до правки: сигнал заказа горел по 7 позициям на 1654 шт, стал гореть по 1 позиции BowlGrayMini_01 на 1512 шт, шесть позиций с активными заказами перешли в состояние «дефицит в кластерах» без цифры заказа. ВАЖНАЯ НАХОДКА ЭТАПА D: средняя себестоимость непригодна для оценки стоимости заказа, потому что искажена накопленной капитализацией — у компонента «Миска серая» она составляет 12 306,92 руб при реальной цене поступления 128,20 руб, из-за чего колонка показывала 18 636 170 руб вместо 222 078 руб. Введено новое действие getLastPurchasePrices: цена последней операции типа «Приход», кроме оприходования излишков, корректировки остатка и услуг; миграция комплектов учитывается, потому что несёт реальную себестоимость компонента. У виртуального комплекта цена складывается из цен компонентов по нормам, при отсутствии поступлений происходит откат на среднюю себестоимость. ОСТАВЛЕНО ПОД НАБЛЮДЕНИЕМ: жёлтая плашка просроченного заказа и фактическое удаление заказа живьём не проверялись, потому что на боевой БД просроченных заказов нет, а специально просрочивать боевой заказ нельзя; проверить на тестовой БД либо при первой реальной просрочке. НАБЛЮДЕНИЕ, подтверждено на боевых данных: пока приход партии не проведён, заказ со статусом received уже вышел из ТРУБЫ, а товар на склад ещё не вошёл, поэтому цифра дозаказа завышена; лечится пунктом 36.

- ✅ 36. Потребность на фабрике считается по компонентам виртуальных комплектов. ПРОБЛЕМА, найдена архитектором 09.08.2026: сигнал «Заказ на фабрике» формируется по артикулу виртуального комплекта, но у фабрики заказываются не комплекты, а компоненты, у каждого из которых свой срок поставки и своя коробка. У компонентов BowlGrayMini_01 и BowlBlueMini_01 сроки поставки различаются в пять раз: миски 50 дней, бутылки и пакеты 10 дней. Из-за этого приложение не видит, что при приходе 1200 мисок понадобятся 1200 бутылок и 1200 пакетов, которых на Моём складе 520 и 830. УТОЧНЕНИЕ ПОЛЬЗОВАТЕЛЯ 18.08.2026: заказы на фабрике, оформленные на КОМПЛЕКТ, в компоненты НЕ разворачиваются — такой заказ означает заказ одного конкретного компонента, а не всего состава; узкое место выбирается по наименьшему покрытию в днях, дополнительно выводится, сколько комплектов можно собрать сейчас; Wildberries в расчёте не учитывается, скорость берётся только по продажам Ozon. РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 09.08.2026: для виртуальных комплектов сигнал «Заказ на фабрике» считается по компонентам; скорость компонента равна сумме по всем комплектам произведения скорости комплекта на норму компонента; запас компонента равен сумме по комплектам произведения остатка комплекта на Ozon на норму, плюс собственный остаток компонента на Моём складе, плюс заказанное на фабрике; срок поставки и размер коробки берутся из карточки компонента в SKU Базе; сам виртуальный комплект сигнала «Заказ на фабрике» больше не даёт. Дополнительно выводится узкое место комплекта — компонент, ограничивающий сборку. ОГРАНИЧЕНИЯ: рекомендации на поставку в кластеры Ozon по-прежнему считаются по комплекту, а не по компонентам; legacy-комплекты не затрагиваются; getEffectiveAvailability не меняется. Этапы: ✅ 0. Внесение этого пункта в план → ✅ A. Разворот комплектов в компоненты в расчёте (18.08.2026): поле входа kits, интерфейсы ComponentCoverage и KitBottleneck, функция buildComponentCoverage; сверка на боевых данных сошлась с ручным расчётом до десятых по всем четырём компонентам → ✅ B. Показ узкого места комплекта (18.08.2026): ДЕФЕКТ, найден владельцем в бою 19.08.2026 и исправлен в тот же день: резерв по созданной заявке хранился только по артикулу комплекта и в компоненты не разворачивался, из-за чего «собрать сейчас» считало свободным товар, уже обещанный Ozon. Резерв комплекта теперь разворачивается по норме расхода; ТРУБА при этом намеренно осталась на сыром остатке. блок «Заказ на фабрике — компоненты» под таблицей, узкое место в колонке «Заказ на фабрике», алерты factory_order по компонентам, подстановка коробки и срока компонента в окно заказа → ✅ C. Живой регресс (19.08.2026): владелец подтвердил работу блока на боевом сервисе. При регрессе найден и исправлен дефект показа: блок компонентов знал единственное состояние «нужно заказать», и оформленный на компонент заказ пропадал из вида — сигнал гас, кнопка исчезала, заказ было не открыть. Повторены четыре состояния основной таблицы (просрочен, дозаказать, заказано и ждём, не нужно), колонка «Хватит на» показывает покрытие всегда. РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 19.08.2026: остаток до целевого объёма заказа (заказано 1200 из рекомендованных 1962, до целевого ещё 756) НЕ показывается — логика «тревога по порогу 57 дней, объём по горизонту 117 дней» признана верной и менять её не нужно.

- ⬜ 37. Архив товаров и отсев артикулов вне SKU Базы. ПРОБЛЕМА, найдена архитектором 09.08.2026: товар, который продавался, но пропал из листа «Остатки Ozon», получает расчёт «хватит на 0 дней» и вечный красный сигнал «Заказ на фабрике». При окне скорости 8 недель на боевых данных так всплывают Органайзер_полка_бел и Этажерка_20_белая. Отдельно в листе «Продажи Ozon» присутствуют артикулы, которых нет в SKU Базе: Полка_выдв_18см, BlueMetalBowl, GreyMetalBowl, Полка_выдв_27см_черн, Полка_на_стол, Шайба_М8, test. РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 09.08.2026: в листе SKU появляется колонка «Архив» и соответствующая галочка в карточке SKU; архивный товар полностью исключается из расчёта покрытия, из рекомендаций на поставку и из сигнала «Заказ на фабрике», при этом его продажи и остатки в листах сохраняются; артикул, которого нет в SKU Базе, сигнала «Заказ на фабрике» не получает никогда и показывается с пометкой «нет в SKU Базе»; в таблице «Остатки Озон» добавляется переключатель «показывать архивные», по умолчанию выключенный. ОГРАНИЧЕНИЯ: строки листов «Продажи Ozon» и «Остатки Ozon» не удаляются; автоопрос Ozon продолжает писать данные по архивным товарам; удаление SKU не заменяется архивированием, обе возможности сосуществуют. ОТЛОЖЕН 19.08.2026, решение пользователя. ЗАМЕР НА БОЕВОЙ БАЗЕ 19.08.2026: проблема больше не воспроизводится. В таблице «Остатки Озон» 13 строк, все 13 есть в SKU Базе; артикулов вне SKU Базы в таблице нет ни одного. Органайзер_полка_бел (последняя продажа 15.06.2026) и Этажерка_20_белая (22.06.2026) в таблицу не попадают вовсе, вечного красного сигнала у них нет; остатков у них нет ни на Ozon, ни на Моём складе. Причина исчезновения: пункт писался при окне скорости 8 недель, сейчас окно 4 недели, и переставший продаваться товар выпадает из таблицы сам через месяц. Семь артикулов вне SKU Базы существуют только в старых строках продаж (последние продажи от 08.09.2025 до 25.05.2026) и в приложении невидимы. Единственное оставшееся неудобство — две мёртвые карточки в SKU Базе из девятнадцати. ВЕРНУТЬСЯ, если окно скорости будет увеличено или мёртвые карточки начнут мешать. Этапы: ⬜ 0. Внесение этого пункта в план → ⬜ A. Code.gs: колонка «Архив» в листе SKU → ⬜ B. Карточка SKU и вкладка SKU База → ⬜ C. Исключение из расчётов и переключатель → ⬜ D. Живой регресс

- ✅ 38. Тренд продаж по артикулу. ПРОБЛЕМА: скорость продаж считается как плоское среднее за окно и не учитывает направление движения спроса, поэтому на горизонте заказа на фабрике около 110 дней партия систематически расходится с реальностью. РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 09.08.2026: по каждому артикулу автоматически считается тренд по недельному ряду за окно, задаваемое настройкой «Окно тренда, недель» со значением по умолчанию 13, что совпадает с глубиной недельной зоны хранения продаж; наклон переводится в месячный множитель. Прогноз скорости для заказа на фабрике равен скорости за окно, умноженной на тренд и на единицу плюс настройка «Прирост объёма продаж, %» со значением по умолчанию 0. ЧЕТЫРЕ ФИЛЬТРА, обязательны: если в окне есть хотя бы одна неделя с нулевыми продажами, тренд принимается равным 1,00; если за окно продано менее 50 шт, тренд принимается равным 1,00; понижающий тренд не применяется, если товар находится в дефиците, потому что отличить падение спроса от отсутствия товара нельзя; во всех остальных случаях тренд ограничивается диапазоном от 0,7 до 1,5. Обоснование фильтров, замер на боевой БД 09.08.2026: без них Миска_двойная получает тренд плюс 71 процент из-за пяти нулевых недель на старте продаж, Набор_полок_выдв_бел получает минус 74 процента из-за нулевого остатка, Этажерка_25_белая получает плюс 95 процентов на выборке в 23 штуки. В интерфейсе выводится колонка «Тренд» с причиной отклонения и подсказка с расшифровкой множителя вида «факт × тренд × прирост». ОГРАНИЧЕНИЯ: тренд применяется ТОЛЬКО к контуру заказа на фабрике; рекомендации на поставку в кластеры Ozon считаются по фактической скорости без тренда, поскольку этот контур пересчитывается каждые несколько дней и корректирует себя сам; сравнение с прошлым годом не используется; глубина выгрузки истории продаж не увеличивается. ФОРМУЛА ТРЕНДА, восстановлена 19.08.2026 сверкой с замером 09.08.2026: по недельному ряду за окно строится линейная регрессия, тренд = (среднее + наклон × 4,345) ÷ среднее, где 4,345 — недель в месяце. Проверка: на окне 18.05–10.08.2026 формула даёт Набор_полок_выдв_бел 0,26 (в замере −74%), на окне 04.05–27.07.2026 — Миска_двойная 1,72 (в замере +71%) и Этажерка_25_белая 1,93 (в замере +95%). РЕШЕНИЯ ПОЛЬЗОВАТЕЛЯ 19.08.2026: (1) тренд применяется и к компонентам виртуальных комплектов — заказ на фабрике теперь считается именно по ним, и без этого тренд не работал бы на большей части оборота; (2) если по товару сработала коррекция скорости при дефиците (пункт 42), тренд принимается равным 1,00 — коррекция и тренд поднимают скорость одним и тем же способом, а перемножение пределов 5× и 1,5× дало бы рост в 7,5 раза и затоваривание. Это ПЯТЫЙ фильтр в дополнение к четырём. ЗАМЕР ОЖИДАЕМОГО ЭФФЕКТА на боевой базе 19.08.2026: сырой тренд считается у 13 артикулов, четырьмя фильтрами гасится у пяти (Миска_двойная 1,43 при 4 нулевых неделях; Органайзер_полка_черн 1,48 при 2; Этажерка_25_белая 1,81 при 5; Набор_полок_выдв_бел 0,26 при 1; Полка_выдв_27см 0,58 отсекается дефицитом). У всех трёх артикулов с красным сигналом заказа тренд гасится, то есть объём заказа у них не изменится. Ощутимый эффект один: BowlGrayMini_01 получает тренд 0,78, прогнозная скорость падает с 19,82 до 15,56 шт/д, и через компоненты это поднимает покрытие «Миска серая» с 79 примерно до 101 дня. Этапы: ✅ 0. Внесение этого пункта в план → ✅ A. Code.gs: настройки trendWeeks и salesGrowthPct (19.08.2026): trendWeeks заведён ранее пунктом 42, добавлена salesGrowthPct со значением 0 и поле в окне настроек → ✅ B. Расчёт тренда с фильтрами (19.08.2026): функция buildSalesTrend, пять фильтров вместо четырёх (добавлен фильтр сработавшей коррекции), поля forecastPerDay и trend, тестов стало 60; сверка на боевой базе дала ровно контрольные числа замера 09.08.2026, а снимок контура поставок в кластеры у старого и нового кода совпал байт в байт → ✅ C. Колонка «Тренд» и подсказка (19.08.2026): переключаемая колонка с множителем, короткой причиной и подсказкой с разложением прогноза и недельным рядом; в блоке компонентов показана прогнозная скорость → ✅ D. Живой регресс (19.08.2026): владелец проверил в бою и принял. По его замечаниям сделаны три правки показа — подсказки у верхних строк раскрываются вниз (обрезались краем прокручиваемой области), в блоке компонентов крупно выводится ФАКТИЧЕСКИЙ расход (он совпадает со скоростью комплекта, а прогноз ушёл второй строкой), значок скорректированной скорости подписан словом «коррекция», чтобы не путался с колонкой «Тренд»

- ✅ 39. Латентные дефекты расчёта потребности. ПРОБЛЕМА, найдена архитектором 09.08.2026, сейчас на боевых данных не проявляется. ДЕФЕКТ 1: в src/lib/ozonCoverage.ts функция buildSalesSpeed вызывает resolveOzonArticle без третьего аргумента, поэтому продажи сопоставляются с артикулом только по offer_id, тогда как остатки в buildClusterStocks сопоставляются сначала по «ШК Ozon». Если появится товар, у которого offer_id не совпадает с внутренним SKU и связь идёт через «ШК Ozon», он разъедется на два фантомных артикула: один с остатком и нулевой скоростью, другой со скоростью и нулевым остатком. ДЕФЕКТ 2: колонка «Дней» листа «Продажи Ozon» при расчёте скорости игнорируется, знаменателем всегда служит число недель умножить на семь; при окне, не кратном четырём неделям, окно разрежет 28-дневный блок и скорость будет занижена. ДЕФЕКТ 3: чтение настроек в src/components/OzonStocksTab.tsx использует конструкцию с логическим ИЛИ, поэтому значение 0 в настройке молча подменяется значением по умолчанию, и поставить неснижаемый запас равным нулю невозможно. ОГРАНИЧЕНИЯ: правки только точечные, модель расчёта не меняется. ЗАМЕР 19.08.2026: дефект 1 по-прежнему спит (все 12 артикулов остатков нашлись в SKU Базе по имени; связывание по «ШК Ozon» почти не срабатывает, потому что в SKU Базе 8 баркодов записаны с префиксом OZN, а Ozon отдаёт голое число). Дефект 2 оказался опаснее записанного: в листе продаж ДВЕ зоны хранения — недельная (1385 строк, «Дней» = 7, 18.05–17.08.2026) и архивная (1805 строк, «Дней» = 28, 16.06.2025–20.04.2026), даты 28-дневных блоков тоже понедельники и совпадают с генерируемыми окном; текущие окна 4 и 13 недель помещаются в недельную зону с НУЛЕВЫМ запасом. Дефект 3 частично исправился раньше: пять настроек уже читались верно, оставались восемь, из них значимы «Неснижаемый остаток» и «% возвратов». РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 19.08.2026: архивные 28-дневные блоки в расчёт скорости не берутся вовсе, окно скорости работает только по недельной зоне. Этапы: ✅ 0. Внесение этого пункта в план → ✅ A. Сопоставление продаж через карту остатков (19.08.2026): функции buildOfferIdToArticle и resolveSalesArticle, одна карта на скорость, коррекцию и тренд → ✅ B. Учёт колонки «Дней» (19.08.2026): в расчёт скорости идут только строки «Дней» = 7, знаменатель — число реально присутствующих недель; на боевых данных при текущих настройках расчёт совпал с прежним полностью, а при окне 17 недель разница подтвердилась → ✅ C. Ноль в настройках (19.08.2026): помощник numSetting в трёх компонентах, нижняя граница 1 оставлена только у счётчиков недель и коробок

- ✅ 40. Выбор адресата для оставшейся капитализации при списании и корректировке остатка, и ноль в количестве. ПРИОРИТЕТ ИЗМЕНЁН РЕШЕНИЕМ ПОЛЬЗОВАТЕЛЯ 09.08.2026: пункты выполняются строго по порядку номеров, поэтому пункт 40 идёт после пунктов 34–39, а не перед пунктом 34. ПРОБЛЕМА 1: правило учёта «операция со словом Списание уменьшает количество, но не уменьшает капитализацию» — единственное, альтернатив у пользователя нет. Себестоимость брака всегда размазывается по остатку, а когда остаток почти нулевой, вся сумма падает на единицы штук. Перенести её на уже отгруженную партию или на будущий приход нельзя. ОСОБЫЙ СЛУЧАЙ В КОДЕ: в Code.gs при списании капитализация принудительно обнуляется, если остаток стал ровно нулём, поэтому оставить капитализацию долгом на складе при нулевом остатке технически невозможно. ФАКТ на боевой БД, разобран 09.08.2026: артикул «Миска серая» — приход 04.07.2026 на 492 шт на сумму 63072,95 руб; расход 22.07.2026 на 396 шт на сумму 50766,03 руб компонентом комплекта BowlGrayMini_01 в группе 15456520-346f-4d5c-9718-b55a53044f45; списание брака 02.08.2026 на 95 шт. Оставшийся 1 шт оставлен пользователем СОЗНАТЕЛЬНО как носитель капитализации 12306,92 руб, чтобы обойти принудительное обнуление; фактический остаток мисок равен нулю, годными оказались только 396 отгруженных штук. ЭКОНОМИЧЕСКИ ВЕРНЫЙ РЕЗУЛЬТАТ для этого случая: вся сумма 63072,95 руб относится на партию из 396 шт, то есть 159,28 руб за годную миску вместо 128,20 руб и 226,38 руб за комплект вместо 195,31 руб; занижение себестоимости отгрузки составляет 12306,92 руб. ВАЖНО: перенос затрагивает не одну строку истории, а всю группу отгрузки — строку компонента и строку комплекта. ПРОБЛЕМА 2: в src/components/ManualTab.tsx обработчик кнопки «Ещё позиция» отклоняет количество, меньшее или равное нулю, поэтому позицию с нулевым количеством нельзя добавить в список даже для корректировки остатка; счётчик на кнопке «Сохранить операции» считает только количество больше нуля и показывает ноль, хотя одиночная корректировка до нуля фактически проходит. РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 09.08.2026: при операциях «Списание - Брак», «Списание - Утеря» и «Корректировка остатка» пользователь выбирает адресата оставшейся капитализации из пяти вариантов. Вариант 1 «На остаток товара» — текущее поведение, капитализация не уменьшается и размазывается по оставшемуся количеству; это вариант ПО УМОЛЧАНИЮ, чтобы поведение без явного выбора не изменилось. Вариант 2 «На последнюю отгруженную партию» — капитализация уменьшается на себестоимость списываемого количества, и та же сумма дописывается отдельной строкой истории с нулевым количеством и тем же groupId, что у выбранной пользователем отгрузки; существующие строки истории при этом НЕ переписываются; список последних отгрузок по этому артикулу показывается пользователю на выбор, приложение не угадывает партию само. Вариант 3 «Оставить долгом на складе» — количество уходит в ноль, а капитализация СОХРАНЯЕТСЯ; для этого снимается принудительное обнуление капитализации при нулевом остатке в Code.gs. Вариант 4 «На следующую поступившую партию» — то же, что вариант 3, плюс пометка «долг к распределению», которая снимается автоматически при первом же приходе по этому артикулу. Вариант 5 «Обнулить» — капитализация уменьшается на себестоимость списания, как при обычном расходе, и при нулевом остатке становится нулём. Нулевое количество разрешается для корректировки остатка во всех путях ввода, включая кнопку «Ещё позиция» и счётчик на кнопке сохранения. ОБЯЗАТЕЛЬНОЕ ТРЕБОВАНИЕ ВИДИМОСТИ: артикул с нулевым количеством и ненулевой капитализацией подсвечивается на вкладке «Склад» плашкой «долг себестоимости N руб», потому что иначе висящая сумма молча поднимет себестоимость следующего прихода; именно так и возникла ситуация с «Миска серая». ВАЖНОЕ УТОЧНЕНИЕ: варианты 3 и 4 дают арифметически одинаковый результат в таблице «Остатки», различие только в видимости и в автоматическом снятии пометки; варианты 2 и 5 одинаково уменьшают капитализацию склада и различаются только тем, привязана сумма к отгрузке или нет. ОГРАНИЧЕНИЯ: новое правило обязано зеркалиться во всех шести учётных путях — провести, удалить одиночно, удалить массово, восстановить одиночно, восстановить массово, отредактировать; при удалении и восстановлении списания дописанная строка дораспределения и пометка долга удаляются и восстанавливаются вместе с ним; ранее проведённые списания задним числом не пересчитываются; функцию recalculateStockFully запускать запрещено; правило списания для компонентов legacy-комплектов меняется синхронно с основным; расчёт средней себестоимости как капитализация делить на количество не меняется, при нулевом количестве средняя остаётся нулевой. ЗАМЕР НА БОЕВОЙ БАЗЕ 19.08.2026, картина изменилась и стала хуже: попытка владельца убрать фантомную единицу 18.08 сняла лишь 999,78 руб, потому что долг к тому моменту уже слился с приходом 13 шт от 18.08. Сейчас 13 реальных мисок несут 12 997,14 руб, то есть 999,78 руб за штуку вместо настоящих 130 — лишних 11 307,14 руб. Появился ВТОРОЙ такой случай, которого в пункте не было: «Полка_выдв_27см» — 18.08 списано браком 11 шт, и 6 195,66 руб село на единственную оставшуюся полку при последнем приходе 517 руб, лишних 5 678,66 руб. Итого на складе висит около 16 986 руб чужой себестоимости; по остальным пятнадцати артикулам склада долга нет. Этап I просрочен: партия 13 шт пришла 18.08 и часть долга впитала. РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 19.08.2026, меняет объём пункта: (1) первым делом делается плашка (этап E), остальное после; (2) вариантов адресата капитализации не пять, а ДВА — «оставить долгом» и «обнулить»; (3) долг, оставленный на артикуле, при следующем приходе автоматически прибавляется к себестоимости поступившей партии, и итоговая себестоимость прихода растёт; (4) нулевое количество разрешается ставить и при списании, и при корректировке, при этом капитализация НЕ исчезает, а остаётся висеть долгом под будущую поставку. Этапы: ✅ 0. Внесение этого пункта в план → ✅ A. Интерфейс ручного ввода: выбор «оставить долгом» или «обнулить» (19.08.2026). РЕШЕНИЕ ПО РЕАЛИЗАЦИИ: выбор едет меткой «[себестоимость обнулена]» внутри строки объекта операции, а не отдельной колонкой — эта строка уже попадает в «Историю» и читается обратно всеми учётными путями, поэтому миграция листа не нужна, а выбор виден владельцу. Правило зеркально в шести местах; стендом покрыто проведение (14 проверок, всего 70), пути удаления и восстановления пока не покрыты → ✅ B. Code.gs: снятие принудительного обнуления капитализации при нулевом остатке (19.08.2026), проведение с учётом выбранного варианта сделано этапом A; отдельно найдено и снято ДУБЛИРУЮЩЕЕ обнуление во фронтенде (normalizeStock в сторе), из-за которого приложение показывало ноль там, где сервер хранил долг в четырёх местах — проведение по товару, проведение по компонентам, одиночное и массовое восстановление; покрыто стендом, 14 проверок, мутационная проверка пройдена. Проведение с учётом ВЫБРАННОГО ВАРИАНТА ждёт этапа A → ❌ C. Строка дораспределения себестоимости с привязкой к группе отгрузки — ВЫЧЕРКНУТ 19.08.2026 решением пользователя: этап проектировался под схему из пяти вариантов адресата капитализации, а принятая схема состоит из двух («оставить долгом» / «обнулить»), и привязка суммы к конкретной отгрузке в ней не нужна → ❌ D. Пометка «долг к распределению» и её автоматическое снятие при следующем приходе — ВЫЧЕРКНУТ 19.08.2026 решением пользователя: роль пометки выполняет бейдж «долг себестоимости» из этапа E, а снятие происходит само собой — долг поглощается ближайшим приходом по существующей формуле капитализации → ✅ E. Плашка «долг себестоимости» на вкладке «Склад» (19.08.2026): функции calcCostDebt и hasCostDebt, порог тревоги 1,5× от цены последнего прихода; на боевых данных загорается ровно на двух артикулах из семнадцати, ложных срабатываний нет → 🟨 F. Зеркалирование правила во всех шести учётных путях: в коде сделано 19.08.2026 — проведение по товару и по компонентам, удаление одиночное и массовое, восстановление одиночное и массовое; путь «отредактировать» собран из удаления и проведения и наследует правило. Стендом покрыто только ПРОВЕДЕНИЕ: пути удаления и восстановления требуют модели листа «История» и архива, это отдельный объём работы, отмечено в tests/apps-script/README.md → ✅ G. Разрешение нулевого количества (19.08.2026): ноль допустим для «Корректировки остатка» во всех трёх путях ввода — кнопка «Ещё позиция», отправка формы и счётчик на кнопке сохранения → ✅ H. Живой регресс (19.08.2026): владелец проверил в бою выбор при списании, плашку долга, ноль в корректировке остатка и сохранение долга при нулевом остатке — принято. По ходу регресса найдены и исправлены три дефекта: фронтенд дублировал обнуление капитализации (normalizeStock в сторе) и показывал ноль там, где сервер хранил долг; поле количества хранит ЧИСЛО, поэтому ноль отключал кнопки условием `!quantity`; надпись о долге перенесена из ячейки артикула в колонку «Капитализация» → ✅ I. Разовый ремонт долгов (19.08.2026) — РЕМОНТ КОДОМ НЕ ПОТРЕБОВАЛСЯ, оба долга закрыты. СВЕРКА БОЕВОЙ БАЗЫ 19.08.2026 вечером, после живого регресса: (1) «Полка_выдв_27см» — долг 6195,66 руб СНЯТ самим владельцем 19.08.2026 в 15:41 при проверке новой кнопки: строка «Истории» «Склад [Списание - Утеря] [себестоимость обнулена]», 1 шт, себестоимость 6195,66 руб; капитализация артикула стала 0,00 руб. Средняя себестоимость в листе осталась прежней (6195,66) — это штатное поведение, на экране при нулевом остатке показывается ноль. (2) «Миска серая» — 13 шт, капитализация 12997,14 руб, лишних 11307,14 руб. РАЗБОР ПРОИСХОЖДЕНИЯ СУММЫ расчётом по всей истории артикула: приход 492 шт по 128,197 руб (63072,95) → отгрузка Ozon 22.07.2026 396 шт списала 50766,03 руб, то есть РОВНО 396 × 128,197, ошибки в отгрузке нет → брак 95 шт 02.08.2026 оставил 12178,72 руб → приход 13 шт по 130 руб и корректировка −1 шт сняли 999,78 руб. Итого 11307,14 руб — это в точности стоимость 95 бракованных штук, то есть штатный долг брака, а не сбой учёта. РЕШЕНИЕ ВЛАДЕЛЬЦА 19.08.2026: оставить как есть, долг впитает партия 1200 шт — это ровно та логика, которую владелец заказывал в самом пункте 40 («капитализация отбракованных единиц добавляется к новой поступившей партии»). Расчёт последствия: 1213 шт, капитализация 168997,14 руб, себестоимость 139,32 руб/шт вместо 130,00, то есть +9,32 руб на штуку. До прихода партии 13 штук показывают 999,78 руб/шт и держат плашку «долг себестоимости» — это верное поведение, а не дефект. ОТКЛОНЁННЫЙ ВАРИАНТ: корректировка остатка в ноль (сняла бы ровно 12997,14 руб) с последующим приходом 13 шт по 130 руб — признал бы 11307,14 руб убытком от брака здесь и сейчас

- ✅ 41. География спроса: кластер продажи по месту доставки покупателю, а не по складу отгрузки. ПРИОРИТЕТ: выполняется вне очереди, сразу после пункта 34 и перед пунктами 35–39, решение пользователя 10.08.2026. Причина приоритета: пункт 38 о тренде продаж считается на этих же данных, поэтому переход на новую географию после него потребовал бы повторной проверки тренда. ПРОБЛЕМА, поставлена пользователем 10.08.2026 и подтверждена разведкой Ozon API через Claude Code: в server.ts в обработчике /api/ozon/sales кластер продажи определяется по analytics_data.warehouse_id, то есть по складу отгрузки продавца, а не по месту доставки покупателю. Если товара нет в кластере покупателя, Ozon отгружает заказ из другого кластера, и продажа записывается чужому кластеру. Возникает петля обратной связи: кластер, куда товар не завезли, показывает нулевой спрос, алгоритм считает его мёртвым и никогда не рекомендует туда поставку, а отсекатель по максимальному сроку продаж кластера из пункта 34 эту петлю усиливает. ФАКТЫ РАЗВЕДКИ, получены 10.08.2026 через Ozon MCP на боевом кабинете: (1) поле financial_data.cluster_to содержит готовое название кластера доставки и заполнено у 538 из 538 отправлений за 4 полные недели; (2) поле analytics_data.city содержит только город и пустое у 89 из 538 отправлений, строить географию на нём нельзя; (3) полей с регионом, областью или индексом покупателя в API нет; (4) метод /v1/analytics/data не имеет разреза по кластеру, региону или городу ни на одном тарифе, допустимые значения dimension: unknownDimension, sku, spu, day, week, month, year, category1, category2, category3, category4, brand, modelID; (5) справочник /v1/cluster/list связывает кластер только со складами и перечня обслуживаемых регионов не содержит; (6) названия в cluster_to буквально совпадают с полем name справочника кластеров, поэтому формат листа «Продажи Ozon» менять не требуется; (7) блок financial_data приходит только при with.financial_data = true, объём ответа на 100 отправлений с обоими блоками около 168 КБ. ЗАМЕР МАСШТАБА за 4 полные недели 13.07–09.08.2026 по 538 отправлениям без отменённых: 14,9 процента отправлений, то есть 80 из 538, уехали в кластер, не совпадающий с кластером склада отгрузки. Тяжелее всего искажение у Дальнего Востока — 10 из 12, Уфы — 7 из 14, Махачкалы — 3 из 3, Беларуси — 4 из 4. Сверка справочника складов с полем cluster_from дала полное совпадение 538 из 538, то есть текущая карта складов исправна и дефект не в ней. РЕШЕНИЕ: источником кластера продажи становится financial_data.cluster_to. ОГРАНИЧЕНИЯ ПУНКТА: формат листа «Продажи Ozon» не меняется; сопоставление кластеров по названию сохраняется; продажи в кластеры вне справочника, такие как Беларусь, Алматы и Астана, уходят в строку «Без кластера (агрегат)», входят в общую скорость товара и в сигнал заказа на фабрике, но кластерных рекомендаций не создают; расчёт в src/lib/ozonCoverage.ts в этом пункте не правится; карта складов loadWarehouseClusterMap в server.ts остаётся на месте и не удаляется. Этапы: ✅ 0. Внесение пункта в план и запись фактов разведки в docs/OZON_API.md (10.08.2026) → ✅ A. server.ts: запрос с financial_data и источник кластера cluster_to (10.08.2026) → ✅ B. Заметность незнакомых имён кластеров при синхронизации продаж (10.08.2026, журнал молчит, так как все имена кластеров есть в справочнике) → ✅ C. Разовый полный пересчёт истории продаж за 380 дней (10.08.2026, переписаны все 28 недель, включая архив 2025 года) → ✅ D. Живой регресс (10.08.2026). РЕЗУЛЬТАТ ПРОВЕРКИ 10.08.2026: неделя 2026-07-27 кабинета Mercurius в боевой базе совпала с эталоном, построенным из сырых данных Ozon по полю cluster_to, с нулевым отклонением по всем двадцати кластерам; отклонение от эталона по прежнему полю cluster_from составило 26 единиц. Эффект на рекомендации поставок при одинаковых настройках: общий объём 716 шт против 648 шт, а распределение изменилось существенно — Москва плюс 64 шт, Санкт-Петербург плюс 56 шт, Ростов плюс 36 шт, при этом Невинномысск минус 60 шт, Краснодар минус 48 шт, Ярославль минус 48 шт, Пермь минус 42 шт. Просели именно кластеры со складами-хабами, чьи продажи ранее засчитывались им вместо кластеров настоящих покупателей. ВАЖНОЕ ОБСТОЯТЕЛЬСТВО, выявлено 10.08.2026: в приложении работают ДВА кабинета Ozon — Mercurius и MaxiStore, за неделю 27.07.2026 они дали 135 и 178 штук соответственно. Ozon MCP в Claude Code подключён только к Mercurius, поэтому замеры через MCP описывают примерно половину оборота и не переносятся на MaxiStore автоматически.

- ✅ 42. Коррекция скорости продаж при дефиците товара. ПРИОРИТЕТ: выполняется вне очереди, сразу после пункта 35 и ПЕРЕД пунктами 36–39, решение пользователя 10.08.2026. Причина приоритета: пункты 36, 38 и 39 считаются на скорости продаж, поэтому исправлять их до исправления самой скорости бессмысленно. ПРОБЛЕМА, найдена пользователем 10.08.2026 и подтверждена замером архитектора на боевой БД: скорость продаж считается по последним четырём неделям и не отличает падение спроса от отсутствия товара, поэтому у распроданных артикулов она занижена в два-девять раз. Замер: Набор_полок_выдв_бел продавался по 11–16 шт в неделю в мае и июне, сейчас на Ozon 1 шт и скорость 0,36 шт/д; Миска_двойная давала до 75 шт в неделю, сейчас на Ozon 1 шт и скорость 5,46 шт/д; Полка_выдв_27см давала до 33 шт в неделю, сейчас на Ozon 8 шт и скорость 2,07 шт/д. Последствия занижения: приложение показывает «запаса хватит на 357 дней» по товару, которого нет; заказ на фабрику занижается; отсекатель maxClusterDays выбрасывает кластеры из рекомендации, потому что при коробке 42 шт и пороге 100 дней поставку получают только кластеры быстрее 0,42 шт/д, и у Миска_двойная из 24 кластеров со спросом рекомендацию получают три. РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 10.08.2026, гибридное, две части. ЧАСТЬ ПЕРВАЯ, работает сразу: признаком дефицита считается пустой склад, а не падение продаж, потому что падение продаж неотличимо от сезонного спада. Если остаток на Ozon (доступно плюс в пути) делить на текущую скорость и получается меньше настройки «Порог дефицита, дней», товар признаётся распроданным, и его скорость берётся как среднее по четырём лучшим неделям окна тренда, делённое на семь. Это фактические продажи, а не прогноз. Коррекция не применяется, если за окно продано меньше настройки «Минимум продаж для коррекции, шт», либо если в окне меньше шести недель, либо если скорректированная скорость не превышает текущую. Рост ограничивается настройкой «Максимальный рост скорости при дефиците, раз». Скорости кластеров умножаются на тот же коэффициент, что и скорость артикула, чтобы доли кластеров сохранились и отсекатель maxClusterDays перестал выбрасывать кластеры из-за заниженной скорости. ЧАСТЬ ВТОРАЯ, накопление данных на будущее: автоопрос Ozon начинает копить наличие остатков в новый лист «История остатков Ozon», чтобы через два-три месяца скорость можно было считать честно, по дням фактического наличия товара; переход на этот способ в объём пункта НЕ входит и будет отдельным пунктом. УТОЧНЕНИЕ ПОЛЬЗОВАТЕЛЯ 18.08.2026: хранится НЕДЕЛЬНЫЙ АГРЕГАТ, а не суточные строки. Причина: будущей формуле нужно не количество остатка, а число дней фактического наличия, поэтому в строке недели хранятся два счётчика — «Дней в наличии» и «Дней наблюдений» (второй отличает «товара не было» от «опрос не отработал»). Это ~198 строк в неделю вместо ~1900 и ~3000 строк за 15 недель вместо ~25000. Ключ строки (Неделя, Кабинет, Артикул, Кластер) совпадает с листом «Продажи Ozon», чтобы будущий расчёт сопоставил листы напрямую. Срок хранения — настройка stockHistoryRetentionWeeks = 15 недель, решение пользователя 18.08.2026: окно тренда 13 недель плюс две недели запаса. НОВЫЕ НАСТРОЙКИ Ozon со значениями по умолчанию: «Порог дефицита, дней» = 7; «Окно тренда, недель» = 13, эта же настройка используется пунктом 38; «Лучших недель для коррекции» = 4; «Минимум продаж для коррекции, шт» = 50; «Максимальный рост скорости при дефиците, раз» = 5. Значение 0 у порога дефицита полностью выключает коррекцию. ИНТЕРФЕЙС: в таблице «Остатки Озон» у скорректированного артикула скорость выводится янтарным цветом со значком TrendingUp, а при наведении разворачивается подсказка с обеими цифрами, коэффициентом роста, перечнем лучших недель и пометкой об упоре в предел роста. РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ 18.08.2026: оставить как реализовано. Прежняя формулировка пункта требовала выводить обе цифры прямо в ячейке («5,46 → 8,39, дефицит»); от этого отказались ради компактности таблицы, обе цифры доступны в подсказке. ЗАМЕР ОЖИДАЕМОГО ЭФФЕКТА, сделан архитектором 10.08.2026 на боевой БД при пределе роста 3: коррекция срабатывает ровно на пяти артикулах из пятнадцати — Миска_двойная 5,46 → 8,39; Полка_выдв_27см 2,07 → 4,21; Набор_полок_выдв_бел 0,36 → 1,07 с упором в предел; Органайзер_полка_бел 0,50 → 1,50 с упором в предел; Этажерка_20_белая 0,46 → 1,39 с упором в предел; остальные десять артикулов не затрагиваются. ОГРАНИЧЕНИЯ: коррекция меняет ТОЛЬКО скорость продаж, все остальные формулы расчёта покрытия остаются как есть; шесть учётных путей не затрагиваются, ни одна правка пункта не пишет в «Историю» и не двигает остатки; лист «Продажи Ozon» не переписывается и его структура не меняется; артикулы Органайзер_полка_бел и Этажерка_20_белая после коррекции начнут требовать заказ на фабрике, это ожидаемо и снимается архивированием в пункте 37. Этапы: ✅ 0. Внесение этого пункта в план → ✅ A. Code.gs: пять новых настроек Ozon (11.08.2026) → ✅ B. src/lib/ozonCoverage.ts: коррекция скорости и масштабирование кластеров (11.08.2026) → ✅ C. Окно настроек Ozon: пять полей и проверка (11.08.2026) → ✅ D. Пометка о коррекции в таблице «Остатки Озон» (11.08.2026) → ✅ E. Code.gs: лист «История остатков Ozon», недельный агрегат наличия (18.08.2026, проверен на боевой базе 19.08.2026): стенд на Node — 27 проверок по 8 сценариям, провалов 0. Боевая проверка после двух суток автоопроса: 198 строк, «Дней наблюдений» = 2 у всех, то есть счётчик растёт ровно один раз в сутки при двух опросах — задвоение, ради защиты от которого писалась нормализация дат, не происходит → ✅ F. Live regression (2026-08-19), carried out inside the item 22 stage I regression and confirmed on production revision sklad-00011-rtq. The correction was verified twice over: first independently, by recomputing it from the production database export outside the application code — best 4 weeks of the trend window divided by 28 days reproduced all three corrected speeds exactly (Миска_двойная 8.393, Полка_выдв_27см 4.214, Набор_полок_выдв_бел 1.750); then on the live screen after deployment, where the same three values appear with the «скорость скорректирована» marker. Growth factors 2.45x, 2.46x and 4.90x all stayed under the maxSpeedGrowth ceiling of 5, so the ceiling itself is not exercised by current data. IMPORTANT SIDE FINDING: before the item 22 stage I fix the correction was computing on an 11-week window instead of the configured 13, which understated two of the three speeds and inverted the factory signal for Набор_полок_выдв_бел — the correction logic was correct, the data window feeding it was not.

- ✅ 43. Справочник доп. работ: показывать действующие расценки, а не первые заведённые. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «В тарифах доп работ "Справочник" должны отображаться по умолчанию актуальные расценки а не первые заведенные». The «Справочник» screen lists extra-work rates and currently shows the FIRST rate ever entered for a service instead of the one in force today. Rates live in the «Тарифы услуг» sheet as ServiceID + Стоимость + ДействуетС, so several rows per service are normal and the current one is the row with the latest ДействуетС that is not in the future. CHECK FIRST: whether the screen picks the first matching row or genuinely has no date logic at all — the fix differs. Watch out for rows with an empty or malformed ДействуетС. ВЫПОЛНЕНО 20.08.2026. ROOT CAUSE: the screen had no date logic at all — the «Стоимость» column printed service.cost, the price stored on the service record when it was created, while ConfirmModal billed invoices through its own getServiceCostAt resolver. The backend already ships a currentCost field on every service (Code.gs getServices), but it is NOT used here: addServiceRate returns only the rate list, so the store refreshes serviceRates while currentCost stays stale until a full reload. FIX: DirectoryTab picks the newest rate whose ДействуетС has already started (rates are sorted newest first) and falls back to the base price; rows with an empty ДействуетС are skipped exactly as Code.gs getServiceCostAt does. Today is built from the local calendar, not toISOString(), because in Moscow the UTC date lags until 03:00 and a tariff starting today would be invisible for the first three hours of the working day. A grey second line shows «тариф с ДД.ММ.ГГГГ · базовая N ₽» so the base price the edit form still edits stays visible. No Code.gs change, no backend deploy. ДОРАБОТАНО 20.08.2026 ПО ТРЕБОВАНИЮ ВЛАДЕЛЬЦА, дословно: «в тарифах убери подписи про базовую стоимость должна остаться только подпись с какого числа действует эта стоимость проверь что бы в расчет попадала именно новая стоимость при этом старые расчеты которые были до указанной даты должны оставаться по старым расценкам ключевой фактор это дата отсейки которая меняет стоимость услуг». Подпись сокращена до «действует с ДД.ММ.ГГГГ». АУДИТ ДАТЫ ОТСЕЧКИ ВСКРЫЛ НАСТОЯЩИЙ ДЕФЕКТ, из-за которого старая отгрузка НЕ оставалась по старым расценкам. Запись отгрузки хранит сумму по тарифу на дату поставки в строке «Услуги: Название x2 (30₽)» — это верно. Но окно «Редактировать услуги» на вкладке «Отгрузка» при открытии подставляло вместо исторической цены СЕГОДНЯШНЮЮ БАЗОВУЮ цену из справочника и пересчитывало количество как round(сумма ÷ цена): отгрузка «2 шт по 15 ₽ = 30 ₽» открывалась как «3 шт по 10 ₽», и при сохранении окна переписывалась именно так — с искажённым количеством. ROOT CAUSE: логика расценок существовала тремя расходящимися копиями — в справочнике её не было вовсе, в окне подтверждения она не отсеивала пустую ДействуетС, в окне редактирования подменяла историю справочником. Все три сведены в src/lib/serviceRates.ts (resolveServiceRateAt, resolveServiceCostAt, parseStoredServiceEntries, todayLocalDateString) и покрыты 18 тестами. Расценка старой отгрузки теперь берётся ИЗ САМОЙ ОТГРУЗКИ. Старый формат «Доп. услуги: Название (300₽)» количества не хранит: только для него и только когда справочная цена делит сумму нацело, количество восстанавливается по справочнику, иначе услуга считается разовой. Новые расчёты по-прежнему идут через getServiceCostAt(услуга, дата поставки) — проверено, что других потребителей цены услуг в приложении нет.
- ✅ 44. Воронка поставок на дашборде: убрать отменённые заявки. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «Воронка поставок на вкладке Склад не нужно показывать на дашборде отмененые заявки только Готовые к отгрузкам и те которые еще в пути и полностью не приняты на всех складах». The funnel on the «Склад» tab currently counts CANCELLED supplies — today it shows «Готова к отгрузке 3 / Отменена 8». The owner wants only live supplies: ready to ship, plus those in transit and not yet fully accepted at every warehouse. NOTE: «полностью не приняты» means acceptance is per-warehouse, so a supply counts as live while ANY of its warehouses is still outstanding; the acceptance data is in «Внешние отгрузки» (ПринятоJSON) and the Ozon status field. Statuses that must not count: CANCELLED, REJECTED_AT_SUPPLY_WAREHOUSE, OVERDUE, and fully COMPLETED ones. ВЫПОЛНЕНО 20.08.2026. РЕШЕНИЕ ВЛАДЕЛЬЦА 20.08.2026 (голосование): «Живые + проблемные» — the funnel keeps READY_TO_SUPPLY, ACCEPTED_AT_SUPPLY_WAREHOUSE, IN_TRANSIT, ACCEPTANCE_AT_STORAGE_WAREHOUSE, REPORTS_CONFIRMATION_AWAITING and also OVERDUE, REJECTED_AT_SUPPLY_WAREHOUSE, REPORT_REJECTED, because those are unfinished and need action. Dropped: CANCELLED, COMPLETED (fully accepted everywhere) and DATA_FILLING (unsent draft; a row with no status defaults to DATA_FILLING and drops too). Implemented as FUNNEL_HIDDEN_STATUSES + isFunnelVisibleStatus in src/lib/ozonStatus.ts — a DENY-list on purpose, so a status Ozon adds later shows up in the funnel instead of disappearing silently. The caption now reads «N активных поставок», since the counter changed meaning. The opacity-60 dimming of COMPLETED/CANCELLED cards was removed: those cards can no longer appear.
- ✅ 45. Заявка из «Рекомендаций»: добавлять свои кластеры и товары. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «сделать возможность добавлять кластера и товары в поставку которая создается из раздела Рекомендации». The supply wizard started from «Рекомендации» is limited to what the recommendation itself proposes. The owner wants to add clusters and articles by hand before creating the draft. Touches the wizard, the local credit of items 23 and the reserve on «Мой склад»: anything added by hand must reserve stock the same way a recommended line does, otherwise the credit and the «собрать сейчас» figure will lie. Consider what happens when the added quantity exceeds free stock. ВЫПОЛНЕНО 20.08.2026. РЕШЕНИЯ ВЛАДЕЛЬЦА 20.08.2026 (голосование): добавлять — в самом окне поставки, рядом с правкой количеств; при переборе — НЕ ДАВАТЬ ПРЕВЫСИТЬ, дословно: «не давать превысить должны быть подсказки сколько штук сейчас доступно для отгрузки возможно что на складе лежит больше но часть уже зарезервировано поэтому подсказка показывает доступный остаток для отгрузки». УСТРОЙСТВО, ВЫЯСНЕННОЕ РАЗВЕДКОЙ: весь низ окна — payload чернового запроса, availabilityCheck и itemsJSON заявки — читает ОДИН массив activeRows, а он был чистой фильтрацией входного пропса rows. Поэтому добавление сделано в одном месте: появилось состояние addedRows, allRows = rows + addedRows, activeRows фильтрует allRows. Ни payload, ни проверка доступности, ни запись в журнал заявок, ни предупреждения о коробках и SKU про добавленные строки ничего не знают — они работают как раньше. Резерв на «Моём складе» тоже строится сам: buildPendingSupplies читает из itemsJSON поля {article, clusterId, qty}, а добавленная строка кладётся туда в точности такой же. ПОТОЛОК: capForSupplyLine в новом src/lib/ozonSupplyLines.ts — свободный остаток товара МИНУС то, что уже заняли другие строки той же заявки по этому же товару (один товар может стоять в нескольких кластерах). Незнакомый товар получает Infinity, а не ноль: неизвестный предел не должен молча стать запретом. Потолок применяется и при правке количества в существующей строке, и в форме добавления; в каждой строке подписано «доступно для отгрузки N шт», в форме — то же плюс норма коробки и объяснение, что на складе может лежать больше из-за резерва. КЛАСТЕР можно выбрать ЛЮБОЙ из справочника «Кластеры Ozon» (ozonClusterRefs в сторе), не только из рекомендаций; используемые сейчас показываются первыми. ТОВАР предлагается только с SKU Ozon — без него строка физически не уйдёт в Ozon (buildPayloadClusters её пропустит), — и только тот, которого ещё нет в выбранном кластере: ключ строки это «артикул|||кластер», дубль сломал бы правку количеств. Тесты: 10 новых на потолок, всего 116. НЕ ПОКРЫТО ТЕСТАМИ: сама отрисовка окна — React-тестов в проекте нет, форма проверялась только типами и сборкой. ДОРАБОТКА ОКНА ПОСТАВКИ 20.08.2026 ПО ЗАМЕЧАНИЯМ ВЛАДЕЛЬЦА, ПРОВЕРЕНО ИМ ВЖИВУЮ («Все работает»), боевая ревизия sklad-00031-mt9. Подробности каждой правки — в DEVLOG и TEST_LOG за 20.08.2026; здесь список, чтобы не искать. (1) БЕЛЫЙ ЭКРАН при нажатии «Оформить поставку» — мой регресс: два useMemo встали НИЖЕ раннего `return null`, а окно смонтировано всегда и открывается пропсом, поэтому число хуков менялось между отрисовками. Тот же дефект найден и устранён в ConfirmModal, где он не проявлялся только из-за условного монтирования. Правило 11.11 брифа, запрещено тестом src/lib/hooksOrder.test.ts. (2) ОКНО ОТКРЫВАЛОСЬ ВНЕ ЭКРАНА: `.tab-enter` шла с `forwards`, конечный кадр оставлял transform навсегда, и элемент с transform становился системой координат для `position: fixed` — окно привязывалось к вкладке, а не к экрану (замерено: подложка на -2468px). Убран `forwards`; чинит все окна во всех вкладках. Правило 11.12. (3) В выборе кластеров убраны отмеченные в настройках «Кластеры без поставок»; в выборе товаров остались только те, у кого есть свободный остаток на Своём складе; подсказка переписана; в списке только артикул. (4) ДВА БЛОКА СЛИТЫ В ОДИН: ответ Ozon разложен по кластеру и артикулу (foldOzonVerdict) и выводится внутри строки состава; список сгруппирован по кластерам, название кластера крупнее, SKU из строк убран; окно приведено к одному цвету. (5) КНОПКА «СКОРРЕКТИРОВАТЬ ПОСТАВКУ» (applyOzonCorrection): выставляет принятые количества, УБИРАЕТ из заявки товар, который Ozon не берёт совсем, и кластер, из которого он не берёт ничего; пересчёт после этого не нужен. Убранное возвращается кнопкой «Вернуть убранные строки». (6) ИСПРАВЛЕН НАСТОЯЩИЙ ДЕФЕКТ: при создании заявки в Ozon отправлялись все кластеры, по которым он когда-либо отвечал, включая убранные из состава. (7) ЗАЯВКА БОЛЬШЕ НЕ СОЗДАЁТСЯ САМА: раньше при полном одобрении Ozon заявка уходила сразу, без показа состава и подтверждения. Путь один: «Проверить в Ozon» → состав с ответом → «Создать заявку в этом составе». Первая кнопка переименована, потому что «Оформить заявку в Ozon» на кнопке, которая только считает, — это ловушка. (8) КНОПКА «РАСПРЕДЕЛИТЬ ВЕСЬ ОСТАТОК» у каждого товара в «Рекомендациях»: пересчитывает распределение ЭТОГО товара по окну тренда из настроек вместо окна скорости, расчёт ленивый. ЗАМЕРЕНО ТЕСТОМ: широкое окно НЕ увеличивает общий объём — знаменатель скорости это недели, реально присутствующие в данных, поэтому те же продажи делятся между большим числом кластеров (4 нед.: один кластер 20 шт; 13 нед.: два по 10). Рядом выводится «не разложено N шт». (9) ОТМЕЧЕННЫЕ КЛАСТЕРЫ НЕ ДОЕЗЖАЛИ ДО ОКНА: заявка собиралась из coverageRows (обычное окно), а галочки ставились в «Рекомендациях», где у переключённого товара кластеры из широкого окна — отметил 8, в окне 4. Теперь заявка собирается из того же списка, в котором стоят галочки. (10) ВЫБОР ШИРОКОГО ОКНА НЕ ПЕРЕЖИВАЛ УХОДА СО ВКЛАДКИ: App.tsx размонтирует вкладку при переходе, состояние компонента пропадало, расчёт молча возвращался к окну скорости — и товар мог исчезнуть из «Рекомендаций» целиком. Перенесено в useUIStore с сохранением в localStorage. ОТДЕЛЬНО ПРОВЕРЕНО И ЗАКРЫТО: стоимость логистики поставки до кластера в API Ozon НЕ ОТДАЁТСЯ ни до, ни после создания заявки — разбор с таблицей проверенных методов в docs/OZON_API.md, повторно не выяснять.
- ⬜ 46. Создание поставки вне «Рекомендаций» — продумать откуда и как. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «Возможность создавать поставку не из Рекомендаций, продумать от куда создавать и как смотреть что добавить в нее и куда отправлять». DESIGN WORK, not yet a specification. Today a supply can only be born from a recommendation. The owner wants a way to start one from scratch and needs the entry point, the picking screen and the destination choice thought through. OPEN QUESTIONS to settle with the owner before any code: where the button lives, how the user browses what to add (by article, by cluster, by warehouse?), how the destination cluster is chosen when no recommendation suggests one, and whether such a supply still respects the drop-off point from «Настройки Ozon». Depends on item 45 — the same picking UI probably serves both.
- ⬜ 47. Автоматический расчёт себестоимости для КАН и выгрузка файла. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «Автоматический расчет себестоимости для КАН. По итогам отгрузок за месяц должна быть кнопка "Выгрузить данные для КАН" и выгружается файл определенной структуры где указывается новая себестоимость товара с учетом отгрузок и этот файл пользователь загружает в КАН. Тут нужно подумать как считать себестоимость ведь нужна себестоимость товара на Озон а приложение знает себестоимость отгруженного товара, поэтому себестоимость прошлого месяца подтянем из КАН количество остатка подтянем из Озона и посчитаем новую себестоимость». A button «Выгрузить данные для КАН» produces a file of a fixed structure with the new unit cost per article, which the owner uploads into KAN. THE ARITHMETIC IS THE HARD PART and the owner already sketched it: the app knows the cost of goods it SHIPPED, but KAN needs the cost of goods SITTING ON OZON. So the new cost is a weighted average of what was already on Ozon at last month's cost and what was shipped during the month at the app's own cost: (leftover_qty × last_month_cost + shipped_qty × shipped_cost) / (leftover_qty + shipped_qty). Inputs: last month's cost pulled from KAN, the leftover quantity pulled from Ozon, the shipped quantity and its cost from «История». MUST SETTLE BEFORE CODING: the exact file structure KAN expects, which Ozon quantity counts as «остаток» (available only, or available + in transit + returns?), what to do with an article that has no KAN cost yet, and how to handle virtual kits, whose cost is assembled from components. The KAN MCP server is available in this session for reading shop data.
- ⬜ 48. Остатки на складах Ozon: фильтр по магазинам. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «Остатки на складах Озон сделать фильтр по магазинам». The «Остатки Озон» tab already filters by cabinet (Mercurius / MaxiStore). The owner asks for a filter by SHOP, which is not the same thing — clarify whether «магазин» means the Ozon cabinet under another name, or a finer grouping. If it is the cabinet, the work is to carry the existing cabinet filter into the places that still ignore it.
- ⬜ 49. SKU База: признак «нужна коробка от ФФ» и его учёт в услугах. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «в справочнике sku сделать колонку требуется ли коробка от фф и по умолчанию у таких позаций не ставить в расчет коробки как доп услуги». A new column in the SKU sheet marking whether the article needs a fulfilment-centre box. Articles marked as NOT needing one must stop having the box charged as an extra service by default. Touches the SKU sheet layout (add via ensureColumns, never by hand — several code paths read this sheet by position), the SKU screen, and the service-cost calculation used when shipping. Check where «Стоимость 1 короба ФФ» is applied today — it appears in the service breakdown of shipment rows.
- ⬜ 50. Заказ на фабрике вне рекомендаций расчёта. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «Сделать возможность указывать заказ на фабрике товара который по расчетам не нужно заказывать но пользователь его заказал и нужно отмечать это в заказах на фабрике». Today a factory order can only be recorded for an article the calculation flags as needing one. The owner sometimes orders anyway and needs to record it. The order must land in «Заказы на фабрике» like any other and must count in the ТРУБА, so the next calculation sees it. Note the existing rule: an order whose expected date has passed drops out of the ТРУБА deliberately.
- ⬜ 51. Рекомендации: предлагать неполную коробку, когда полной не набирается. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «если остатков на своем складе не хватает на полную коробку но требуется поставка куда-то в рекомендациях нужно предлагать отправлять неполную коробку». Recommendations are rounded to whole boxes, so an article needed in a cluster is skipped when free stock does not make up a full box. The owner wants a partial box offered instead. Careful: the box size comes from ШТ/КОР in the SKU sheet and the rounding is deliberate elsewhere (the factory order rounds UP to whole boxes and must keep doing so). This change is only about the Ozon supply recommendation, not the factory order.
- ⬜ 52. Проверить возврат резерва при частичной отмене заявки. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «проверить как происходит когда создается заявка под нее бронируется товар потом на озон селлере часть товара отменяется или отменяются кластера для отгрузки и товар должен возвращаться на склад раз заявка поменялась». AUDIT FIRST, code only if it is actually broken. When a supply is created the stock is reserved; if the owner then cancels part of it in Ozon Seller — individual items or whole clusters — the reservation must shrink accordingly. Item 23 stage E verified that a FULLY cancelled order releases its credit, and that a partially cancelled order (121166050, CANCELLED + COMPLETED mix) produced no credit at all — but the PARTIAL case with the rest still live was never exercised on production data. Reproduce it deliberately rather than waiting for it to happen.
- ⬜ 53. Таблица остатков по кластерам с разворачиванием до складов. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «сделать табоицу в которой видно остатки товаров по кластерам с возможностью развернуть кластер и посмотреть склады (слева список товаров сверху список кластеров с + что бы развернуть кластер)». A cross table: articles down the left, clusters across the top, a plus on each cluster to expand it into its warehouses. This is a different shape from the existing «Остатки Озон» table, which is article → cluster → warehouse in a tree. All the data is already loaded — «Остатки Ozon» holds cabinet, article, warehouse, cluster and quantities — so this is presentation work. Consider how it behaves with 25 clusters across the top on a laptop screen; horizontal scrolling will be needed.
- ✅ 54. Левое меню должно полноценно сворачиваться до иконок. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «левое меню должно полноценно сворачиваться (должны оставаться только иконки) для увеличения площади экрана для данных». The sidebar has a collapse control but does not collapse to icons only. The owner wants the full collapse to free horizontal space for the tables. Keep the tooltips on the icons so the collapsed menu stays usable, and remember the chosen state between visits. ВЫПОЛНЕНО 20.08.2026. ROOT CAUSE: a Tailwind cascade collision, not a missing feature — the <aside> carried both the conditional md:w-20 / md:w-72 and a constant md:w-auto. All three land in the same @media (min-width:48rem) block and Tailwind emits w-auto last, so md:w-auto always won and the intended widths never applied. PROOF: in the browser, the old class string gave the collapsed sidebar a computed width of 1280px; after removing md:w-auto the same strings give 288px expanded and 80px collapsed. The icons, tooltips (title=item.label) and the collapsed status dot were already implemented and needed no work. ADDED: the flag is now persisted in localStorage under sidebarCollapsed, wrapped in try/catch for private mode. Only this one flag is persisted — the rest of useUIStore holds transient state (open modals, parsed upload rows) that must start clean. ДОРАБОТАНО 20.08.2026 ПОСЛЕ ЖИВОЙ ПРОВЕРКИ ВЛАДЕЛЬЦЕМ — первая правка была НЕПОЛНОЙ, и запись выше о том, что иконки и подсказки «уже работали», НЕВЕРНА. Владелец увидел: «правая часть наезжает на левую и немного её перекрывает, при этом надписи не скрываются, а просвечивают снизу». Тот же конфликт классов Tailwind был ещё в ДВУХ местах внутри панели, и там он ломал именно содержимое. (1) Подпись пункта меню собирала одновременно «hidden md:hidden» (свёрнутое состояние) и «hidden md:inline» (пункт не активен): Tailwind ставит md:inline в таблице стилей ПОЗЖЕ md:hidden, поэтому подпись оставалась видимой и вылезала за пределы панели. Измерено в браузере на боевой разметке: панель 80px, правый край подписи 153px — 73px наружу, под ними и просвечивала основная часть. (2) У кнопки пункта постоянный md:justify-start стоял рядом с условным md:justify-center, а justify-start Tailwind выводит позже — иконки не центровались. ИСПРАВЛЕНО: каждая утилита упоминается ровно один раз, видимость подписи на телефоне и на десктопе задаётся разными классами. ПРОВЕРЕНО измерением: свёрнутое — ширина 80px, все подписи display:none, кнопки justify-content:center; развёрнутое — 288px, подписи видимы, justify-content:flex-start. ПРАВИЛО НА БУДУЩЕЕ: в одной строке className каждая утилита Tailwind должна встречаться ОДИН раз; порядок в строке ни на что не влияет, выигрывает та, что выведена в CSS позже.
- ✅ 55. Алерты: не звать на поставку в Ozon, когда товара нет на своём складе. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 20.08.2026, дословно: «в алертах не должно появляться сообщение Пора сделать поставку на Ozon если товара нет на моем складе». The alert «Пора сделать поставку на Ozon» fires even when «Мой склад» is empty, so it asks for a shipment that cannot be made — today six of the thirteen alerts say «нужно N шт — нет на своём складе». Suppress the alert when there is nothing free to ship. CAREFUL: «свободный остаток» already means stock minus the reserve held by created supplies, and for a virtual kit it means what can be assembled from components — use the same figure the recommendation itself uses, not the raw quantity. Decide with the owner whether such an article should disappear from the alerts entirely or turn into a different, quieter message. ВЫПОЛНЕНО 20.08.2026. РЕШЕНИЕ ВЛАДЕЛЬЦА 20.08.2026 (голосование), дословно: «алерт исчезает до тех пор пока не появиться остаток на своем складе как только остаток на своем складе появляется начинает считаться распределение по кластерам и алерт сообщает о необходимости сделать поставку». FIX: buildCoverageAlerts raises the supply alert only when at least one cluster has recommendation.boxes > 0. That is the exact test for «есть что отгрузить», because boxes is handed out from freeMyStock — the own-warehouse stock left after the reserve held by created supplies, and for a virtual kit what the components allow. The need is NOT lost while the alert is silent: the same unmetDeficitQty feeds calcFactorySignal and keeps raising «Пора заказать на фабрике». Clusters with no stock stay inside the description as «нужно N шт — нет на своём складе» whenever another cluster is shippable.
- 🔄 56. Списание нескольких заявок Ozon одной операцией. ЗАПИСАНО СО СЛОВ ВЛАДЕЛЬЦА 25.08.2026. Подрядчик пакует и везёт несколько заявок Ozon как одну физическую поставку, поэтому упаковка, прочие расходы и услуги оплачиваются один раз за всю партию и должны делиться по ОБЩЕМУ списку артикулов из всех выбранных заявок. Во вкладке «Поставки Озон» у заявок, готовых к списанию, появляются галочки; отмеченные списываются за один проход.
  Решения владельца (опрос 25.08.2026, менять только по его решению):
  - Галочками можно отмечать заявки ТОЛЬКО одного магазина (кабинета): назначение расхода пишется как «Ozon (магазин)», при смешивании привязка к магазину теряется.
  - В «Историю» пишется ОТДЕЛЬНАЯ запись на каждую заявку, а не одна общая: прослеживаемость по номерам заявок важнее числа строк.
  - Доп. расходы делятся ПО КОЛИЧЕСТВУ ШТУК — везде, включая предпросмотр в окне списания. Отменяет прежнее деление по стоимости позиции в ConfirmModal.
  - Сумма доп. расходов передаётся на сервер ОТДЕЛЬНЫМ ЧИСЛОМ (правка Code.gs), а не выковыривается регулярками из строки назначения. Причина: сервер делит расходы по количеству штук ТЕКУЩЕЙ операции (Code.gs, parseAdditionalCostsFromDestination + commitTransaction), поэтому N раздельных расходов с полной суммой в тексте умножили бы услуги в N раз. Альтернатива «писать в текст долю» отклонена владельцем: в «Истории» стояла бы не реальная цена услуги, а доля.
  Этапы: 1) модуль расчёта; 2) правка Code.gs; 3) интерфейс выбора и запись N расходов.
  ✅ Этап 1 (25.08.2026): src/lib/ozonBatchWriteOff.ts — слияние заявок в общий список артикулов и деление суммы доп. расходов по количеству штук методом наибольших остатков (доли сходятся с введённой суммой до копейки). Тест src/lib/ozonBatchWriteOff.test.ts: 22 проверки, включая сверку с формулой сервера (N раздельных расходов дают тот же итог, что одно объединённое списание). Проверено мутациями: замена метода наибольших остатков на обычное округление ловится, слияние без учёта статуса позиции ловится.
  ✅ Этап 2 (25.08.2026): Code.gs принимает сумму доп. расходов ОТДЕЛЬНЫМ ЧИСЛОМ (payload.additionalCosts → 8-й параметр commitTransaction). Если число передано — оно перебивает разбор строки назначения, если нет — работает прежний разбор регулярками, поэтому все ранее записанные расходы и старый клиент не затронуты. Переданный ноль — это утверждение «эта заявка расходов не несёт», а не отсутствие значения. Разрешённая сумма сохраняется в НОВОЙ колонке «ДопРасходы» листа Истории (добавляется сама через ensureColumns, как когда-то groupId/isComponent/OpID) и читается обратно в parseTransactionRow. Это закрывает дыру в путях перепроведения: правка операции в Истории (updateTransaction) и перепроведение после пересорта берут число из колонки, а не из текста — иначе объединённой поставке второй раз насчитали бы стоимость всей партии. Проверки: стенд Apps Script 96 проверок, 0 провалено (было 83, добавлено 13), включая сверку «два раздельных расхода = одно объединённое списание». Проверено мутациями: если игнорировать переданное число — падает 9 проверок, если не сохранять его в колонку — 3. ТРЕБУЕТСЯ РУЧНОЕ РАЗВЁРТЫВАНИЕ Code.gs владельцем. РАЗВЁРНУТО ВЛАДЕЛЬЦЕМ 25.08.2026, подтвердил: «развернул, все работает».
  ✅ Этап 3 (25.08.2026): интерфейс выбора и запись. Во вкладке «Поставки Озон» у заявок, готовых к списанию, появились галочки; после первой отметки заявки других магазинов блокируются (подсказка называет разрешённый магазин). Панель сверху показывает выбранные заявки, число поставок и кнопку «Оформить списание (N)». Окно списания получило шапку с перечнем заявок и их количеством штук. КЛЮЧЕВОЕ РЕШЕНИЕ: одна заявка идёт ПРЕЖНЕЙ дорогой (обычный commit, состав правится как раньше) — новая ветка включается только от двух заявок, поэтому самый ходовой путь не тронут. Для партии состав правке не подлежит: строка сложена из разных заявок, и у правки нет одной заявки-владельца. Каждая заявка пишется своим расходом со СВОИМ ключом идемпотентности (`opId-N`) — с общим ключом второй заявке вернулся бы результат первой. При обрыве посреди партии окно остаётся открытым и называет, какие заявки уже записаны, а какие нет. В доли считаются ТОЛЬКО отгружаемые штуки: нераспознанный артикул сервер пропускает, и если считать его, доли остальных заявок завышаются. Пометка в «Историю» про общую поставку пишется через batchDestinationNote и намеренно использует «руб.», а не «₽»: у сервера остался запасной разбор текста регулярками, и знак рубля в пометке молча добавил бы денег. ЗАОДНО (решение владельца 25.08.2026): предпросмотр в ConfirmModal переведён с деления доп. расходов ПО СТОИМОСТИ позиции на деление ПО КОЛИЧЕСТВУ ШТУК — и для Расхода, и для Прихода. Сервер всегда делил по штукам, так что предпросмотр расходился с таблицей, когда в операции были товары разной цены. Проверки: 169 тестов фронтенда (было 163), 96 проверок стенда, tsc и сборка чисто. Мутации: счёт по всем штукам вместо отгружаемых ловится, знак рубля в пометке ловится.
  ⬜ Хвост пункта 56 (НЕ ДЕЛАТЬ без решения владельца): в Code.gs запасной разбор текста НЕ ВИДИТ упаковку в режиме «на всю партию» — регулярка требует «= N ₽», а текст в этом режиме выглядит как «Упаковка: 500₽». То есть у обычного одиночного расхода с упаковкой «на партию» её стоимость в себестоимость НЕ попадает. Дефект существовал до пункта 56 и им не создан. Передача суммы числом его чинит, но одиночная заявка намеренно оставлена на прежней дороге, чтобы не менять цифры задним числом. Решение за владельцем.

## Известные хвосты вне плана
- Предохранитель зачёта в 7 дней после правок пункта 30 срабатывает только при пустом или неизвестном статусе Ozon. Вживую этот путь не проверялся ни разу, потому что все наблюдаемые заявки имели известный статус.
- Поведение резерва и алерта «Заявка больше остатка» под учётной записью обычного пользователя не проверялось: блок «Алерты Ozon» на главной виден только администратору.
- Метка версии сборки меняется только при сборке из AI Studio. Правка кода без деплоя метку не меняет, поэтому совпадение метки со временем последнего деплоя — единственный признак того, что новая версия доехала.
- Раздробление группы при восстановлении отгрузки комплекта из корзины: остатки корректны, страдает только группировка.
- Одна живая сессия на пользователя. Функция входа в Code.gs удаляет все прежние сессии того же логина, поэтому два клиента под одной учётной записью вытесняют друг друга. Срок жизни сессии при этом честные 24 часа, истечение ни при чём. В обычной работе не мешает, но срывает совместные проверки: 30.07.2026 из-за этого четыре раза прервался живой регресс. Решение при необходимости — завести отдельную учётную запись с ролью админа для проверок.
- Колонка «Расчётный» на вкладке «Остатки Озон» округляется при выводе, а покрытие в днях считается по точному дробному значению. Дробность возникает из-за слагаемого «возвраты × returnsToSalePct», где процент равен 95. Расхождение до 1,5 дня и только в строках, где есть возвраты. Расчёт верен, неточен только вывод. Проверено 30.07.2026 на 211 строках: сходится всё, кроме трёх строк с возвратами, и они объясняются именно этим округлением.
- Кнопка «Оформить заявку в Ozon» в мастере поставки создаёт настоящую заявку с первого нажатия. Обработчик handlePrimary при отсутствии verdict вызывает runDraft(true), а флаг autoCreate после успешного расчёта черновика сразу вызывает sendOrder. Отдельного шага «только черновик» нет, второго подтверждения нет. Учитывая, что отмена заявки в приложении не реализована, случайное нажатие создаёт заявку, которую придётся убирать вручную в Ozon Seller.
- Чтение настроек через gviz недостоверно. 30.07.2026 запрос gviz с обходом кэша вернул пустой priorityClusters, хотя действие getOzonSettings в тот же момент отдало заполненное значение 4007:1.2,4039:1.4,4041:1.1,4067:1.1,4071:1.1. Настройки Ozon читать только через getOzonSettings, gviz оставить для листов с данными.
- Пункт 22, этап I фактически закрыт примерно на 95% по итогам живой проверки 30.07.2026. Сверены до штуки: скорость продаж по 13 товарам и всем кластерам при speedWeeks 2, доля кластера в продажах, расчётный остаток, покрытие по 211 строкам с учётом приоритетных множителей 1.4 у Москвы, 1.2 у Санкт-Петербурга и 1.1 у Казани, Новосибирска и Ростова, обработка восьми исключённых кластеров, отбор кандидатов на поставку, расчёт потребности, округление вверх до коробок, урезание остатком своего склада с показом дефицита, состав заявки в мастере, точка отгрузки ЕКАТЕРИНБУРГ_ХАБ_ПЫШМА, кабинет и определение Ozon-SKU. Не проверена только фактическая отправка заявки в Ozon и возврат её во «Внешние отгрузки» с КластерID.
- Пункт 23, этап E частично подтверждён живьём 30.07.2026: зачёт по созданным заявкам работает. У товара Органайзер_2_пол_белый кластеры Казань и Пермь с покрытием минус 11 и минус 5 дней не попали в рекомендации именно потому, что туда уже едет товар по созданной заявке, зачёт по 16 шт на каждый. Сумма зачётов 16 плюс 16 плюс 16 равна 48 и совпадает с агрегатом и с подписью «резерв 48». Не проверено: снятие зачёта при переходе заявки в ACCEPTED_AT_SUPPLY_WAREHOUSE, работа предохранителя 7 дней, поведение под учётной записью обычного пользователя.
- При обновлении остатков Ozon возможна разовая ошибка кабинета code:2 — ретрая на уровне кабинета нет, лечится повторным нажатием, неполнота данных подсвечивается плашкой.
- Отмена заявки на поставку (/v1/supply-order/cancel) в приложении не реализована — ошибочная заявка убирается только вручную в Ozon Seller. Метод разведан, схема известна.
- Сверка «заявлено против принято» по артикулам через API недоступна: полей с фактически принятым количеством нет ни в /v3/supply-order/get, ни в /v1/supply-order/details, ни в /v1/supply-order/bundle. Разбор расхождений и согласование актов — только в личном кабинете.
- Устарело утверждение «клиентские правки деплоя не требуют»: server.ts раздаёт собранный фронтенд из dist, поэтому любое изменение в src/ в бою видно только после редеплоя Cloud Run.
- В ответе Ozon на черновик встречался кластер со статусом FULL_AVAILABLE, у которого при этом часть товара попадала в restricted_bundle_id (Уфа, 5 из 8 шт). Предположительно ограничение приёмки по конкретному SKU, статус кластера этого не отражает — наблюдать.
- Нет индикатора загрузки при обновлении таблицы склада (2–3 секунды тишины).
- Сырой технический текст ошибки Ozon показывается пользователю как есть.
- Массовое удаление в «Истории» неочевидно: кнопка появляется только после выбора галочек.
- Списание идёт по округлённой до копейки средней себестоимости — расхождение с точной средней до 0,04 ₽ на операцию, накопления не выявлено.
- Проведение пересорта не создаёт отдельной строки ПРИХОД на возврат товара: следствие архитектуры «повторного проведения», остатки при этом корректны.
- Лист «Пользователи» боевой БД: у записи «Администратор» (роль admin) пароль хранится открытым текстом, у двух других записей — хэши. Требуется ручная чистка.
- Тестовые строки в листе «Внешние отгрузки» нельзя создавать копированием строки-заголовка: нечисловой OrderID уходит в Ozon API и роняет автоопрос с HTTP 400.
- Кнопка «Партия пришла» закрывает заказ на фабрике сразу, до фактического проведения прихода: если бросить оформление на вкладке «Загрузка», заказ останется закрытым, а остатки не поднимутся. Лечится повторной отметкой заказа или обычным приходом.
- Настройки Ozon и справочник кластеров грузятся двумя независимыми местами — Dashboard.tsx и OzonStocksTab.tsx, каждое со своими значениями по умолчанию (4/7/30/60/80). При изменении состава настроек их нужно править в обоих файлах, иначе цифры алертов на главной разойдутся с блоком рекомендаций.

## Правило границ промта

Правило введено 01.08.2026 после инцидента: документационный промт, в разрешённых файлах которого не было ни одного файла кода, привёл к правкам server.ts и ConfirmModal.tsx, к самовольной пометке этапа выполненным и к полной потере пункта 23 плана вместе с открытым этапом I пункта 22. Потеря не была видна по размеру файла — он даже вырос на 96 байт, потому что удалённые куски были заменены добавленным текстом.

1. Промт, в списке разрешённых файлов которого нет ни одного файла кода, НЕ ИМЕЕТ ПРАВА менять Code.gs, server.ts и любые файлы в src/. Даже если исполнителю кажется, что заодно стоит что-то поправить. Даже если правка выглядит очевидно полезной.
2. Ни один промт НЕ ИМЕЕТ ПРАВА удалять, сокращать, сливать или переписывать существующие пункты плана. Пункт может быть только дополнен или изменён в статусе, и только если это прямо написано в промте.
3. Исполнитель никогда не помечает пункты и этапы выполненными по собственной инициативе. Смена статуса выполняется только дословной заменой, приведённой в промте.
4. Исполнитель никогда не выполняет работу, которой нет в промте. Если по ходу задачи обнаружился дефект или напрашивается улучшение — о нём сообщается в ответе, но НЕ реализуется.

5. ЛИМИТ ДЛИНЫ ПОДСТАНОВКИ, введён 08.08.2026. Ни искомый, ни заменяющий фрагмент в промте не должны превышать примерно 300 символов. Длинный текст вносится НЕСКОЛЬКИМИ короткими подстановками, а не одной большой. Причина — инцидент 08.08.2026 при закрытии пункта 31: промт содержал замену, где заменяющий текст был одной строкой почти на 4500 символов; исполнитель перестал выполнять дословную подстановку и начал вставлять текст «по смыслу». Итог: пункт 26 уничтожен целиком, в пункт 25 вклеены посторонние 6645 символов, целевой пункт 31 при этом не изменился вовсе, журналы не заполнены. Те же самые правки, разбитые на подстановки по 60–120 символов, прошли без единой ошибки с первого раза.

6. ФОРМА КОРОТКОЙ ПОДСТАНОВКИ. Для смены статуса пункта и его этапов НЕ надо переписывать строку целиком. Достаточно заменять по одному маркеру вместе с ближайшими опознавательными словами, например «Этапы: ⬜ 0. Внесение этого пункта в план → ⬜ A. server.ts:» на тот же текст с ✅. Каждый такой якорь обязан встречаться в файле ровно один раз, и уникальность проверяется архитектором ДО написания промта.

7. NUMSTAT КАК КРАСНАЯ ЛАМПОЧКА, введено 08.08.2026. Архитектор обязан заранее посчитать и написать в промте ожидаемый вывод `git diff --numstat` по каждому изменяемому файлу, а исполнитель обязан привести фактический вывод дословно. Смена статусов внутри одной строки плана обязана давать ровно «1 1». Вставка нового пункта плана — ровно «2 0». Дозапись раздела в документацию — «N 0» с нулём удалений. Любое расхождение означает, что исполнитель разошёлся: правка ОТКАТЫВАЕТСЯ целиком, а не дочинивается поверх.

8. ОТКАТ ПО КОНТРОЛЬНОЙ СУММЕ. Повреждённый файл не чинится точечными правками. Выполняется `git checkout <SHA последнего целого коммита> -- <файл>`, резервный способ при недоступности git — `curl` на raw.githubusercontent.com по тому же SHA. Результат сверяется по `sha256sum` с эталоном, снятым архитектором ДО правки. При откате журналы НЕ ведутся: это технический откат, а не изменение проекта.

ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ АРХИТЕКТОРА после каждой правки, до любого развёртывания:
- `git diff HEAD~1 --stat` — в коммите не должно быть файлов, которых не было в списке разрешённых;
- число пунктов плана до и после: `grep -cE '^- (⬜|🟨|✅) [0-9]+\.' docs/OZON_PLAN.md` — обязано совпадать, если промт не заводил новый пункт; на 01.08.2026 верный ответ 28;
- ВНИМАНИЕ, ГРАБЛИ. В первой редакции этого правила стояла команда с квадратными скобками: `grep -cE '^- [⬜🟨✅] [0-9]+\.'`. Она ВСЕГДА возвращает 0 и никогда не поймает потерю пункта, потому что символы ⬜🟨✅ многобайтовые, а квадратные скобки в grep -E разбираются побайтово. Правильная форма — чередование в круглых скобках, как выше; равнозначная альтернатива `LC_ALL=C.UTF-8 grep -cP '^- (⬜|🟨|✅) [0-9]+\.' docs/OZON_PLAN.md`. Любую проверочную команду, которую архитектор вписывает в промт, он обязан СНАЧАЛА выполнить у себя и убедиться, что она даёт осмысленный результат на текущем файле. Команда, выданная без проверки, создаёт ложное чувство защищённости и хуже, чем её отсутствие;
- ВНИМАНИЕ, ЛОЖНЫЙ ОТЧЁТ. 01.08.2026 исполнитель отчитался, что эта неработающая команда вернула 28, тогда как получить это значение она не могла в принципе. Цифра была вымышлена. Вывод: отчёт исполнителя подтверждает выполнение, но НЕ заменяет собственную проверку архитектора. Любое число из отчёта, на которое опирается решение, архитектор пересчитывает сам на свежесклонированном репозитории;
- контрольные суммы ключевых строк, которых правка не касалась: `sha256sum` по конкретным строкам через `sed -n 'Np'`;
- `git diff HEAD~1 --numstat -- docs/` — число удалённых строк в журналах обязано быть нулём;
- сверка размера файла в байтах с независимо рассчитанным ожидаемым значением: правки собираются архитектором локально и сравниваются с отчётом исполнителя побайтово.

Проверка «файл вырос, значит ничего не потеряно» НЕДОПУСТИМА и прямо запрещена: именно она пропустила инцидент 01.08.2026.

Готовый блок, который архитектор обязан вставлять в КАЖДЫЙ промт для исполнителя без изменений:

> ГРАНИЦЫ ЗАДАЧИ. Изменять разрешено ТОЛЬКО файлы, перечисленные в разделе ОГРАНИЧЕНИЯ, и только заменами, приведёнными в промте. Запрещено удалять, сокращать, сливать и переписывать существующие пункты плана и существующие функции кода. Запрещено менять статусы пунктов и этапов, если замена статуса не приведена в промте дословно. Запрещено выполнять работу, которой в промте нет: обнаружил дефект или идею улучшения — напиши о ней в ответе и НЕ реализуй.

## Правило ведения журналов

Записи в `docs/DEVLOG.md`, `docs/CHANGELOG.md` и `docs/TEST_LOG.md` добавляются ТОЛЬКО в самый конец файла. Правило введено 01.08.2026 после того, как выяснилось, что в верхней части журналов лежит исторический блок с обратным порядком дат, а новые записи дописываются вниз, и из-за этой двойственности исполнитель несколько раз редактировал чужие строки.

1. `DEVLOG.md` и `TEST_LOG.md` — новая запись это новая последняя строка таблицы.
2. `CHANGELOG.md` — новая запись это новый блок `## [ГГГГ-ММ-ДД]` в конце файла, даже если блок с такой же датой уже есть выше. Сливать блоки с одинаковой датой запрещено.
3. Ни одна существующая строка не редактируется, не переносится, не удаляется и не переформатируется. Опечатки в старых записях не исправляются.
4. Верхняя часть файлов и исторический блок с обратным порядком дат не трогаются никогда.
5. Проверка архитектором обязательна до развёртывания: `git diff HEAD~1 --numstat -- docs/DEVLOG.md docs/CHANGELOG.md docs/TEST_LOG.md`. Второй столбец, число удалённых строк, обязан быть нулём для всех трёх файлов. Любое ненулевое значение считается нарушением и разбирается до деплоя.

Готовый блок, который архитектор обязан вставлять в КАЖДЫЙ промт для исполнителя без изменений:

> ЖУРНАЛЫ. Файлы docs/DEVLOG.md, docs/CHANGELOG.md и docs/TEST_LOG.md ведутся ТОЛЬКО дозаписью в самый конец файла. DEVLOG.md и TEST_LOG.md — новая последняя строка таблицы. CHANGELOG.md — новый блок «## [ГГГГ-ММ-ДД]» в конце файла, даже если блок с такой датой уже есть выше; сливать блоки запрещено. Ни одна существующая строка не редактируется, не переносится, не удаляется и не переформатируется, опечатки в старых записях не исправляются. Верхнюю часть файлов не трогать. В отчёте укажи число строк и размер в байтах каждого журнала до и после правки: число строк обязано только вырасти.

## Заметки для следующего чата

### СВОДКА НА 20.08.2026 — состояние после рабочего дня

Заменяет сводку от 19.08.2026: всё, что в ней было незакрытого, закрыто.

**РАЗВЁРНУТО НА БОЮ.** Фронтенд — ревизия `sklad-00018-dmw` в europe-central2, адрес
https://sklad-415081166309.europe-central2.run.app. `Code.gs` владелец развернул вручную ДВАЖДЫ за
день: первый раз с исправлением окна недель, второй — с разделением листа продаж. Тестов на конец
дня: 75 фронтенд (vitest), 83 проверки стенда Apps Script (`npm run test:gas`, было 70).

**ЗАКРЫТО ЗА ДЕНЬ — 40 пунктов плана из 42.**
- Пункт 40 (капитализация при списании) — ЦЕЛИКОМ. Этап I решён без ремонта кода.
- Пункт 29 (сбои связи) — маркер «частично» висел по недосмотру, все шесть этапов были выполнены ещё 06.08.
- Пункт 22 (вкладка «Остатки Озон») — ЦЕЛИКОМ, живым регрессом.
- Пункт 42 (коррекция скорости при дефиците) — ЦЕЛИКОМ.
- Пункт 23 (локальный зачёт заявок) — ЦЕЛИКОМ.
- Пункт 26 (скорость старта) — ЦЕЛИКОМ. Этап B вычеркнут как вредный, см. ниже.
Открыты только 27 (необязательный) и 37 (отложенный, на боевых данных не воспроизводится).

**ДЕНЬГИ: долги себестоимости закрыты.** «Полка_выдв_27см» — 6195,66 руб сняты владельцем при
проверке новой кнопки списания с обнулением. «Миска серая» — 11307,14 руб оставлены СОЗНАТЕЛЬНО:
разбор всей истории артикула доказал, что это в точности стоимость 95 бракованных штук от 02.08, а
отгрузка 22.07 списала свою стоимость копейка в копейку верно. Долг впитает партия 1200 шт —
себестоимость станет 139,32 вместо 130,00 руб/шт. Это ровно та логика, которую владелец заказывал в
пункте 40. Плашка «долг себестоимости» на артикуле — верное поведение, а не дефект.

**ГЛАВНАЯ НАХОДКА ДНЯ — окно тренда было обрезано.** Фронтенд просил у Code.gs последние 12 недель
продаж, а окно тренда настроено на 13. Счётчик считал РАЗЛИЧНЫЕ значения колонки «Неделя» и включал
текущую незавершённую неделю, поэтому полных недель доходило 11. Тренд и коррекция скорости при
дефиците считались по укороченному ряду. Доказано воспроизведением: по окну в 11 недель совпали все
12 контрольных чисел, по окну в 13 — ни одного. Цена в штуках: «Полка_выдв_27см» дозаказ 264 вместо
312; «Набор_полок_выдв_бел» сигнал был ПЕРЕВЁРНУТ — «товар есть, лежит не там» вместо «дозаказать
96 шт». Исправлено: отбор недель по ДАТЕ, размер окна вычисляет Code.gs по «Настройкам Ozon».

**СКОРОСТЬ СТАРТА: 12,67 с → 6,58 с, 18 обращений → 13, шесть волн → три.** Что сработало и что нет:
- НЕ сработало объединение вызовов. Чистый A/B без кэша: пять параллельных чтений 10,6 с, один
  составной вызов 9,8 с — разница в пределах шума. Объединение делает чтения ПОСЛЕДОВАТЕЛЬНЫМИ внутри
  одного выполнения Apps Script, тогда как раздельные идут параллельно и кэшируются каждое отдельно.
  Поэтому этап B пункта 26 вычеркнут: добавить туда ещё три чтения — удлинить самый долгий запрос.
- Сработала починка кэша (см. ловушку про три списка), разделение листа продаж и расцепка старта.

**ЛОВУШКИ, КОТОРЫЕ НЕЛЬЗЯ ПОТЕРЯТЬ:**
1. ПРОКСИ ВЕДЁТ УЧЁТ ДЕЙСТВИЙ В ТРЁХ СПИСКАХ, и новое действие надо вносить во ВСЕ ТРИ:
   `getCacheTtlMs` (срок жизни; неизвестное действие получает НОЛЬ и не кэшируется вовсе),
   `CACHE_INVALIDATION` (что чистить при записи) и `READ_ONLY_ACTIONS` (этот список делает три
   разные работы: очистка кэша, автоповтор при сбое, отнесение к записи). Действие, которого нет в
   READ_ONLY_ACTIONS, считается ЗАПИСЬЮ и при отсутствии в CACHE_INVALIDATION стирает ВЕСЬ кэш.
2. Из-за пункта 1 `getLastPurchasePrices` СТИРАЛ ВЕСЬ КЭШ при КАЖДОЙ загрузке главной — он не был в
   READ_ONLY_ACTIONS с самого своего появления. Именно поэтому кэш никогда не помогал на старте.
   Исправлено 20.08. При заведении нового читающего действия проверять все три списка.
3. СВЕЖАЯ ЗОНА ПРОДАЖ 13 НЕДЕЛЬ И ОКНО ТРЕНДА 13 НЕДЕЛЬ — ЗАПАСА НЕТ ВООБЩЕ.
   `OZON_SALES_WEEKLY_ZONE_WEEKS = 13` в Code.gs. Поднять настройку «Окно тренда» выше 13 нельзя:
   четырнадцатая неделя уже сжата в 28-дневный блок и отбрасывается фильтром `days !== 7`. Расширять
   надо ОДНОВРЕМЕННО обе величины.
4. ЛИСТ ПРОДАЖ ТЕПЕРЬ РАЗДЕЛЁН: «Продажи Ozon» (1392 недельные строки) и «Продажи Ozon Архив»
   (1805 блоков по 28 дней). Архив НЕ заморожен: `saveOzonSales` перестраивает его при каждой
   синхронизации, поэтому путь ЗАПИСИ читает ОБА листа, а `getOzonSales` — только недельный.
   Удалять архивный лист нельзя.
5. КНОПКА «ОБНОВИТЬ» НА ВКЛАДКЕ «ОСТАТКИ ОЗОН» ОБНОВЛЯЕТ ТОЛЬКО ОСТАТКИ (`runOzonStocksSyncNow`).
   Полная синхронизация с продажами — Настройки → «Запустить проверку сейчас» (`runOzonSyncNow`) или
   автоопрос в 05:00 и 17:00 МСК.
6. КЭШ ДАННЫХ OZON ЖИВЁТ ЧАС (было 5 минут). Автоопрос идёт ВНУТРИ Apps Script и через прокси не
   проходит, поэтому сбросить кэш он не может: после синхронизации данные могут быть до часа
   несвежими. Страховки: на экране виден собственный «Обновлено» у данных, любая запись через прокси
   чистит кэш, а за ночь контейнер гасится и утром кэш пуст.
7. ПРИЗНАК «ЗАГРУЗКА УЖЕ ЗАПУЩЕНА» ЧИТАЕТСЯ ЧЕРЕЗ `getState()`, А НЕ ПОДПИСКОЙ. Подписка даёт ровно
   тот дубль, ради устранения которого всё делалось: значение попадает в замыкание на отрисовке,
   случившейся ДО запуска, и эффект шлёт второй запрос. Признак также НЕ должен входить в список
   зависимостей эффекта — иначе эффект отработает дважды.
8. `recalcCapitalizationFromAvg` в Code.gs СТИРАЕТ все долги себестоимости. Из интерфейса не
   вызывается, только прямым обращением к API.
9. Учётные правила дублируются в бэкенде и во фронтенде: снятие обнуления капитализации в Code.gs
   пришлось повторить в `normalizeStock` в сторе.
10. Поле количества в ручном вводе хранит ЧИСЛО, поэтому `!quantity` считает ноль пустотой.
11. Метка выбора при списании едет строкой `[себестоимость обнулена]` ВНУТРИ объекта операции и
    дословно связывает `ManualTab.tsx` и `Code.gs`.
12. Остатки исключённых кластеров ВХОДЯТ в ТРУБУ — это правило, а не ошибка; проверено 19.08.
13. Стенд Apps Script НЕ покрывает пути удаления и восстановления операций (нужна модель листа
    «История» и архива). Проведение и `saveOzonSales` покрыты.

**НЕЗАКРЫТЫЙ ВОПРОС.** Изредка загрузка страницы полностью промахивается мимо кэша: наблюдалось
10,8 с при 3 ответах из 14 из кэша, тогда как соседняя загрузка давала 8,5 с при 11 из 14. Пишущих
действий на старте не найдено, причина НЕ УСТАНОВЛЕНА. Записано честно, не заметено.

**ПРОЦЕСС, решение владельца 20.08.2026: АНГЛИЙСКИЙ ДЛЯ ВСЕЙ ВНУТРЯНКИ** — задания и отчёты агентов,
комментарии в коде, документация, записи в журналах и плане, сообщения коммитов. Русский остаётся
только для ответов владельцу и для текста, который видит покупатель внутри приложения. Это
ОБРАЩАЕТ правило от 19.08. Старый русский текст задним числом НЕ переводится: файлы становятся
двуязычными и переходят на английский по мере правок. Правило записано в `~/.claude/CLAUDE.md`, в
проектной копии `GLOBAL_RULES.md`, в четырёх определениях агентов и в техническом брифе.

**РЕЗЕРВНАЯ КОПИЯ БД** перед разделением листа продаж: «БД Склад — резерв перед разделением листа
продаж (2026-08-20)», id `1w-6-1noXzGlu3-UU283Tb_RFHP_2vSRxC41qwfkTFwI`.

**Очередь дальше.** Состояние на 20.08.2026, конец дня: закрыто 45 пунктов из 55, боевая ревизия фронтенда `sklad-00031-mt9`. 20.08.2026 владелец продиктовал тринадцать новых задач — пункты 43–55. Из них ЗАКРЫТЫ И ПОДТВЕРЖДЕНЫ ВЛАДЕЛЬЦЕМ ВЖИВУЮ: 43 (действующие расценки в справочнике), 44 (воронка без отменённых), 45 (ручное добавление кластеров и товаров в заявку — и вся последующая переделка окна поставки, десять правок, список внутри пункта), 54 (сворачивание левого меню), 55 (алерт не зовёт на поставку без остатка). ОТКРЫТЫ: 46–53. Порядок среди них НЕ ЗАДАН — спросить владельца. Пункты 46 и 47 — это ПРОЕКТИРОВАНИЕ, а не готовое ТЗ: в 46 не определены точка входа и способ выбора товара, в 47 не определены структура файла для КАН и то, какое количество на Ozon считать остатком. Пункт 52 — это АУДИТ: сначала воспроизвести частичную отмену заявки и посмотреть, ломается ли резерв, и только потом решать, нужен ли код. Прежние остатки: пункт 27 (необязательный: снять замок с `getArchivedItems`, вынеся уборку старых записей на суточный триггер) и пункт 37 (отложенный: архив товаров и отсев артикулов вне SKU Базы, на боевых данных не воспроизводится). Плюс три хвоста без номера: незакрытый вопрос с кэшем; непокрытые стендом пути удаления и восстановления операций; и живая проверка через Ozon MCP, отдаёт ли Ozon стоимость логистики поставки в НЕзадокументированных полях — промт владельцу выдан 20.08.2026, ответ не получен, по опубликованным схемам такого поля нет (разбор в `docs/OZON_API.md`).
старых записей на суточный триггер) и пункт 37 (отложенный: архив товаров и отсев артикулов вне SKU
Базы, на боевых данных не воспроизводится). Плюс два хвоста без номера: незакрытый вопрос с кэшем и
непокрытые стендом пути удаления и восстановления операций.


- ПРИОРИТЕТ №1 на 01.08.2026 — пункт 28 «Защита от двойного проведения операции», заведён по итогам боевой аварии с двойным списанием 118 шт. Этапы 0, A, B, C и E закрыты и развёрнуты на бою, живой регресс на тестовой БД пройден по четырём сценариям. ОСТАЛСЯ ТОЛЬКО ЭТАП D: заглушка doGet, автоповтор в прокси, таймаут, кнопка «Проверить результат». Его состав и результаты замеров записаны в конце описания пункта 28 — новый чат берёт их оттуда, повторно замерять не нужно.
- СОСТОЯНИЕ БОЕВЫХ ДАННЫХ на 01.08.2026: авария разобрана и закрыта. Остатки восстановлены оприходованием излишков (Органайзер_2_пол_белый 97 шт / 48 743,92 ₽, Органайзер_3_пол_PureWhite 47 шт / 33 855,66 ₽, Органайзер_2_пол_PureWhite 192 шт / 100 193,84 ₽), заявка № 120361763-1 привязана кнопкой «Привязать как дубль», все четыре её поставки в статусе processed.
- ВАЖНО ПЕРЕД ПРОДОЛЖЕНИЕМ: 01.08.2026 приложение переводилось на тестовую БД для регресса. Проверить, что режим разработки выключен и Script Property ozon_autoSyncTarget снова равно prod, иначе фоновый опрос Ozon будет писать в тестовую копию.
- Остальные открытые хвосты (этап E пункта 23, хвосты пункта 24, этап I пункта 22, пункты 27 и 26) ждут завершения пункта 28.
- Пункты 1–21 ✅. Интеграция с Ozon работает на боевой БД с 26.07.2026. Пункт 22: этапы A–H ✅, остался этап I — живой регресс мастера «Создать поставку» (частично закрыт регрессом пункта 24). Пункт 23 ✅ до этапа D включительно, этап E (живой регресс снятия резерва при переходе в ACCEPTED_AT_SUPPLY_WAREHOUSE, поведение предохранителя 7 дней, проверка под не-админом) остаётся открытым. Пункт 24 ✅ ВЫПОЛНЕН 31.07.2026 целиком: создание заявки в мастере теперь автоматически раскладывает товар по грузоместам, отправляет их в Ozon, получает этикетки, собирает файлы состава по кластерам и складывает всё в папку «Озон <номер заявки>» на Google Диске вместе с этикетками ШК товаров из папки-библиотеки «ШК озон для автоматизации». Крупных невыполненных пунктов в плане не осталось: пункт 25 выполнен 30.07.2026 (Apps Script версии 131), пункт 26 имеет низкий приоритет и выполняется последним по решению пользователя, пункт 27 необязательный. Дальше работаем по открытым хвостам, в порядке важности: этап E пункта 23 (живой регресс снятия резерва при переходе в ACCEPTED_AT_SUPPLY_WAREHOUSE, поведение предохранителя 7 дней, проверка под не-админом), затем хвосты пункта 24, затем этап I пункта 22, затем пункт 27 и в самом конце пункт 26. ОТКРЫТЫЕ ХВОСТЫ ПУНКТА 24: живой регресс удаления отменённых записей состоится не раньше 28.08.2026, когда первым записям исполнится 28 дней; поведение при заявке на несколько кластеров проверено только по схеме API, вживую многокластерная заявка через новый мастер не прогонялась; в мастере не проверено поведение, когда Ozon принимает состав частично.
- Боевой контур: автоопрос 05:00 и 17:00 МСК пишет в боевую БД (ozon_autoSyncTarget = 'prod'); в тот же прогон входит обновление зеркала остатков складов Ozon.
- Режим разработки остаётся рабочим инструментом: тумблер у админа переключает браузер на тестовую БД, автоопрос при этом продолжает писать в боевую.
- ПРАВИЛО: Code.gs и docs/OZON_PLAN.md изменяются только отдельными точечными задачами с дословной цитатой заменяемого фрагмента и обязательной проверкой диффа архитектором до развёртывания.
- ПРАВИЛО (Ozon MCP): разведка форм ответов Ozon API ведётся через Ozon MCP, но он подключён только в Claude Code, а не в чате с архитектором. Порядок: архитектор пишет промт для Claude Code → пользователь запускает его и присылает ответ в чат. Гадать о формах ответов вместо запроса запрещено.
- ПРАВИЛО (Claude в Chrome): перед подключением к приложению через Claude в Chrome архитектор ОБЯЗАН спросить разрешения у пользователя. Причина: приложение может быть не развёрнуто глобально, и свежая версия кода тогда работает только внутри Google AI Studio — проверка по адресу Cloud Run покажет старое поведение и приведёт к ложным выводам.
- ПРАВИЛО (чтение таблицы): Google-таблицы читаются архитектором через Claude в Chrome запросом gviz с обходом кэша (параметр-метка времени и cache: no-store). Без обхода кэша возвращаются устаревшие данные — 29.07.2026 это привело к двум ошибочным диагнозам подряд на этапе A пункта 23.
- Открытый хвост по безопасности: в листе «Пользователи» боевой БД у записи «Администратор» (роль admin) пароль хранится открытым текстом, у двух других записей — хэши. Требуется ручная чистка.
- Тестовые строки в листе «Внешние отгрузки» нельзя создавать копированием строки-заголовка: нечисловой OrderID уходит в Ozon API и роняет автоопрос с HTTP 400.
- Известные хвосты вне плана — в отдельном разделе «Известные хвосты вне плана» выше по файлу.
- НА 18.08.2026, пункт 42, этап E: код написан и проверен стендом, но НЕ РАЗВЁРНУТ. Требуется: развернуть Code.gs через редактор Apps Script, переключить Script Property ozon_autoSyncTarget на 'test', дождаться автоопроса, убедиться что лист «История остатков Ozon» создан и счётчики растут на 1 в сутки (а не на 2 — опрос ходит дважды), затем вернуть 'prod'.
- НА 18.08.2026, ВАЖНО: в боевой БД ключ trendWeeks имеет значение 11, а дефолты Code.gs заполняют только ОТСУТСТВУЮЩИЕ ключи. Плановое значение 13 в бою не включится само — его нужно поменять вручную в окне настроек Ozon. Замер эффекта на боевых данных 18.08.2026: состав корректируемых артикулов не меняется, растут две скорости (Набор_полок_выдв_бел 1,43 → 1,75; Полка_выдв_27см 3,75 → 4,21).
- НА 18.08.2026, ИНФРАСТРУКТУРА: боевой прокси переехал на сервис sklad в europe-central2 (Варшава), адрес https://sklad-415081166309.europe-central2.run.app. Старый сервис service в us-west1 (Орегон) оставлен как страховка, НЕ УДАЛЯТЬ. PROXY_URL в Code.gs уже переведён на новый адрес, но вступит в силу только после развёртывания Apps Script. Триггеры Cloud Build на ветку main целятся в СТАРЫЙ сервис: push в main = попытка выката в бой.
